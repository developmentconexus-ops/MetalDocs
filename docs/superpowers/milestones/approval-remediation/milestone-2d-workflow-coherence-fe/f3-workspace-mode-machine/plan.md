# Feature F2d.3 — Plan

> Input: `spec.md` (approved pre-code). Output the "how". TDD: failing test first → green.

## Plan

**TDD order.**

### Step 0 — failing unit tests (write first)
`frontend/apps/web/src/features/approval/lib/workspaceMode.test.ts` (vitest, pure — no DB, matches
`templateApprovalActions.test.ts` harness: `describe/it/expect`, small factory helpers `makeInstance`,
`makeViewer`, `makeStage`). One `it` per Validation-Gate row:
- `deriveWorkspaceMode`: author-editing, author-waiting, author-changes-requested, reviewing, approving,
  approving-via-delegation, observing-ineligible, observing-non-author-draft, lifecycle-terminal,
  author-dominates-over-stage (SoD).
- `deriveStageMode` (subject-agnostic): called with only `(stage_kind, eligible)` — asserts
  review+eligible→reviewing, approval+eligible→approving, either+¬eligible→observing, undefined→observing.
Red first (module absent).

### Step 1 — the selector (implement to green)
`workspaceMode.ts`:
- `export type WorkspaceMode = 'author-editing'|'author-waiting'|'author-changes-requested'|'reviewing'|'approving'|'observing'|'lifecycle'`.
- `export function deriveStageMode(stageKind, eligible)` — pure `stage_kind`×bool map (no document arg;
  survives M3 kernel/templates reuse).
- `export function deriveWorkspaceMode(doc, instance, viewer)` — precedence per spec:
  is_author → lifecycle/author sub-derivation (changes_requested via `instance.status`, else document
  status); else active stage → `deriveStageMode`; else terminal status → lifecycle, else observing.
- Types: `doc: { status: string } | null`, `instance: ApprovalInstanceByDocumentResponse | null`,
  `viewer: ApprovalInstanceByDocumentResponse['viewer'] | null`. No React, no I/O.

### Step 2 — shrink TRANSITION_POLICY (`approvalWorkflow.ts`)
- Remove `signoff: boolean` from `TransitionPolicy.actions` (line 36) and the `signoff: …` field from all
  9 `TRANSITION_POLICY` entries. `cancelInstance` + `publishOrSchedule` stay (document-lifecycle).

### Step 3 — rewire the one consumer (`useDocumentApprovalArtifact.ts:206`)
- `signoffOffered` replaces the status gate `policy.actions.signoff` with viewer truth:
  `deriveWorkspaceMode(doc, instance, instance?.viewer ?? null) === 'approving'`. `instance` is a
  direct alias of the generated `ApprovalInstanceByDocumentResponse` (viewer typed + always present,
  F2d.1) — no plumbing needed. Keep the `sidebarReady && lockedByInstanceId != null && contentHash != null`
  readiness guards (they gate render-readiness, not eligibility).

### Step 4 — green + gates
- `make test` (frontend scope) — new suite green, existing green.
- `npm run typecheck` (or the frontend typecheck target) clean after the `TransitionPolicy` type change.
- Grep gate: `policy.actions.signoff` gone; no `signoffOffered`-style status-only stage-eligibility gate
  remains outside the selector; `deriveStageMode` takes no document arg.

## Files touched
| File | Change |
|------|--------|
| `features/approval/lib/workspaceMode.ts` | NEW — selector + subject-agnostic helper |
| `features/approval/lib/workspaceMode.test.ts` | NEW — per-branch unit tests |
| `features/documents/lib/approvalWorkflow.ts` | remove `signoff` from `TransitionPolicy` + 9 entries |
| `features/documents/adapters/useDocumentApprovalArtifact.ts` | rewire `signoffOffered` to selector |

## Non-goals carried from spec
resolveEditorMode deletion (F2d.7), DecisionFooter variant (F2d.5), instance→react-query (F2d.4),
lifecycle capability gates in `useDocumentArtifact` stay.

## Execution notes

Executed TDD order per plan. Highlights and deviations:

- **Step 0/1:** `workspaceMode.test.ts` (17 cases) written first (red — module absent), then
  `workspaceMode.ts` to green. `deriveStageMode` takes only `(stageKind, eligible)` — no document arg
  (subject-agnostic, survives M3). Precedence: `is_author` dominates → author/lifecycle lens;
  else active stage → `deriveStageMode`; else terminal → lifecycle, else observing.
- **Step 2:** removed `signoff` from `TransitionPolicy.actions` type + all 9 `TRANSITION_POLICY` entries
  (2 edits via replace_all: the `signoff: false,` and `signoff: true,` prefixes). `cancelInstance` +
  `publishOrSchedule` stay.
- **Step 3:** rewired `signoffOffered` (useDocumentApprovalArtifact.ts) — `policy.actions.signoff`
  (status gate, the M2c defect) → `deriveWorkspaceMode(doc, instance, instance?.viewer ?? null) ===
  'approving'`. `ApprovalInstance` is a direct alias of the generated by-document response, so
  `instance.viewer` is typed + always present (F2d.1) — no plumbing. Updated two stale doc comments.
- **Deviation / added work (not in original plan):** the cockpit hook's own test
  (`useDocumentApprovalArtifact.test.tsx`) (a) was already type-broken by F2d.1/F2d.2 making
  `viewer`/`verdicts` required (frontend `tsc` was not run at those closes) — fixed the mock; and
  (b) three tests (236/275/286) asserted the signature decision is offered on `under_review` with a
  **fully-passed** stage set (no active stage) — the exact M2c defect. Under the corrected contract that
  offers nothing, so those three now use a `makeApprovingInstance()` fixture (active approval stage +
  eligible viewer). This updates the guard to the CORRECT offering rule (the cockpit + this test are
  deleted in F2d.7; they stay green until then). All other cockpit tests unchanged.
- **Green:** selector 17/17 + cockpit 19/19 = 36/36; `tsc --noEmit` clean; grep gate clean
  (no `policy.actions.signoff` read; `signoff` gone from `TransitionPolicy`; `deriveStageMode` doc-free).
- DecisionFooter's stale `policy.actions.signoff` comment (DecisionFooter.tsx:211) left for F2d.5
  (its variant logic is explicit F2d.5 scope — not touched here to avoid drift).
- Review: independent `caveman:cavecrew-reviewer` (sonnet) — see `evidence.md`.
