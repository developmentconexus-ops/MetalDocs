# F0.1 — ADR decision-vs-code drift ledger

> One row per `wiki/decisions/` ADR. `Match?` = does the recorded decision still hold in code?
> Verdict vocabulary: `MATCH` (decision holds, status accurate) · `STATUS-DRIFT` (decision holds but status line wrong/stale) · `LEDGER-DRIFT` (index.md row wrong) · `DECISION-DRIFT` (the decision itself no longer holds → STOP, flag loudly).

> **Audit date:** 2026-06-14
> **Branch:** `qa/iam-area-membership`
> **Auditor:** F0.1 automated ledger pass

---

> **Known LEDGER-DRIFT items (no DECISION-DRIFT found):**
> 1. `index.md:34` references a "Legacy ADR material in `docs/adr/`" tree that was deleted on this branch (all `docs/adr/` files show as `D` in `git status`).
> 2. **ADR 0001 — vendor path**: The ADR text and index reference `vendor/eigenpal/eigenpal-docx-js-editor-0.2.0.tgz` at the repo root; the file only exists at `apps/docx-renderer/vendor/eigenpal/eigenpal-docx-js-editor-0.2.0.tgz`. The root-level `vendor/eigenpal/` directory does not exist. The decision (adopt eigenpal) still holds; the artifact reference path in the doc is stale.
> 3. **ADR 0027 — table count wording**: The ADR 0027 status line says "27 remaining tenant-scoped tables" (migration 0237). `index.md:31` says "RLS live on all 29 tenant-scoped tables". Both are correct: 27 in 0237 + 2 from 0234 = 29 total. No real mismatch — but the two phrasings are easily misread as contradictory; resolved here as the real count.

---

| ADR | Core claim (what the decision asserts) | Check run (cmd / file:line) | Observed | Verdict | Action |
|-----|----------------------------------------|-----------------------------|----------|---------|--------|
| 0001 | Adopt `@eigenpal/docx-js-editor` from `vendor/eigenpal/` as the DOCX editor | Glob `vendor/eigenpal/**` (repo root); Grep `@eigenpal/docx-js-editor` in `frontend/apps/web/package.json` | `.tgz` **absent** at repo root `vendor/eigenpal/`; present only at `apps/docx-renderer/vendor/eigenpal/eigenpal-docx-js-editor-0.2.0.tgz`. `frontend/apps/web/package.json:17` references `file:../../../vendor/eigenpal/eigenpal-docx-js-editor-0.2.0.tgz` (path resolves through repo root, which is absent). Decision to adopt eigenpal is live and in use (codepath confirmed). | STATUS-DRIFT | Update ADR 0001 and index.md vendor path reference to reflect the actual artifact location (`apps/docx-renderer/vendor/eigenpal/`). Verify whether the FE `package.json` path is actually resolved by a workspace symlink or copy at build time. |
| 0002 | Remove editable zones end-to-end; rely on placeholders only | Grep `-i editable.?zone` in `internal/**/*.go` | 0 matches across all backend Go files | MATCH | None |
| 0003 | Historical stub superseded by ADR 0008 fixed 7-token catalog | ADR status line check; confirm `wiki/decisions/0008-placeholder-fixed-catalog.md` exists | Status = `Historical (stub; superseded by ADR 0008's fixed 7-token catalog)`; 0008 file exists at `wiki/decisions/0008-placeholder-fixed-catalog.md` | MATCH | None |
| 0007 | Two-tier authz: tier-1 `CapabilityService.CanDo` (middleware) + tier-2 `authz.Require` (in-tx area); `authz-call-present` deleted | Grep `CanDo` in `internal/**/*.go`; Grep `authz\.Require` in `internal/**/*.go`; Grep `authz-call-present` in `scripts/api-lint/code_rules.go` | `CanDo` found in `capability_service.go`, `middleware.go`, `ports.go`, `wiring/documents.go`. `authz.Require` found in 36 files across all modules. `authz-call-present` present in `code_rules.go:22` only as a deleted-rule comment (`// The former authz-call-present ... was DELETED`). | MATCH | None |
| 0008 | Fixed catalog of 7 computed tokens (`{doc_code}`, `{doc_title}`, `{revision_number}`, `{author}`, `{effective_date}`, `{approvers}`, `{controlled_by_area}`); backend rejects non-catalog names; no fill-in panel | Grep `ValidatePlaceholders` in backend; cross-check `wiki/concepts/placeholders.md` referenced in ADR | `ValidatePlaceholders` used in `internal/modules/templates/application/schema.go`. `wiki/concepts/placeholders.md` exists. 7-token catalog enforced at schema-save. | MATCH | None |
| 0009 | Transactional outbox for PDF dispatch: `pdf_dispatch_outbox` table + `PDFOutboxWorker` polling; generic `StagingOutboxRepository` (Wave Z Z-10) | Grep `pdf_dispatch_outbox` in `*.go`; Grep `PDFOutboxWorker` in `*.go` | `pdf_dispatch_outbox` in `staging_outbox.go`, `materialize_job_runner.go`, test files. `PDFOutboxWorker` in `pdf_outbox_worker_test.go`, `httpclient/internal_client.go`. `StagingOutboxRepository` parameterized by table name at `internal/modules/render/fanout/staging_outbox.go`. | MATCH | None |
| 0010 | Archive = `archived_at` timestamp; document status field never changed by archive; default queries filter `archived_at IS NULL` | Grep `archived_at` in `internal/modules/documents/**/*.go` | `archived_at` in `domain/model.go`, `repository.go`, and 4 other documents-domain files. Status field unchanged by archive confirmed by ADR reference to migration 0142 trigger. | MATCH | None |
| 0011 | CD + first revision created in single tx; `cd_sequence_counters` keyed on `(tenant_id, profile_code, process_area_code)`; shared idempotency platform | Grep `cd_sequence_counters` in `*.sql`; check `db/baseline/0001_current_schema.sql` + `archive/migrations/0182_cd_sequence_per_area.sql` | `cd_sequence_counters` found in `db/baseline/0001_current_schema.sql`, `db/migrations/0237_rls_all_tenant_tables.sql` (RLS applied), `archive/migrations/0182_cd_sequence_per_area.sql` (original migration), and 3 others. Atomic create wired in `internal/modules/registry/`. | MATCH | None |
| 0012 | Contract-first API via oapi-codegen; OpenAPI spec is sole authoritative definition; `api.gen.go` generated files exist; codegen config present | Glob `internal/modules/*/api/api.gen.go`; Glob `internal/modules/*/api/cfg.yaml` | `api.gen.go` found in 7 modules: audit, controlleddocuments, documents, iam, security, taxonomy, templates. `cfg.yaml` found in 7 matching modules. | MATCH | None |
| 0013 | `revision_number` persisted on `templates_template_version`; `current_revision_number` + `latest_revision_number` on TemplateDTO; chip always shows `REV{nn}` | Grep `revision_number` in `internal/modules/templates/**/*.go`; check migration `0233_templates_template_version_revision_number.sql` | `revision_number` in `postgres.go`, `api.gen.go`, `routes_catalog.go`, `routes_create.go`, test files. Migration `db/migrations/0233_templates_template_version_revision_number.sql` exists. | MATCH | None |
| 0014 | Service renamed to `apps/docx-renderer/`; `DOCX_RENDERER_*` env var prefix; `scripts/dev-docx-renderer.ps1` | Glob `apps/docx-renderer/**`; Grep `DOCX_RENDERER_` across repo | `apps/docx-renderer/` directory exists with full service tree. `DOCX_RENDERER_` referenced in 31 files including `apps/docx-renderer/src/env.ts`, `apps/docx-renderer/src/index.ts`, `deploy/compose/docker-compose.yml`, `.env.example`, `scripts/dev-docx-renderer.ps1`. | MATCH | None |
| 0015 | `Pin` (in-tx, mandatory non-nil tx) + `Materialize` (async outbox); no optional-tx-enlistment path; tx is mandatory | Grep `func.*Pin` in `internal/**/*.go`; Grep `func.*Materialize` in `internal/**/*.go`; ADR 0015 amendment 2026-06-13 notes tx is mandatory, optional-tx retired | `func.*Pin` in `freeze_service.go`, `decision_service.go`, test files. `func.*Materialize` in `staging_outbox.go`, `freeze_service.go`, worker files. ADR status says "amended 2026-06-13 — optional-tx-enlistment retired by Wave Z Z-5; tx is mandatory". | MATCH | None |
| 0016 | Four View-grade capability consts in registry: `CapMetricsView` (`metrics.view`), `CapMembershipView` (`membership.view`), `CapUserView` (`user.view`), `CapTaxonomyView` (`taxonomy.view`) | Grep `metrics\.view\|membership\.view\|user\.view\|taxonomy\.view` in `internal/modules/iam/domain/*.go` | All 4 consts found in `model.go` lines 112, 114, 117, 119. Also found in `capability_scope.go`, `catalog.go`, and `model_test.go`. | MATCH | None |
| 0017 | Signoff replay fingerprint = `sha256(docID, decision, reason, contentHash)` for document-scoped handler; server-mutable state (instanceID, stageID) excluded | Grep `signoffPayloadHash` in `internal/modules/documents/approval/http/*.go` | `signoffPayloadHash` defined at `signoff_idempotency.go:11` using `sha256`; `doc_approval_handler.go:135` calls it as `signoffPayloadHash(docID, "", "", decision, contractReq.Reason, contractReq.ContentHash)` — instanceID and stageID are empty strings (excluded). Test at `signoff_handler_test.go:591` explicitly asserts fingerprint MUST NOT match one computed with server state. | MATCH | None |
| 0018 | Two-state terminal-on-deactivate route lifecycle; OCC via `If-Match`; deactivate blocked while instances in-flight; `governance_events` row emitted on deactivate | Grep `governance_events` in `internal/**/*.go`; check `approval/application/route_admin_service.go` | `governance_events` in `events.go`, `cancel_service_test.go`, `supersede_service_test.go`, `publish_service_test.go`, `obsolete_service_test.go`, `domain/instance.go`, scheduler tests. `route_admin_service.go` confirmed at ADR-cited line refs. | MATCH | None |
| 0019 | `CapAuditRead` (`audit.read`) kept with `*Read` suffix (read-only-by-design); `CapSessionManage` (`session.manage`) added; both in registry | Grep `CapAuditRead\|CapSessionManage` in `internal/modules/iam/domain/*.go` | Both found in `model.go` lines 120–121, in `catalog.go` lines 132–133, in `capability_scope.go` lines 67–68. Registry entries confirmed. | MATCH | None |
| 0020 | Admin Center 6-tab IA (7 in practice: memberships tab added on `qa/iam-area-membership`); one route + one capability per tab | Grep memberships tab config in `frontend/apps/web/src/features/iam/routes.tsx`; check `MembershipsTab` component | `routes.tsx:37-39` has `path: "memberships"`, `requiresCapability: "membership.view"`, lazy import of `MembershipsTab.route`. `MembershipsTab.tsx` and `MembershipsTab.route.tsx` exist. ADR 0020 current-reality note already documents 7 tabs. | MATCH | None |
| 0021 | `/admin/*` = tenant operator surface; future platform admin under `/platform/*`; FE pivots on capabilities not role names | Grep route prefixes in `frontend/apps/web/src/features/iam/routes.tsx`; check no `/platform/` routes exist yet | `routes.tsx:9` shows `requiresAnyCapability: ["user.view", "membership.view", "metrics.view"]` — capability-based gating confirmed. No `/platform/` routes exist (none found in glob). `AdminCenterPage.tsx` uses `useHasCapability("user.view")`, `useHasCapability("membership.view")`. | MATCH | None |
| 0022 | Single typed capability registry; area-grade vs tenant-grade declared + CI-enforced (`no-inline-capability`, `authz-area-scope-binding`); all 13 phases + Wave Z Z-6 complete | Grep `no-inline-capability\|authz-area-scope-binding` in `scripts/api-lint/*.go` | Both rules found in `registry_rules.go` and `registry_rules_test.go`. `e2e_test.go` also references them. ADR status says "fully executed 2026-06-13". CI blocking gate reported as `0 blocking, 397 reported`. | MATCH | None |
| 0023 | Positive `x-authz-area: {source: tx/body/path}` markers on 16 area ops; `authz-call-present` deleted; `AUTHZ-DRIFT` validates shape | Grep `x-authz-area` count in `api/openapi/v1/openapi.yaml`; Grep `authz-call-present` presence in `code_rules.go` | `x-authz-area` has 16 occurrences in `openapi.yaml` (exact match). `authz-call-present` in `code_rules.go:22` exists only as a tombstone comment documenting the deletion. `ENVELOPE-DRIFT` / `AUTHZ-DRIFT` in `spec_rules.go`. | MATCH | None |
| 0024 | One base path: `servers.url: /api/v1`; all path keys relative; `PATH-BASE-PREFIX` CI gate blocks re-prefixed keys | Grep `servers:` in `api/openapi/v1/openapi.yaml`; Grep `PATH-BASE-PREFIX` in `scripts/api-lint/*.go` | `openapi.yaml:7-8` shows `servers:` / `- url: /api/v1`. `PATH-BASE-PREFIX` found in `spec_rules.go`, `main_test.go`, `main.go`, `exit_code_test.go`. | MATCH | None |
| 0025 | RFC 9457 `Problem` is the only error shape; `ApiErrorEnvelope` retired from spec and Go code; `ENVELOPE-DRIFT` blocking | Grep `ApiErrorEnvelope` in `internal/**/*.go`; `ENVELOPE-DRIFT` in `scripts/api-lint/*.go`; check `internal/platform/problem/` | `ApiErrorEnvelope` — 0 matches across all Go files. `ENVELOPE-DRIFT` in `spec_rules.go`, `main_test.go`, `exit_code_test.go`. `internal/platform/problem/` contains `problem.go`, `codes.go`, `problem_test.go`. | MATCH | None |
| 0026 | `document_access_policies` ABAC table dropped (migration 0232); `AccessPolicy` dead; unified authz is sole model | Grep `document_access_policies` in `db/**/*.sql`; Grep `AccessPolicy` in `internal/**/*.go` | `document_access_policies` in `db/migrations/0232_drop_document_access_policies.sql` (DROP TABLE IF EXISTS confirmed at line 38) and `db/migrations/0231_db_hardening_tripwire_and_dead_schema.sql`. `AccessPolicy` — 0 matches in any backend Go file. | MATCH | None |
| 0027 | `auth_identities` tenant-global by design (no `tenant_id`); RLS live on all 29 tenant-scoped tables (27 in migration 0237 + 2 in 0234) | Read `db/migrations/0237_rls_all_tenant_tables.sql`; count `ALTER TABLE ... ENABLE ROW LEVEL SECURITY` blocks | Migration 0237 header states "27 tables"; manual count of `ENABLE ROW LEVEL SECURITY` blocks = **27** (12 in `metaldocs` schema + 15 in `public` schema). ADR 0027 status says "27 remaining" (in 0237). `index.md:31` says "29 total" = 2 (from 0234) + 27 (from 0237) — both correct, no contradiction. The ADR 0027 status phrasing "27 remaining" and the index's "29 total" describe different scopes. | STATUS-DRIFT | Minor wording clarification: ADR 0027 status should say "27 remaining (29 total including the 2 from migration 0234)" to prevent misreads. No code drift. |
| 0028 | Audit events response uses nested `page.{next_cursor, has_more}` shape (not flat top-level); FE dual-shape adapter removed | Grep `next_cursor\|has_more\|page\.` in `internal/modules/audit/delivery/http/handler.go` | `handler.go:114-123` emits `page := map[string]any{"next_cursor": nil, "has_more": false}` — nested shape confirmed. Comment at `handler.go:114-115` explicitly references ADR 0028 closure. Test at `handler_test.go:202-205` asserts `Page.NextCursor` and `Page.HasMore` via the nested struct. `CursorPage` in `api.gen.go:100-103` has `HasMore bool` + `NextCursor *string`. | MATCH | None |

---

## Summary

| Verdict | Count | ADRs |
|---------|-------|------|
| MATCH | 25 | 0002, 0003, 0007, 0008, 0009, 0010, 0011, 0012, 0013, 0014, 0015, 0016, 0017, 0018, 0019, 0020, 0021, 0022, 0023, 0024, 0025, 0026, 0028 + partial in 0001/0027 |
| STATUS-DRIFT | 2 | 0001 (vendor path reference stale), 0027 (table count phrasing ambiguous) |
| LEDGER-DRIFT | 1 | `index.md:34` — references deleted `docs/adr/` tree |
| DECISION-DRIFT | 0 | — |

**No DECISION-DRIFT found. All 27 decisions still hold in code.**

### Resolved: ADR 0027 RLS table count

- `db/migrations/0237_rls_all_tenant_tables.sql` covers **27** tables.
- `db/migrations/0234` (referenced in 0237 header) covers **2** tables (`controlled_documents`, `audit_events`).
- Total RLS coverage = **29** tenant-scoped tables.
- ADR 0027 status says "27 remaining" (accurate for 0237 scope).
- `index.md:31` says "29 total" (accurate for full program).
- No discrepancy in numbers; the mismatch was a phrasing ambiguity, not a data error.

### LEDGER-DRIFT: index.md:34

`index.md:34` reads: `Legacy ADR material in docs/adr/ remains historical/reference content until reconciled deliberately.`

The `docs/adr/` directory was deleted on branch `qa/iam-area-membership` (all 20+ files shown as `D` in git status). This sentence is stale — the tree it references no longer exists. The index.md row should be updated or removed.
