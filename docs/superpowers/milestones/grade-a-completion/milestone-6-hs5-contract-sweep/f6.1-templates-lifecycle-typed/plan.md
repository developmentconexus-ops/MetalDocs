# Feature F6.1 — Plan

## Files touched

- `api/openapi/v1/openapi.yaml` — add 4 component schemas + 200 body refs on 4 ops; verify 1 already-aligned op.
- `internal/modules/templates/api/api.gen.go` — regenerated by `oapi-codegen`.
- `internal/modules/templates/delivery/http/routes_lifecycle.go` — swap 5 writeJSON sites to typed.
- `internal/modules/templates/delivery/http/routes_typed_response_f61_test.go` — NEW test file: `TestLifecycle_TypedResponseShape` (per-op subtest).

## Task list (TDD order)

1. **Author OpenAPI schemas.** In `api/openapi/v1/openapi.yaml`:
   - Under `components.schemas`, add (alphabetical placement near `TemplateDTO`):
     - `TemplateVersionEnvelope`: object `{data: object{version: $ref VersionDTO required}}` required `[data]`. Reused by submit + review.
     - `ArchiveTemplateResponse`: object `{data: object{template: $ref TemplateDTO required}}` required `[data]`.
     - `TemplateApprovalConfig`: object `{template_id: uuid required, reviewer_role?: string, approver_role: string required}` (nullable reviewer_role for None case).
     - `UpsertTemplateApprovalConfigResponse`: object `{data: object{approval_config: $ref TemplateApprovalConfig required}}` required `[data]`.
   - Under `paths`, attach `content.application/json.schema: $ref` to the existing `200` of:
     - `/templates/{id}/versions/{n}/submit` → `TemplateVersionEnvelope`.
     - `/templates/{id}/versions/{n}/review` → `TemplateVersionEnvelope`.
     - `/templates/{id}/archive` → `ArchiveTemplateResponse`.
     - `/templates/{id}/approval-config` → `UpsertTemplateApprovalConfigResponse`.
   - Verify `/templates/{id}/versions/{n}/approve` already has `$ref: ApproveTemplateVersionResponse` (line 1556) — no change.

2. **Regen BE codegen.** `GOFLAGS=-mod=mod go generate ./internal/modules/templates/api/...`. Inspect new types in `api.gen.go`; confirm `TemplateVersionEnvelope`, `ArchiveTemplateResponse`, `TemplateApprovalConfig`, `UpsertTemplateApprovalConfigResponse` exist and field names/types match expectations.

3. **TDD step 1 — write failing test.** Add `routes_typed_response_f61_test.go` with `TestLifecycle_TypedResponseShape`, subtests `submit`, `review`, `approve_accept_publish`, `approve_reject`, `archive`, `upsert_approval_config`. Each subtest:
   - drives the corresponding handler with the same fixture pattern as `routes_lifecycle_test.go`,
   - decodes the response body via `json.NewDecoder(bytes.NewReader(rr.Body.Bytes()))` + `DisallowUnknownFields()` into the matching generated `*Response` / envelope type,
   - asserts at least one typed field (e.g. `Data.Version.Status`, `Data.Template.ArchivedAt`, `Data.ApprovalConfig.ApproverRole`, `Data.NextDraft.VersionNumber`) is round-tripped non-zero.
   - Test fails today because handler emits `map[string]any` with field shape compatible BUT typed decoder may catch wrong types (e.g. `version_number` as JSON number into `int` is fine — typed decode is mainly future-drift insurance). Test passes once handler emits typed value.
   - Actually the strict-decoder approach guarantees future-drift catch. Today's wire is byte-identical, so the test will pass even before swap. To make TDD-first meaningful, ALSO add a `routes_typed_response_f61_static_test.go` assertion that uses Go's `_ = templatesapi.TemplateVersionEnvelope(...)` cast at handler-output position via reflect — too brittle. Instead: write the typed-shape test, and prove TDD by running before-handler-swap; if it already passes, that's evidence the wire shape was already correct, then the handler swap converts the proof from "wire happens to match" to "code path is typed". Document the order in evidence.md.

4. **Handler swap.** In `routes_lifecycle.go`:
   - `submitForReview`: `writeJSON(w, http.StatusOK, templatesapi.TemplateVersionEnvelope{Data: templatesapi.TemplateVersionEnvelopeData{Version: dto}})` (exact nested struct name follows codegen output — may be inline anonymous; match the codegen).
   - `review`: same envelope.
   - `approve`: construct `templatesapi.ApproveTemplateVersionResponse`; set `Data.Version = verDTO`; conditionally set `Data.NextDraft` pointer when `res.NextDraft != nil` (mirrors lines 158–162).
   - `archiveTemplate`: `templatesapi.ArchiveTemplateResponse{Data: ...{Template: dto}}`.
   - `upsertApprovalConfig`: construct `TemplateApprovalConfig` from `cfg.TemplateID` (UUID conversion via `openapi_types.UUID`-cast from string if codegen emits UUID; same pattern as existing handlers — confirm after regen), `cfg.ReviewerRole`, `cfg.ApproverRole`.
   - Remove the existing `data map[string]any` builder for approve (lines 154–163); replace with typed struct directly.

5. **Build + tests.**
   - `go build ./...`
   - `go test -count=1 ./internal/modules/templates/...`
   - Whole-repo: `go test -count=1 ./...`.

6. **Sanity grep.** `grep -nE 'map\[string\]any' internal/modules/templates/delivery/http/routes_lifecycle.go` → 0.

7. **Evidence.** Fill `evidence.md` with commands + real output; capture before/after handler diff snippet; record codegen regen diff summary.

8. **Commit.** Single commit: openapi.yaml + api.gen.go + routes_lifecycle.go + new test file. Conventional message `feat(m6/f6.1): templates lifecycle typed responses + OpenAPI 200 schemas`.

## Test strategy

- TDD: new typed-shape test added before handler swap (per task 3 — see note about wire-already-matches caveat; document order honestly in evidence.md).
- Existing lifecycle tests act as wire-shape regression sentinels (`data.version.status`, `data.next_draft`, `data.template.archived_at`, `data.approval_config.approver_role` decoders unchanged).
- Whole-repo `go test` ensures no cross-module regression (BE codegen change can only affect templates module).

## Ordering / dependencies

- F6.1 is the first feature touching OpenAPI + codegen. Lands before F6.2 (which reuses the same `cfg.yaml` and re-runs codegen for query ops).
- Independent of F6.3/F6.4/F6.5 (different modules / different handlers).
