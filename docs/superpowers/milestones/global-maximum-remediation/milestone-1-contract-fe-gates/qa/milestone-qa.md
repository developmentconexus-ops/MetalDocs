# Milestone 1 — Validation Verdict (C1–C7)

> **Written by:** the `milestone-validator` subagent — *not* the main session (separation of powers).
> **Validates against:** `../milestone.md` (up-front spec) + `../validation-contract.md` (D4) + each
> feature's `spec.md` / `evidence.md`.
> **Binding procedure:** `.claude/skills/milestone/references/milestone-end-validation.md` (C1–C7).
> **Run:** 2026-07-03  ·  **Verdict:** **PASS** (see C7).
> **Base for aggregate diff:** `1f29fd98` (M1 docs scaffold, before impl) → `HEAD` (`57ef7fdf`).
> Every command below was re-run by the validator from clean state; outputs are real, not trusted from
> the evidence transcripts. The validator wrote only this file — no source/spec/gen/doc was edited.

## Inputs loaded (none missing)

milestone.md · validation-contract.md (incl. §F1.2 AMENDMENT) · mission.md §7 M1 · program README ·
governing review (referenced) · all 4 features' spec.md + evidence.md · aggregate git diff
`1f29fd98..HEAD`. Artifact shape for this mission = `spec.md` + `evidence.md` per feature; the "how"/
plan role is served by the milestone-level `validation-contract.md` (no per-feature `plan.md` — the
mission's established, pre-approved structure). C1 judged against that shape.

**Local-only confirmed:** `git log origin/main..HEAD --oneline` lists exactly the 4 M1 impl commits
(`e7a08fb4` F1.1, `7517082c` F1.2, `41681642` F1.4, `57ef7fdf` F1.3) + the M1 docs scaffold
`1f29fd98`. Nothing pushed. Working tree clean except untracked `docs/release/` (pre-existing, not
touched). No `docs/release/` committed.

## C1 — Spec & plan conformance (per feature)

Every feature: `spec.md` present with `Approved before code: 2026-07-03 / Leandro`; Interview record
populated ("none needed — why" justification grounded in the pre-code contract + a cited code read,
not a guess); consumer contract declared FIRST and honored (producer matches the CI/PR-author/
generated-client consumer, cross-checked below); `evidence.md` acceptance table maps row-for-row to
the `spec.md` Validation Gate; non-goals respected; deviations (F1.2 response-scope, F1.3
FrontendTypeAllowlist, F1.4 one call-site cast) each carry a written rationale in evidence + the D4
contract. The "how" (plan) role is filled by `validation-contract.md` §F1.1–§F1.4 — thick,
execution-shaped (exact commands, files, positive/negative proofs, CI wiring). Not a re-spec.

| Feature | Consumer contract honored? | Acceptance met? | Non-goals respected? | Evidence |
|---------|----------------------------|-----------------|----------------------|----------|
| F1.1 oasdiff-gate | ✅ CI/PR-author: blocking `oasdiff breaking <base> <head> --fail-on ERR`, path-filtered to spec + workflow | ✅ all 4 gate rows | ✅ no spec-content edits (F1.2 owns struct fix) | `f1.1-oasdiff-gate/evidence.md` |
| F1.2 shape-lint | ✅ api-lint job + redocly job + generated clients: rule blocks nullable-not-required response fields | ✅ all rows incl. response-scope case | ✅ no handler/FE edits; no gen hand-edits | `f1.2-shape-lint/evidence.md` |
| F1.3 contract-sync-ci | ✅ CI + contract authors: wrapper reds on drift, names module | ✅ all 4 modules 0 DRIFT + wrapper + CI | ✅ approval NOT reconciled (M9 F9.5, HS-2 avoided) | `f1.3-contract-sync-ci/evidence.md` |
| F1.4 fe-boundaries | ✅ eslint job + FE authors + gen-types: new cross-feature import errors; DTOs consume `components['schemas'][...]` directly | ✅ all rows | ✅ 50+ edges grandfathered not refactored; zero new deps | `f1.4-fe-boundaries/evidence.md` |

**C1 = PASS.**

## C2 — Gates re-run, isolated (real commands, real output)

| Feature | Command re-run by validator | Real output | Pass? |
|---------|-----------------------------|-------------|-------|
| build | `go build ./...` | exit 0 | ✅ |
| F1.2 | `go test ./scripts/api-lint/... -count=1` | `ok metaldocs/scripts/api-lint` | ✅ |
| F1.2 | `go run ./scripts/api-lint -only SHAPE-NULLABLE-NOT-REQUIRED api/openapi/v1/openapi.yaml .` | `0 violation(s)` exit 0 | ✅ |
| F1.2 | 4 `TestShapeNullableNotRequired/*` subtests (incl. `request_only_exempt_response_still_checked`) | all `--- PASS` | ✅ |
| F1.2 NEG (rule) | rule vs `testdata/shape_nullable_not_required.openapi.yaml` | `…:67: SHAPE-NULLABLE-NOT-REQUIRED: schema Widget property "published_version" is nullable but not in required…` `1 violation(s)` exit 1 | ✅ |
| F1.2 | `npx @redocly/cli@latest lint api/openapi/v1/openapi.yaml` (`struct: error`) | `Your API description is valid` exit 0 | ✅ |
| F1.2 NEG (struct) | inject empty `components.parameters` into throwaway copy → redocly | ``Expected type `NamedParameters` (object) but got `null` … generated by the struct rule`` exit 1 | ✅ |
| F1.2 | `oasdiff breaking <M1-base spec> <HEAD spec> --fail-on ERR` (burn-down non-breaking) | `No breaking changes to report` exit 0 | ✅ |
| F1.2 | `go generate ./...` then `git diff` | empty (generated in sync, zero hand-edits) | ✅ |
| F1.1 POS | `oasdiff breaking <spec> <spec> --fail-on ERR` | `No changes detected` exit 0 | ✅ |
| F1.1 NEG | `oasdiff breaking <spec> <spec-with-/templates-deleted> --fail-on ERR` | `[api-path-removed-without-deprecation]` exit 1 | ✅ |
| F1.3 POS | `pwsh -File ./scripts/check-contract-sync-all.ps1` | `contract-sync OK - zero DRIFT across templates, documents, controlleddocuments, taxonomy` exit 0 | ✅ |
| F1.3 NEG | inject `export interface ValidatorProbeDrift{}` into `taxonomy/api/taxonomy.ts` → wrapper | `[DRIFT] taxonomy … handwritten frontend types: interface ValidatorProbeDrift`; `contract-sync FAILED - drifted module(s): taxonomy` exit 1 (reverted) | ✅ |
| F1.4 grep | `grep -rnE "Omit<\s*(Generated\|components\[)" …/features …*.ts` | 0 hits; `TemplateDTO`/`VersionDTO` = `components['schemas'][…]` direct | ✅ |
| F1.4 POS | `pnpm run lint` (root, `eslint .`) | exit 0 | ✅ |
| F1.4 NEG | temp `features/documents/__boundary_probe.ts` importing `../../tokens/api/tokensTypes` → `pnpm run lint` | `F1.4: cross-feature import into "documents" from sibling feature "tokens" … no-restricted-imports` exit 1 (probe removed) | ✅ |
| F1.4 allowlist | temp probe importing allowlisted `../../templates/api/templates` → `pnpm run lint` | exit 0 (allowlisted edge passes — allowlist genuinely grandfathers, not a blanket ban) | ✅ |

pnpm-junction-drift did **not** block `pnpm run lint` this run (ESLint needs no vite); no defer needed
on that axis. **C2 = PASS.**

## C3 — Senior review of the aggregate milestone diff

Whole diff `1f29fd98..HEAD` = 35 files, all inside expected surfaces (`.github/workflows`,
`scripts/api-lint`, `scripts/check-*.ps1`, `api/openapi/v1/openapi.yaml`, `redocly.yaml`,
`eslint.config.mjs`, `features/templates/api/templates.ts`, and regenerated `*.gen.go` +
`index.d.ts`). No stray edits outside the four features' declared boundaries.

- **openapi.yaml diff** is *only* additions to existing response `required:` arrays + removal of the
  empty `components.parameters` node — no `nullable:`/`type:`/`enum:`/path edits (verified by filtering
  the diff). Backward-compatible (oasdiff base→head = 0 breaking). Contract-first honored.
- **Generated files in sync:** `go generate ./...` → zero further diff — the committed `*.gen.go` are
  the true regen, no hand-edits. (`index.d.ts` is FE-gen; its diff is the same 50-field required
  reflection.)
- **No split-brain:** the nullable⇒required fact has one source of truth (the api-lint rule); the
  `Omit<>` removal *eliminates* a second source of truth for the DTO shape (the review's named drift
  vector) — DTOs now derive from generated types only. F1.3's `FrontendTypeAllowlist` and F1.4's
  19-pair `ALLOWLIST` are finite, named, shrink-only grandfathers (the repo idiom), not
  detection-weakening — proven by both negatives still firing with the allowlists in place.
- **No dead code / no feature breaking another:** F1.4 runs before F1.3's FE-type-usage regex was
  extended to bless `components['schemas'][…]` aliases — ordering handled; F1.3 wrapper green on the
  post-F1.4 tree.
- **`shape_rules.go`** is senior-quality: 9f86828b/AIP-134 rationale in-code, 3.0+3.1 nullable styles,
  allOf limitation documented as a bounded defer (errs toward no-false-positive), response-scope
  set-difference (`reqReach \ respReach`) keeps shared schemas checked.

- Findings: none blocking. Minor (non-blocking) note: `openapi-breaking.yml` runs
  `git fetch … --depth=1` after an `actions/checkout` with `fetch-depth: 0` (redundant, harmless).
- Staff-engineer bar met? ✅

## C4 — Workflow-class QA + regression

Workflow class = backend-api contract-gate + CI-workflow wiring (build-time; no live/runtime drive
required per mission §7 M1).

| Check | Outcome | Notes |
|-------|---------|-------|
| Canonical checklist (contract-gate / api-lint / eslint) | pass | `go build ./...` 0; `go run ./scripts/api-lint/ -strict …` `0 violation(s)` (full CI gate cmd, all rules incl. new SHAPE); redocly valid; `pnpm run lint` 0 |
| All gates blocking + wired | pass | `openapi-breaking.yml` (new, path-filtered, blocking); `api-design-system-lint` auto-picks up SHAPE rule (blocking); `openapi-lint` reads `struct: error`; new `contract-sync` job (`shell: pwsh`, blocking); `lint.yml` eslint job. No `continue-on-error` anywhere |
| Regression vs M0 (VersionRef) | all still pass | redocly lint clean; `go build ./...` clean; templates `template_dto_nullable_fields_test.go` + `routes_typed_response_f53_test.go` (published_version present-and-null pins) green; no shipped M0 shape changed (diff adds fields to `required`, removes none) |

**C4 = PASS.**

## C5 — Quality-bar re-measure + retrospective

| Bar / class | Before | After | Root-cause-fixed evidence |
|-------------|--------|-------|---------------------------|
| 9f86828b nullable-not-required class | discipline-only; 60 live violations on spec | **structurally closed** for response DTOs | `SHAPE-NULLABLE-NOT-REQUIRED` blocking in `RunSpecRules`; live spec `0 violation(s)`; negative fixture reds; the whole *class* burned down (50 response fields → `required`), not just the one historical field. Response-scope is a root-cause narrowing (invariant is a generated-consumer concern), operator-approved, not a symptom mask — the 10 request fields carry no null-vs-absent semantics (handler-classified) |
| Contract↔runtime↔FE drift | advisory-only script, stale (all modules red) | **blocking**, reconciled | checker reconciled to AD-1 relative keys + real mount files; 0 live DRIFT ×4; **detection power PROVEN preserved** — injected `interface` → wrapper exit 1 (C6 symptom-patch test passes) |
| FE cross-feature coupling | unenforced convention | **blocking** eslint boundary | new cross-feature import → exit 1; 19-pair shrink-only allowlist grandfathers existing edges; zero `Omit<>` gen-type overrides remain |
| redocly `struct` | `off` (stale "133" comment) | `error`, 1→0 | empty `parameters` node removed via spec; redocly valid; struct negative reds |

- Could it be built better? The four gates are the correct global-maximum construction (blocking CI +
  named shrink-only grandfathers, no new deps). Retrospective note (not a fail; input to later
  milestones): `check-module-contract-sync.ps1` remains a bespoke regex checker — a future
  generated-manifest approach (M9 governance) would be more robust than regex reconciliation, but the
  current construction is sound and its detection power is proven. No unsound construction found.

**C5 = PASS** — root causes fixed, not symptom-patched.

## C6 — Forbidden-list (any hit = FAIL)

- [ ] Suite-green reported as a pass without per-feature acceptance mapped to evidence — *clean:* each
  feature's acceptance re-run individually with positive **and** negative captured.
- [ ] Fixture/mock passed off as real-provider proof — *clean:* build-time milestone; fixtures
  explicitly labelled; live spec / real oasdiff / real redocly / real eslint / real pwsh runs used for
  positives; no provider mocking claimed as real.
- [ ] Consumer contract guessed rather than read from the consumer — *clean:* each spec's consumer
  contract cites the consumer site; request-field semantics were handler-classified, not assumed.
- [ ] Split-brain (one fact, two sources of truth) — *clean:* `Omit<>` removal collapses a
  second DTO-shape source; allowlists are finite/named/shrink-only, not dual truth.
- [ ] Self-judged close / validator edited or fixed code — *clean:* validator wrote only this file;
  all probes reverted; tree clean; status NOT flipped (main session's action on PASS).
- [ ] Scope drift (work beyond the spec, no rationale) — *clean:* F1.2 response-scope, F1.3
  FrontendTypeAllowlist, F1.4 call-site cast each carry a written rationale + operator approval /
  contract §; runtime-truth expansions recorded in `milestone.md §Discovered runtime truth` (HS-6
  surfaced, not silent).
- [ ] Symptom-patch (bar moved by masking, root cause intact) — *clean:* F1.3 detection-power negative
  proves the reconciliation did not silence real drift; F1.2 burned down the whole class.

All bounded defers carry owner + trigger (verified present): F1.3 approval carve-out → M9 F9.5;
F1.4 19-pair shrink-only allowlist; F1.2 `operation-summary`/`security-defined` still off (pre-v1
hygiene); F1.2 `upsertTemplateApprovalConfig` missing-requestBody → contract-hygiene/M9;
`FrontendTypeAllowlist` {TemplateSchemas, WirePlaceholder} shrink-only. Presence is not a fail.

**C6 = clean (no hits).**

## C7 — Verdict

- **VERDICT: PASS**
- Both dimensions pass. **Code-wise:** contract-first honored (spec-only edits + zero-diff regen), no
  split-brain, no dead code, senior-quality rule implementation, no stray edits. **Function-wise/QA:**
  all four gates demonstrated failing-then-passing from clean state with real captured output; the
  9f86828b class is structurally closed; M0 regression intact.
- On **PASS** — handed back to the main session to flip status and present the HS-1 operator gate.

> **Main-session actions (post-verdict, NOT the validator's):**
> - Operator gate (HS-1): pending — surface the F1.2 response-scope + F1.3 runtime-truth expansions.
> - Status flipped in `README.md`: no — only the main session, only on this PASS.
