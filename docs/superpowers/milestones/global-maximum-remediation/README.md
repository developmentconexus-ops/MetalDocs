# Program: Global Maximum Remediation

> **Governing spec:** `./mission.md`
> **Status:** M0 + M1 + M2 passed (operator-approved HS-1, 2026-07-03). M3 milestone-validator **PASS** (2026-07-03) — negative-RLS proof **run GREEN for real** — **HS-1 operator gate pending**. M4 **passed (operator-approved HS-1, 2026-07-04)** — milestone-validator PASS (re-validated after fix-feature F4.5, HEAD db2a3b92); F4.2 live-run defer **CLOSED** (publish-race live-green on real Postgres, `task_e03a4383` resolved); F4.3 HS-7 ADR-split ratified. **M5 (async River consolidation) milestone-validator PASS (Run-2, 2026-07-04)** — Run-1 FAIL (HS-4) closed by fix-features F5.7 (proof-gap closure: River-provisioned testdb + audit/dispatch fixtures + H-PRE-1-LIVE doc-truth) and F5.8 (watchdog collapsed to alert-only, ADR 0068 — orphaned `auto_cancel`/`SystemCancelInstance` dead code removed, HS-2/HS-6 surface-not-fold); build+runnable+live-drive green (fresh watchdog tick id=30 completed under the F5.8 binary). **M5 passed (operator-approved HS-1, 2026-07-04)** — operator "Approvado M5"; HS-7 H-PRE-1-stays-LIVE erratum **ratified** at this gate. **M6 (eQMS review + reason-for-change) milestone-validator PASS (Run-2, 2026-07-05)** — Run-1 FAIL (HS-4) closed by fix-feature F6.4 (surfacer conformed to binding §4.2/§4.3 per-tenant seed + explicit `tenant_id` predicate; `review_surfaced_at` marker wired to the review-due worklist filter + DTO; due-core predicate single-sourced; full M6 integration suite executed on real Postgres, every test `--- PASS`); all 4 run-1 fix-reqs source-verified. **M6 passed (operator-approved HS-1, 2026-07-05)** — operator "Aprovado M6". **M7 (tenant lifecycle) passed (operator-approved HS-1, 2026-07-05 — both F7.4 deviations ratified).** **M8 (ops readiness) passed (operator-approved HS-1, 2026-07-06).** **M9 (governance & hygiene close-out — LAST planned milestone) passed (operator-approved HS-1, 2026-07-06 — operator "Approved").** Terminal Acceptance run 1 (`mission-validator` §8) **FAILED (HS-5)** with 3 DEBT dimensions → bounded remediation micro-milestone **M10 terminal-remediation** opened. **M10 passed (operator-approved HS-1, 2026-07-06 — operator "Okay approved")** — milestone-validator PASS; all 3 fix-features (F-R1 metrics-listener isolation / Dim 9, F-R2 blocking ADR-status CI gate / Dim 10, F-R3 test-discipline green + ratified defers / Dim 8) closed; every gate re-run clean from scratch; C0 scope fence held. **Terminal Acceptance run 2 (`mission-validator` §8) PASSED (2026-07-06)** — 10/10 in-scope dimensions CONFIRMED, no new DEBT, all 6 mission-installed CI gates green from clean with negative proof, findings 1–25 traceable. Verdict `qa/mission-validation-run2.md` (run-1 record preserved). **All 10 milestones passed; program terminal acceptance met.** Operator final mission sign-off **GRANTED 2026-07-13** — program SEALED. Push consolidation authorized same date.
> **Owner / operator:** Leandro

Convert every finding of the 2026-07-03 final architecture review (commit 778f494a) into shipped, gate-enforced remediation: enforcement automation for the hand-sync defect class, kernel correctness, async consolidation onto River, the two ISO-core eQMS product gaps, tenant lifecycle, ops readiness, and governance hygiene. Terminal acceptance: an independent re-run of the 10-dimension review reaching CONFIRMED on every in-scope dimension, plus every mission-installed CI gate green from clean state with negative proof — judged by `mission-validator`.

## Milestones

| # | Milestone | Objective (one line) | Status | Gate result |
|---|-----------|----------------------|--------|-------------|
| 0 | `milestone-0-versionref-contract` | Land the planned VersionRef nested-ref contract cutover + ADR 0065 | passed (2026-07-03) | [PASS](milestone-0-versionref-contract/qa/milestone-qa.md) |
| 1 | `milestone-1-contract-fe-gates` | oasdiff CI gate, nullable⇒required lint, blocking contract-sync, ESLint feature boundaries | passed (2026-07-03) | [PASS](milestone-1-contract-fe-gates/qa/milestone-qa.md) |
| 2 | `milestone-2-authz-enforcement-generation` | Tripwire arms generated from capability registry + CI drift check; cap-name divergences closed | passed (2026-07-03) | [PASS](milestone-2-authz-enforcement-generation/qa/milestone-qa.md) |
| 3 | `milestone-3-tenancy-chokepoint` | TxRunner auto-seed GUCs; RLS backstop covers worker/jobs; ADR 0027 amended | **passed** — validator PASS 2026-07-03; HS-1 subsumed by operator final mission sign-off 2026-07-13 | [PASS](milestone-3-tenancy-chokepoint/qa/milestone-qa.md) |
| 4 | `milestone-4-versioning-kernel` | Unified 9-status state machine; publish race proven safe + **live-green** (F4.5 closed the defer); concurrency idiom decided (ADR-split, operator-ratified) | **passed** (2026-07-04) | [PASS](milestone-4-versioning-kernel/qa/milestone-qa.md) |
| 5 | `milestone-5-async-river-consolidation` | Janitors + staging outbox onto River; lease scheduler retired; outbox retention; fanout ordering; +F5.6 fanout queue-wiring fix, +F5.7 proof-gap closure, +F5.8 watchdog alert-only (ADR 0068) | **passed** (2026-07-04) — HS-7 H-PRE-1-LIVE erratum ratified at HS-1 | [PASS](milestone-5-async-river-consolidation/qa/milestone-qa.md) |
| 6 | `milestone-6-eqms-review-reason` | Periodic review/expiry + structured reason-for-change (ISO 9001 §7.5.3 / Part 11) | **passed** (2026-07-05) | F6.1–F6.4; run-1 FAIL closed by HS-4 fix-feature F6.4 (surfacer §4 conform + marker consumer + due-core single-source + real-DB suite `--- PASS`) |
| 7 | `milestone-7-tenant-lifecycle` | Tenant onboarding, export, erasure design (crypto-shredding ADR) + **F7.4 RLS-truth sweep** (carried from M6: flip CI role to NOSUPERUSER+NOBYPASSRLS so isolation tests stop being false-green; prod already safe) | **passed** (2026-07-05) | [PASS](milestone-7-tenant-lifecycle/qa/milestone-qa.md) |
| 8 | `milestone-8-ops-readiness` | Dockerfiles, distributed rate limiter, metrics/trace-correlation/backup posture | **passed** (2026-07-06) | [PASS](milestone-8-ops-readiness/qa/milestone-qa.md) |
| 9 | `milestone-9-governance-hygiene` | ADR hygiene, REQ-ID traceability gate, test policy, CLAUDE.md/wiki truth, structure renames | **passed** (2026-07-06) | [PASS](milestone-9-governance-hygiene/qa/milestone-qa.md) |
| 10 | `milestone-10-terminal-remediation` | HS-5 bounded remediation of the 3 terminal-acceptance DEBT dimensions: F-R1 metrics-listener isolation (Dim 9), F-R2 blocking ADR-status CI gate (Dim 10), F-R3 test-discipline green + REQ-SEARCH-1/REQ-SEC-3 defers ratified (Dim 8) | **passed** (2026-07-06) | [PASS](milestone-10-terminal-remediation/qa/milestone-qa.md) |

Status vocabulary: `planned` → `in-progress` → `passed` (operator-approved) / `blocked` (hard-stop open). The **Gate result** column links the milestone-validator's verdict (`<milestone>/qa/milestone-qa.md`); `passed` requires a validator **PASS** *and* operator HS-1 approval.

**Mission-specific execution rules (from mission.md D2/D4):** each milestone runs in a fresh session dispatched via `spawn_task` with a self-contained context brief opening with `/goal`; each milestone authors `validation-contract.md` BEFORE implementation; runtime-visible milestones (M0, M5–M8) close with a live/preview QA drive. M5/M6/M7 require a `developing-new-work` gate (and M5/M7 an ADR) before planning.

## Hard-stops raised

| When | HS id | What | Resolution |
|------|-------|------|------------|
| M3 close (2026-07-03) | HS-4 | milestone-validator FAIL, finding F-1: three durable records falsely claimed `idempotency_keys` has no `tenant_id` / RLS N/A (split-brain vs schema + ADR 0027 body) | Opened fix-feature F3.4 (idempotency-keys-rls-truth); corrected all 3 records + `async-tenant-tables.txt`; re-dispatched validator → **PASS**. Docs-only, RLS byte-identical. |
| M3 F3.4 (2026-07-03) | HS-7 | F-1 fix required editing the committed D4 validation-contract | Operator approved the re-open; corrected §0.3/§2.4/§4 in place + dated auditable erratum; acceptance bar unchanged. |
| M3 real-run (2026-07-03) | HS-4 | Operator required a REAL run of the F3.2 negative-RLS proof (no defer). Real run RED: proof targeted `documents`, whose M2 capability write-tripwire (P0001) fires before the RLS tenant policy — leak-before + 42501 assertions invalid. Core isolation subtest (0-row SELECT/UPDATE/DELETE under `SeedTxTenant`) passed. | **RESOLVED** — F3.5 (rls-proof-real-green) retargeted the proof to `metaldocs.notifications` (FORCE-RLS tenant table, real F3.2 async seed site, NOT tripwired); ran GREEN for real (validator re-ran it live itself); defer closed; validator re-dispatched → **PASS**. |
| M4 F4.3 (2026-07-04) | HS-7 | F4.3 verification proved the contract §3.4 premise false: templates has **zero** If-Match usage; its body `expected_lock_version` is self-consistent; the cited "system-wide If-Match standard" is documents-module-internal CON-01, not a cross-module ADR. Migrating templates would create a new cross-module standard, not finish a convergence — scope creep in a correctness milestone. | **RESOLVED (pending HS-1 ratification)** — took contract §3.5-permitted fallback: **ADR 0066** records the two-idiom split (documents=If-Match, templates=body lock_version) as intentional with If-Match named the target; full unification deferred to its own change (M9 candidate). Contract §3 re-opened with a loud dated erratum (§3.7), not silent. No templates/documents wire change. **Operator ratification carried to the M4 HS-1 gate.** |
| M4 post-close (2026-07-04) | HS-2 | Operator directed a complete audit + in-milestone fix of the F4.2 live-run defer (authz NULL-GUC gap) — "não deixar defer nem gap em M4, mesmo um pouco fora do escopo". Root cause was in `internal/modules/iam/authz` (one module out from documents/M4): `current_setting(...,true)` returns SQL NULL for a never-set placeholder GUC; `MustActorID`/`MustTenantID` scanned it into a plain `string` → driver error instead of the sentinel. | **RESOLVED** — fix-feature **F4.5** (`4ce4d308`): one shared `readSoftGUC` (`sql.NullString`) backs all four GUC readers; fail-closed unchanged (stricter-or-equal); TDD-pinned. F4.2 harness made production-faithful → `TestPublishRace` **live-green on real Postgres** (all 4 subtests); authz unit+integration green; review clean. `task_e03a4383` resolved, not deferred. Milestone re-validation dispatched. |
| M4 close (2026-07-04) | HS-4 | milestone-validator FAIL, finding F-1: commit `51950c26` (F4.1 Task C) accidentally tracked `docs/release/v2-name-inventory.md` (untracked `??` at session start), breaching contract §5 / CLAUDE.md "never commit `docs/release/`". All 3 feature deliverables otherwise judged sound. | **RESOLVED** — fix-feature F4.4 (`c7aebfe3`): `git rm --cached` untracked the file (kept on disk), dedicated commit + f4.4 records; net M4 diff for `docs/release/` is empty; validator re-dispatched → **PASS**. |
| M7 close (2026-07-05) | HS-1 | Two F7.4 deviations surfaced at the milestone gate: (1) §4.2 — dedicated non-owner `metaldocs_ci` role instead of literal 63-table ownership-reassignment (avoids a dev-bootstrap/janitor-breaking owner migration); (2) §4.2/§4.5 — "no-GUC → 0 rows" reconciled to wrong-GUC-blocks + owner-bypass-contrast (the ratified M3 null-GUC escape hatch, ADR 0027, makes literal 0-rows false-by-design; removing it across 34 policies breaks the leader-elected janitors). Both prove the same-or-stronger isolation property at lower blast radius; both surfaced, not patched. | **RATIFIED by operator at the M7 HS-1 gate, 2026-07-05 ("Okay approved").** Each carries a named literal-form follow-on if ever wanted. M7 sealed **passed**. |
| Terminal acceptance run 1 (2026-07-06) | HS-5 | `mission-validator` §8 terminal run **FAILED**: 3 dimensions returned DEBT rather than CONFIRMED — Dim 8 (test-discipline: `check-test-discipline.sh` RED at HEAD, 4 violations incl. one M9-F9.5-introduced stale allowlist path; + REQ-SEARCH-1/REQ-SEC-3 uncovered MUSTs unratified), Dim 9 (ops: `/metrics` served on the public API port, not isolated), Dim 10 (governance: ADR status-field budget swept only manually, no blocking CI gate). | **RESOLVED (pending HS-1 ratification)** — bounded remediation micro-milestone **M10 terminal-remediation** opened per HS-5 (milestone.md + validation-contract.md authored before code). 3 fix-features F-R1/F-R2/F-R3 closed one-per-dimension (commits `157a81bb`, `a5d3bdbf`, `0f6333cf`); milestone-validator re-ran every gate clean from scratch → **PASS**. **M10 passed — operator-approved HS-1 2026-07-06 ("Okay approved").** `mission-validator` re-dispatched for the second §8 terminal run. |
| M5 F5.2 (2026-07-04) | HS-7 | False premise in committed D4 artifacts: validation-contract §2.6, ADR 0067 (title/supersedes/§H-PRE-1/consequences), and the F5.2 spec/plan all claimed M5 "**retires H-PRE-1**". Runtime-truth verification falsified it: (a) H-PRE-1 governs authz-recording reads inside lock-holding txs — motivated by the **audit hash-chain writer's `pg_advisory_xact_lock`** (`writer.go:59`) + `authz.Require` bypass-audit in the caller's tx (`authz.go:119`), canonical instance the CD-create path; (b) the stuck-instance-watchdog's `pg_try_advisory_lock` M5 removes is an *unrelated* single-runner mutex on its **own dedicated connection**, never enclosing an authz read — it never even instantiated H-PRE-1. | **RESOLVED (pending HS-1 ratification)** — H-PRE-1 kept **LIVE** everywhere; dated errata added in place to ADR 0067 (§H-PRE-1 + header banner), validation-contract §2.6, F5.2 spec/plan; `invariant-checklist.md:58` and memory `advisory-lock-deadlock-constraint` corrected to LIVE + watchdog-lock clarifier. Watchdog-lock removal stands (River elector+queue subsume single-runner; P4 singleton proof gates it). No code semantics changed by the correction. **RATIFIED by operator at the M5 HS-1 gate, 2026-07-04 ("Approvado M5").** |

## Program close-out / reconciliation

All 10 milestones (M0–M9 planned + M10 HS-5 remediation) passed with operator HS-1 approval; terminal
acceptance met on run 2.

- [x] **Every planned feature has a complete evidence row.** M0–M9 each carry per-feature
  `spec.md`/`plan.md`/`evidence.md`; M10's 3 HS-5 fix-features (F-R1/F-R2/F-R3) likewise. Every
  milestone has a milestone-validator PASS in `<milestone>/qa/milestone-qa.md`.
- [x] **Zero unplanned scope** — every addition beyond the governing spec is recorded with rationale in
  the Hard-stops table above (F3.4/F3.5, F4.4/F4.5, F5.7/F5.8, F6.4, F7.4 deviations, and the whole of
  M10 as the HS-5 micro-milestone). Nothing added silently.
- [x] **Every bounded defer has a written trigger and an owner.** The two terminal uncovered-MUSTs are
  ratified with external triggers + module owners (REQ-SEARCH-1 → search module, reindex/DR trigger;
  REQ-SEC-3 → security module, pre-prod sign-off / pentest / audit trigger — F-R3 evidence). REQ-AUTHN-1/3
  doc-vs-runtime drift handled in M9. The HS-7 errata (M4 ADR 0066 full-unification defer, M5 H-PRE-1)
  each carry a named follow-on.
- [x] **Terminal acceptance PASSED (run 2, 2026-07-06).** Run 1 FAILED (HS-5, 3 DEBT dims) → M10 closed
  them → `mission-validator` re-run: **10/10 in-scope dimensions CONFIRMED, no new DEBT, all 6
  mission-installed CI gates green from clean with negative proof, findings 1–25 traceable, 3
  out-of-scope exclusions intact.** Verdict: [`qa/mission-validation-run2.md`](qa/mission-validation-run2.md)
  (run-1 record preserved at `qa/mission-validation.md`).
- [x] **Operator final mission sign-off:** 2026-07-13 / operator (hub session, explicit "Aprovar") — program SEALED.

> Remaining after sign-off: all program commits are **local, not pushed** (never push without explicit
> operator permission). Per the release plan, the repo is re-baselined to a fresh clean repo at v1
> (memory `f18-history-fresh-repo-at-release`), so no history purge is done here.
