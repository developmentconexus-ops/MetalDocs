# Unit 4.6 — Integration suite-runtime budget — evidence

**Status:** IN PROGRESS — infra blocker RESOLVED by hub (see §1.1); entry gate running.
**Branch:** `unit-4.6-suite-runtime` (worktree `.claude/worktrees/unit-4.6-suite-runtime`), based on `main` @ `a120254d`.
**Exit gate (unchanged):** full `go test -tags=integration ./...` ≤ 15 min wall, zero isolation regressions, suite GREEN. Doubles as the deferred full-suite validation for merged units 4.2/4.4 (operator-ratified defer), so the exit run must be on current main.

---

## 1. BLOCKER — dev postgres :5433 wedged in end-of-recovery checkpoint

Unit 4.6's entry gate requires a live DB. `metaldocs-postgres` is not reachable and does not self-heal.

**Symptom:** `pg_isready` → `rejecting connections`; `FATAL: the database system is not yet accepting connections / DETAIL: Consistent recovery state has not been yet reached.`

**Sequence (from container logs + `/proc`):**
| Fact | Evidence |
|---|---|
| Crashed once already *during* a previous recovery | `database system was shut down in recovery at 2026-07-14 16:09:52 UTC` |
| WAL redo **completes** in ~100s | `redo starts at 8/5C782AC0` (17:32:22) → `redo done at 8/70D7BC98 ... elapsed: 101.70 s` (17:34:04) |
| Redo is dominated by clone churn | repeated `WAL redo at ... for Database/DROP: dir 1663/379806` records |
| Then the checkpoint wedges | `checkpoint starting: end-of-recovery immediate wait` (17:38:39) — never completes |
| Checkpointer is blocked, not slow | `ps`: `27 Ds ... postgres: checkpointer performing end-of-recovery checkpoint`, CPU TIME `00:00:00` across 14:23 elapsed; `state: D` (uninterruptible sleep) confirmed via `/proc/27/stat` |
| Zero IO progress | `docker stats` BlockIO `192MB / 508MB` unchanged across a 45s window; container CPU 0.01% |
| **Deterministic, not a one-off** | reproduced across 2 independent restarts (17:32 start → wedge; ~18:25 restart → redo again → same wedge at 14:23 elapsed). A 15-min readiness waiter expired with PG still rejecting. |

**Ruled out:**
- **Disk full** — `/var/lib/postgresql/data` (`/dev/sde`) 1007G total, **933G avail (3% used)**; host C: 246.9GB free.
- **Docker VM IO stack broken generally** — a fresh `docker run --rm alpine df -h /` runs fine.
- **PGDATA size** — 1.3G total, 96 dirs under `base/` (orphan clones from killed runs; small).
- **Windows bind-mount on PGDATA** — PGDATA is a *named volume* (`compose_metaldocs_postgres_data` → `/dev/sde`, VM-side ext4). Only the three `db/*.sql` initdb scripts are bind-mounted.

Deeper kernel forensics (`/proc/27/stack`, `/proc/27/io`, `/proc/27/syscall`) are blocked by container hardening even as root — `Operation not permitted`.

**Assessment:** the wedge is in the Docker Desktop VM's fsync/disk layer for this specific volume, triggered by an end-of-recovery checkpoint over a PGDATA carrying ~96 orphan clone databases. It is a restart→redo→wedge loop and will not clear on its own.

**Shared infra = hub owns it** (HARNESS §2: "the hub owns shared infra; chips never take it"; §9.3a). This unit took no corrective action on :5433 and will not.

**Smallest unblock asks, ranked (hub/operator decision):**
1. **Restart Docker Desktop / the VM** — clears a wedged VM disk layer. Non-destructive. Try first.
2. **If it re-wedges: recreate the postgres volume** — `docker compose down` + `docker volume rm compose_metaldocs_postgres_data` + `up -d postgres`. The compose already mounts `db/prerequisites/0001_extensions.sql`, `db/baseline/0001_current_schema.sql`, `db/reference-data/0001_product_reference_data.sql` as `/docker-entrypoint-initdb.d/*`, so initdb re-bootstraps automatically, and the testdb template rebuilds on first `Open` (content-addressed, `testdb/db.go:143`). Cost = dev row data only (re-seedable); test DBs are disposable by design. **Destructive → needs explicit operator/hub OK.**

### 1.0 Motivation — this unit's target pathology caused the outage

The WAL being replayed is dominated by `Database/DROP` records. The per-test `CREATE DATABASE ... TEMPLATE` / `DROP DATABASE` churn that unit 4.6 exists to eliminate is exactly what produced the un-replayable WAL volume and the 96-directory PGDATA that the checkpoint choked on. Every killed suite run leaves orphan clone directories behind; they accumulate, and the next unclean shutdown has to replay a DROP-heavy WAL over a PGDATA carrying all of them.

**This reframes the unit.** The charter scopes 4.6 as a *runtime budget* (~50 min → ≤15 min). The evidence says the current isolation design is not merely slow on this platform — **it destabilises the dev database**, and did so today, costing the dev data volume. The ≤15-min target is the acceptance criterion; removing a live availability hazard is the actual motivation. Accepted by hub as evidence and recorded here at hub's request (operator-visible).

### 1.1 RESOLVED (hub, 2026-07-14)

Ask 1 (Docker Desktop / VM restart) **FAILED** — the checkpointer re-wedged in D-state on the same volume within minutes, deterministic as predicted. Hub escalated to ask 2 with operator concurrence: `compose_metaldocs_postgres_data` **deleted** and postgres recreated; initdb re-bootstrapped from the mounted `db/*.sql` scripts. Wedge class eliminated (new PGDATA, zero orphan clone DBs).

Unit-side verification before resuming:

| Check | Result |
|---|---|
| `pg_isready` | `accepting connections`; container `Up (healthy)`, `0.0.0.0:5433->5432/tcp` |
| Tables in `public` + `metaldocs` | 69 |
| `metaldocs.tenants` | 1 (System Tenant — reference data seeded) |
| `metaldocs.role_capabilities` | 112 |
| `schema_migrations` | 54 (baseline ledger) |
| Orphan clone DBs (`metaldocs_test_%`) | **0** |

Consequence for this unit: the content-addressed template must rebuild from scratch on first `Open`, so the first package startup pays the full template build. Expected and accounted for in the entry-gate numbers (§3).

---

## 2. Scope finding — ROADMAP lever (3) was dropped from the dispatch prompt → **RULED IN**

ROADMAP row 4.6's ranked plan lists **(3) Linux-native/tmpfs PGDATA for the disposable test stack (never a Windows bind-mount; separate compose profile)**. The dispatch prompt's ranked plan renumbered 1–8 and **omitted it** (its (3) is the lock-free readiness fast-path, ROADMAP's (4)).

**Hub ruling: IN.** The dispatch prompt was in error; ROADMAP row 4.6 is the charter and lists the lever explicitly. §1's wedge independently proves materiality — a disposable VM-native/tmpfs test postgres makes this failure class impossible. Constraints attached to the ruling, binding on implementation:
- separate compose **profile/service** for the disposable test stack;
- **never** touch the persistent dev postgres definition;
- **no Windows bind-mount** for PGDATA.

---

## 3. ENTRY GATE — RUN, PASSED, and it INVERTS the charter's premise

Instrumentation: `tests/integration/testdb/trace.go` + phase timing in `Open` (commit `db6a6b9d`). Env-gated on `METALDOCS_TESTDB_TRACE=1`, emitting `TESTDB_PHASE\t<test>\t<phase>\t<seconds>`. `dropDatabase` split into `terminateBackends` + `dropDatabaseOnly` so the last two phases time independently.

Subject substitution: the charter names `tests/integration/approval`, but that package **does not compile on main** (§9 — unit 4.2's debt, escalated). Substituted `tests/integration/tenantdata`, which is the one package with both numbers already published in the charter's own diagnosis (14.6s solo / 4 clones vs 356s in the full run) — so it splits storage-cost vs contention at least as well. Approval's gate will still be run once the break is resolved.

Command (through the fail-loud chokepoint): `scripts/test-integration.ps1 -Package ./tests/integration/tenantdata/... -GoFlags -v,-count=1,-p=1,-parallel=1`.

### 3.1 Measurements

**Template size: 11 MB.** (`metaldocs_test_template_f3a6738db7911839`.)

Run 1 — **cold** (fresh volume from §1.1, template absent). Per-phase seconds:

| Test | ensure_template | create_database | first_ping | terminate_backends | drop_database |
|---|---|---|---|---|---|
| TestTenantDataPortCoverage | **62.54** | 1.49 | 0.009 | 0.007 | **42.38** |
| …_SyntheticGapGoesRed | 0.00 | 0.34 | 0.008 | 0.007 | 0.49 |
| …_TablesNonEmpty | 0.00 | 0.27 | 0.007 | 0.008 | 0.73 |
| …_ModuleNamesUnique | 0.00 | 0.31 | 0.010 | 0.007 | 1.36 |

Package wall 117.6s. Test 1 alone = 106.45s, and `62.54 + 1.49 + 42.38 ≈ 106.4` — **the phases account for ~99% of it. The test's own work is ~0.1s.**

Runs 3 & 4 — **warm** (template present), the steady state that the suite actually pays:

| Test | ensure_template | create_database | first_ping | terminate_backends | drop_database |
|---|---|---|---|---|---|
| TestTenantDataPortCoverage | 0.017 | 0.14 | 0.008 | 0.007 | 0.37 |
| …_SyntheticGapGoesRed | 0.000 | 0.19 | 0.008 | 0.008 | 0.45 |
| …_TablesNonEmpty | 0.000 | 0.24 | 0.019 | 0.007 | 0.52 |
| …_ModuleNamesUnique | 0.000 | 0.51 | 0.009 | 0.009 | 0.83 |

Package wall **18.9s / 27.3s** (`go test` reported 11.2s / 20.7s). Consistent with the charter's published 14.6s solo — which was evidently a warm-template measurement.

### 3.2 Verdict — the charter's dominant-cost claim is WRONG

The charter states: *"dominant cost = per-test physical CREATE DATABASE clones … amplified by contention"*, and instructs that this not be re-derived. The entry gate exists precisely to test that split, and it comes out the other way:

| Term | Measured | Charter's premise |
|---|---|---|
| Physical clone (`CREATE DATABASE ... TEMPLATE`) | **0.14–0.51s** warm | "dominant cost" |
| Steady-state DROP | 0.37–0.83s | not called out |
| Template build | 62.5s **once per fingerprint** | acknowledged, amortized |
| tenantdata solo, warm | **~11–27s** | 14.6s ✓ |
| tenantdata in full run | **356s** | 356s ✓ |

Clone + drop together cost **~1s per test solo** against an 11 MB template. Four clones = ~3.3s of tenantdata's ~11s. That cannot produce 356s. The **~24× gap between solo and full-run is contention, and contention is essentially the entire cost.**

**This does not reorder the levers — it re-grounds them.** Levers 1/3/4 remain correct and top-ranked, but the reason changes, and the reason determines the design:

- We are **not** removing clones because a clone is expensive (it is ~0.3s). We are removing them because **`CREATE`/`DROP DATABASE` serialize catastrophically under concurrency** — they take cluster-wide locks and `DROP DATABASE` forces a checkpoint. The pathology is the *concurrent* behaviour, not the unit cost.
- Consequence for **lever 2**: adding `-p=4 -parallel=8` *before* lever 1 would multiply the contending operation, not divide the wall time. The charter's "ONLY after lever 1" ordering is confirmed by measurement, not just prudence.
- Consequence for **lever 7** (explicit `STRATEGY = WAL_LOG` as a regression guard): now clearly right for the stated reason (0 saving, cheap guard). An 11 MB template is far below any threshold where `FILE_COPY` would pay.

### 3.2a Contention curve — **lever 2's `-p=4` is a REGRESSION, measured**

The charter prescribes "explicit `-p=4 -parallel=8` in `scripts/test-integration.ps1`". Before implementing that, I measured it. Same 6 clone-using packages (`tenantdata`, `migrations`, `security`, `documents`, `audit`, `templates`), warm template, `-count=1`, only `-p` varying. Artifacts: `.artifacts/contention-p{16,4,1}.log`.

| `-p` | Wall | vs best |
|---|---|---|
| **16** (= NumCPU, today's implicit default) | **125.0s** | — |
| 4 (charter's prescription) | 181.5s | **+45%** |
| 1 | 215.7s | +73% |

Per-package, same runs:

| Package | `-p=1` | `-p=4` | `-p=16` |
|---|---|---|---|
| tenantdata | 12.0s | 40.8s | 52.0s |
| migrations | 96.3s | 171.7s | 115.3s |
| security | 13.9s | 44.0s | 53.1s |
| documents | 34.3s | 73.3s | 60.5s |
| audit | 16.4s | 41.3s | 52.9s |
| templates | 10.7s | 29.2s | 46.8s |

**Two facts, and they point the same way:**

1. **Contention is real and large** — tenantdata inflates 12.0s → 52.0s (4.3×) purely from concurrent packages. This confirms §3.2's diagnosis.
2. **But throttling concurrency makes the wall clock WORSE, not better.** Overlap repays the per-package inflation several times over. `-p=1` pays no contention at all and is the *slowest* configuration by 73%.

**So the charter's `-p=4` must not be implemented as written — it would regress the suite ~45%.** The runner passing no `-p` today is not an oversight to correct; `go test` already defaults `-p` to NumCPU (16 here), which is near-optimal for wall clock.

**Where the win actually is.** At high `-p`, wall ≈ **the slowest package** (migrations 115.3s ≈ the 125.0s wall). Cross-package parallelism is already saturated, so the only remaining lever is **making individual packages faster** — i.e. removing per-test clone+drop (lever 1) and then adding *within*-package parallelism via `t.Parallel` + `-parallel`, which lever 1 is the precondition for. The charter's "lever 2 ONLY after lever 1" ordering survives; its `-p=4` half does not.

**Revised lever 2 spec (proposed, hub to ratify):** keep `-p` at the default (do not pin it to 4); add `t.Parallel()` to reset-safe tests and set `-parallel` explicitly, after lever 1 lands. Re-measure the `-p` curve after lever 1, since removing the clone/drop contention changes the optimum.

**Caveat on this data — stated, not buried:** one sample per configuration. The wall-clock ordering is monotonic and the p=16-vs-p=1 gap (125.0 vs 215.7) is far outside plausible noise, so the conclusion "do not throttle to `-p=4`" is safe. But per-package numbers are noisy (migrations reads 96.3 / 171.7 / 115.3 — non-monotonic), so no per-package claim should be drawn from this table beyond the tenantdata inflation, which the §3.1 solo baseline independently corroborates.

### 3.3 The first DROP took 42s — and it is the wedge mechanism

`drop_database` = **42.38s** on test 1, then 0.37–1.36s on every subsequent test. That is not size (every clone is the same 11 MB). `DROP DATABASE` **forces a checkpoint**; the first drop after a 62.5s template build has to flush that build's entire dirty-buffer backlog.

This is the direct mechanistic link to §1: the operation that wedged the cluster in uninterruptible D-state *is the checkpoint that per-test `DROP DATABASE` forces*. The suite does not merely leave orphan databases behind — **it forces a checkpoint per test**, and on this platform's disk layer that is the operation that hangs. Lever 4 (stop per-test DROP; drop at lease end) is therefore not a minor optimization; it removes the outage mechanism. Recorded per hub's request that §1 be folded in as motivation.

---

## 4. Isolation classification — COMPLETE (read-only, no DB required)

Binding isolation contract for lever 1. **163 files call `testdb.Open`.** Classified by 7 sonnet investigator dispatches (§7) against 7 fresh-clone criteria: (1) DDL, (2) DB/session-default settings, (3) extension state, (4) deliberate cross-connection/cross-session, (5) sequence/identity assertions, (6) global/singleton state TRUNCATE won't restore, (7) goroutine storms.

### 4.1 Verdict counts (by scope)

| Scope | RESET-SAFE | FRESH-CLONE | STORM | Uncertain |
|---|---|---|---|---|
| `internal/modules/approval/**` | 17 | 3 | 0 | 1 |
| `internal/modules/documents/**` + `controlleddocuments/**` | 13 | 9 | 0 | 2 (classes) |
| `internal/modules/{taxonomy,templates,search,notifications,iam,tokens,distribution,render}/**` | 15 | 6 | 1 | 1 |
| `internal/platform/**` + `internal/modules/jobs/**` | 11 | 2 | 0 | 0 |
| `tests/integration/{approval,migrations,controlleddocuments}` | 23 | 1 | 0 | 2 |
| `tests/integration/{templates,security,scenarios,documents,tenantdata,iam,audit,testdb}` | 9 | 3 | 1 | 3 |

### 4.2 MUST keep `OpenFreshDatabase` — and why

| File | Criterion |
|---|---|
| `internal/modules/iam/authz/seed_tx_tenant_rls_integration_test.go` | 2/4 — `CREATE ROLE ... NOBYPASSRLS` :60-73, `SET ROLE`/`RESET ROLE` :84-135 |
| `internal/modules/templates/infrastructure/tenant_id_rls_integration_test.go` | 2/4 — `CREATE ROLE rls_tester_tpl_version NOLOGIN NOBYPASSRLS` :129, `SET ROLE` :146 |
| `tests/integration/security/rls_truth_test.go` | 4 — `testdb.OpenAsCIRole(t, dbName)` :99 (second connection as non-superuser role; the RLS-truth proof) |
| `tests/integration/scenarios/trigger_bypass_test.go` | 4 — `OpenAsCIRole` :21 (file-bundled with a reset-safe sibling) |
| `tests/integration/iam/migration_0170_test.go` | 2 — session-level (`is_local=false`) `metaldocs.bypass_authz` on a pooled conn :138-155; leak on mid-run failure = silently disabled authz = false green |
| `tests/integration/migrations/migration_0302_test.go` | 1 — real DDL: `CREATE TABLE`, re-executes the committed migration file, asserts `DROP TABLE` :56-104 |
| `internal/modules/distribution/infrastructure/coverage_repository_integration_test.go` | 1 — `DROP VIEW`/`CREATE VIEW metaldocs.v_cd_obligated_readers` :431-444 |
| `internal/modules/render/fanout/dispatchjobs/dispatch_integration_test.go` | 6 — untenanted `SELECT count(*) FROM river_job WHERE kind=$1` :110-119 asserted `==1` |
| `internal/platform/jobs/river/{client_test.go, singleton_integration_test.go}` | 1 — `CREATE SCHEMA` + `rivermigrate.Migrate` + `DROP SCHEMA CASCADE` |
| `tests/integration/testdb/template_bounded_test.go` | SPECIAL — drives `sweepStaleTemplates` via raw admin-DB `CREATE/DROP DATABASE`; never calls `Open`; must keep the physical path |
| 9 files using `ALTER DATABASE "<db>" SET search_path` | 2 — see §4.4 |
| 5 files using `testdb.SetCapsOnDB` | 2 — session (`is_local=false`) `set_config` on a pooled conn (`fixtures.go:163-166` self-documents "Safe only for isolated per-test databases") |

### 4.3 STORM-SPLIT — real contention that MUST NOT be serialized

- **`tests/integration/scenarios/concurrency_test.go`** — does **not** use `testdb.Open`; it calls its own `openDirectDB(t)` (:452) → `sql.Open` straight at the shared DSN. So the "mixes clones with storms" problem is at *package* level (siblings `membership_fn_test.go` / `trigger_bypass_test.go` do clone), not within this file. Must preserve: OCC race (`runSingleOCCRace` :69-152, N goroutines on a shared pool released by a `close(start)` barrier, each in its own tx racing `UPDATE ... WHERE revision_version = $3`); `SELECT ... FOR UPDATE SKIP LOCKED` exclusion across concurrently-open txs (:154-305); concurrent duplicate `INSERT` asserting exactly one `23505` (:307-450). Forcing `MaxOpenConns(1)` or serializing turns all three **false-green**.
- **`tests/integration/documents/concurrent_revision_test.go`** — genuine `pg_advisory_xact_lock` contention, 8 workers; comment :155-157 explicitly forbids `SetMaxOpenConns(1)`.
- **`internal/modules/notifications/infrastructure/fanout_worker_race_integration_test.go`** — two goroutines racing `worker.Work` :134-144.

### 4.4 Design-decisive finding — the hazard is the *handle*, not the *database*

Three independent investigators converged on this. The dominant FRESH-CLONE driver is **not** genuine isolation need — it is per-test mutation of shared `*sql.DB` / DB-level state that is only safe today *because* every test owns a private DB:

- **`ALTER DATABASE "<dbName>" SET search_path TO public, metaldocs`** — 9 files (approval ×2, documents ×4, templates ×3). Every site sets the **same** value; none *tests* search_path. Pure setup boilerplate compensating for the clone's default.
- **`db.SetMaxOpenConns(1)`** — pervasive (~15+ files). Pool-wide; on a shared handle one test clamps every later test in the package.
- **`testdb.SetCapsOnDB(...)`** — session-level `set_config` on a pooled connection; leaks to whatever test next draws that connection.

**Consequence for the design:** if `Open(t)` returns a *shared package handle*, ~20 files stay on the slow clone path for reasons that are artifacts, not requirements. If instead **the factory owns the handle** — `Open(t)` returns a **per-test `*sql.DB` (own pool) onto the package-leased database**, with `search_path` supplied via the **DSN connect parameter** rather than `ALTER DATABASE`, and the pool closed at test end — then all three hazard classes evaporate: `SetMaxOpenConns` becomes test-local, session GUCs die with the pool, and the `ALTER DATABASE` sites can be deleted rather than accommodated.

This is the difference between the decisive lever converting ~130 files or ~110. It requires touching those 9 test files to drop their `ALTER DATABASE` line — **a scope question for the hub**, since the charter's hard rule ("a test that mutates schema or session/DB defaults must keep fresh-clone semantics") would otherwise pin them to clones on a technicality, when the correct fix is to stop the boilerplate mutation at the factory seam. Not absorbed unilaterally.

### 4.5 Prior CORRECTED — `tests/integration/migrations` does **not** keep clones wholesale

The charter states "migrations pkg keeps clones". Evidence says otherwise: **only 1 of 7 files** (`migration_0302_test.go`) does DDL in-test. The other 6 never execute DDL — every migration's schema effect is already baked into the template once at `buildTemplate` (`testdb/db.go:224-252`); the tests only read `information_schema`/`pg_indexes` or exercise triggers/constraints via rows. `0154`/`0169` are gated only on the reference-data question (§4.6). **The prior is inverted: 6 reset-safe + 1 fresh-clone.**

### 4.6 TRUNCATE reset set — derived, viable

64 tables total (33 `metaldocs`, 25 `public`, 6 River), derived from the bundle in `curatedBundlePaths()` order.

- **Partition A (TRUNCATE): 58 tables** — including all 5 River runtime tables. Verified: no River up-migration inserts rows into `river_job`/`river_leader`/`river_queue`/`river_client`/`river_client_queue`; `MigrateRiverSchema` (`bootstrap/jobs.go:83-94`) is pure DDL. A fresh clone has them empty.
- **Partition B (DO NOT truncate): 6 tables** — `metaldocs.tenants` (System Tenant, reference-data:72-77), `metaldocs.role_capabilities` (≤112 grants, :80-223), `public.templates_template` + `public.templates_template_version` (system blank template, :264-322), `public.schema_migrations` (~101 ledger rows), `river_migration` (River's own ledger).
- **FK hazard — cleared.** TRUNCATE CASCADE propagates parent→child only. Every Partition-A→Partition-B FK is child→parent (safe). Verified `metaldocs.tenants`, `role_capabilities`, `schema_migrations` have **zero outbound FKs**; the templates pair references only each other + `tenants`. **Partition B is a closed cluster — no CASCADE can reach it.** A single `TRUNCATE <58 tables> RESTART IDENTITY CASCADE` is the correct shape; no manual ordering needed.
- **Recommended: derive the list at runtime** from `pg_tables` (schemas `public`,`metaldocs`) MINUS the 6-item exclusion set — self-heals against schema drift, exactly as `curatedBundlePaths()` self-heals the fingerprint (`db.go:449-453`). A hand-maintained list would reproduce the very drift class that produced the stale `metaldocsOwnedObjects` entries (§5). Plus a post-reset row-count invariant (`tenants == 1`, `templates_template == 1`) failing **loud** — no-fallback principle.
- **Verdict: viable, no blockers.**

### 4.7 UNCERTAIN — carried, not guessed

- `internal/modules/render/fanout/retention/retention_integration_test.go` — `PurgeDispatched` purges **globally** (no tenant filter) and asserts exact whole-table counts (:208, :318-330). Reset-safe **only if** `pdf_dispatch_outbox`/`materialize_dispatch_outbox` are in the truncate set (§4.6 puts them in Partition A → resolves to RESET-SAFE, to be re-confirmed against the implemented list).
- `tests/integration/migrations/migration_0154_test.go`, `migration_0169_test.go` — assert specific `role_capabilities` reference rows. Safe under §4.6's exclusion set; RED if `role_capabilities` were ever truncated without re-seed.
- `internal/modules/approval/jobs/scheduled_publish_job_test.go` — two investigators split (FRESH vs RESET) purely on the `SetMaxOpenConns(1)` + session `search_path` hazard. **Resolved by §4.4's per-test-handle design**; re-confirm after implementation.
- `tests/integration/approval/eligibility_test.go` — does **not** use the factory at all; `sql.Open` direct against the ambient DB with a hardcoded dev tenant, mutating shared `governance_events` (:94-108). Pre-existing shared-state test, outside the split. Candidate `ESCALATION` (out-of-scope debt), not silently reclassified.
- `tests/integration/security/{rls_truth,tenant_crypto}_test.go` and `scenarios/{trigger_bypass,membership_fn}_test.go` — lease granularity is **per package**, so a package containing one FRESH-CLONE test may force its reset-safe siblings onto the clone path unless split by file. To settle at implementation.

### 4.8 Secondary check — real-sleep audit (charter: secondary only). Confirmed secondary.

- `tests/integration/approval` (1471s, the worst package): **zero** `time.Sleep|time.After|Ticker|Eventually|WaitFor` hits. Runtime is ~61 `testdb.Open` sites → up to 61 clones. 100% clone cost.
- `internal/platform/idempotency` (1041s): 3 sleeps totalling **~400ms/run** (`middleware_concurrency_test.go:32`, `two_phase_test.go:228,289`) — deliberate held-lock delays in race tests, not polling. Negligible vs 1041s.
- `tests/integration/tenantdata`: exactly **4 tests, 4 clones**, trivial catalog work — the 14.6s→356s blowup is ~100% clone + contention.

**The original sleep-wait hypothesis is dead; the clone/contention diagnosis is confirmed.**

---

## 5. Micro-debt — `metaldocsOwnedObjects` drift (ROADMAP register)

- `tests/integration/testdb/db.go:422` — `grant_area_membership`: a **function**, not a table, dropped by `db/migrations/0307`. Stale. **Scheduled for removal with the factory work** (as the ROADMAP register requires).
- **NEW drift found (not previously registered):** `db.go` maps `governance_events` to schema `metaldocs`, but the DDL creates **`public.governance_events`** (baseline:2268, :3081); zero occurrences of `metaldocs.governance_events` across `db/baseline` + all migrations. `runtimeSchemaName("governance_events")` therefore returns the wrong schema. Currently **dormant** (no caller uses `Qualified(_, "governance_events")` — grep clean), so latent, not live-breaking. Will be fixed alongside the stale entry and reported at close.

---

## 6. Files changed so far

None (source untouched). This evidence file only. No factory change will precede the entry gate (§3).

---

## 7. Dispatch ledger (HARNESS §4 obligation 5)

| Slice | Agent | Model | Verdict |
|---|---|---|---|
| Inventory attempt #1 (aborted) | general-purpose | sonnet | Backgrounded nested children → misrouted per HARNESS §2; abandoned, re-dispatched sync. No output used. |
| Classify `internal/modules/approval/**` | general-purpose | sonnet | Delivered — 17 reset-safe / 3 fresh / 1 uncertain |
| Classify `documents/**` + `controlleddocuments/**` | general-purpose | sonnet | Delivered — 13 / 9; surfaced the `SetCapsOnDB` + `SetMaxOpenConns` handle hazard |
| Classify 8 remaining modules | general-purpose | sonnet | Delivered — 15 / 6 / 1 storm / 1 uncertain |
| Classify `internal/platform/**` + `jobs/**` | general-purpose | sonnet | Delivered — 11 / 2; **corrected the river deletion premise** (§8) |
| Classify `tests/integration/{approval,migrations,controlleddocuments}` | general-purpose | sonnet | Delivered — 23 / 1 / 2 uncertain; **inverted the migrations prior** (§4.5) |
| Classify `tests/integration/` remainder + `testdb` pkg | general-purpose | sonnet | Delivered — 9 / 3 / 1 storm; storm-preservation contract (§4.3) |
| Derive TRUNCATE reset set from `db/**` | general-purpose | sonnet | Delivered — §4.6; found the second `metaldocsOwnedObjects` drift |

Reviewer dispatches: none yet (no code written). Dual-reviewer parity (Claude + hub-side GPT) applies at the final gate per HARNESS §4 obligation 2; codex is **not** attempted unit-side (sandbox broken machine-wide).

---

## 8. Charter premise CORRECTED — lever 5 is not a 2-line delete

Charter: "Delete the 2 redundant `rivermigrate.Migrate` calls (~273s); template already bakes River schema at `db.go:491`."

The premise is right; the prescription is not. Both tests deliberately migrate into a **uniquely-named private schema** (`river_task1_<ts>` at `client_test.go:41`; `river_h_pre_1_<ts>` at `singleton_integration_test.go:68`) — **not** the default schema the template baked. Deleting only the `Migrate()` call while still creating that empty schema breaks the test with `42P01 relation "river_job" does not exist`.

Neither test validates River migration itself — no assertion on migration version, idempotent re-run, or applied-migrations table. `client_test.go` proves `InsertTx` transactional visibility; `singleton_integration_test.go` proves exactly-once dequeue across two clients (the H-PRE-1 retirement proof). So **no coverage is lost by removal.**

Correct change (per test, ~3 lines): drop `CREATE SCHEMA` + `DROP SCHEMA ... CASCADE` cleanup, drop `Migrate()`, and set `riverjobs.Config{Schema: ""}` to use the already-migrated default schema. Safe because both files are FRESH-CLONE (own physical DB), so the private-schema indirection is no longer load-bearing for isolation.

---

## 9. PRE-EXISTING COMPILE BREAK ON MAIN — unit 4.2's deferred-validation debt

`tests/integration/approval` **does not compile on main**:

```
tests\integration\approval\route_preview_parity_integration_test.go:203:29:
cannot use previewTemplateVersionReader{} as application.TemplateVersionReader value ...
previewTemplateVersionReader does not implement
"metaldocs/internal/modules/approval/application".TemplateVersionReader
(missing method LoadTemplateInboxMeta)
```

**Attribution — not this unit's:** reproduced at the repo root on main via `go vet -tags=integration ./tests/integration/approval/...` (exit 1). This unit's diff touches 3 files (`docs/superpowers/reports/2026-07-14-unit-4.6-evidence.md`, `scripts/test-integration.ps1`, `tests/integration/testdb/db.go`, + new `tests/integration/testdb/trace.go`) — none under `tests/integration/approval`. Zero overlap.

**Root cause:** unit 4.2 added `LoadTemplateInboxMeta` to the `TemplateVersionReader` interface (`internal/modules/approval/application/template_submit_service.go:55`) and updated its own double (`fakeTemplateInboxMetaReader`, `read_service_worklist_subject_generic_integration_test.go:43`, implements all three methods) but missed the second, out-of-module double in `tests/integration/approval`.

**Why it escaped:** a compile break in an `integration`-tagged test package is invisible to `go build ./...` and to `go test ./...` without `-tags=integration`. That is exactly why 4.2/4.4's full-suite validation was deferred to this unit's exit gate. **The defer worked as designed; this is its first casualty** — and a real finding about the defer, not a nit: 4.2 closed shipping a non-compiling integration package.

**Blast radius — bounded:** full `go vet -tags=integration ./...` on main reports **exactly one** broken package. Everything else in the integration tree compiles.

**Impact on 4.6 — dual:** it blocks the charter-designated entry-gate package (worked around per §3), and it **hard-blocks the exit gate** regardless, since the exit criterion is a GREEN full suite on current main. Escalated to hub with a proposed ~5-line fix mirroring 4.2's own canonical double; ownership decision (repair here as a 4.6 prerequisite vs. dispatch back to 4.2) is hub's. Not touching another unit's merged code unilaterally.

---

## 10. Measurement-integrity note — this unit's own first run was a false green

Run 2 of the entry gate reported `ok metaldocs/tests/integration/tenantdata (cached)` in 1.5s, replaying run 1's phase lines **verbatim** — identical to four decimal places. Cause: the runner was invoked as `pwsh -File script.ps1 -GoFlags $flags`, and `-File` flattens array arguments on the command line, so only the first element (`-v`) bound to `-GoFlags`; `-count=1`, `-p=1`, `-parallel=1` were silently dropped. Without `-count=1`, `go test` served its cache.

Caught by reading the emitted `go test` line (`go test -tags=integration -v ./...` — flags visibly missing) rather than trusting the PASS. Fixed by invoking the runner with the `&` call operator, which preserves array binding; re-run confirms `go test -tags=integration -v -count=1 -p=1 -parallel=1 ./...` and uncached timings. Both warm runs are from the corrected invocation.

Recorded because it is the same class of defect the runner itself exists to prevent (§ `scripts/test-integration.ps1` header: a silent skip reads as green) — and because a cached replay of a *timing* measurement is indistinguishable from a real one unless the command line is checked.

---

## 11. Next actions

1. ~~Hub/operator resolves §1 (:5433)~~ — **done** (§1.1), verified.
2. ~~Hub rules on the two scope questions~~ — **done**: tmpfs lever **IN** (§2); factory-owns-the-handle **APPROVED**, charter hard rule amended (§4.4).
3. ~~Run the entry gate~~ — **done** (§3). Premise inverted: contention dominates, clone unit-cost does not.
4. Hub rules on §9 ownership (4.6 repairs vs 4.2 re-dispatch). Exit gate cannot go green until it lands.
5. Levers in ranked order, per-lever before/after wall times recorded here. Lever 2 strictly after lever 1 — now confirmed by measurement (§3.2), not just prudence.
6. Re-run the entry gate on `tests/integration/approval` once §9 clears, since the charter names it.
