# Unit 3.2 — M4 ActorSelector — evidence ledger (living)

**Branch:** `unit-3.2-actor-selector` (worktree `.claude/worktrees/unit-3.2`)
**Started:** 2026-07-13 · **Budget:** ≤400k main-session
**Design:** `docs/superpowers/specs/2026-07-13-unit-3.2-actor-selector-design.md`
**Gate (P0):** `docs/superpowers/analysis/2026-07-13-unit-3.2-actor-selector-system-impact.md` — 🟡 Yellow (ADR 0084 + backfill parity carried)

## Integration RED baseline (accepted, from 3.1a evidence)
Zero NEW failures beyond the 9-accepted baseline. Known flaky: `TestConcurrencyScenarios/OCC_Race_N50`
(green isolated). Runner: `pwsh -File scripts/test-integration.ps1` with `GOFLAGS=-count=1`.

## Commit range
- `5c5abbd8` docs(analysis): system-impact gate — Yellow
- `04d8a800` docs(spec): unit design (9-slice plan, ADR 0084)

## Dispatch ledger (per slice: implementer · reviewer · verdict)
| Slice | Task | Implementer | Reviewer | Verdict | Commit |
|---|---|---|---|---|---|
| 1 Domain | #1 | sonnet adbe242f | sonnet a41a9c3b | APPROVE (+legacy-bridge refinement, tree green) | e214cb1a |
| 2 DB+backfill | #2 | sonnet aae34b14 | main-session review (independent; impl≠rev on prod diff) | APPROVE (prod clean; reviewer fixed 2 test-fixture defects; full pkg green 850s) | a34e9ea7 |
| 3 Resolver+parity | #3 | sonnet a74bfae9 | main-session review (independent) | APPROVE (prod clean; reviewer fixed 1 test-fixture role; full pkg green 618s) | 582568e2 |
| 4 Route contract | #4 | sonnet a96fd1eb | main-session review (independent) | APPROVE (prod clean; build+api-lint 0; full pkg green 400s) | 09f29ebc |
| 5 Submit choice | #5 | sonnet (cut off mid-slice by session limit; impl+unit done) | main-session review (independent; reviewer authored missing real-DB tests) | APPROVE (prod clean; build/vet/api-lint 0; regen verified idempotent both modules; full pkg green 565s) | 548e9925 |
| 6a Instance snapshot + drift-rewire | #1 | sonnet a1594c97 | main-session review (independent; reviewer authored real-DB round-trip + backfill-parity tests) | APPROVE (prod clean; build/vet 0; full pkg green 488s) | b362f1c8 |
| 6b Drop flat route cols | #5 | sonnet a616e245 | main-session review (independent; verified un-spec'd loadRouteStagesTx selector-load + canonicalSelectors idemp rework, migration 0305, wire-stable synthesis relocation) | APPROVE (prod clean; build/vet/unit 0; api.gen.go/openapi/contracts untouched; full pkg green 627s) | 9b87d3f5 |
| 7a Wire contract extermination | #2 | sonnet ac66f432 | main-session review (independent, Opus; verified openapi/api.gen.go/contracts flat-field removal + minItems:1 + ADR 0022 validateAreaRole re-home onto sel.Role for the 3 role-kinds excl. named_user + 422 empty-selectors guard; reviewer repaired a 6b straggler `template_submit_service_integration_test.go` + exterminated 2 dead infra.RouteStage flat fields + 3 vestigial test fixtures that 7a's non-integration-tag gate missed) | APPROVE (prod clean; build/vet plain+`-tags=integration` 0; api-lint 0; full pkg green; integration 581s) | 60fe4e45 |
| 7b FE builder | #2 | sonnet a708025b | main-session review (independent, Opus; verified routeDraft SelectorDraft round-trip + per-kind toWireSelector normalization parity w/ backend mapSelectorsFromWire + validateDraft per-kind rules + StageCard builder CRUD/reorder/per-kind-fields + useUsersQuery named_user picker + optimistic-cache mapper fix; re-ran gates) | APPROVE (impl fixed 3 extra compile-forced consumers: useRouteAdminMutations + 2 test files; FE api-types regen catches slices 4/5/7a; tsc clean; vitest 225 pass/29 files) | (pending commit) |
| 8 FE picker | #8 | — | — | pending | — |
| 9 ADR+wiki+ladder | #9 | — | — | pending | — |

## Verification ladder (P4)
| Level | Command | Result |
|---|---|---|
| L0 build/lint | `go build ./...` · tsc · api-lint · module-boundaries | go build/vet/api-lint ✓ (thru 7a) · FE `tsc --noEmit -p tsconfig.build.json` clean (7b) |
| L1 unit+integration | approval+templates unit PASS · integration `./tests/integration/approval/...` PASS (slice2 850s, slice3 618s, slice4 400s, slice5 565s, slice6a 488s, slice6b 627s, slice7a 581s; `-timeout=1800s`) | slice 2/3/4/5/6a/6b/7a ✓ |
| L2 :80 stack | gateway smoke | pending |
| L3 browser QA | mixed-selector route + submit picker, doc AND template | pending |

## Slice 2 review notes
Production diff (migration 0303, `route_admin_service.go` insertRouteStageSelectors,
`postgres_approval_repository.go` LoadRoute selector read path) reviewed independently
by main session — APPROVE. Write path uses `INSERT ... SELECT ... JOIN` correlating
selectors to just-inserted stage ids by (route_id, stage_order) — no RETURNING
round-trip. Read path batch-loads via `pq.Array` + `= ANY($1)` ordered by
selector_order — no N+1. Migration is fail-closed (end-of-migration DO block RAISE
EXCEPTION if any stage lacks a selector), idempotent (WHERE NOT EXISTS), correct
schema_migrations ledger row.

Two **test-fixture** defects the implementer's own run never caught (it paused on a
slow monitor before verifying) — fixed by reviewer, production code untouched:
1. `WithRole("qms_admin")` → `chk_iam_user_roles_role_code` violation; valid set is
   {system_admin,approver,author,editor,viewer}. Changed to `system_admin` (matches
   sibling `route_admin_service_subject_integration_test.go`, carries CapRouteManage).
2. BackfillCoverage test asserted a **false property**: a stage seeded at test time
   postdates 0303's one-time backfill, so it can never be covered by it. Rewrote to
   execute 0303's verbatim backfill INSERT against the bare stage + assert idempotency
   — the property that actually validates the migration SQL run on real deploys.

Targeted `-run Selector|RouteStageSelector` → PASS (57s). Full-package run pending
(with raised `-timeout`; default 600s cap tipped by the 3 new tests, infra not logic).

## Slice 3 review notes
`ResolveEligibleActorsForSelectors(tenantID, selectors, subjectArea)` unions
per-selector pools + dedups + sorts ascending (via pure `unionDedupSort`,
unit-tested no-DB). named_user → tenant-active check (`isActiveUser` on
v_active_user_areas), else contributes nothing (not an error). role_in_fixed_area
→ delegates to existing flat resolver with sel.AreaCode. role_in_document_area →
delegates with subjectArea. submit_choice → no snapshot pool (slice 5). Drift path
(decision/review) UNTOUCHED — still flat resolver on snapshot columns. Both submit
paths wired via `stage.EffectiveSelectors()`; for existing/backfilled routes the
bridge yields role_in_fixed_area(stage.AreaCode) so resolution is byte-identical
as a SET (sort is a determinism improvement — EligibleActorIDs is set-semantic).
Reviewer fixed 1 test-fixture defect: union test used `safety_officer`, invalid
per `user_process_areas_role_check` (valid: viewer/editor/approver/author/signer/
area_admin/qms_admin) → swapped to `area_admin`. Parity + union integration PASS
(54s targeted).

## Slice 4 review notes
Contract-first: flat `ActorSelector` schema (required:[kind], 4-kind enum,
user_id/role/area_code w/ patterns) — zero oneOf/discriminator (aligns ADR
0035, matches codebase-wide flat convention). Added to StageRequest +
StageSummary; `go generate` regen (api.gen.go, +342/-155). Hand-written
`contracts/route.go` gains `SelectorKind`+`ActorSelector` DTO with per-kind
`Validate()` that mirrors `domain.selector.Validate` exactly (named_user→UserID
only; role_in_fixed_area→Role+AreaCode; role_in_document_area→Role only;
submit_choice→Role+AreaCode). Handler `mapSelectorsFromWire` lowercases+trims
role/area, trims UserID; `mapSelectorsToWire` inverse. Repo LoadRoute + both
list paths (ListRoutes r.db / ListRoutesTx tx) batch-load via
`hydrateRouteListSelectors` reusing `loadRouteStageSelectors` (pq.Array `= ANY`,
ORDER BY selector_order, no N+1); tenant-safe via stage-id gating. RouteStage
gains `ID`+`Selectors`. Note: CreateRoute/UpdateRoute emit RouteResponse
without Stages and no GET-single-route handler exists, so StageResponse.Selectors
has no emit path yet — added for completeness; selector round-trip on writes is
covered by the real-DB list test. Reviewer found no production defect (implementer
run was already green: build, vet, api-lint 0, unit suite). Full-package
integration regression green 400s.

## Slice 5 review notes
Implementer (sonnet) was cut off by an API session limit AFTER finishing all
production code + the shared-branching unit suite, BEFORE writing the real-DB
tests. Reviewer (main session) independently reviewed the full production diff
and authored the missing real-DB suite — implementer≠reviewer holds on the
production logic. Production diff:
- `SubmitRequest`/`TemplateSubmitRequest` gain `ChosenActors []StageChosenActors`
  (one shared application type). Both submit loops: a **pre-pass**
  (`validateChosenActorsTargets`) rejects any chosen entry whose stage_order
  has no submit_choice selector BEFORE any stage instance is created
  (fail-closed, no-fallback); then per submit_choice stage
  `resolveSubmitChoiceEligibleIDs` requires a non-empty entry
  (`ErrSubmitChoiceRequired`) and validates+unions via
  `repo.ValidateSubmitChoiceActors`. Symmetric across document + template paths
  (shared helpers in submit_service.go).
- `ValidateSubmitChoiceActors` reuses the `ResolveEligibleActors` role×area
  query as the constraint pool + membership check → `ErrSubmitChoiceConstraintViolated`
  on any non-member; returns `unionDedupSort` of the validated ids. Non-recording
  single-tx read (H-PRE-1 clean — no `authz.Require`).
- `unionUserIDs` is a small intentional app-layer duplicate of the unexported
  infra `unionDedupSort` (documented), not a cross-package reach.
- Contract-first: flat shared `SubmitChosenActors` schema on both
  `SubmitDocumentRequest` and a NEW **optional** request body on the template
  submit-for-approval endpoint (was body-less → backward-compatible; existing
  callers omit it). `api.gen.go` regenerated for BOTH modules; reviewer re-ran
  `go generate` in both and confirmed idempotent (no drift). Distinct
  problem+json codes in both error maps, both → 422.
Minor (non-blocking): the template handler decodes the optional body only when
`r.ContentLength != 0`; a chunked request (ContentLength -1) with an empty body
would attempt a decode and 400 — not reachable from the real FE client (sends
Content-Length; existing callers send no body). Noted, not fixed.
Reviewer real-DB suite (`validate_submit_choice_actors_integration_test.go`):
accept-all-members, reject-wrong-role, reject-non-member, dedup — targeted PASS
7.5s, full-package PASS 565s.

## Slice 6b review notes (flat route-column drop — contract step)
Independent review of the full production diff (sonnet implementer a616e245) —
APPROVE, no production defect. Wire contract kept BYTE-STABLE (FE still sends
flat fields until slices 7/8); dropping them now would break the un-migrated FE
and exceed the slice, so synthesis relocated to the HTTP boundary. Change set:
- migration `0305`: `DROP COLUMN IF EXISTS required_role/area_code` from
  `approval_route_stages` + `schema_migrations` ledger row (`ON CONFLICT DO
  NOTHING`), `BEGIN/COMMIT`. Instance-side `*_snapshot` cols + `ResolveEligibleActors`
  untouched (verified — only ROUTE-STAGE flat cols dropped).
- `domain/route.go`: `Stage.RequiredRole`/`AreaCode` deleted; `EffectiveSelectors()`
  bridge deleted; `Route.Validate` reads `s.Selectors` directly (still fires
  `ErrStageNoSelector` on empty). `RequiredCapability` kept (orthogonal ADR 0022).
- `domain/selector.go`: `FlatRoleArea(sels)` reverse-map helper — exactly-one
  role_in_fixed_area → its (role,area), else ("",""). Repopulates audit-only
  `RequiredRoleSnapshot`/`AreaCodeSnapshot` + compat wire fields.
- `http/route_admin_handler.go`: the SOLE surviving flat→selector synthesis site
  (relocated `EffectiveSelectors`) — write: wire selectors win, else synthesize
  one lowercased role_in_fixed_area from flat wire fields (∴ `stage.Selectors`
  always non-empty); read (`mapListRoute`): `RequiredRole`/`AreaCode` reverse-derived
  via `FlatRoleArea`. `contracts/route.go` + `api.gen.go` + `api/openapi/**`
  UNTOUCHED (confirmed via diff-stat).
- `route_admin_service.go`: `insertRouteStages` colsPerRow 11→9 (flat cols out of
  INSERT+args, required_capability kept); `insertRouteStageSelectors` iterates
  `st.Selectors`; both LoadRoute SELECT/Scan paths stop reading the dropped cols.
  Un-spec'd but CORRECT additions: (a) `loadRouteStagesTx` now also loads
  `approval_route_stage_selectors` (`ORDER BY route_stage_id, selector_order` —
  column verified vs 0303) so Update-path `stagesEqual` DeepEqual doesn't
  spuriously version-bump against handler-populated `in.Stages[].Selectors`;
  (b) `route_admin_idemp.go` `canonicalStages` drops flat fields, adds
  deterministic `canonicalSelectors` fingerprint (order-stable per request).
- submit paths (`submit_service.go`/`template_submit_service.go`):
  `EffectiveSelectors()`→`Selectors`; snapshots via `domain.FlatRoleArea(stage.Selectors)`;
  template path passes derived `flatArea` (parity with old `stage.AreaCode`) into
  `ResolveEligibleActorsForSelectors`.
- seed + tests: `e2e_seed.go` route-stage insert split to RETURNING-id + one
  role_in_fixed_area selector per stage; raw-SQL stage inserters across
  `tests/integration/approval/*` + application unit tests converted flat→selector
  (intent preserved); `TestRouteStageSelectors_BackfillCoverage_RealDB` deleted
  (exclusively re-ran 0303's now-invalid `required_role`/`area_code` backfill);
  `domain/selector_test.go` EffectiveSelectors test → FlatRoleArea test.
Verification: `go build ./...` + `go vet ./...` exit 0; `go test
./internal/modules/approval/...` all PASS; full `integration ./tests/integration/
approval/...` PASS **627s** (`-timeout=1800s`). Selectors is now the sole domain+DB
source of truth for a route stage's actor pool; flat pair survives only as a
transitional HTTP compat shim (documented, removed in a future post-FE slice).

## Slice 6a review notes (Option C′ additive cutover)
Independent review of the full production diff (sonnet implementer a1594c97) —
APPROVE, no production defect. Change set:
- `domain/selector.go`: json tags (`kind`/`user_id,omitempty`/`role,omitempty`/
  `area_code,omitempty`) — the domain type was NOT JSON-serialized anywhere
  before (wire uses the separate `contracts.ActorSelector` DTO), so tags only
  define the new snapshot jsonb shape.
- `domain/instance.go`: `StageInstance.SelectorsSnapshot []ActorSelector`.
- migration `0304`: additive `ADD COLUMN IF NOT EXISTS selectors_snapshot jsonb
  NOT NULL DEFAULT '[]'`; backfill synthesizes ONE role_in_fixed_area selector
  per in-flight row from `(required_role_snapshot, area_code_snapshot)` —
  **not** `[]` — so the rewired drift path resolves the identical set the flat
  path did (empty snapshot would collapse the quorum denominator under a
  re-evaluate policy). Idempotent via `WHERE selectors_snapshot = '[]'::jsonb`.
- `InsertStageInstances`: colCount 16→17; `selectors_snapshot` bound as $base+16
  (marshaled JSON, nil→`[]`), due_in_days re-bound as $base+17 for the due_at
  CASE; new column placed before `due_at` in the INSERT list. Verified index
  arithmetic: 16 `$` binds + CASE = 17 cols; args 15/16/17 = due_in_days /
  selectorsJSON / due_in_days. Correct.
- both stage scan sites (`loadStageInstances`, `LoadInstancesByIDs`):
  `selectors_snapshot` appended to SELECT + `&selectorsJSON` last Scan arg +
  guarded unmarshal into `s.SelectorsSnapshot`.
- submit-time population (`submit_service.go` + `template_submit_service.go`):
  `chosenIDs` hoisted out of the submit_choice block; `SelectorsSnapshot:
  materializeSelectorsSnapshot(stage.EffectiveSelectors(), chosenIDs)` — every
  submit_choice selector materialized to one named_user per validated chosen id,
  all other kinds pass through. So a persisted snapshot never carries a
  submit_choice selector (exempt = frozen named_user).
- drift rewire (`drift_resolve.go` + both call sites): `currentEligible =
  ResolveEligibleActorsForSelectors(poolSelectors, liveArea) ∪ exemptNamedUserIDs`.
  Pool = role_in_fixed_area/role_in_document_area (liveArea resolved via the
  existing `resolveSubjectAreaCode` port ONLY when a document_area selector is
  present); exempt named_user ids unioned unconditionally (the §2 "never drifts"
  guarantee — resolves the Option-A F5 quorum defect at the root). Both services
  already carried `cdRead` + an in-scope `instance` (LoadInstance) with a
  populated `.Subject`; no new DB load added. Union sort is set-irrelevant to
  `ApplyEligibilityDrift` (map-based). H-PRE-1 clean (no authz.Require in the
  drift read).
Reviewer real-DB suite (`selectors_snapshot_integration_test.go`): (1) mixed
snapshot round-trips intact through InsertStageInstances→LoadInstance (marshal +
both scan sites + json tags); (2) migration 0304's verbatim backfill synthesizes
the parity role_in_fixed_area selector and is idempotent. Slice-3 already proves
resolver parity (single role_in_fixed_area == flat); scan-site column alignment
is additionally guarded live by every submit→load test in the full regression.

## Bounded defers
- **Integration runner default timeout.** Full `./tests/integration/approval/...`
  now runs ~850s, over Go's default 600s per-package `-timeout` cap. Slice 2's run
  needed `GOFLAGS='-timeout=1800s'`. `scripts/test-integration.ps1` does not set a
  `-timeout`, so an unadorned full-package run FALSE-FAILs with a panic at
  `testdb.Open`. Not a unit-3.2 logic issue — pre-existing infra scaling. DEFER:
  surface to hub as a runner hardening item (add `-timeout` default). Verified green
  850s with the raised timeout this slice.

## Architecture notes (surface to hub, out of unit-3.2 boundary)
- **Fixed role enums are pre-existing, not introduced here.** ActorSelector storage
  (`approval_route_stage_selectors.role` = free text, no value CHECK) and the resolver
  (`WHERE role = $role`, any string) are role-agnostic — a future tenant-defined role
  (e.g. `production_admin`) works with zero migration. The hardcoded role enums are the
  pre-existing CHECKs `user_process_areas_role_check` and `chk_iam_user_roles_role_code`
  on the membership/iam-assignment tables. Those are the local-maximum a future
  role-management feature must retire (tenant-scoped roles table + FK, not a frozen
  ARRAY CHECK). ActorSelector is already forward-compatible. Capabilities invariant
  intact: pool = role_in_area (BPMN candidateGroups grouping), authority = RequiredCapability.

## HS-1 items (for operator gate)
- **RESOLVED 2026-07-13 — operator ratified Option C′ (reversing A).** An
  independent architecture evaluation (mnfs-workflow:architecture-analyst, opus)
  found Option A defective and the operator re-ratified **Option C′**: snapshot
  the stage's selectors onto the **stage-instance** as `selectors_snapshot jsonb`
  at submit, materializing `submit_choice`→`named_user`; drift =
  `reResolvePoolKinds(snapshot, liveDocArea) ∪ frozenExemptNamedUserIDs`. Why A
  lost: (F5) A re-resolves exempt `named_user` through the active-user filter and
  DROPS an inactive named user under a re-evaluate policy → WRONG quorum
  denominator on the regulated signature path, contradicting §2 "never drifts";
  (F2) A reintroduces the route reach-back the self-contained-instance freeze was
  built to eliminate and couples the in-tx drift recompute to route-row retention.
  C′ keeps the instance self-contained (no route loader change, no `active=TRUE`
  bypass) and is the natural continuation of the existing snapshot pattern
  (mirrors `eligible_actor_ids` JSON). Design §8b rewritten to C′. Folded into
  Slice 6 (6a additive snapshot+drift-rewire+parity, 6b flat-column drop).
  (Original A text and carried-risk history below.)
- **SUPERSEDED — Option A (operator ratified then reversed 2026-07-13).** Drift
  re-reads the pinned route-version's stage selectors. Rejected on the F5 quorum
  defect + F2 freeze-violation above.
- **Multi-selector drift representation (carried — superseded by Option A).** B3 says pool selectors
  "re-evaluate per drift policy" and the snapshot columns stay untouched. A
  *single*-selector stage of any kind is fully represented by the resolved
  `AreaCodeSnapshot`/`RequiredRoleSnapshot` (document-area resolves to a concrete
  area at submit; named_user is drift-exempt), so the existing decision/review
  drift path keeps working unchanged — this is the parity guarantee. But a stage
  with **2+ selectors** (a genuine union) cannot be represented by the two single
  snapshot columns, so drift re-eval for multi-selector pools has no representation
  yet. Not reachable in production until multi-selector routes can be authored (FE
  slice 7). RESOLUTION OPTIONS to settle before slice 6/close: (a) drift re-reads
  the pinned route-version's stage selectors (RouteVersionSnapshot already pinned,
  route versions immutable) — keeps snapshot columns untouched; or (b) accept
  single-selector-only drift for v1 and gate multi-selector authoring. Surface to
  hub for a decision; slice 3 stays scoped to submit-time union + parity.
