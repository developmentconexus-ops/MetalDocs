# Unit 4.1 — lifecycle-ux-coherence M3 "journey-closure" — Evidence

**Date:** 2026-07-14
**Harness:** `docs/superpowers/HARNESS.md` (binding)
**Governing spec:** `docs/superpowers/specs/2026-07-06-lifecycle-ux-coherence-design.md` §3 (findings 9–12, 20)
**Milestone doc:** `docs/superpowers/milestones/lifecycle-ux-coherence/milestone-3-journey-closure/milestone.md`
**Scope (ROADMAP row 4.1):** Deep links (cockpit↔detail, notifications, fanout CTA); delete dead FE affordances (findings 9–12, 20).

## Branch & commit range
- **Worktree:** `C:\Users\leandro.theodoro\Documents\MetalDocs-wt-4.1`
- **Branch:** `unit-4.1-journey-closure`
- **Base:** `a684cb4e` (== `main` at dispatch)
- **Range:** `a684cb4e..daf471a5` (4 commits)
- **Net diff:** 17 files, +177 / −380

```
c4f5def3 fix(fe-journey): point Fanout CTA at real distribution child route (F12)
203142e0 feat(fe-journey): deep-link notification rows to their resource (F11)
f9598a63 feat(fe): delete dead FE affordances (F20, unit 4.1 slice C)
daf471a5 fix(unit-4.1): restore tracked files lost to incomplete worktree checkout
```

## Finding dispositions (runtime-truth reconciliation)

The spec predates **ADR 0080** (standalone approval cockpit retired → collapsed into the
mode-adaptive `DocumentWorkspacePage` at `/documents/:id`). Grounded against current code:

| # | Spec finding | Disposition | Evidence |
|---|---|---|---|
| 9 | Cockpit → detail: no link | **Already resolved (ADR 0080 / M2)** — evidence-only, no code | `WorkspaceSidebar.tsx:148` renders `<Link to="/documents/:id/details">` |
| 10 | Detail → cockpit: no link when instance exists | **Already resolved (ADR 0080 / M2)** — evidence-only, no code | `DocumentDetailRoute` "Visualizar documento" → `/documents/:id` (the workspace IS the mode-adaptive approval surface under 0080) |
| 11 | Notifications not clickable (`resource_id` ignored) | **BUILT** (Slice B) | fail-closed `notificationDeepLink()` → `/documents/{id}`; row becomes Link, still marks read |
| 12 | "Abrir Fanout" → nonexistent `/distribution` → dashboard | **BUILT** (Slice A) | relative route fix: `distribution` (not `/distribution`) resolves to `/documents/:id/details/distribution` |
| 20 | Dead FE: `obsolete()`, `archiveDocument()`, `ActivityPanel`, "···" stub | **DELETED** (Slice C) | 4 affordances removed; live symbols untouched |

F9/F10 = "runtime truth beats docs": already satisfied by ADR 0080 / M2 code; documented,
no code change (within unit boundary — not an architecture contradiction requiring BLOCKED).

## Slices, dispatch ledger & review disposition

Every code slice: sonnet implementer + INDEPENDENT sonnet reviewer (implementer ≠ reviewer)
BEFORE the next slice. Session never self-reviewed.

| Slice | Finding | Implementer | Reviewer (independent) | Disposition | Commit |
|---|---|---|---|---|---|
| A | F12 fanout CTA | sonnet builder | cavecrew-reviewer | No issues | `c4f5def3` |
| B | F11 notification deep-link | sonnet builder | cavecrew-reviewer | No issues | `203142e0` |
| C | F20 dead-affordance delete | sonnet builder | cavecrew-reviewer | No issues | `f9598a63` |
| (fix) | worktree-checkout collateral restore | session | cavecrew-reviewer (final) | No issues | `daf471a5` |
| final | full net diff | — | cavecrew-reviewer (git read-only) | **No issues** | — |

## Code substance

**F12 (Slice A):** `DocumentDetailRoute.tsx` `navigate('/distribution')→navigate('distribution')`;
`useDocumentArtifact.ts` kpi `href '/distribution'→'distribution'`. Relative resolution under
`/documents/:id/details` → working child `.../details/distribution` (the real fanout route).
Pinning tests updated to assert the resolved path (`ArtifactDetailView.test.tsx` now wraps the
node in a real `<Routes><Route path="/documents/:documentId/details">` so Links get genuine match
context; `useDocumentArtifact.test.tsx` asserts `'distribution'`).

**F11 (Slice B):** new `notifications/lib/notificationDeepLink.ts` — fail-closed:
returns `/documents/${resource_id}` only when `resource_type === 'document'` AND `resource_id`
is a non-empty string; `null` otherwise (no guessed routes). Grounded on backend emission
(`fanout_worker.go` sets `resource_type="document"`, `resource_id=<doc id>` for the user-facing
lifecycle events). `NotificationRow` renders a `<Link>` when target non-null (navigates + marks
unread→read), preserves the mark-read button / inert-read behavior when target null. 3 mapper +
5 row test cases; `NotificationBell`/`NotificationsSpotlight` tests wrapped in MemoryRouter
(test-only, no component change).

**F20 (Slice C):** removed `approvalApi.ts obsolete()` (+ orphaned `ObsoleteRequest`/`ObsoleteResponse`),
`documents.ts archiveDocument()` (both zero-caller), `ActivityPanel.tsx`+`.module.css` (permanent
"Em breve" placeholder) + all `LibraryPage` wiring, `LibraryPage` "···" `moreBtn` stub with the
table grid reduced 7→6 columns (header + row aligned). Live symbols untouched:
`features/iam/ActivityPanel`, `obsoleteControlledDocument`, generated
`api-types operations["archiveDocument"]` (backend contract), pager `activityButton`.

## Defect found & remediated (this session)

Slice A's `git add -A` executed while the fresh worktree checkout was still incomplete (the
`git worktree add` had been interrupted by a Windows-slow-checkout timeout). 2237 tracked files
still missing from the working tree were staged as **spurious deletions** and committed into
`c4f5def3` (Go source, `docs/superpowers/plans/*`, `.claude/agents/*`, `tmp/*.png`). None were in
F12 scope (a pure 4-file modification). **Fix `daf471a5`** restored every one from base `a684cb4e`.
Verified: net `git diff --diff-filter=D a684cb4e HEAD` yields **only** the two intended
`ActivityPanel` deletions. Final review independently confirmed zero out-of-scope deletions remain.

## Gates (verification ladder)

Run from clean committed worktree HEAD (`daf471a5`), post-restoration:

| Gate | Command | Result |
|---|---|---|
| L0 typecheck | `npx tsc --noEmit -p tsconfig.build.json` (`frontend/apps/web`) | **exit 0** |
| L1 vitest (touched pkgs) | `npx vitest run src/features/{documents,approval,notifications,shared/controlled-artifact}` | **81 files / 546 tests passed**, exit 0 |

FE-only unit → no Go build / DB-integration required (L1 selective policy: full `./...` only on
db/platform touch). Restored Go files are byte-identical to base (restoration, not modification).

## L3 browser QA

**CANNOT (operator-deferred).** Consolidated operator session; interactive login is a
password-entry prohibition (cannot authenticate to drive the live UI), and the dev :80 stack is
hub-owned. Route/relative-resolution correctness is instead pinned by the updated vitest suites
(F12 resolved-path assertions, F11 Link-render assertions). Deferred to operator HS-1.

## Boundary compliance
- Contract-first: **no** route/OpenAPI change (all work is FE consumer-side; the fanout child
  route `/documents/:id/details/distribution` already exists). No `oapi-codegen` run needed.
- Capabilities-not-roles (ADR 0022): no authz reasoning introduced; deep links are navigation only.
- `.env`: copied by file only into the worktree; never read/printed.
- Never pushed. No :80 stack touched.

## Defers
- L3 browser QA → operator (HS-1).
- ROADMAP row 4.1 + milestone README status flips: applied on close.
