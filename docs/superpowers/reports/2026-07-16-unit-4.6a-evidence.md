# Unit 4.6a — auth de-bypass (silent-skip integration tests) — evidence

Chip session · branch `unit/4.6a-auth-debypass` off base `60d0bf12` · worktree `nice-wu-353cd4`.
Doctrine: HARNESS-CORE 0.2.0 §4–§5 + docs/HARNESS-PROFILE.md (read in full before work).

## Scope (charter: 2026-07-14-unit-4.6-evidence.md §11.32 census)

Three raw-DB integration files silent-skip under the canonical runner (`scripts/test-integration.ps1`, exports `DATABASE_URL` only):
1. `internal/modules/auth/infrastructure/postgres/repository_test.go` (`TEST_DATABASE_URL` gate) → migrate to testdb factory.
2. `internal/modules/approval/infrastructure/postgres_approval_repository_displayname_integration_test.go` (`METALDOCS_DATABASE_URL` gate, deliberate live probe) → classify: LOUD skip or migrate/delete.
3. `tests/docx_v2/templates_integration_test.go` (`PGCONN` gate, inert stub) → delete unless live value.

Exit gate: zero silent-skips in integration tree under canonical runner; auth coverage green on leased testdb clones.

## Bootstrap notes

- Assigned worktree dir `nice-wu-353cd4` was EMPTY and unregistered (git worktree list showed only main + determined-driscoll); git calls from inside it silently fell through to the MAIN repo (which sits on branch `claude/quirky-engelbart-dd65a6`, HEAD 4a91aadf). Materialized via `git worktree add -b unit/4.6a-auth-debypass <dir> 60d0bf12`; first checkout timed out at 2 min (Windows FS, 7195 files) leaving stale `index.lock` — removed lock, `git checkout -f HEAD -- .` completed; status clean at 60d0bf12. **FINDING for hub:** chip worktrees can arrive unmaterialized; empty-dir git fall-through to main repo is a wrong-code hazard (same class as cwd-drift trap).
- `.env` copied from main checkout (file copy only). node_modules symlink SKIPPED — Go-only scope; will REQUEST if needed.

## Dispatch ledger

| # | Role | Model / effort | Path | Prompt pack | Output artifact | Status |
|---|---|---|---|---|---|---|
| 1 | Feature planner (P2 batch) | gpt-5.6-sol / medium | OS-process codex | scratchpad `prompt-plan-46a.md` | scratchpad `agent__plan46a.log` (partial) | KILLED mid-run — hub correction: codex/GPT retired for MetalDocs (profile amendment 1070e94c, operator-ratified 2026-07-16); no output consumed |
| 2 | Feature planner (P2 batch, redispatch) | claude sonnet subagent | Agent tool (sync) | scratchpad `prompt-plan-46a.md` (same pack) | plan pasted at §Plan below | DONE — plan accepted |
| 3 | Implement worker — Slice A (auth repo test → testdb) | claude sonnet subagent | Agent tool (sync) | inline prompt (slice card A verbatim) | slice A diff on branch | dispatched |
| 4 | Implement worker — Slice B (displayname LOUD skip + header fix) | claude sonnet subagent | Agent tool (sync) | inline prompt (slice card B verbatim) | slice B diff on branch | dispatched |
| — | Slice C (delete inert docx stub) | chip inline (trivial glue: `git rm`, zero lines authored) | — | plan slice card C | deletion commit | per CORE §4.2 trivial-glue allowance; covered by slice review + dual gate |
| 6 | Per-slice reviewer — Slice B | claude sonnet subagent (independent, ≠ implementer) | Agent tool (sync) | inline prompt (REVIEW-STANDARD pinned) | APPROVE, zero findings (verified comment-only diff, BYPASSRLS claim vs 0284_ci_rls_role.sql, vet clean) | DONE |
| 7 | Per-slice reviewer — Slice C | claude sonnet subagent (independent) | Agent tool (sync) | inline prompt (REVIEW-STANDARD pinned) | APPROVE (4/4 claims verified: inert stub, no CI refs, governance rule diff-scoped incl. deletions, zero coverage lost; no dangling package refs) | DONE |
| 8 | Per-slice reviewer — Slices A+A2 (auth pkg) | claude sonnet subagent (independent, ≠ implementer) | Agent tool (sync) | inline prompt (REVIEW-STANDARD pinned) | verdict below | dispatched |
| 5 | Implement worker — Slice A2 (sessions_admin_integration_test.go → testdb; census-gap 4th file, same package/gate) | claude sonnet subagent (continuation of #3, agent af60f3cd) | Agent tool (sync) | inline prompt (slice card A applied to sibling file) | slice A2 diff on branch | dispatched |

(rows appended at dispatch time; scratchpad = `C:\Users\LEANDR~1.THE\AppData\Local\Temp\claude\C--Users-leandro-theodoro-Documents-MetalDocs--claude-worktrees-nice-wu-353cd4\dae72151-cfa6-4c3c-9784-f69670ca18e2\scratchpad`)

## Plan (P2 batch, sonnet planner — accepted verbatim)

See dispatch #2. Slice cards: A = auth repository_test.go → testdb.Open + NewTenant (careful; drop TEST_DATABASE_URL gate, lib/pq import, manual cleanups; NewTenant for FK-bearing session inserts). B = approval displayname test ratified as by-design live probe: LOUD skip naming METALDOCS_DATABASE_URL + reason; header corrected (BYPASSRLS caveat — off-tx H-PRE-1 claim retained, RLS-enforcement overclaim removed). C = delete tests/docx_v2/templates_integration_test.go (inert stub; no script/CI references; governance rule diff-scoped). Write-sets disjoint (3 single-file leaves); contract-satisfiability clean (no openapi/generated/migrations/factory-internals touch); zero hub REQUESTs. Full plan text archived in session transcript; key verified facts: fk_auth_sessions_tenant → tenants(id) (baseline:3749), governance rule per-diff (check-governance.ps1:27-30), no PGCONN reference anywhere in scripts/.github.

## Hub correction mid-flight

Codex/GPT dispatch RETIRED for MetalDocs (profile amendment `1070e94c` on main, operator-ratified 2026-07-16; verified via `git show` before acting). In-flight codex planner killed (ledger #1); Claude-only matrix from then on. ACK sent to hub.

## Census gap finding (scope extension, exit-gate-governed)

Charter census (§11.32) listed 3 silent-skip files. Implementation surfaced a 4th: `internal/modules/auth/infrastructure/postgres/sessions_admin_integration_test.go` — same package, same `openTestDB`/`TEST_DATABASE_URL` gate, hardcoded live-dev tenant UUIDs. Migrated as Slice A2 (ledger #5) under the exit gate's plain text ("zero silent-skips ... every raw-DB file"). Not an unauthorized scope widening: same file seam (auth test files), same defect class, compile-coupled to Slice A (deleting `openTestDB` broke it).

## Slice reviews

- Slice A+A2 (ledger #8): APPROVE, zero findings. Reviewer traced `NewTenant` seed path (`seedWithCapsIdentity` with empty actorID → GUC-only, tenants row only — cannot pollute `len(items)==1` or no-JOIN assertions); confirmed queries under test unmodified; idiom parity with existing testdb consumers; vet clean.
- Slice B (ledger #6): APPROVE, zero findings (comment-only diff verified; BYPASSRLS claim cross-checked vs migration 0284).
- Slice C (ledger #7): APPROVE (4/4 rationale claims repo-verified; no dangling package refs; CI globs reference the dir, never the file).

## Ladder (all from worktree, canonical runner semantics)

- **L0:** `go build ./...` OK · `go vet -tags integration` on touched pkgs OK · `check-governance.ps1 -BaseRef 60d0bf12` OK · `check-test-discipline.sh`: 7 violations, ALL pre-existing (identical run on main checkout — zero delta from this unit).
- **L1:** canonical `.\scripts\test-integration.ps1` (DATABASE_URL derived, pg_isready gated):
  - Touched packages `-v -count=1` (cache-busted to prove real execution): all 5 migrated tests **RUN + PASS** on leased clones — TestUpdateUserResetLockState (0.15s), TestWithinLoginLock_BoundsConcurrentFailedLogins (0.33s), TestUpdateUserMissingIdentityReturnsNotFound (0.13s), TestListOnlineUsers_FiltersByTenant (0.09s), TestListActiveSessions_NoIamUsersJoin (0.07s). `auth/infrastructure/postgres` ok 9.855s; `approval/infrastructure` ok 10.744s; `tests/docx_v2` ok 0.743s.
  - Displayname probe: SKIP with the new LOUD message (verbatim in output, names METALDOCS_DATABASE_URL + by-design reason).
  - Guard suites: tenantdata ok 34.1s · scenarios ok 158.9s · iam ok 88.7s.
- **L2:** N/A — test-only diff, zero product code / endpoints / contract surface touched.
- **Exit-gate sweep:** `TEST_DATABASE_URL`/`PGCONN` grep → vendor-only. Remaining `t.Skip("... not set")` sites gate on DATABASE_URL/METALDOCS_DATABASE_URL, which the canonical runner sets (charter census: EXECUTES ✓). Unconditional named-reason skips in `tests/docx_v2/{documents,exports}_integration_test.go` ("requires testcontainers or running infrastructure", 7 sites) are NOT env-gated silent skips — flagged as out-of-charter finding for hub queue, untouched.

## Dual gate (ledger #9, #10)

| # | Role | Model | Path | Input | Output |
|---|---|---|---|---|---|
| 9 | Dual-gate arm 1 | COLD Opus subagent (model=opus, clean context) | Agent tool (sync) | fixed-SHA diff 60d0bf12..a50a20b1, git read-only | verdict below |
| 10 | Dual-gate arm 2 | independent sonnet subagent (clean context; replaces GPT arm per amendment 1070e94c) | Agent tool (sync) | same fixed-SHA diff | verdict below |

### Dual-gate verdicts (fixed SHA a50a20b1, diff vs 60d0bf12)

- **Arm 1 — cold Opus:** **ACCEPT.** No blocking/important findings. One `suggestion` (non-blocking, no action): the retained displayname live probe gates on METALDOCS_DATABASE_URL which the canonical runner doesn't set → loud-skips in CI forever; its AC5 proof stays manual-only — exactly the ratified by-design shape. Exit gate judged MET; A2 restraint and docx out-of-scope flagging judged appropriate.
- **Arm 2 — independent sonnet (clean context):** **ACCEPT.** Zero findings. Verified: assertions byte-preserved vs base (incl. cross-tenant negatives + 40-goroutine burst), factory contract read in full (cleanup removal redundant-not-lost), deleted stub zero-value + zero references, BYPASSRLS caveat corroborated by ci_role.go, build+vet clean.
- **Reconciliation:** none needed — both ACCEPT; the single suggestion is charter-ratified behavior.

## Close-out

- QA (P7): N/A live-drive — test-infrastructure-only unit, zero user surface; the binding proof is the L1 cache-busted execution of the migrated tests on leased clones under the canonical runner (precedent: unit 4.6 exit-gate form).
- Defers/findings for hub queue:
  1. Chip worktree arrived unmaterialized (empty dir, git fall-through to main repo) — process finding for profile §3.
  2. `tests/docx_v2/{documents,exports}_integration_test.go`: 7 unconditional `t.Skip("integration test requires testcontainers or running infrastructure")` sites — named-reason skips, not env-gated, out of charter; dead weight or future testcontainers work, hub to classify.
  3. Census-gap Slice A2 (4th silent-skip file) absorbed under exit-gate text — hub to ratify.
  4. `check-test-discipline.sh`: 7 pre-existing violations repo-wide (identical on main) — not this unit's; hub queue.
