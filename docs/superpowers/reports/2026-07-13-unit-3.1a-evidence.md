# Unit 3.1a — template legacy-approval retirement — EVIDENCE

**Unit:** ROADMAP 3.1a · **Branch:** `worktree-agent-a0d0c8a51f43bb656` (forked from main @ f2680303)
**Agent:** Opus unit agent (Transport A). **Started:** 2026-07-13.
**Scope (operator-ratified Option A, ADR 0082 §Transitional coexistence):** (a) migrate `CreateTemplate` + `PublishTemplateVersion` off the role model; (b) rebuild template-approval FE onto kernel routes; (c) delete legacy path + DROP `templates_approval_config`.

> Environment note: this agent env exposes **no native task-board tool** (`TaskCreate`/`TodoWrite` absent). Per harness §4.6 the slice board is maintained as the live markdown table in §Slice board below and kept current (in_progress at dispatch, done at reviewed-green). Recorded as an env-limitation, not a skipped obligation.

---

## P0 gate — developing-new-work — DONE
- Analysis: `docs/superpowers/analysis/2026-07-13-unit-3.1a-template-legacy-approval-retirement-system-impact.md` (commit `95697824`).
- **Verdict: 🟡 Yellow** — proceed. Hard-stops AS-1/AS-2/AS-3: none. Executes ADR 0082's already-accepted retirement; no new ADR required.
- Locked constraints carried into the plan: (1) mandatory (a)→(b)→(c) sequence; (2) `CapTemplateReview` removal decrements `TestCapabilityRegistrySize` + keeps 5-surface coherence + `tripwire_caps_test.go` green (regen arms via registry path, never hand-edit); (3) contract-first route deletion (regen, never hand-edit `api.gen.go`); (4) forward-only DROP w/ pre-drop emptiness assert; (5) kernel (`internal/modules/approval`) untouched; (6) legacy tests deleted / guard tests kept; (7) full ladder from clean after each RED.

## Accepted-RED integration baseline (NOT mine to fix — gate = zero NEW failures beyond these 9)
- `internal/modules/controlleddocuments/application` — TestTenantIsolation_SequenceCounters_CrossTenant
- `internal/modules/jobs/approval_sla_surfacer` — 4 SLA surfacer tests
- `tests/integration/scenarios` — TestGrantAreaMembershipFn, TestGrantAreaMembershipIdempotent, TestTriggerBypassBlocked
- `tests/integration/tenantdata` — TestTenantDataPortCoverage

## Environment
- `.env` present in worktree root (existence-tested only, never read). `.env.local` absent.
- Highest migration on disk: `0300_tripwire_signoff_parent_discriminator.sql` → DROP migration will be `0301`.

---

## Dispatch ledger (per slice: implementer · reviewer(s) · verdict)
*(Investigator dispatch below is inventory, not a code slice.)*
- **INV-1 inventory** — general-purpose(sonnet) investigator `acb5193545c4fb048`, dispatched 2026-07-13. Read-only legacy-surface map (SECTIONS A–I). Verdict: pending.

## Slice board
*(finalized after inventory returns; sequence locked (a)→(b)→(c))*
| Slice | Goal | Status |
|-------|------|--------|
| _pending inventory_ | | |

---

## Gates run + results
*(filled during P4 verify)*

## Bounded defers / HS-1 items
*(filled at close)*
