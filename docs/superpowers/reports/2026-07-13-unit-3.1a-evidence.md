# Unit 3.1a — template legacy-approval retirement — EVIDENCE

**Unit:** ROADMAP 3.1a · **Branch:** `worktree-agent-a0d0c8a51f43bb656` (forked from main @ f2680303)
**Agent:** Opus unit agent (Transport A). **Started:** 2026-07-13.
**Scope (operator-ratified Option A, ADR 0082 §Transitional coexistence):** (a) migrate `CreateTemplate` + `PublishTemplateVersion` off the role model; (b) rebuild template-approval FE onto kernel routes; (c) delete legacy path + DROP `templates_approval_config`.

> Environment note: this agent env exposes **no native task-board tool** (`TaskCreate`/`TodoWrite` absent). Per harness §4.6 the slice board is maintained as the live markdown table in §Slice board below and kept current (in_progress at dispatch, done at reviewed-green). Recorded as an env-limitation, not a skipped obligation.

---

## P0 gate — developing-new-work — DONE
- Analysis: `docs/superpowers/analysis/2026-07-13-unit-3.1a-template-legacy-approval-retirement-system-impact.md` (commit `95697824`).
- **Verdict: 🟡 Yellow** — proceed. Hard-stops AS-1/AS-2/AS-3: none. Executes ADR 0082's already-accepted retirement; no new ADR required.
- Locked constraints carried into the plan: (1) mandatory (a)→(b)→(c) sequence; (2) `CapTemplateReview` removal decrements `TestCapabilityRegistrySize` + keeps 5-surface coherence + `tripwire_caps_test.go` green (regen arms via registry path, never hand-edit); (3) contract-first route deletion (regen, never hand-edit `api.gen.go`); (4) forward-only DROP w/ pre-drop emptiness assert; (5) kernel (`internal/modules/approval`) untouched; (6) legacy tests deleted / guard tests kept; (7) full ladder from clean after each RED.

## Accepted-RED integration baseline (NOT mine to fix — gate = zero NEW failures beyond these 9)
- `internal/modules/controlleddocuments/application` — TestTenantIsolation_SequenceCounters_CrossTenant
- `internal/modules/jobs/approval_sla_surfacer` — 4 SLA surfacer tests
- `tests/integration/scenarios` — TestGrantAreaMembershipFn, TestGrantAreaMembershipIdempotent, TestTriggerBypassBlocked
- `tests/integration/tenantdata` — TestTenantDataPortCoverage

## Environment
- `.env` present in worktree root (existence-tested only, never read). `.env.local` absent.
- Highest migration on disk: `0300_tripwire_signoff_parent_discriminator.sql` → DROP migration will be `0301`.

---

## Dispatch ledger (per slice: implementer · reviewer(s) · verdict)
*(Investigator dispatches are inventory, not code slices.)*
- **INV-1 inventory (bg)** — general-purpose(sonnet) `acb5193545c4fb048`, background. COMPLETED — full SECTIONS A–J report (delivered late via task notification). Verdict: accepted.
- **INV-1b inventory (sync)** — general-purpose(sonnet) `addd563664004a37a`, synchronous re-dispatch after hub correction (bg wake would never fire). COMPLETED — SECTIONS A–J + 12 deletion-order risks. Verdict: accepted; consistent with INV-1; resolves SECTION J (template.review not load-bearing for kept writes; arms re-render migration forced by M2 parity lint). Both consolidated into design §7.
- **S1 implementer** — general-purpose(sonnet) `ad3cda459730f6de6`. Round 1 COMPLETE: 9 files, RED→GREEN evidenced (application `TestCreateTemplate_Happy` red at create_test.go:69; http `TestCreateTemplate_NoRoleSeeding` red via temp-stash of api.gen.go, "got 1 calls"); build/vet/unit green; api-lint strict 0 violations (real entrypoint `go run ./scripts/api-lint/ -strict`, not a ps1). Notables: `pnpm gen:api` auto-materialized node_modules (NO explicit install; zero tracked-state change — lockfile/package.json untouched; possibly self-healed junction drift); FE `index.d.ts` regen legitimately caught up stale M3 kernel ops (kept — S3 needs them); flagged the create→legacy-submit trap.
- **S1 reviewer** — cavecrew-reviewer(sonnet) `a3ccfa5c9f7961903`. Two-stage review + live-DB run. Verdict: **REQUEST CHANGES** — 3🔴, single root cause: legacy `SubmitForReview` fails closed (`GetApprovalConfig` → ErrNotFound) for post-S1 templates; `TestLifecycle_NoAutoNextDraft` + `TestTripwire_ReviewerStageWritesVersionRow` RED live ("SubmitForReview: templates: not_found"). All other hunks verified correct (contract-first, ADR 0022, no scope creep, regen authentic, assertions non-tautological).
- **S1 fix round** — orchestrator disposition per legacy-test-deletion rule + design §2 (transitional coexistence collapses inside the unit; interim commits must stay green): DELETE retired reviewer-stage tripwire pin now (S5 removes `template.review` from arm #14 regardless); REWRITE `TestLifecycle_NoAutoNextDraft` onto kept direct-publish flow (invariant assertions unchanged). Behavioral note recorded: between S1 and S4 the legacy submit route is vestigially broken for NEW templates — acceptable because the unit merges atomically (no interim state ships) and S4 deletes the route; FE stops calling it in S3.
- **S1 fix implementer** — `ad3cda459730f6de6` resumed (background; result routed via hub relay). Applied: reviewer-stage tripwire test + `seedContentHashTx` + unused `domain` import deleted (archive pin APP-09/0270 untouched; header notes APP-08/0269 retired per ADR 0082 (a)); `TestLifecycle_NoAutoNextDraft` rewritten onto direct publish (approver actor ≠ author; content-hash seed kept for publish gate; post-publish assertions byte-identical; pre-verified `templates_template_version` has no DB transition-graph trigger — status CHECK only, transition app-gated).
- **S1 fix reviewer (delta)** — general-purpose(sonnet) `a8c937e30970e6946`, sync. ZERO edits (detected concurrent writer, verified instead of clobbering): line-by-line spec + domain-precondition trace (draft status · content hash · CheckSegregation approver≠author, nil reviewer · `RoleBindingFor("")`→capability-only · CapTemplatePublish via SeedSystemAdmin) + fresh live-DB PASS both tests (`-count=1`) + archive-pin byte-identical + scope = 2 files only. Verdict: fix correct.
- **S1 first-party re-verify (orchestrator)** — `go build ./...` clean; `go test ./internal/modules/templates/... -count=1` all ok; `GOFLAGS=-count=1 scripts/test-integration.ps1 -Package ./tests/integration/templates/... -Run "TestLifecycle_NoAutoNextDraft|TestTripwire_ArchiveWritesTemplateRow"` → `ok 8.381s` PASS non-cached. Round-1 findings closed (root cause = accepted transitional break, documented; both RED tests rerouted/deleted). **S1 GREEN → committed `62a415f9`.**
- **S2 implementer** — general-purpose(sonnet) `af499be128dfe8dd5`, sync. 5 files: publish role-binding gate + denial-audit deleted; CheckSegregation + CapTemplatePublish kept verbatim; status gate draft|approved (finding: was a HARD draft-only check, NOT CanTransition-gated); `Cmd.ActorRoles` dropped + wrapper stops passing `actorRolesFromReq` (helper/`containsRole`/`ErrForbiddenRole`/MapErr kept — legacy Review/Approve use them until S4); publish-role test file deleted → re-homed as `FromApproved_KernelCompletion` + `NoRoles_CapabilityAlone` + `SegregationViolation_SelfPublish`; HTTP `ForbiddenRoleRFC9457` rewritten → `SelfPublishForbiddenRFC9457` (ISO 403). Before/after evidence incl. captured old-test FAIL post-change ("expected 403, got 200") proving the role gate is really gone. All gates green.
- **S2 reviewer** — cavecrew-reviewer(sonnet) `abe915b041ca07756`, sync. Verified file-set, out-of-scope zero-diff, status-gate exhaustive vs 5-value enum, legacy symbols still load-bearing, coverage genuinely re-homed; ran build/unit/integration live. Verdict: **REQUEST CHANGES** — 1🔴 real bug: publish unconditionally stamped `version.ApprovedAt = &now`, clobbering the kernel-stamped true approval time (`ApprovalCompletionWriter.MarkTemplateVersionApproved`) on the new approved→published path. Also explicitly cleared "legacy-Review-approved version can now publish capability-only" as ratified transitional debt (ADR 0082), not a defect.
- **S2 fix (orchestrator inline — reviewer-prescribed one-conditional + test hardening, within ≤10-line glue allowance):** `ApprovedAt` stamped only when nil (draft direct-publish keeps stamp-at-publish; approved source preserves kernel time). `FromApproved_KernelCompletion` hardened: sentinel kernel `ApprovedAt` asserted preserved + `PublishedAt` asserted distinct (distinctness passing also proves fake-clock ≠ sentinel → old code would fail the new assertion; RED direction by construction). Re-verified: build clean; templates unit `-count=1` all ok; templates integration `ok 7.816s` non-cached; api-lint `-strict` 0. **S2 GREEN → committed `e6cb25ff`.**

## Slice board (FINAL SNAPSHOT at handoff; env has no TaskCreate — markdown board per §4.6 note above)
| Slice | Goal | Deps | Implementer | Reviewer | Status |
|-------|------|------|-------------|----------|--------|
| S1 | CreateTemplate off role-seeding (contract+create.go) | — | ad3cda45 (sonnet) | a3ccfa5c + a8c937e3 (sonnet) | done (reviewed-green, `62a415f9`) |
| S2 | PublishTemplateVersion kernel-driven (drop role-binding, keep identity-SoD, approved→published) | S1 | af499be1 (sonnet) | abe915b0 (sonnet) + orchestrator fix | done (reviewed-green, `e6cb25ff`) |
| S3 | FE rebuild onto kernel routes (wrappers + screens; types already regenerated) | S2 | successor | successor | pending |
| S4 | Delete legacy backend + contract (4 ops, service, domain, repo, tests) | S1,S2,S3 | successor | successor | pending |
| S5 | CapTemplateReview removal + tripwire re-render migration + DROP table | S4 | successor | successor | pending |
| S6 | Docs sweep (wiki templates.md, ADR 0082 note, evidence) | S4,S5 | successor | successor | pending |

---

## Gates run + results (through S2 — slice-scoped only; FULL ladder pending, successor-owned)
| Gate | Scope run | Result |
|------|-----------|--------|
| `go build ./...` | whole repo | PASS (after S1, after S2, after S2-fix) |
| `go vet ./internal/modules/templates/...` | templates | PASS |
| `go test ./internal/modules/templates/... -count=1` | templates unit (4 pkgs) | PASS (fresh, non-cached, after each slice + fix) |
| `scripts/test-integration.ps1 -Package ./tests/integration/templates/...` (`GOFLAGS=-count=1`) | templates integration, live DB | PASS non-cached (S1: scoped 8.381s; S2: whole pkg 7.751s; post-fix 7.816s) |
| `go run ./scripts/api-lint/ -strict api/openapi/v1/openapi.yaml .` | contract | 0 violations (after S1 contract edit; after S2) |
| NOT YET RUN | module-boundaries, check-test-discipline, capability-coherence lint, FE typecheck/vitest, full `go test ./...`, full `test-integration.ps1` (all pkgs vs 9-test accepted-RED baseline), L3 browser QA | successor runs full L0/L1 from clean after last slice; L3 per harness §6 |

## Bounded defers / HS-1 items (running list)
- `templates_template_version.pending_reviewer_role`/`pending_approver_role` DB COLUMNS stay (write-never/read-never after S4) — named debt, reopen trigger: next schema-hygiene migration (ratified DROP list covers only `templates_approval_config`). Go fields/DTO fields DO go in S4.
- Transitional (until S4): legacy submit route vestigially broken for NEW templates (no config row; fails closed ErrNotFound). Ratified consequence of retirement order; unit merges atomically.
- Transitional (until S4, reviewer-cleared as ratified ADR 0082 debt): a version brought to `approved` by LEGACY Review can now publish capability-only (role gate gone). Legacy Review deleted in S4.

---

## HANDOFF → successor (hub-approved budget split 2026-07-13; this session executed P0/P1 + slices S1-S2 of (a))

**RESUME POINT: slice S3** (FE rebuild onto kernel routes). Then S4 → S5 → S6 → full P4 ladder → P5 QA → P7 close. All specs are ON THIS BRANCH:
- **Slice specs:** `docs/superpowers/analysis/2026-07-13-unit-3.1a-design.md` **§7.2** (per-slice file lists w/ line anchors) + §7.1 (resolved risks/facts). Line anchors predate S1/S2 — re-grep before editing; the symbol names are stable.
- **Scope authority:** operator-ratified Option A in ROADMAP row 3.1a + ADR 0082 §Transitional coexistence. P0 gate: `docs/superpowers/analysis/2026-07-13-unit-3.1a-template-legacy-approval-retirement-system-impact.md` (🟡, 7 locked constraints). Deviation = BLOCKED event, never silent adaptation.
- **This ledger** = dispatch/review/verdict history + gates table above.

**State delivered (commit range `95697824..e6cb25ff`):** P0 analysis · P1 design+plan · S1 (create off role-seeding; contract regen BOTH codegens) · S2 (publish off role-binding; draft|approved sources; kernel `ApprovedAt` preserved). Worktree clean at `e6cb25ff`.

**S3 head start:** FE `lib/api-types/index.d.ts` was ALREADY regenerated in S1 and contains the kernel ops (`submitTemplateVersionForApproval`, `signoffTemplateVersion`) — do NOT re-run `pnpm gen:api` for S3 (S4 re-runs it after the contract deletion). Kernel read-surface rule (design §7.2 S3): reuse existing subject-generic read surface if present under `features/approval/api/`; NO new backend routes in this unit; if rich chain display lacks read surface → degrade to version-status truth + record defer.

**Gotchas learned (cost us ~2× S1 budget):**
1. **Dispatch SYNCHRONOUSLY** (`run_in_background: false`). Background child wakes were lost TWICE (hub-confirmed env race). If a background result must be recovered: check `git status` + the task output file under the session temp `tasks\` dir before re-dispatching; a re-dispatched agent may collide with late edits from the "dead" one (happened in S1 fix round — second agent correctly verified instead of clobbering; instruct fix agents to check for concurrent edits on Edit failure).
2. **Env:** no TaskCreate/TodoWrite (markdown board in this file is the §4.6 substitute). No Skill tool for the P0 gate (was run manually). `api-lint` entrypoint is `go run ./scripts/api-lint/ -strict api/openapi/v1/openapi.yaml .` (no ps1). Integration runner: `GOFLAGS=-count=1 pwsh -File scripts/test-integration.ps1 [-Package ...] [-Run ...]` — derives DATABASE_URL itself, never hand-set; first run may report `(cached)` without the GOFLAGS prefix. Live DB container `metaldocs-postgres` (host port 5433) was up throughout.
3. **FE toolchain:** `pnpm gen:api` auto-materialized node_modules in this worktree (~11s, zero tracked-state change — lockfile/package.json untouched). The `fe-node-modules-junction-drift` breakage may therefore be healed HERE: try `pnpm -C frontend/apps/web typecheck`/vitest before assuming broken; NEVER run explicit `pnpm/npm install`.
4. **Review discipline pays:** both slices' reviewers found real issues (S1: runtime break of legacy submit-dependent tests, caught only by LIVE-DB run — always make reviewers run integration, not just read; S2: kernel `ApprovedAt` clobber). Keep implementer ≠ reviewer, reviewers must execute tests.
5. **S4 additions beyond design §7.2 list:** `TestLifecycle_NoAutoNextDraft` is already rerouted (S1) — no S4 action; `tripwire_caps_test.go` reviewer-stage test already deleted (S1) — S4's "same-commit as Service.Review deletion" note is satisfied early; `AuditPublishForbiddenRole` (audit const for the deleted denial-audit) — check for orphanhood in S4 and delete if unreferenced; S2 kept `actorRolesFromReq`/`containsRole`/`ErrForbiddenRole`+MapErr row/`RoleBindingFor` alive for legacy — ALL become orphans in S4 (design already lists them).
6. **Budget actuals:** S1 ≈ 2× estimate (dispatch races + fix round), S2 ≈ 1.3× (one surgical fix round). Plan successor budget ≥ 150k for S3-S6 + full ladder + QA + close, with reviewer rounds priced in.

**Verification debt (successor-owned):** full L0 (module-boundaries, test-discipline, capability-coherence, FE typecheck) + full L1 (`go test ./...`, full integration vs the 9-test accepted-RED baseline listed at top) after S4/S5 land; L3 browser QA of the rebuilt template-approval flow (personas in design §5; hub owns :80 — vite dev-server on a free port or honest CANNOT); P7 evidence close + CLOSED event.

---

## SUCCESSOR SESSION (successor unit agent — S3→S6 + verify + QA + close)

### Boot verification
- `git status -sb`: clean on `worktree-agent-a0d0c8a51f43bb656` @ `573560ee` (matches handoff). `.env` present (existence-tested only). HARNESS §2/§4/§5/§6/§8 + design §7.1/§7.2 + P0 (7 locked constraints) re-read.
- Env limitation confirmed persists: NO `TaskCreate`/`TodoWrite` tool → slice board stays the markdown table above (§4.6 substitute). FE toolchain **RUNS** in this worktree (see S3 investigation) — junction drift NOT reproduced here.

### S3 pre-implementation investigation (INV-S3, sonnet general-purpose, sync — accepted)
Design §7.2 S3 under-listed the FE surface + didn't anticipate two facts. Findings (evidence file:line):
- **Full legacy-coupled FE surface = 14 files** (6 src + 6 test + api + editor): `api/templates.ts` (submitForReview 162-172 / reviewVersion 174-186 / approveVersion 188-204 + orphan type aliases `TemplateVersionEnvelope` 27-28 / `ApproveTemplateVersionResponse` 29-30 — all break S4 gen:api if kept), `pages/TemplateEditorPage.tsx:160`, `pages/TemplateApprovalRoute.tsx`, `lib/canActOnVersion.ts`, `lib/templateApprovalActions.ts`, `lib/templateApprovalChain.ts`, `lib/templateApprovalDecision.ts`, `adapters/useTemplateApprovalArtifact.ts` + the 6 sibling test files. Design missed `templateApprovalChain.ts`/`templateApprovalDecision.ts`.
- **Kernel backend contract (`routes_approval_kernel.go`):** submit `SubmitTemplateVersionForApproval` (cap `template.submit`, no body, Idempotency-Key hdr, resp enveloped `{data:{instance_id, version_status:"under_review"}}`); signoff `SignoffTemplateVersion` (cap `template.approve`, body `{decision:approve|reject, reason?, password}` **password REQUIRED**, resp FLAT `{signoff_id, was_replay, outcome∈recorded|stage_completed|instance_approved|instance_rejected}`). Version-status vocabulary UNCHANGED (draft→under_review→approved→published).
- **Read-surface decision = DEGRADE** (not reuse). `features/approval/api/approvalApi.ts` is document-collection-scoped (`/documents/{id}/...`), not template-compatible → templates need NEW wrappers, not param reuse. No `GET /templates/.../approval-instance` route exists (contract gap) → a faithful N-stage kernel chain is unbuildable client-side. Rebuild the fixed 3-step chain off surviving `VersionDTO` fields (status + submitted_at/reviewed_at/approved_at + author/reviewer/approver ids; S4 drops only `pending_*_role`). **DEFER (named debt):** rich per-stage kernel chain display needs a new template approval-instance read route OR `VersionDTO.approval_instance_id` — out of 3.1a scope.
- **Password:** NOT a contradiction — kernel signoff genuinely requires e-signature re-auth (same eQMS kernel as documents, ADR 0082 intent). `ArtifactDecisionPanel` already collects password when `model.password != null` (documents use it); S3 flips template decision model to non-null password + threads it through the submit callback. Legacy "templates carry no legal e-signature" comments are now stale → retire.

### GM FORK (publish path) → **BLOCKED to hub** (see event)
- Kernel signoff lands versions on **approved**, never auto-publishes (`approval_completion_writer.go:91` marks approved only). Legacy `approveVersion` (accept-and-publish, POST /approve) is deleted in S4. After S4 the ONLY publish path is **POST /publish** (`routes_generated.go:197`).
- POST /publish REQUIRES client body `{schema_key}`, but schema_key is deterministically server-derivable (exported `TemplateVersionSchemaKey(tenant,id,n)`, `keys.go:38`) and used ONLY for AuditPublished metadata (`lifecycle.go:457`; cmd doc `lifecycle.go:369`). FE has NEVER wired publish.
- Fork: **A** (global max) server-derive schema_key + drop from `PublishTemplateVersionRequest` (contract-first) → clean bodyless FE publish wrapper; crosses S3 FE-only boundary + unit "approval-retirement" framing (touches kept publish endpoint / S2 territory). **B** FE-construct the storage key — FORBIDDEN by reversibility veto (new FE caller of a leaky seam). **C** defer publish UI — ships a merged functional regression (approved versions unpublishable from UI). Recommend **A**. Surfaced to hub for the scope decision before dispatching the S3 implementer (avoids a wrong-brief implementer round-trip).
