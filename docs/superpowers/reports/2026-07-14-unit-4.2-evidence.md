# Unit 4.2 evidence — template reviews in the single approver inbox

**Track:** A (contract lock — owns `api/openapi` this batch) · **Branch:** `unit-4.2-template-inbox` (off `7e046056`)
**Intent:** Surface template reviews in the SINGLE approver worklist/inbox, contract-first, consuming the extracted approval kernel worklist (subject_kind='template'). Never a legacy template-review endpoint.
**System-impact analysis:** `docs/superpowers/analysis/2026-07-14-unit-4.2-template-inbox-system-impact.md` (Verdict 🟡 Yellow; committed e6254981).

## GM-fork (HARNESS §3)
Chosen **C — reader-bridge**: worklist selects approval's own `ai.subject_kind`/`ai.subject_key`; document rows keep the existing `LEFT JOIN documents` UNCHANGED; template rows resolve `{templateId, name}` via the approval-owned `TemplateVersionReader.LoadTemplateInboxMeta` port (new batch method) AFTER the query. Boundary-clean (grows the sanctioned port, not the smell), no migration.
- **A (raw-join templates tables)** rejected — deepens the boundary smell (2nd module's tables in approval SQL), violates invariant 6.
- **B (snapshot columns)** is the global maximum but needs a migration (Track C owns 0306+) → filed as **named debt**. Reopen trigger: next `approval_instances` migration OR measured worklist perf regression from per-row reader calls.

## Locked constraints (from analysis §10)
1. approval→templates ONLY via `TemplateVersionReader` port; documents path byte-for-byte unchanged.
2. Hard-break wire contract (drop `document_id`/`document_title`; add subject-generic fields) + update sole FE consumer same unit — no relax-to-optional (legacy-fallback-extermination).
3. No migration (Track A). Snapshot B = named debt.
4. Contract-first: regen only, never hand-edit `api.gen.go`.

## Collision-matrix compliance
- OWN `api/openapi` edits this batch ✓ (contract lock).
- NO migration added ✓ (max on main = 0305).
- Did NOT edit `TenantDataPort` registration (Track B) ✓.
- Stayed OFF `sla_overdue_reader.go` / `sla_surface_writer.go` (Track B live SLA fix) ✓ — worklist query never touched them.

---

## Slice 1 — backend (subject-generic worklist + template inbox meta)

### Files
- `api/openapi/v1/openapi.yaml` — `ApprovalInboxItem` redefined subject-generic (subject_kind enum document|template, subject_key, subject_title, subject_ref; controlled_document_id nullable; DROP document_id/document_title).
- `internal/modules/approval/api/api.gen.go` — regenerated (oapi-codegen; not hand-edited).
- `internal/modules/approval/domain/subject.go` — `TemplateInboxMeta{TemplateID,Title}` (domain leaf, breaks import cycle).
- `internal/modules/approval/application/template_submit_service.go` — `TemplateVersionReader` port + `LoadTemplateInboxMeta(...)` method (returns `map[string]domain.TemplateInboxMeta`).
- `internal/modules/approval/application/read_service.go` — `InboxView` subject-generic; `ListWorklist` selects subject_kind/subject_key, keeps document join, resolves template rows via reader post-scan.
- `internal/modules/approval/application/services.go` — `newReadService` takes `templateRead`; `WithTemplateVersionReader` wires it.
- `internal/modules/templates/infrastructure/approval_version_reader.go` — `LoadTemplateInboxMeta` adapter (set-based ANY($) join, tenant-scoped).
- `internal/modules/approval/http/contracts/instance_read.go`, `http/inbox_handler.go` — wire struct + handler mapping subject-generic.
- Tests: `read_service_worklist_subject_generic_integration_test.go` (NEW, failing-first); `template_submit_service_integration_test.go` fake gains `LoadTemplateInboxMeta` (required-for-compile drive-by from widening the port).

### Gates
- **L0 go build ./...** — EXIT 0 ✓
- **L0 gofmt** — all touched files clean ✓ (repo has pre-existing gofmt-dirty files outside scope; not touched)
- **L0 go vet** (approval + templates/infrastructure) — clean ✓
- **L0 unit tests** (`go test ./internal/modules/approval/{application,http,http/contracts}/...`) — ok ✓
- **L0 api-lint** (`-strict api/openapi/v1/openapi.yaml .`) — 0 violations ✓. Caught 2 SHAPE-NULLABLE-NOT-REQUIRED (`controlled_document_id`, `due_at` nullable but not in `required` → drifts to optional). Fixed: added both to `required` (present-and-null), and changed the wire DTO `InboxItem.ControlledDocumentID` `string,omitempty` → `*string` (json `controlled_document_id`, no omitempty) so template rows emit explicit `null`, never a dropped key (legacy-fallback-extermination; mirrors the existing `DueAt *string` pattern). Handler maps `""`→nil.
- **L0 module-boundaries** (`scripts/check-module-boundaries.ps1`) — OK ✓ (approval→templates stays via the `TemplateVersionReader` port)
- **L0 contract-first parity** — `go generate ./...` on `internal/modules/approval/api` reproduces `api.gen.go` from `openapi.yaml`; working tree = generator output (never hand-edited) ✓
- **L1 integration** — the 3 new worklist tests PASS ✓ via `./scripts/test-integration.ps1 -Run "TestListWorklist_DocumentInstance_StillReturnsTitleAndControlledDocID_RealDB|...TemplateInstance_AppearsWithSubjectTitleAndRef...|...CrossTenantVersion_NoLeak..."` → `ok metaldocs/internal/modules/approval/application 387.314s`. Execution (not skip) proven by prior runs of these same tests failing with real scan errors before the fix.
  - **2 correctness bugs found & fixed during L1** (implementer's slice had them; caught by the failing-first tests):
    1. `ListWorklist` scanned `ai.document_id` (NULL for template rows since 0297) into a bare `string` → `sql: Scan error ... converting NULL to string is unsupported`. Fix: scan into `sql.NullString`, `SubjectRef = documentID.String` on the document branch.
    2. Deprecated `listInboxItems` shared the same `in_progress + eligibility` WHERE with no subject filter → template rows reached its bare-string `document_id` scan (crash) and would mislabel templates as documents. Fix: explicit `AND ai.subject_kind = 'document'` (subject_kind NOT NULL since 0296) — makes its documented document-only contract true in SQL.
- **Test-infra note (Track B / 4.3 domain):** shared-Postgres template-sweep (`prepareTemplateDatabase`) drops cross-fingerprint templates → concurrent parallel-track integration runs on different branches evict each other's template mid-run (`template ... does not exist` SQLSTATE 3D000). 349 orphaned `metaldocs_test_*` DBs (leaked by prior drop-timeouts) bogged Postgres, widening the collision window; dropped them to get a clean run. Flagged to hub.
- **Independent slice-1 reviewer** (caveman:cavecrew-reviewer, read-only, git-diff only, independent of the implementer + of the main-thread fixes) — **APPROVE, "No issues"** ✓ (42 tool-uses; checked boundary/tenancy/NULL-safety/present-and-null/no-fallback/import-cycle per the binding constraint list).

---

## Slice 2 — FE (inbox consumes subject-generic rows) — DONE (`15ae8e03`)

### Files
- `frontend/apps/web/src/lib/api-types/index.d.ts` — regenerated (`npm run gen:api`, openapi-typescript; not hand-edited).
- `features/approval/pages/InboxPage.tsx` — `openDocument` branches on `subject_kind`: document→`/documents/:subject_ref`, template→`/templates/:subject_ref/approval`. `openDecisionFlow` early-returns unless `subject_kind === 'document' && controlled_document_id != null` (doc-only quick-decision gate).
- `features/approval/components/{InboxApprovalCard,InboxStack,InboxTimeline}.tsx` — `document_title`→`subject_title`; subject-aware code/labels; template rows get a single "Abrir revisão do modelo" action (no doc-only Aprovar/Devolver).
- `features/dashboard/pages/DashboardPage.tsx`, `lib/inbox/sortByDue.test.ts` — **premise correction**: the analysis assumed the inbox was the SOLE FE consumer of `ApprovalInboxItem`; the slice-2 implementer flagged 2 more broken by the dropped fields. Per the hard-break rule (fix all consumers same unit) both were migrated here, not deferred. Flagged to hub.
- Tests: `InboxPage.test.tsx`, `InboxStack.test.tsx` — fixtures updated to the generated shape + new template-row coverage (nav target, doc-only actions absent, `fetchActiveDocumentInstance` not called).

### Gates
- **L0 vitest** — `npx vitest run src/features/approval src/lib/inbox src/features/dashboard` → **33 files / 245 tests pass** ✓
- **L0 tsc** — clean for all touched files ✓. **One TS2719 on `src/lib/inbox/sortByDue.test.ts` is a worktree ENV artifact, not a defect** — root-caused and triangulated:
  1. the worktree's `node_modules` is a **symlink to the main repo** → tsc realpath dual-identity;
  2. `tsc --listFiles` shows `api-types/index.d.ts` + `approvalTypes.ts` included **exactly once** each → single symbol → TS2719 is semantically impossible from a real duplicate;
  3. `--traceResolution`: every import resolves to the same single file (no second copy, none in node_modules);
  4. the same construct compiles **clean in the main repo's real (non-symlinked) install**;
  5. vitest is green, so the fixture shape is correct.
  It is field-independent (it surfaced only once the fixture matched the new shape; before that a different error masked it). Not fixable from here: the remedy is a real `pnpm install`, but `node_modules` is shared with the main repo/other tracks, so reinstalling would touch the hub-owned stack. Known issue `fe-node-modules-junction-drift`. **Flagged to hub as env, not code.**
- **Independent FE reviewer** (frontend-code-reviewer, read-only, subagent fan-out, independent of the implementer) — **APPROVE** ✓. Verified regen parity byte-for-byte, hard break honored (no resurrection/relax-to-optional), `subject_ref` (not `subject_key`) at every nav site, action gating, no `any`/`!`/`@ts-ignore`, both-subject-kind test coverage. 1 Minor: `DashboardPage` `?? ''` silently blanked a contractually-non-null value → **FIXED** (honest non-value `'—'`, no-fallback-principle) before commit.

---

## Close

### Final ladder (re-run on the full range at `9dd47e8b`)
| Gate | Result |
|---|---|
| `go build ./...` | EXIT 0 ✓ |
| `go vet` (approval + templates/infrastructure) | clean ✓ |
| `gofmt` (touched files) | clean ✓ |
| `go test ./internal/modules/approval/...` | ok ✓ |
| `api-lint -strict` | **0 violations** ✓ |
| `check-module-boundaries.ps1` | **OK** ✓ (approval→templates stays behind `TemplateVersionReader`) |
| **Contract-first parity** | `go generate` (api.gen.go) + `npm run gen:api` (FE types) → **git status empty** ✓ — both are pure generator output, never hand-edited |
| **L1 integration** | 3 new real-DB worklist tests PASS ✓ (`ok metaldocs/internal/modules/approval/application 387.314s`) — unchanged since `f6a266b9`; slice 2 + `9dd47e8b` touched no query/scan path (FE + a json tag) |
| **L1 FE** | vitest 33 files / 245 tests pass ✓ |

### Final-gate review — Claude (leg 1 of dual-reviewer parity)
**caveman:cavecrew-reviewer**, read-only (git diff/show/grep only), independent of the implementer and of the main-thread fixes, briefed on the 7 binding invariants. Reviewed the full `7e046056..HEAD` diff (35 tool-uses).
**VERDICT: APPROVE** — no CRITICAL, no MAJOR. Confirmed clean: module-boundary port scoping; tenant-scoped adapter (`tv.tenant_id = $1`, no cross-tenant leak, proven by the added no-leak test); NULL-safety on `ai.document_id` (`sql.NullString` + the `subject_kind='document'` guard on deprecated `listInboxItems`); `subject_ref` vs `subject_key` correct at every producer/consumer; no relax-to-optional / `as any` / non-null-assertion / COALESCE substitution; **no row-drop** — an unresolved template meta leaves title/ref empty rather than removing the row (a vanishing template review would have been a correctness bug).
1 nit → **FIXED in `9dd47e8b`**: unit 4.2 added `stage_kind` to the openapi `required` list, but the hand-written `contracts.InboxItem.StageKind` still carried `omitempty` — an empty value would drop the key and contradict the spec. Inert at runtime (`approval_stage_instances.stage_kind` is NOT NULL DEFAULT 'approval', 0286) but the same present-and-null drift class already closed for `controlled_document_id`/`due_at`, and **api-lint cannot catch it** (SHAPE-NULLABLE-NOT-REQUIRED only covers *nullable* fields). Ladder re-run green after the fix.

### Final-gate review — GPT-5.6 (leg 2) — HUB-OWNED, per HARNESS codex sandbox fallback
Not run unit-side, **by directive**. `5183628b` (landed on main after this branch point at `7e046056`) supersedes the earlier unit-side codex instruction: on this machine `codex exec`'s Windows sandbox is broken machine-wide (`codex-windows-sandbox-setup.exe` missing → `orchestrator_helper_launch_failed`; fix = operator runs interactive `/codex:setup`). Until fixed, **units do NOT attempt codex themselves and do NOT waive/hold — they emit close-ready and the HUB runs the GPT-5.6 pass via stdin evidence-pack** (`codex exec --sandbox read-only -`).
⇒ This unit emits **close-ready**, not CLOSED. Hub runs leg 2 and records both verdicts + any reconciliation in the CLOSED event.

### Hub escalations
1. **Test-infra (Track B / 4.3):** shared-Postgres template-sweep (`prepareTemplateDatabase`) drops cross-fingerprint templates → concurrent parallel-track integration runs on different branches evict each other's template mid-run (`template ... does not exist`, SQLSTATE 3D000). 349 orphaned `metaldocs_test_*` DBs (leaked by prior drop-timeouts) widened the window; dropped them to get a clean run.
2. **Premise correction:** `ApprovalInboxItem` had **3** FE consumers, not 1 (inbox + dashboard + sortByDue fixture). All migrated in-unit per the hard-break rule; recorded so future contract breaks grep for consumers rather than assuming.
3. **Env (not code):** worktree `node_modules` symlinked to the main repo causes a tsc realpath dual-identity TS2719 (details above). Needs a real `pnpm install` in the worktree; not doable from here without touching the shared/hub-owned tree.

### Named debt (unchanged)
Snapshot columns on `approval_instances` (design B) remain the global maximum but need a migration (Track C owns 0306+). Reopen trigger: next `approval_instances` migration OR a measured worklist perf regression from the reader-bridge path.

### Close-ready
- **Branch:** `unit-4.2-template-inbox`
- **Commit range:** `7e046056..9dd47e8b` (4 commits: `e6254981` analysis · `f6a266b9` backend slice 1 · `15ae8e03` FE slice 2 · `9dd47e8b` stage_kind present-and-null fix)
- **Evidence:** this file
- **Claude final review:** APPROVE · **GPT-5.6 leg:** hub-owned (sandbox fallback)
