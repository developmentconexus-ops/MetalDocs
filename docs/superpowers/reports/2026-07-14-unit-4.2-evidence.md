# Unit 4.2 evidence — template reviews in the single approver inbox

**Track:** A (contract lock — owns `api/openapi` this batch) · **Branch:** `unit-4.2-template-inbox` (off `7e046056`)
**Intent:** Surface template reviews in the SINGLE approver worklist/inbox, contract-first, consuming the extracted approval kernel worklist (subject_kind='template'). Never a legacy template-review endpoint.
**System-impact analysis:** `docs/superpowers/analysis/2026-07-14-unit-4.2-template-inbox-system-impact.md` (Verdict 🟡 Yellow; committed e6254981).

## GM-fork (HARNESS §3)
Chosen **C — reader-bridge**: worklist selects approval's own `ai.subject_kind`/`ai.subject_key`; document rows keep the existing `LEFT JOIN documents` UNCHANGED; template rows resolve `{templateId, name}` via the approval-owned `TemplateVersionReader.LoadTemplateInboxMeta` port (new batch method) AFTER the query. Boundary-clean (grows the sanctioned port, not the smell), no migration.
- **A (raw-join templates tables)** rejected — deepens the boundary smell (2nd module's tables in approval SQL), violates invariant 6.
- **B (snapshot columns)** is the global maximum but needs a migration (Track C owns 0306+) → filed as **named debt**. Reopen trigger: next `approval_instances` migration OR measured worklist perf regression from per-row reader calls.

## Locked constraints (from analysis §10)
1. approval→templates ONLY via `TemplateVersionReader` port; documents path byte-for-byte unchanged.
2. Hard-break wire contract (drop `document_id`/`document_title`; add subject-generic fields) + update sole FE consumer same unit — no relax-to-optional (legacy-fallback-extermination).
3. No migration (Track A). Snapshot B = named debt.
4. Contract-first: regen only, never hand-edit `api.gen.go`.

## Collision-matrix compliance
- OWN `api/openapi` edits this batch ✓ (contract lock).
- NO migration added ✓ (max on main = 0305).
- Did NOT edit `TenantDataPort` registration (Track B) ✓.
- Stayed OFF `sla_overdue_reader.go` / `sla_surface_writer.go` (Track B live SLA fix) ✓ — worklist query never touched them.

---

## Slice 1 — backend (subject-generic worklist + template inbox meta)

### Files
- `api/openapi/v1/openapi.yaml` — `ApprovalInboxItem` redefined subject-generic (subject_kind enum document|template, subject_key, subject_title, subject_ref; controlled_document_id nullable; DROP document_id/document_title).
- `internal/modules/approval/api/api.gen.go` — regenerated (oapi-codegen; not hand-edited).
- `internal/modules/approval/domain/subject.go` — `TemplateInboxMeta{TemplateID,Title}` (domain leaf, breaks import cycle).
- `internal/modules/approval/application/template_submit_service.go` — `TemplateVersionReader` port + `LoadTemplateInboxMeta(...)` method (returns `map[string]domain.TemplateInboxMeta`).
- `internal/modules/approval/application/read_service.go` — `InboxView` subject-generic; `ListWorklist` selects subject_kind/subject_key, keeps document join, resolves template rows via reader post-scan.
- `internal/modules/approval/application/services.go` — `newReadService` takes `templateRead`; `WithTemplateVersionReader` wires it.
- `internal/modules/templates/infrastructure/approval_version_reader.go` — `LoadTemplateInboxMeta` adapter (set-based ANY($) join, tenant-scoped).
- `internal/modules/approval/http/contracts/instance_read.go`, `http/inbox_handler.go` — wire struct + handler mapping subject-generic.
- Tests: `read_service_worklist_subject_generic_integration_test.go` (NEW, failing-first); `template_submit_service_integration_test.go` fake gains `LoadTemplateInboxMeta` (required-for-compile drive-by from widening the port).

### Gates
- **L0 go build ./...** — EXIT 0 ✓
- **L0 gofmt** — all touched files clean ✓ (repo has pre-existing gofmt-dirty files outside scope; not touched)
- **L0 go vet** (approval + templates/infrastructure) — clean ✓
- **L0 unit tests** (`go test ./internal/modules/approval/{application,http,http/contracts}/...`) — ok ✓
- **L0 api-lint** (`-strict api/openapi/v1/openapi.yaml .`) — 0 violations ✓. Caught 2 SHAPE-NULLABLE-NOT-REQUIRED (`controlled_document_id`, `due_at` nullable but not in `required` → drifts to optional). Fixed: added both to `required` (present-and-null), and changed the wire DTO `InboxItem.ControlledDocumentID` `string,omitempty` → `*string` (json `controlled_document_id`, no omitempty) so template rows emit explicit `null`, never a dropped key (legacy-fallback-extermination; mirrors the existing `DueAt *string` pattern). Handler maps `""`→nil.
- **L0 module-boundaries** (`scripts/check-module-boundaries.ps1`) — OK ✓ (approval→templates stays via the `TemplateVersionReader` port)
- **L0 contract-first parity** — `go generate ./...` on `internal/modules/approval/api` reproduces `api.gen.go` from `openapi.yaml`; working tree = generator output (never hand-edited) ✓
- **L1 integration** — the 3 new worklist tests PASS ✓ via `./scripts/test-integration.ps1 -Run "TestListWorklist_DocumentInstance_StillReturnsTitleAndControlledDocID_RealDB|...TemplateInstance_AppearsWithSubjectTitleAndRef...|...CrossTenantVersion_NoLeak..."` → `ok metaldocs/internal/modules/approval/application 387.314s`. Execution (not skip) proven by prior runs of these same tests failing with real scan errors before the fix.
  - **2 correctness bugs found & fixed during L1** (implementer's slice had them; caught by the failing-first tests):
    1. `ListWorklist` scanned `ai.document_id` (NULL for template rows since 0297) into a bare `string` → `sql: Scan error ... converting NULL to string is unsupported`. Fix: scan into `sql.NullString`, `SubjectRef = documentID.String` on the document branch.
    2. Deprecated `listInboxItems` shared the same `in_progress + eligibility` WHERE with no subject filter → template rows reached its bare-string `document_id` scan (crash) and would mislabel templates as documents. Fix: explicit `AND ai.subject_kind = 'document'` (subject_kind NOT NULL since 0296) — makes its documented document-only contract true in SQL.
- **Test-infra note (Track B / 4.3 domain):** shared-Postgres template-sweep (`prepareTemplateDatabase`) drops cross-fingerprint templates → concurrent parallel-track integration runs on different branches evict each other's template mid-run (`template ... does not exist` SQLSTATE 3D000). 349 orphaned `metaldocs_test_*` DBs (leaked by prior drop-timeouts) bogged Postgres, widening the collision window; dropped them to get a clean run. Flagged to hub.
- **Independent slice-1 reviewer** (caveman:cavecrew-reviewer, read-only, git-diff only, independent of the implementer + of the main-thread fixes) — **APPROVE, "No issues"** ✓ (42 tool-uses; checked boundary/tenancy/NULL-safety/present-and-null/no-fallback/import-cycle per the binding constraint list).

---

## Slice 2 — FE (inbox consumes subject-generic rows) — PENDING
Regen FE types (`npm run gen:api`); InboxPage.openDocument branch document→`/documents/:subject_ref` template→`/templates/:subject_ref/approval`; gate doc-only quick-decision (controlled_document_id); InboxApprovalCard/InboxStack/InboxTimeline `document_title`→`subject_title` + subject-aware code/label; vitest failing-first; independent FE reviewer.

## Close — PENDING
Ladder L0-L1; **dual-reviewer parity** (Claude cavecrew-reviewer + GPT-5.6 SOL via codex:rescue, both subagent-fanned, git-read-only) on final diff — BOTH verdicts + reconciliation in CLOSED event; wiki-curator refresh `wiki/modules/approval.md`; CLOSED event to hub with branch + commit range + this evidence path.
