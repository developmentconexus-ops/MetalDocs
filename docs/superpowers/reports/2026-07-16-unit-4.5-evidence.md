# Unit 4.5 evidence — `document_profiles` tenant-scoped PK

**Date:** 2026-07-16 · **Branch:** `unit-4.5-profile-tenant-pk` (worktree `.claude/worktrees/unit-4.5-profile-tenant-pk`, base main @ `57615ecd`)
**Charter:** ROADMAP row 4.5 — PK `(code)` → `(tenant_id, code)`, resolve code-only child FKs, app-layer sweep, baseline fold-in, acceptance test GREEN.

## §1 P0 gate (developing-new-work)

Verdict 🟡 Yellow, zero hard-stops. Artifact: `docs/superpowers/analysis/2026-07-16-unit-4.5-profile-tenant-pk-system-impact.md` (`445d2bd0`, ratification amendments `e869470c`).

**Charter drift found (blast radius verified against schema truth, as charter demanded):**
1. `template_drafts` already dropped by migration 0260 → 4 code-only children are actually **3**.
2. All 3 remaining (`document_profile_governance`, `document_profile_schema_versions`, `document_sequences`) are DEAD: no tenant_id, zero Go refs, zero writes anywhere (baseline DDL only), archive-era origin, live superseding mechanisms exist.
3. Acceptance test real path: `internal/modules/controlleddocuments/application/tenant_isolation_test.go:203` (charter's `tests/integration/iam/...` wrong).

## §2 Hub ratification (2026-07-16)

Ratified: (a) DROP the 3 dead children (extermination over retrofit, 0260 precedent); (b) drop redundant `ux_document_profiles_tenant_code`; (c) migration number 0308.
**Escalation ruled IN SCOPE:** `document_profile_template_defaults` (no tenant_id/no FK/global PK, LIVE writer) — same-migration-or-never (0308 destroys the profile_code→tenant backfill derivation). Fork resolved on evidence: **FORK 1 (DROP)** — zero readers repo-wide (no product Go SELECT, no SQL function/view/trigger; sole writer = e2e-seed `ensurePODefaultTemplateBinding`, write-only vestige; superseded by `document_profiles.default_template_version_id`).

## §3 Dispatch ledger (HARNESS §4 obligations)

| Slice | Implementer | Reviewer | Verdict |
|---|---|---|---|
| Inventory (blast radius) | cavecrew-investigator (sonnet, a69c875b) | n/a (read-only) | report consumed; charter drift found |
| S1 migration 0308 + shape tests | general-purpose sonnet (a7997b1c) | cavecrew-reviewer sonnet (a6197cb7) | **No issues** |
| S2 app-layer sweep | general-purpose sonnet (ae235c1d) | cavecrew-reviewer sonnet (afaa2dc2) | 1 🟡 (0264 drive-by, documented below), no blockers |
| S3 baseline fold-in | general-purpose sonnet (ad9de0bd; resumed for 0260-fold expansion) | cavecrew-reviewer sonnet (a1c7f4f8) | 0 🔴, 1 🟡 (dead-function flag, §9.3) |
| S4 wiki sweep | general-purpose sonnet (ac7a5835) | cavecrew-reviewer sonnet (a7fa9d9c) | 1 🔴 fixed inline (stale two-pool row), 1 nit accepted |
| Final gate (net diff 57615ecd..HEAD) | — | cavecrew-reviewer sonnet (a9cebdeb) | **No issues** (0🔴 0🟡; checked 0308↔baseline convergence, dropped-object orphans, scope discipline, FK-name truth, evidence accuracy) |
| Final gate GPT arm | — | GPT-5.6 hub-side (codex broken machine-wide) | *(pending — hub runs at CLOSE-READY)* |

## §4 S1 — migration 0308 (`1e2aacf7`)

`db/migrations/0308_document_profiles_tenant_pk.sql` (160 lines) + `tests/integration/migrations/migration_0308_test.go` (253 lines, 2 tests) + acceptance-test tripwire fix.

- **Design deviation (evidence-driven, reviewer-verified sound):** `ALTER TABLE ... ADD CONSTRAINT document_profiles_pkey PRIMARY KEY USING INDEX ux_document_profiles_tenant_code` (0264 precedent) instead of drop/recreate-FK sequence — the 3 composite FKs were bound to the ux index (not the old PK) and survive untouched; USING INDEX consumes the index (no separate drop step). After the 4 dead-table drops the old PK has zero dependents. Idempotent DO-block guards (load-bearing: `dev-bootstrap-baseline.ps1` replays every migration unconditionally).
- **TDD failing-first proven:** with migration absent, shape test FAILED (`document_profile_governance` still exists); acceptance test reproduced original schema-RED (23505 `document_profiles_pkey` at `tenant_isolation_test.go:217`). With migration: both shape tests PASS (127.8s/6.4s); full migrations package 30/30 PASS 30.0s (no regressions).
- **Acceptance test GREEN:** `TestTenantIsolation_SequenceCounters_CrossTenant` + package suite 6/6 PASS 123.7s. Required one test-side fix: `testdb.SetCapsOnDB` for the `cd_sequence_counters` `trg_require_cap_asserted` tripwire (allocator writes on the pool, no caller tx; sanctioned helper, 10+ precedents, safe under the test's existing `MaxOpenConns(1)`).
- `go build ./...` clean; `go vet -tags=integration ./...` clean repo-wide.

## §5 S2 — app-layer sweep (`4e759c48`)

- `internal/test/e2e_seed.go`: `ON CONFLICT (code)` → `(tenant_id, code)` for `document_profiles`; **same fix for `document_process_areas` — PRE-EXISTING defect since 0264 dropped its single-column PK (42P10 on any execution), drive-by repaired and flagged by reviewer as outside-0308-blast-radius-but-correct.**
- `metaldocs-e2e-seed/main.go`: `ensurePODefaultTemplateBinding` + call site deleted (−67 lines), imports cleaned, no stubs.
- sp2 dictionary-pin test: now seeds the SAME profile code for both tenants (exercises the newly-legal shape).
- `family_code_resolver`: stale "global PK / unreachable tie-break" comments corrected; PK-pin test re-pinned to composite (it would have been RED post-0308); NEW `TenantPrecedenceOverSentinel` test proves the tie-break the old comment called unreachable.
- Residual sweep: 5 remaining `ON CONFLICT (code)` hits all target `document_families` (genuinely global) — out of scope. `routes_profiles.go:293` 23505 mapping constraint-name-agnostic — unaffected, verified.
- **Reported, not fixed:** `scripts/sql/perf_db_query_analysis.sql` references dropped tables + a column dropped by 0236 (pre-existing staleness; manual perf script, judgment-call rewrite out of boundary).
- Tests: sp2 4/4 PASS; family_code_resolver 7/7 PASS; live-DB proof of both ON CONFLICT targets (rolled-back tx).

## §6 S3 — baseline fold-in

Run 1: 0306/0307 folded; equivalence gate `check-baseline-equivalence.ps1` PASS (ref vs candidate scratch DBs, 6 object classes, zero diffs); migrations package PASS 320s. **0308 fold blocked:** baseline still creates `template_drafts` with FK to `document_profiles(code)` — from-scratch bootstrap errors without that unique constraint; folding 0308 requires folding 0260's drops first. Worker STOPped per instruction.
**Orchestrator ruling:** expand fold to 0260 (baseline-file-only change; 0260 IF-EXISTS idempotent replay; no other migration references the dropped objects). Run 2 (`54ec5da1`): fold covers 0260+0306+0307+0308, baseline diff +24/−345; equivalence gate PASS (ref = old baseline + full chain vs candidate = folded baseline + same chain — zero diffs, all 6 object classes); all 4 folded migrations replay as clean no-ops; ledgers identical (102 versions); migrations package PASS 259.5s. Review (a1c7f4f8): diff hygiene clean (only claimed object set), header note accurate, PK declaration matches curated idiom; 1 🟡 → §9.3.

## §7 S4 — wiki sweep (`1f40d4dd`)

10 living docs updated (tombstone convention per `templates_approval_config.md`; `_artifacts`/`reviews` point-in-time docs untouched). Review: 1 🔴 self-contradiction (stale "two DB pools" anomaly row) fixed inline; 1 nit accepted.

## §8 Ladder & exit gate

**L0:** `go build ./...` clean; `go vet -tags=integration ./...` clean; module-boundaries check OK; `check-test-discipline.sh` → 7 violations, ALL pre-existing on main (this unit's new violation at `migration_0308_test.go:238` was fixed in `fa2afabd` via `testdb.SetCapsOnTx`).
**L1 (package-scoped, per slice):** migrations package 30/30 PASS (S1, 30.0s; re-run S3b 259.5s incl. 0308 shape tests); controlleddocuments/application 6/6 PASS 123.7s incl. acceptance test GREEN; sp2 dictionary-pin 4/4 PASS; taxonomy family_code_resolver 7/7 PASS; TestMigration0308 re-run post-lint-fix PASS 153.1s.
**Acceptance:** `TestTenantIsolation_SequenceCounters_CrossTenant` GREEN (was schema-RED on main — 4.6's named waived defect).
**Full-suite exit run:** *(pending hub window — §9.3a coordination)*.

## §9 Bounded defers / escalations for hub

1. `scripts/sql/perf_db_query_analysis.sql` stale (pre-existing, references 0236/0308-dropped objects) — manual script, candidate for micro-debt register.
2. 0260's note kept `document_template_versions` alive because e2e-seed's `ensurePODefaultTemplateBinding` SELECTed it; that reader is now deleted — `document_template_versions` liveness may warrant a fresh census (observation only, NOT investigated here).
3. `prevent_published_template_mutation` (baseline fn, deliberately kept in fold): its sole trigger attacher (`document_template_versions_mddm.trg_template_immutable`) is now folded out — zero live tables attach it. Dead-function candidate (S3 reviewer 🟡); drop belongs to a future dead-object migration, not this fold.
4. `public.templates_audit_log` still created by baseline but dropped by unfolded 0262 — future fold candidate (pre-existing, noted during S3).
