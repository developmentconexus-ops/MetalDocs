# T11 — B11 Access Administration — P8 R2 operator re-LOCK

> **Status:** SUPERSEDED BY MATERIAL R2 CHALLENGE / OP32 SCOPE REOPENED.  
> **Date:** 2026-08-25.  
> **Implementation:** BLOCKED.  
> **Artifact class:** temporary frontend-planning Evidence; preserve exact identity before `docs/work/**` cleanup.

## Subsequent disposition

Final challenge R2 proved a reachable fresh-review/fresh-key path while the op32 outcome remained ambiguous. This exact LOCK remains historical Evidence for its blobs but is not transferred to R3.

```text
challenge record  docs/work/current/t11-b11-final-challenge-r2.md
R2 verdict        NOT CONVERGED
current P8        R3 CANDIDATE / RE-LOCK REQUIRED
```

## 1. Decision chain

The initial P8 LOCK was materially falsified by independent challenge R1 and preserved as superseded Evidence. Lead accepted the smallest reopen and produced R2, closing:

```text
exact fixed Role/Permission bundles
operable /admin/access 403 denial
operable selected-Group 404 reconciliation
drawer aria-controls + focus/inert/Escape behavior
P9 Previous/fixture-coverage/eligibility precision
```

The four clean-rebaseline failures remained closed. The operator then operated the R2 HTML in the in-app browser and replied:

```text
aPROVADO
```

This was the direct response to the explicit R2 re-LOCK gate. The decision is therefore:

```text
B11 P8 R2             LOCKED
decision owner        operator
assistant LOCK        none
P9/P10                authorized to reclose against exact R2
final challenge       required after P9/P10
```

## 2. Exact locked package

The only post-approval P8 change was the visible title/status/note marking the operator decision. No interaction, fixture, layout or semantic behavior changed.

```text
HTML
  path      docs/work/current/t11-b11-access-administration-p8.html
  Git blob  c0dfe7b942b83f53374307dbdb3d3524b7d47c69

CSS
  path      docs/work/current/t11-b11-access-administration-p8.css
  Git blob  9ce012007613777187ae70956c2bfa09e7066c16

JavaScript
  path      docs/work/current/t11-b11-access-administration-p8.js
  Git blob  3e923746cfea01142249b8166500833c807c5ce5
```

Pre-status candidate HTML operated by the user was blob `02fb202ff8031fc4f2a760537d53cb4680eb515b`; CSS and JavaScript were already the exact final blobs above.

## 3. Protected R2 structure

The re-LOCK protects the full integrated P8 interaction structure, including:

```text
Por Área / Grupos / Funções local IA
separate Area-specific and Company-wide grants
Group direct-grant footprint before membership mutation
raw visible op6/op16/op22/op27/op31 page traversal
DISABLED User visible/unavailable without eligibility prefilter
unknown membership reconciled by op28 201/204
fixed exact RoleView meaning and scope compatibility
initial op32 success + exact completed replay + ambiguous same-key recovery
operation-targeted 403 denial and 404 disclosure-safe reconciliation
focus-managed responsive drawer and deliberate security dialogs
no operation 90, Group.area_id, custom Role editor or effective-access engine
```

## 4. Boundary

This LOCK authorizes exact P9/P10 proof and the final independent challenge only. It does not authorize implementation, B12, FP2/P11, roadmap mutation by reviewer/assistant, PR merge or any excluded backend/Product capability.
