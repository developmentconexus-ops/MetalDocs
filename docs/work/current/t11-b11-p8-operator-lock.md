# T11 — B11 Access Administration — P8 operator LOCK

> **Status:** SUPERSEDED BY MATERIAL EVIDENCE / SMALLEST SCOPE REOPENED.  
> **Date:** 2026-08-25.  
> **Implementation:** BLOCKED.  
> **Artifact class:** temporary frontend-planning Evidence; exact durable identity must be recorded before `docs/work/**` cleanup.

## Subsequent disposition

The final independent challenge R1 materially falsified the exact locked candidate through incorrect fixed Role bundles and non-operable 403/404 Evidence. The operator decision below remains preserved as historical Evidence for its exact blobs; it is not silently transferred to corrected bytes.

```text
challenge record  docs/work/current/t11-b11-final-challenge-r1.md
R1 verdict        NOT CONVERGED
current P8        R2 CANDIDATE / RE-LOCK REQUIRED
```

## Decision

The operator opened and operated the current clean B11 P8 candidate, then replied:

```text
Aprovado
```

This was the direct response to the explicit gate asking whether to declare the B11 P8 LOCK or request another revision. It therefore records the operator decision:

```text
B11 P8 clean candidate  LOCKED
additional P8 revision  not requested
P9                      authorized next
P10                     authorized after P9
```

The assistant did not set the decision; it records the operator's disposition.

## Locked artifact package

```text
docs/work/current/t11-b11-access-administration-p8.html
  Git blob  bbf4018739f6abdf4299adbeb8b056125cee0dd7

docs/work/current/t11-b11-access-administration-p8.css
  Git blob  3336919859fa9fefff7019525c251e20853d9bc9

docs/work/current/t11-b11-access-administration-p8.js
  Git blob  6c1e28dae16957a37f1e29d335d9e20a796e57c6
```

The package is one browser-operable P8 artifact. The hashes above were computed from the exact post-LOCK local bytes after JavaScript syntax verification.

## Protected interaction structure

```text
/admin/access
Por Área / Grupos / Funções
Area-specific and Company-wide grants remain separate
Group multi-scope footprint without Group.area_id
fixed Role meaning from RoleView only
Subject × Role × Scope grant review
exact assignment_id revoke
visible op6/op16/op22/op27/op31 page traversal
raw op6 page boundaries with DISABLED visible/unavailable
op28 201 first-add vs 204 already-member reconciliation
same-key grant replay with zero second semantic mutation
bounded membership/revoke consequence language
no frontend effective-access authority
```

## Gate consequence

P9 must trace this exact locked structure bidirectionally to accepted Product/backend authority. P10 may consolidate only repeated semantics already proved by B01–B11; neither phase may silently redesign the LOCK.
