-- guard fixture migration 0002 (branch-new -- does not exist on origin/main)
-- FALSE POSITIVE shape: edited AGAIN in a second commit on this same
-- branch, before merge. This file has never touched origin/main, so this
-- second intra-branch edit is normal development, not a violation -- PR
-- #113's exact shape (db/migrations/0318_capability_bindings_schema_
-- backfill.sql was added, then edited twice, entirely within one branch).
