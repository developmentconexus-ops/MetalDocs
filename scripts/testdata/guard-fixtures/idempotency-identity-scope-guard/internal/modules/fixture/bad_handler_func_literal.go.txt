package fixture

import (
	"context"

	"metaldocs/internal/platform/tenant"
)

var badActorFromCtxFuncLiteral = func(ctx context.Context) (string, string, error) {
	_, err := tenant.FromContext(ctx)
	return "", "", err
}
