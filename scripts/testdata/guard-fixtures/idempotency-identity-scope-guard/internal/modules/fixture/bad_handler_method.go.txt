package fixture

import (
	"context"

	"metaldocs/internal/platform/tenant"
)

type handler struct{}

// A receiver method is still assignable wherever actorFromCtx is expected.
func (handler) badActorFromCtxMethod(ctx context.Context) (string, string, error) {
	_, _ = tenant.FromContext(ctx)
	return "", "", nil
}
