openapi: 3.0.3
info:
  title: guard fixture — path key re-includes the server base
  version: '1.0'
servers:
  - url: /api/v1
paths:
  /api/v1/widgets/{id}:
    get:
      operationId: getWidget
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: ok
          content:
            application/json:
              schema:
                type: object
