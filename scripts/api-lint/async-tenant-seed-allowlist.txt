# ASYNC-TENANT-SEED sanctioned allowlist (M3 F3.2 — validation-contract.md
# §2.4). Format mirrors seed-chokepoint-allowlist.txt / tripwire-allowlist.txt:
# `relative/path.go:LINE  # category — reason`. `#`-comments and blank lines
# ignored. Only these categories are sanctioned:
#   (A) outbox claim steps (ADR 0054 rule 1) — cross-tenant FOR UPDATE SKIP
#       LOCKED; tenancy enforced one row later at processing time.
#   (B) cross-tenant scans with no per-row mutation.
#   (C) system tables with no tenant_id column (idempotency_keys).
#   (D) audit-integrity scan — read-only, system-wide by design.
#   (E) same-file handler-local helper called only from a seeded caller in
#       the same file — a genuine instance of the lint's documented
#       function-local (not call-graph) coverage limit (contract §2.3).

# --- (E) same-file seeded-caller helper (handler-local AST limitation) ---
# NotificationsFanoutWorker.insertRow is called only by Work (same file),
# which calls authz.SeedTxTenant before dispatching to fanoutToReaders/
# fanoutToAuthor -> insertRow. The seed and the write are in different
# functions, which this rule's function-local scan cannot see across; Work's
# own sqlmock tests (fanout_worker_test.go) pin the real seed-before-write
# ordering at the call-site level.
internal/modules/notifications/infrastructure/fanout_worker.go:102  # (E) fanoutToReaders — seeded by Work (same file) before dispatch; ordering pinned by fanout_worker_test.go
internal/modules/notifications/infrastructure/fanout_worker.go:128  # (E) insertRow — seeded by Work (same file) before dispatch; ordering pinned by fanout_worker_test.go

# --- (A)/(B)/(C)/(D) — documented for auditability, NOT machine-parsed lines ---
# None of the sites below are live ASYNC-TENANT-SEED AST matches (their write
# is a QueryContext claim/scan, not Exec/ExecContext; or their table has no
# tenant_id column and so is absent from async-tenant-tables.txt by
# construction) — adding them as real allowlist lines would make them
# immediately ASYNC-TENANT-SEED-ALLOWLIST-STALE (the stale-check runs only
# over matched AST call sites). They are recorded here as prose so the
# category set from contract §2.4 stays auditable without corrupting the
# stale-detection invariant:
#   (A) internal/platform/messaging/outbox/postgres/consumer.go — ClaimUnpublished:
#       cross-tenant FOR UPDATE SKIP LOCKED claim; outbox_events has no tenant_id.
#   (A) internal/modules/render/fanout/staging_outbox.go — ClaimPending:
#       cross-tenant FOR UPDATE SKIP LOCKED claim (ADR 0054 rule 1); tenancy
#       enforced per-row in MarkDispatched/MarkFailed (§2.2 site 5, seeded).
#   (B) internal/modules/jobs/stuck_instance_watchdog/job.go — listStuckInstances:
#       cross-tenant read-only scan (QueryContext SELECT, no write).
#   (C) internal/modules/jobs/idempotency_janitor/job.go — idempotency_keys has
#       no tenant_id column; RLS structurally N/A.
#   (D) internal/modules/jobs/audit_integrity_validator/job.go — read-only,
#       system-wide integrity scan by design.
