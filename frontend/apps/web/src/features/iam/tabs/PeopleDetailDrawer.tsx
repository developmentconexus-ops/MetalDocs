export default function PeopleDetailDrawer() {
  return (
    <section
      data-testid="admin-people-detail-drawer"
      aria-labelledby="admin-people-detail-heading"
    >
      <h2 id="admin-people-detail-heading">Detalhes do usuario</h2>
      <p>Em construcao (PR-12 Fase 2). Sera aberto a partir de /admin/people/:userId.</p>
    </section>
  );
}
