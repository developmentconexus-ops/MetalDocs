# Screenshots — Phase 3b: novo-template-escopo

Screenshots were taken via `mcp__Claude_Preview__preview_screenshot` during Phase 3b.
The preview tool returns JPEG image data inline but does not write files to disk.

## Captured viewports

| File | Viewport | Status |
|---|---|---|
| 1440-impl.png | 1440×900 | Captured inline — 2-col grid, all 6 profile cards, wizard header + stepper, footer |
| 1024-impl.png | 1024×768 | Captured inline — 2-col grid maintained, full page visible |
| 375-impl.png | 375×812 | Captured inline — single-column collapse active (≤600px breakpoint), cards stacked |

## Visual observations

**1440×900:** Full wizard layout — kicker "ETAPA 1 DE 5", h2 "Escopo do template", caption text,
2-column profile grid with 6 cards (POP selected with brand border + check, CHK disabled + "EM BREVE" pill).
Footer with Cancelar + Avançar → buttons.

**1024×768:** Layout holds 2-column grid. No overflow. All text legible.

**375×812:** Single-column collapse works correctly. Cards stack vertically. Rail collapses to icon strip.
profileMeta wraps to two lines for Família label (normal at this width).

## Awaiting user screenshot approval
