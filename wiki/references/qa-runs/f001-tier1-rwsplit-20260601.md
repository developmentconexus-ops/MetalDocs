# F-001 Tier-1 read/write split — QA evidence (2026-06-01)

> **Scope:** Validate the surgical row split applied to `apps/api/cmd/metaldocs-api/permissions.go` by commit `f52917495`. Confirms the four view-grade caps from commit `1f9fed6fe` (ADR 0016) correctly gate the 13 audit rows from `wiki/references/qa-runs/plans-f001-f002.md`.
> **Method:** API direct via `curl` after `.\scripts\start-api.ps1 -Build`. Browser smoke via Claude Preview against `localhost:4173` (frontend) → `localhost:8081` (API).
> **Test users:**
> - `approver` / `ApproverMetalDocs123!` — role `approver`; holds `document.*` + `template.{view,review,approve}` + `membership.view` + `taxonomy.view`. Lacks `user.view`, `metrics.view`, all `*.manage`.
> - `admin` / `AdminMetalDocs123!` — role `system_admin`; holds all 27 caps.

## Login

```
POST /api/v1/auth/login {identifier:"approver", password:"…"}
→ HTTP 200; capabilities include "membership.view", "taxonomy.view"
POST /api/v1/auth/login {identifier:"admin", password:"…"}
→ HTTP 200; caps count: 27 (matches TestCapabilityRegistrySize)
```

## Approver read matrix (Tier-1 cap check)

| Method | Path | Expected | Got | Result |
|---|---|---|---|---|
| GET | `/api/v1/notifications` | 200 | 404 | PASS (Tier-1 allowed; handler not yet registered) |
| GET | `/api/v1/access-policies` | 200 | 404 | PASS (Tier-1 allowed; handler stub) |
| GET | `/api/v1/iam/users` | 403 | 403 `AUTH_FORBIDDEN` | PASS (no `user.view`) |
| GET | `/api/v1/iam/admin/overview` | 403 | 403 `AUTH_FORBIDDEN` | PASS (no `user.view`) |
| GET | `/api/v1/taxonomy/profiles` | 200 | 200 | PASS — pre-F-001 was 403 (cap was `taxonomy.manage`) |
| GET | `/api/v1/taxonomy/areas` | 200 | 200 | PASS — pre-F-001 was 403 |
| GET | `/api/v1/taxonomy/families` | 200 | 200 | PASS — pre-F-001 was 403 |
| GET | `/api/v1/iam/area-memberships?userId=approver` | 200 | 200 | PASS — pre-F-001 was 403 (methodless `membership.manage`) |
| GET | `/api/v1/metrics` | 403 | 403 `AUTH_FORBIDDEN` | PASS (no `metrics.view`) |

## Approver write matrix (must all 403)

| Method | Path | Got |
|---|---|---|
| POST | `/api/v1/iam/users` | 403 `AUTH_FORBIDDEN` |
| PATCH | `/api/v1/iam/users/u-1` | 403 `AUTH_FORBIDDEN` |
| POST | `/api/v1/iam/users/u-1/roles` | 403 `AUTH_FORBIDDEN` |
| POST | `/api/v1/taxonomy/profiles` | 403 `AUTH_FORBIDDEN` |
| PATCH | `/api/v1/taxonomy/areas/QA` | 403 `AUTH_FORBIDDEN` |
| DELETE | `/api/v1/taxonomy/families/PROC` | 403 `AUTH_FORBIDDEN` |
| POST | `/api/v1/iam/area-memberships` | 403 `AUTH_FORBIDDEN` |
| PUT | `/api/v1/access-policies` | 403 `AUTH_FORBIDDEN` |

All 8 write attempts blocked at Tier-1 before reaching handler. Confirms the methodless area-memberships row no longer shadows write verbs.

## Admin read matrix (sanity)

| Path | Got |
|---|---|
| `GET /api/v1/iam/users` | 200 |
| `GET /api/v1/iam/admin/overview` | 200 |
| `GET /api/v1/taxonomy/profiles` | 200 |
| `GET /api/v1/taxonomy/areas` | 200 |
| `GET /api/v1/taxonomy/families` | 200 |
| `GET /api/v1/iam/area-memberships?userId=admin` | 200 |
| `GET /api/v1/metrics` | 200 |

All 7 admin reads pass. Migration 0217 successfully seeded the 4 view caps onto `system_admin`.

## Browser smoke

`http://localhost:4173/` loaded as Approver Dev. Dashboard renders cleanly. No console errors. `GET /api/v1/documents` returns 403 from Tier-2 (approver holds `document.view` Tier-1 but lacks any `user_process_areas` row in dev seed) — pre-existing behavior unrelated to F-001; not regressed by this PR.

## Targeted unit tests

```
go test ./apps/api/cmd/metaldocs-api/... -run Permissions
ok  metaldocs/apps/api/cmd/metaldocs-api  (cached)
```

- `TestPermissionResolver` — 13 F-001 audit rows updated, all PASS.
- `TestPermissionsTable_NoMethodlessWriteShadowing` — new invariant, PASS.
- `TestRouteCoverage` — extended for new GET rows on area-memberships, PASS.

Wider scope:
```
go test ./apps/api/cmd/metaldocs-api/... ./internal/modules/iam/... ./internal/modules/taxonomy/...
all packages OK
```

## Commits

- `1f9fed6fe feat(iam): add view-grade capabilities for IAM/membership/taxonomy/metrics (ADR 0016)`
- `f52917495 fix(authz): F-001 split read/write caps in Tier-1 declarative table`

## Closure

F-001 closed. Tier-1 read/write split applied to all 13 audit rows. Viewer-grade reads no longer require Manage-grade caps. Methodless write-shadowing eliminated and CI-locked. Taxonomy Tier-2 reads migrated from `CapDocumentView` workaround to `CapTaxonomyView`. ADR 0016 view caps seeded onto roles per migration 0217.

Defers tracked:
- Approval legacy mount `permissions.go:191-194` — explicitly out of scope per F-001 plan; revisit if approval-module audit re-opens.
- `/api/v1/notifications` and `/api/v1/access-policies` handlers — Tier-1 rules exist but handlers not registered. Either implement handlers or remove rules in a follow-up.
- Pre-existing Tier-2 403 on `/api/v1/documents` for approver in dev seed — needs `user_process_areas` row for approver; separate ticket.

Post-merge action: dispatch `wiki-curator` to refresh module-wiki anchors for notifications, access-policies, taxonomy, iam/users, iam/area-memberships, metrics.
