# API Contract Hardening Program

> **Last verified:** 2026-06-08 (**Phase F shipped — PROGRAM CLOSED.** Final cleanup, 9 gated commits: F1 honest authz-area markers + dormant-lint deletion; F2 per-op errdoc completion (25 ops); F8 Go dead-code + success-code drift reconciliation; F3 bespoke-422 retired as spec+FE fiction; + idempotency-middleware envelope-leak fix; F4 listDocuments/listControlledDocuments → keyset cursor (the one runtime change); F5 approval/templates FE → shared apiFetch transport; F6 9 dead lib/types mirrors deleted; F7 sign-off useMutation+invalidateQueries. Closing 4-dimension re-audit PASSED: 0 CRITICAL, 0 HIGH (the one HIGH found — `/audit/events` flat-vs-nested cursor drift — fixed in-branch, commit `4806167ac`). AD-1/AD-2/AD-3 ADRs authored (0024/0025/0026). All gates green: redocly valid, api-lint 0 blocking/0 reported, full BE+FE suites 0 failures, codegen idempotent + go.mod at HEAD, live QA :8081. Prior: Phase E2 shipped — spec hygiene + standards conformance: root `security: [sessionCookie]` secure-by-default + 5 public `security: []` opt-outs; top-level `tags` + every op tagged/operationId'd/summary'd; one pagination convention (cursor canonical, 19 bounded/offset lists `x-pagination-exempt` w/ reason, `limit` max clamped 200→100 spec+runtime); 64 zero-error ops now document their actual error modes via shared `#/components/responses/*`; two non-standard methods reshaped (`DELETE /iam/area-memberships`→path-param `…/{user_id}/{area_code}`, `DELETE /approval/routes/{id}`→`POST …/{id}/deactivate`) coordinated spec+handler+permissions+FE+tests+regen; `bearerAuth` myth corrected to `sessionCookie`; `AUTHZ-DRIFT`+`PAGINATION-DRIFT` driven to 0 and flipped BLOCKING (reportedOnlyRules now empty). Commit `63a5921ad`. All gates green; live QA on :8081. Bounded defers: partial-coverage error docs + cursor migration (Phase F). Prior: Phase E1 — payload-casing big-bang: every declared JSON property is `snake_case` end-to-end (spec + 6 generated Go packages + generated FE types + 263 hand-written Go emitter keys + FE generated/hand-typed call sites + Go/FE fixtures), locked by the blocking `CASING-DRIFT` lint rule; 1 documented exemption `MDDM_NATIVE_EXPORT_ROLLOUT_PCT`; runtime QA green on every surface. Operator chose full-snake-now over a carve-out, accepting a runtime-QA net for the not-fully-migrated modules (auth/audit/controlled-docs/taxonomy) whose FE consumes wire by string — proper compile-time net deferred to Phase F. Prior: Phase D — error-envelope unification: 113 error-response blocks → one shared `components/responses` set (all RFC 9457 Problem), `ApiErrorEnvelope`/`ApiError` schemas deleted, 42 ops gained a documented `500`, 3 Go legacy emitters migrated to `problem`, FE legacy-fallback removed, `ENVELOPE-DRIFT` driven to 0 and flipped BLOCKING; Go codegen base-path reconciled (`BaseURL:/api/v1`) + all 6 packages regenerated → `ApiErrorEnvelope` gone from generated code, `backend-codegen-drift` green. Prior: Phase C — dead/unserved surface prune; Phase B — signoff password re-auth + search per-document visibility; Phase A — spec base-path normalization + double-prefix kill + CI gate)
> **Owner discipline:** same as [`roadmap.md`](roadmap.md) — each Phase = one fresh implementation session = one PR series. Ship each Phase without compromise; if a Phase bloats mid-flight, split it (e.g. C → C1 + C2) and update this doc.
> **Why this exists:** Plan 8 ("OpenAPI / contract-first completion", roadmap.md, marked done 2026-05-13) landed the codegen plumbing but left the *contract surface itself* incoherent. A 4-subagent audit (2026-06-05) found systemic drift between the OpenAPI spec, the runtime router, `permissions.go`, the generated FE types, and the live handlers — plus two confirmed authz-enforcement gaps. This program closes that gap and brings the API surface to the same industry-standard bar the auth/middleware/authz refactor already reached.
> **Supersedes:** [`contract-first-followups.md`](contract-first-followups.md) — its "no spec coverage" module list is folded into Phase C/E here.

## How to use this doc

1. Before starting a Phase, read this file + the audit ledger at the bottom. Know what the Phase owns and what decision (if any) must be made first.
2. After finishing a Phase: flip its **Status** to `done YYYY-MM-DD`, link the PR(s), record the **Evidence** (commands run / gates passed), and bump `Last verified`.
3. If scope shifts mid-Phase, edit the Phase's `Scope` / `Out of scope` rows here so the next session sees reality. Do **not** silently grow scope.
4. Every finding in the audit ledger maps to exactly one Phase. When a finding is closed, mark its ledger row `closed`. Nothing gets dropped silently — if a finding is intentionally *not* fixed, record it as `wont-fix` with a one-line reason.

## Anchor decisions (locked)

- **AD-1 — One base path.** `servers.url: /api/v1`; **all** OpenAPI path keys are relative (`/documents`, not `/api/v1/documents`). This matches the existing roadmap anchor "every route lives under `/api/v1/*`" and is the root fix for the double-prefix class of bugs. Settled in Phase A.
- **AD-2 — One error envelope.** RFC 9457 `Problem` (`application/problem+json`) is the only error shape. `ApiErrorEnvelope` is retired. (Continues Plan 7.) Settled in Phase D.
- **AD-3 — Unified authz is the only enforcement model.** Per-resource visibility = `controlled_document_{area,user}_grants` + role/area capabilities. The pre-unification `AccessPolicy` / `document_access_policies` ABAC concept is dead (table dropped in migration 0232) and must not be an enforcement path anywhere. Settled in Phase B.

## Open decisions (must resolve before the named Phase)

- **OD-1 (Phase B): RESOLVED 2026-06-05 → A (yes).** Approval-signoff password re-authentication IS a real control (21 CFR Part 11-style e-signature for a regulated QMS). Phase B wires the existing bcrypt `PasswordReauthProvider` into the sign-off path and verifies `password_token` against `iam_users.password_hash` before recording the sign-off. Bad/blank token → sign-off rejected. The "accepted-but-never-verified" state is the bug being closed.
- **OD-2 (Phase C): RESOLVED 2026-06-05 → remove-all + backlog-the-planned.** Every unserved spec path is removed from the live contract (a published-but-unserved path is an OWASP API9 liability and a lie to every SDK/consumer — an OpenAPI spec describes what the API *serves*, so "planned" work does NOT belong in it, even tagged `x-deferred`). The legacy taxonomy bloc + phantom `permissions.go` rows + orphaned schemas are dead → delete outright. `/notifications` and `/workflow/documents/{id}/transitions|approvals` ARE planned future features → before deleting them from the spec, Phase C **captures each one's intended shape (path, method, capability, request/response sketch) into a dedicated backlog doc** (`wiki/backlog/planned-endpoints.md`) so the future build starts spec-ready. Net: clean contract now, nothing forgotten.
- **FD-1 (Phase F): RESOLVED 2026-06-08 → A (retire dead lint + positive markers).** The `x-authz-skip-area` escape hatch (16 ops) exists because the `authz-call-present` lint statically scans for `authz.Require(req.Body.AreaCode)` but MetalDocs derives area in the tx-layer (DB-row, un-spoofable) enforced by the Postgres tripwire. Delete the dormant rule; replace the 16 negative markers with positive `x-authz-area: {source: tx, derived_from: …}` / `x-authz-area-none: "<reason>"`. No runtime/enforcement change. Full rationale in Phase F · FD-1.
- **FD-2 (Phase F): RESOLVED 2026-06-08 → yes (migrate to cursor).** `listDocuments` + `listControlledDocuments` move offset/page → canonical cursor; the two transitional `x-pagination-exempt` reasons are removed. Isolated runtime-behavior change.
- **OD-3 (Phase E): RESOLVED 2026-06-07 → A (full snake_case unification, big-bang now).** All non-snake props (**47 PascalCase + 182 camelCase = 229**, measured) migrate to `snake_case` — the design-system mandate + Google AIP / Stripe convention. The "needs a versioned cutover" caution does NOT apply here: pre-production, server not running, **single in-repo consumer** (the web FE), both sides shipped in one PR → it is a refactor, not a public breaking change. Safety net = the migration is **compile-time**: rename spec fields → `npm run gen:api` regenerates types → `tsc` flags every stale call site (no silent runtime breakage). Done NOW because every dev-day adds more camelCase sites. **Casing census (for the field map):** PascalCase = 3 schemas, documents module only (`DocumentSummary`/`DocumentDetailResponse`/`DocumentCheckpoint`), 6 FE files / ~28 sites; camelCase = 182 props, 41 FE files / ~177 sites; snake_case = 83 props already compliant. Root cause = no casing lint at the contract boundary + the documents schemas were authored from Go struct field names (`oapi-codegen` emitted `json:"ID"` verbatim → Go naming leaked into the wire contract). Executed as **E1** (casing big-bang, haiku fan-out under a sonnet-built **acronym-aware** old→new field map — `DocumentID`→`document_id`, `FormDataJSON`→`form_data_json`, not naive lowercase) BEFORE **E2** (hygiene), so E2's per-op pass runs on already-snake schemas. Going-forward lock: new api-lint `CASING-DRIFT` rule so the split can never regrow.

---

## Execution order

| Prio | Phase | Title | Risk | Status |
|------|-------|-------|------|--------|
| P0 | A | Spec base-path normalization + double-prefix kill + lint gate | Low (mechanical) | done 2026-06-05 |
| P0 | B | Authz enforcement gaps (signoff reauth + search visibility) | Med (security) | done 2026-06-05 |
| P1 | C | Dead / unserved surface prune (OWASP API9) | Med (contract shape) | done 2026-06-07 |
| P1 | D | Error-envelope unification (finish RFC 9457) | Med (contract shape) | done 2026-06-07 |
| P2 | E1 | Payload-casing big-bang → snake_case (OD-3 = A) | Med (whole-contract rename; pre-prod single-consumer = safe, compile-time migration) | done 2026-06-08 |
| P2 | E2 | Spec hygiene + standards conformance (security/tags/pagination/errdoc/delete-shape) | Med | done 2026-06-08 |
| P2 | F | Final cleanup — root-cause all defers/workarounds (authz markers, errdoc, bespoke 422, cursor migration, FE transport, lib/types, cache-invalidation, Go dead-code + success-code drift) | Med | **done 2026-06-08** — program CLOSED; closing re-audit passed (0 CRITICAL/0 HIGH) |

**Sequencing rationale:** A first — it is mechanical, fixes a live 404, and unblocks every consumer (you should not edit the contract while it is self-inconsistent). B second — security-grade, but cleaner to wire once the contract is coherent. C and D are the contract-shape redesigns (hard-stop territory → each opens with a written decision). E and F are cleanup that depends on the shape being settled.

---

## Phase A · Spec base-path normalization + double-prefix kill

- **Goal:** The OpenAPI spec is self-consistent and the `/api/v1/api/v1/...` double-prefix bug class is impossible to reintroduce.
- **Why:** `openapi.yaml:7` sets `servers.url: /api/v1`, yet ~half the path keys re-include `/api/v1/` (templates, documents, taxonomy, controlled-documents, approval) while the rest are relative (auth, iam, audit, search). Every spec consumer/SDK/mock builds wrong URLs for half the API; the FE openapi-fetch client doubles the prefix (live 404 on the documents list and `useBlankTemplateQuery.ts:10`).
- **Scope:**
  - Strip the `/api/v1` prefix from every absolute path key in `api/openapi/v1/openapi.yaml` (AD-1).
  - Regenerate FE types (`npm run gen:api`) → `frontend/apps/web/src/lib/api-types/index.d.ts`.
  - Fix every FE caller that hand-prepends `/api/v1` to an openapi-fetch path (confirmed: `features/documents/queries/useBlankTemplateQuery.ts:10`; sweep for siblings).
  - Add a CI lint gate (`npx @redocly/cli lint`, config already present) that fails on a path key starting with `/api/`.
- **Out of scope:** removing dead paths (Phase C), error/security/tag fixes (Phase D/E).
- **Closes (ledger):** F-DBL-1, F-DBL-2, F-SPEC-BASEPATH.
- **Verify:** redocly lint passes; `go build ./...`; FE `tsc`; Preview QA on documents-list + blank-template (the two confirmed double-prefix callers) return 200.
- **Status:** done 2026-06-05.
- **Evidence (2026-06-05):**
  - **Spec:** stripped `/api/v1` from all 76 absolute path keys in `api/openapi/v1/openapi.yaml`; `servers.url: /api/v1` retained; the other ~60 already-relative keys untouched. No duplicate keys after normalization (`grep '^  /' | sort | uniq -d` empty). Only the spec keys changed — no operations/schemas/tags/responses/security touched. Runtime router + `api.gen.go` NOT regenerated → served paths stay `/api/v1/*` (AD-1: spec was wrong, runtime was right).
  - **FE codegen:** `npm run gen:api` → `src/lib/api-types/index.d.ts` regenerated; 0 `/api/v1`-prefixed `paths` keys remain.
  - **FE callers fixed:** openapi-fetch (`api` client, baseUrl `/api/v1`) double-prefix offenders → relative: `useBlankTemplateQuery.ts:10` (`/templates/system/blank`), `library.ts:35,47` (`/documents`, `/documents/stats`). Type-key lookups `paths['/api/v1/...']` → `paths['/...']` in `documents.ts`, `templates.ts`, `templates/catalog.ts`. String `apiFetch('/api/v1/...')` callers left as-is (guard `apiUrl` passes `/api/`-prefixed paths through unchanged → no doubling; Phase F owns transport migration).
  - **CI gate (blocking):** new `PATH-BASE-PREFIX` rule in `scripts/api-lint/spec_rules.go` (`checkBasePrefix`) + `-only RULE` filter in `main.go`; wired as blocking job `spec-base-path-gate` in `.github/workflows/api-contract.yml`. Unit test `path_base_prefix` + testdata `path_base_prefix.openapi.yaml`. Proven both directions: `go run ./scripts/api-lint -only PATH-BASE-PREFIX api/openapi/v1/openapi.yaml` → `0 violation(s)` exit 0; same against the bad testdata → 1 violation exit 1.
  - **Gates:** `npx @redocly/cli lint api/openapi/v1/openapi.yaml` → valid; `go build ./...` clean; `go test ./scripts/api-lint/...` ok; `npx tsc --noEmit` (frontend/apps/web) clean.
  - **Preview QA (Vite dev :4173 → API :8081, admin login):** documents library `GET /api/v1/documents?page=1&pageSize=20 → 200` + `GET /api/v1/documents/stats → 200` (single prefix); new-document wizard blank-template `GET /api/v1/templates/system/blank → 200` (**single** prefix — no doubling); doubled `GET /api/v1/api/v1/documents → 404` confirms the bug class is dead. Both confirmed double-prefix callers now 200.
  - **Resolved alongside (was a bounded defer):** templates module had 500'd on all reads — `templates_template_version` was missing the `revision_number` column ADR 0013's Go half expected (SQLSTATE 42703); a pre-existing schema gap on `main`, not a Phase A regression (Phase A made no Go/SQL changes). Fixed by forward migration 0233 (commit `34e78259c`, auto-applied on API restart). Post-fix curl: `GET /api/v1/templates/system/blank → 200`, `GET /api/v1/templates → 200`, `GET /api/v1/documents → 200`, doubled `/api/v1/api/v1/documents → 404`.

## Phase B · Authz enforcement gaps (SECURITY)

- **Goal:** Every authz decision flows through the unified model (AD-3); no accepted-but-unverified credential; no open-by-default resource read.
- **Why:** Two confirmed gaps, both tails of the dead ABAC slice.
  - **B1 — Signoff password reauth not enforced.** `approval/http/signoff_handler.go:90-91` stuffs `password_token` into a signature payload; `approval/application/decision_service.go RecordSignoff` only validates + marshals + stores it (lines 112/230/259) — **never verifies it**. The bcrypt `PasswordReauthProvider` + `Registry` are fully built but never wired.
  - **B2 — Search has no per-document visibility.** v2 search is wired in prod (`main.go:219`); `search/application/service.go decidePolicies` default-allows when no policies (line 143-144) and the v2 reader's `ListAccessPolicies` returns nil — so any authenticated user finds every tenant document, ignoring `controlled_document_{area,user}_grants`. The whole `AccessPolicy` search path is now a no-op after migration 0232.
- **Scope (gated on OD-1):**
  - B1: wire `PasswordReauthProvider` (bcrypt against `iam_users.password_hash`) into the signoff path via the existing `Registry`, OR delete the provider + drop `password_token` from the contract — per OD-1.
  - B2: replace the dead `AccessPolicy` search path with enforcement against the unified model (area capabilities + `controlled_document_{area,user}_grants`); remove `decidePolicies`/`ListAccessPolicies`/`shouldBypassPolicy` dead-ABAC code and the dead `AccessPolicy` domain type from search.
- **Out of scope:** non-search dead code (Phase F).
- **Closes (ledger):** F-SEC-REAUTH, F-SEC-SEARCH, F-DEAD-SEARCHPOLICY.
- **Verify:** new tests — signoff rejects a bad/blank `password_token`; search omits documents the caller has no grant for. `go test ./internal/modules/documents/approval/... ./internal/modules/search/... -race`.
- **Status:** done 2026-06-05 (branch `qa/api-contract-hardening-phase-b`, off `qa/iam-area-membership`).
- **Forensic correction (recorded):** the credential bcrypt hash lives in the **auth** module's `metaldocs.auth_identities.password_hash`, not `iam_users.password_hash` as the task brief assumed. B1 resolves it through the existing auth port `authdomain.Repository.FindIdentityByUserID` (adapter `apps/api/cmd/metaldocs-api/reauth.go`) — no cross-module raw SQL.
- **Coupled defect fixed in B2 (recorded):** the v2 search reader (`v2documents/reader.go`) `SELECT`/filtered four columns — `subject_code`, `business_unit`, `classification`, `tags` — that exist only on the decommissioned `metaldocs.documents` schema, **not** on `public.documents`. The live query therefore errored (`42703`) on every call, so v2 search was non-functional (the audit's "returns everything" was a code-reading inference; runtime 500'd). Those four phantom column references + their filters were removed so the reader runs and the visibility predicate is actually exercised. No public contract shape change (the `SearchDocumentResponse` fields remain; they were already unreachable). **Bounded defer:** if subject/business-unit/classification/tag search is wanted, those attributes must first be modelled on `public.documents` (separate data-model work, not Phase B).
- **Standalone-document visibility (deliberate, recorded):** documents with no controlled document (`controlled_document_id IS NULL`) are visible in search to their creator only — mirroring the `/documents` library list's `created_by` scoping. No `system_admin` bypass anywhere in search (matches the controlled-documents list, which also does not bypass). This is the conservative fail-closed choice for a leak-closing phase; whether standalone drafts should be tenant-wide-searchable is a product decision deferred out of Phase B.
- **Evidence (2026-06-05):**
  - **B1 — root cause:** `RecordSignoff` validated + marshalled + stored `password_token` but never verified it; the bcrypt `PasswordReauthProvider`/`Registry` were built but unwired. **Fix:** `DecisionService.resolveSignaturePayload` (new) verifies `password_token` via the Registry (method `password_reauth`, server-set) inside the tx, after the tier-2 `authz.Require(document.signoff)` and before `InsertSignoff`; on success the persisted `signature_payload` is the verified attestation (`{method, bcrypt_cost, verified_at}`) — the raw token is **no longer stored**. nil registry → `ErrReauthNotConfigured` (fail-closed 500, generic body). Failure errors map to RFC 9457: `ErrInvalidCredentials`→401 `authn.signature_invalid` (pre-existing), `ErrRateLimited`→429 `authn.rate_limited` (new). User-existence not disclosed (provider collapses missing-user→`ErrInvalidCredentials`). Composition root injects the registry via `WithSignatureRegistry(newSignoffReauthRegistry(deps.AuthRepo))`. Both signoff handlers (`signoff_handler.go`, `doc_approval_handler.go`) funnel through `RecordSignoff`, so one wiring covers both.
    - **Bounded defer:** the wired throttle is `InMemoryAuthFailureRateLimiter` (process-local; no shared/persistent reauth limiter exists). Primary control = the bcrypt verification; cross-replica throttling would mirror login's pg-advisory-lock approach (separate build).
    - **Tests (TDD, red→green):** `decision_service_reauth_test.go` — `RejectsBlankToken`, `RejectsWrongPassword` (no sign-off recorded), `AcceptsCorrectPassword` (recorded; asserts stored payload contains no `password_token`/raw secret), `RateLimitTrips` (429 after 5 failures).
  - **B2 — root cause:** `service.go decidePolicies` default-allowed on empty policy list, `shouldBypassPolicy` was dead `return false`, and the v2 reader's `ListAccessPolicies` returned `nil` → every authenticated caller would have seen every doc (had the query run at all — see coupled defect). **Fix:** removed the entire dead `AccessPolicy` slice (`decidePolicies`/`shouldBypassPolicy`/`matchesPolicySubject`/`policiesForDocument`/`ListAccessPolicies` + the `AccessPolicy`/`SubjectType`/`Effect` domain types + scope/effect consts) and replaced it with a data-layer (R3) visibility predicate in `reader.go` SQL keyed on the caller (`$13`): company → all; owner; restricted + area-grant (`controlled_document_area_grants` ⋈ active `user_process_areas`) or direct `controlled_document_user_grants`; standalone → creator. This is a verbatim port of the controlled-documents list predicate (`controlleddocuments/infrastructure/repository.go:128`) — one source of truth, no second ABAC system. Unauthenticated caller → empty (service returns before hitting the reader). No cross-module redesign was needed (search reaches the grant tables directly in the same tenant DB) → no hard-stop.
    - **Tests (TDD):** `service_test.go` rewritten — forwards actor+tenant+capped-limit to the reader, denies without auth context (reader not called). `reader_test.go` (sqlmock) — asserts the visibility predicate SQL is present (`controlled_document_area_grants … controlled_document_user_grants`) and the actor is bound. `reader_visibility_integration_test.go` (build tag `integration`, real DB) — seeds company+restricted CDs, area/user grants, a standalone doc, and asserts exact per-actor visibility (no-grant→company only; area-member→+restricted; user-grant→+restricted; owner→+standalone; ungranted never sees restricted). **PASS** against the live dev DB.
  - **Gates:** `go build ./...` clean; `go vet ./...` clean; `go test ./internal/modules/documents/approval/... ./internal/modules/search/... -count=1` green (incl. all new tests); broader regression `go test ./apps/api/... ./internal/modules/iam/... ./internal/modules/controlleddocuments/... ./scripts/api-lint/...` green; `go test -tags=integration ./internal/modules/search/infrastructure/v2documents/ -run TestListDocuments_EnforcesUnifiedVisibility` PASS (real DB). `-race` not run — no cgo/gcc toolchain on the Windows host (documented limitation; CI runs race). `go run ./scripts/api-lint api/openapi/v1/openapi.yaml` → **0 blocking** (391 reported-only are pre-existing Phase D/E spec drift; **no spec changes in Phase B** → no new violations). No contract shape change → no FE `gen:api` needed.
  - **Live QA (API `:8081`, `scripts/start-api.ps1 -Build`):** API boots clean — the new B1 composition-root wiring constructs/injects without panic. `GET /api/v1/search/documents` (admin session) → **200 `{"items":[]}`** (was a `42703` 500 before the coupled fix); the dev DB has **0 `public.documents` rows**, so empty is correct — behavioural restriction is proven by the integration test above. A click-through signoff e2e is not runnable on an empty dev DB (no document/instance to sign); B1 behaviour is proven by the unit tests + the clean wiring boot + the existing handler error-mapping tests.
  - **Security review (`ecc:security-reviewer`, 2026-06-05):** **no CRITICAL.** B1 verified 8/8 — password verified before sign-off persisted, bound to the server-resolved acting user (not client-supplied), constant-time bcrypt, verified flag server-set (unforgeable), blank/empty/missing token rejected (two layers), nil-registry fail-closed on every path, raw token not stored, no user-existence disclosure. B2 — predicate confirmed a faithful port of the controlled-documents list predicate; tenant isolation preserved; no SQL injection; no `system_admin` bypass. Findings dispositioned:
    - **R-1 (HIGH) — FIXED in this branch:** the three verifier-misconfiguration sentinels (`application.ErrReauthNotConfigured`, `signature.ErrUnknownSignatureMethod`, `signature.ErrRateLimiterConfig`) fell through to the generic `internal.unknown` 500. Now mapped to a distinct `internal.signature_misconfigured` 500 (`approval/http/errors.go`) so monitoring separates a misconfig from a DB 500; client body stays the generic "internal error". Approval-http tests green after the fix.
    - **R-2 (MEDIUM) — defer:** `newSignoffReauthRegistry` passes `context.Background()` to `NewPasswordReauthProvider`, which discards it (per-request ctx is used at `Sign`). No runtime/security impact; cosmetic constructor-signature cleanup left to Phase F.
    - **R-3 (LOW) — defer:** in-memory rate limiter is per-replica (same bounded defer already recorded for the B1 throttle).
    - **R-4 (MEDIUM) — documented, not a security gap:** documents with a non-NULL `controlled_document_id` whose CD row is missing/tenant-mismatched (orphaned FK) become invisible to all — fail-safe (hidden, not over-exposed); intended behaviour.
  - **Review:** go-reviewer run on the diff — no security fail-open (nil registry blocks; raw token never persisted; verification in-tx before insert), no SQL injection (all params bound), empty-actor fail-closed. The one "CRITICAL" (standalone-doc creator-only) is the deliberate conservative decision recorded above (more restrictive, not an escalation); MEDIUMs (in-memory limiter; unmapped `ErrReauthNotConfigured`→generic 500) are the recorded bounded defer + intended fail-closed.

## Phase C · Dead / unserved surface prune (OWASP API9)

- **Goal:** Every spec path has a handler; every handler is in the spec; every `permissions.go` row maps to a real route. No published-but-unserved endpoints.
- **Why:** ~30 spec paths have no handler (entire legacy taxonomy bloc — `document-profiles`×11, `process-areas`×3, `document-subjects`×4, `document-types`, `document-families`, `document-departments`×4, `document-areas` — all superseded by the canonical `/taxonomy/*`; plus `notifications`×2, `workflow/documents/*`×2, `operations/stream`, `attachments/{id}/content`, `telemetry/mddm-shadow-diff`). Phantom `permissions.go` rows (`auth/refresh`, `POST /documents`, `documents/{id}/artifact-metadata`) classify routes that cannot be reached. ~22 orphaned component schemas. Same liability class as the `access-policies` slice already removed.
- **Scope (gated on OD-2, per-path):**
  - Remove superseded legacy-taxonomy paths + their schemas + their `permissions.go` rows (mirror the access-policies removal playbook: spec → FE regen → permissions → tests → baseline/dictionary if a table is involved).
  - Remove phantom `permissions.go` rows.
  - Remove orphaned `components/schemas`.
  - For each "planned not dead" path (OD-2): either implement + spec it, or move it to a tracked deferred list with `x-deferred` + a reason.
  - Document the handler-only endpoints that *should* be in the spec (`/iam/presence/stream`, `/healthz`, audit export sub-routes, `documents/{id}/pdf-complete`) — add them or annotate why omitted.
- **Closes (ledger):** F-DEAD-TAXPATHS, F-DEAD-MISCPATHS, F-PHANTOM-PERMS, F-ORPHAN-SCHEMAS, F-UNDOC-HANDLERS.
- **Verify:** a generated route truth-table shows zero spec-without-handler and zero handler-without-spec (excluding intentionally-annotated); redocly lint passes; full test suite green.
- **Status:** done 2026-06-07 (branch `qa/api-contract-hardening-phase-c`, off `qa/iam-area-membership`).
- **Forensic correction (recorded — do NOT trust the audit blindly):** A read-only route investigator initially reported the legacy-taxonomy bloc (`/document-profiles`, `/process-areas`, `/document-subjects`, `/document-types`, `/document-families`, `/document-departments`, `/document-areas`) as **REGISTERED via the taxonomy module** — a circular inference from the spec, not from generated code. Direct inspection of `internal/modules/taxonomy/delivery/http/routes_generated.go` + `api/api.gen.go` proved the taxonomy module's `ServerInterface` implements **only** the canonical `/api/v1/taxonomy/*` operations (`ListTaxonomyProfiles`/`Areas`/`Families` + CRUD); zero legacy-path registration anywhere (`grep` of all `*.gen.go` for legacy paths = 0; `apps/` `HandleFunc` for legacy = 0). The only Go references to the legacy paths were the (now-deleted) `permissions.go` rows and metric-label normalizers in `internal/platform/observability/http.go` (left in place — internal Go dead code, Phase F scope). **Verdict: all 25 candidate paths confirmed unserved by my own forensic check before deletion.**
- **Decision applied (OD-2):** unserved-and-dead → deleted outright; unserved-but-planned (notifications, workflow) → captured to [`planned-endpoints.md`](planned-endpoints.md) **before** deletion, then removed from the live contract.
- **Scope clarifications (recorded):**
  - `/document-templates` (legacy doc-template catalog, not in the original audit's enumerated list) was **also** confirmed unserved (zero Go ref) and removed — part of the same superseded legacy bloc.
  - `ControlledDocumentMutationResponse` (refs=11) was deleted: all 11 `$ref`s sat inside the dead legacy-taxonomy ops; the **real** controlled-documents paths use `AtomicCreateResponse`/`RevisionResponse` and were untouched (name was misleading).
  - Orphan schemas computed **transitively** (reachability from surviving paths/params), not by the audit's "~22" estimate: **80** schemas were unreachable after path deletion (pre-existing zero-ref orphans — editor bundles, content-save variants, version diff, collaboration presence, attachments — **plus** the now-orphaned taxonomy/notification/workflow schemas). All 80 removed; redocly lint confirms zero dangling `$ref`.
  - `DocumentId` component parameter orphaned (both refs were in dead workflow paths) → removed.
- **F-UNDOC-HANDLERS disposition:**
  - **Added to spec:** `GET /audit/events/export/{exportId}` (status) + `GET /audit/events/export/{exportId}/download` (signed-token payload) — plain versioned HTTP, client-facing; new `AuditExportStatusResponse` schema. Handler-backed (live QA: status→`404 NOT_FOUND` Problem for unknown id, download→`400` for missing token).
  - **Annotated wont-fix (intentional omission, with reason):**
    - `GET /healthz` — ops liveness alias served at **root**, outside the `servers.url: /api/v1` base; structurally cannot be a path key. Versioned probes `/health/live` + `/health/ready` remain in spec.
    - `GET /api/v1/iam/presence/stream` — **WebSocket** upgrade (`github.com/coder/websocket`); not representable in OpenAPI 3.x. The HTTP fallback `/iam/presence/snapshot` IS the documented contract.
    - `POST /api/v1/documents/{id}/pdf-complete` — **internal service-to-service webhook**, HMAC-SHA256 authenticated (`X-Docgen-Signature`), called by docgen_v2_pdf workers; not part of the session-auth public client contract.
- **FE disposition:** spec→FE regen (`npm run gen:api`) clean; `tsc` clean → empirically proves **no** FE code consumed the *generated* types of any deleted schema (all live consumers — `NotificationItem`, `WorkflowApprovalItem`, `DocumentListItem`, taxonomy items, etc. — import the **hand-written** `lib/types` mirrors, which are independent of the spec). Deleted 12 provably-dead `lib/types` mirrors of removed schemas (zero importers, definition-only: `ManagedUserItem`, `AttachmentItem`, `VersionDiffResponse`, `DocumentTemplateItem`, `DocumentEditorBundleResponse`, `DocumentBrowserEditorBundleResponse`, `DocumentBrowserContentSaveResponse`, `DocumentContentNative/Save/Pdf/Docx/UploadResponse`). **Bounded defer → Phase F (F-FE-DUPTYPES):** the screen-consumed mirrors (kept — deleting breaks live screens) and the cascade-freed-but-still-defined mirrors (`CollaborationPresenceItem`, `DocumentContentSource`, `VersionListItem`, `RendererPin`, `DocumentEditLockItem`, template-snapshot items) belong to Phase F's wholesale `lib/types` rewrite, not Phase C's surgical prune.
- **Evidence (2026-06-07):**
  - **Route truth-table:** 25 spec paths deleted (legacy-taxonomy bloc ×19 incl. `/document-templates`; `/notifications` ×2; `/operations/stream`; `/attachments/{id}/content`; `/workflow/documents/{id}/{transitions,approvals}`; `/telemetry/mddm-shadow-diff`). Surviving spec paths (~105) each map 1:1 to a runtime handler; the only runtime handlers absent from spec are the 3 annotated above + the `METALDOCS_E2E`-gated test handlers (never public). Zero spec-without-handler; zero handler-without-spec (excluding annotated).
  - **Spec/codegen:** `openapi.yaml` 6674 → 4372 lines (−2302). `npx @redocly/cli lint` → **valid** (zero dangling `$ref`). `go run ./scripts/api-lint` → **0 blocking** (unchanged baseline), reported-only **391 → 273** (dead paths leaving; +2 from the two added audit ops is Phase E global-sec/pagination debt on real served endpoints).
  - **permissions.go:** removed 8 rows — phantom (`POST /api/v1/auth/refresh` public, `POST /api/v1/documents`, `documents/{id}/artifact-metadata`) + dead-path (`/notifications` ×2, `/workflow/documents` ×2, legacy `/document-profiles` ×4, `/process-areas` ×4, `/document-subjects` ×4) and their `permissions_test.go` rows (`TestPermissionResolver`, `TestPublicPathChecker`, `TestRouteCoverage`). All three phantom rows independently verified unserved (auth handler registers only login/logout/me/change-password; spec `/documents` has GET only; no artifact-metadata handler/spec).
  - **Gates:** `go build ./...` clean; `go vet ./apps/api/...` clean; `go test ./apps/api/... ./internal/modules/... ./scripts/api-lint/... -count=1` **green** (incl. `permissions_test.go`). FE `npm run gen:api` clean; `npx tsc --noEmit` clean.
  - **Live QA (API `:8081`, `scripts/start-api.ps1 -Build`, admin cookie session):** neighbour surfaces of the pruned paths all **200** — `/taxonomy/{profiles,areas,families}`, `/documents?page=1&pageSize=20`, `/documents/stats`, `/controlled-documents`, `/search/documents`. Deleted paths all **404** (`/document-profiles`, `/process-areas`, `/notifications`, `/workflow/documents/x/approvals`, `/telemetry/mddm-shadow-diff`) — contract now matches reality. No collateral damage.

## Phase D · Error-envelope unification

- **Goal:** One error shape across the whole API (AD-2). A single FE error handler works everywhere.
- **Why:** `ApiErrorEnvelope` (legacy) and `Problem` (RFC 9457) coexist; older paths still emit the legacy shape; **no operation documents a `500`**. Plan 7 rolled out `Problem` for the handlers it touched but the contract still advertises both.
- **Scope:**
  - Migrate remaining `ApiErrorEnvelope` responses to `Problem` in both the spec and any handler still emitting the old shape.
  - Document the standard error set (400/401/403/404/409/422/500) once via a reusable `$ref`, applied consistently.
  - Retire the `ApiErrorEnvelope` schema + the hand-written FE `ApiErrorEnvelope` type once unreferenced.
- **Out of scope:** tags/security/pagination (Phase E).
- **Closes (ledger):** F-ENVELOPE-SPLIT, F-NO-500.
- **Verify:** zero `ApiErrorEnvelope` refs in spec + FE; sample error responses across modules return `application/problem+json`; FE single error parser handles all.
- **Status:** done 2026-06-07 (branch `qa/api-contract-hardening-phase-d`, off `qa/iam-area-membership`).
- **Go codegen base-path reconciliation (RESOLVED in this phase — commit `966220fa8`):** Regenerating the 6 oapi-codegen packages (needed to purge the generated `ApiErrorEnvelope` type) initially flipped every runtime route from `/api/v1/*` to `/*`. Root cause was a **pre-existing Phase A debt**: Phase A made the spec path keys relative (AD-1) but explicitly did **not** regenerate ("api.gen.go NOT regenerated → served paths stay /api/v1/*"), and Phase C removed schemas without regen — so the committed gen was stale (proven: a fresh `go generate` on the pristine base spec yields a 658-line taxonomy diff; the controlleddocuments/documents-wrapper/taxonomy contract tests 404'd under a naive regen). oapi-codegen registers spec keys verbatim with no implicit base path. **Fix:** set `BaseURL: "/api/v1"` on the **four generated-router mounts that actually route** — `taxonomy` (`delivery/http/handler.go`), `controlleddocuments` (`delivery/http/handler.go`), and `documents` ×2 (`module.go`) — then regenerate all six packages. The generated router now registers `options.BaseURL + <relative key>`, so served routes stay `/api/v1/*` while the generated code matches the relative spec. Modules that consume the generated package for **types only** (`iam`, `templates`, `approval`) hand-register literal `/api/v1/...` routes and were unaffected except for picking up the new types. **Outcome:** `ApiErrorEnvelope` gone from **all** generated code; a fresh `go generate ./...` now leaves `api.gen.go` unchanged → **`backend-codegen-drift` is green** (it was red on base from the Phase A/C staleness — this phase fixed it); the regen also dropped the Phase-C-removed schema types still frozen in the committed gen. Live QA: taxonomy/controlled-docs/documents all serve `/api/v1/*` 200 with problem+json errors. `go build`/`vet`/full test suite green in default (readonly) mode.
- **Bespoke 422 exemption (recorded):** `publishTemplateVersion` 422 returns a domain validation payload `{valid, parse_errors, missing_tokens, orphan_tokens}`, not RFC 9457 — and it is **live contract**, consumed by the template editor FE (`features/templates/api/templates.ts:77-78`). This is the templates-module's own legacy shape, orthogonal to the `ApiErrorEnvelope` split this phase targets; migrating it to `Problem.errors[]` is a coupled spec+runtime+FE change. Marked `x-error-envelope-exempt: "<reason>"` (a new, reviewed marker mirroring the existing `x-pagination-exempt-reason` pattern) so `ENVELOPE-DRIFT` reaches 0 honestly with the exception documented, not silently weakened. **Bounded defer → templates-module contract refresh (Phase E/F).**
- **Evidence (2026-06-07):**
  - **Inventory:** spec had **21** `ApiErrorEnvelope` `$ref`s (`/metrics`, `/auth/*`, `/iam/*`, `/documents`) + **92** inline `Problem` `$ref`s = **113** error-response blocks; **0** documented `500`s (F-NO-500). FE: 1 hand-written `ApiErrorEnvelope` type (`lib/types/index.ts:273`, zero importers) + 1 legacy `{error:{code,message,details}}` fallback branch (`lib/api/client.ts`). Go: the platform `httpresponse.WriteError` already emitted Problem; **3** real legacy emitters remained — `search/delivery/http/handler.go` (`apiErrorEnvelope` struct + `trace_id`), `platform/security/ratelimit.go` (`map[string]any{"error":…}`), and `platform/security/origin_protection.go` (shared `writeAPIError`). The generated `ApiErrorEnvelope` type in `*/api/api.gen.go` was never returned by any handler.
  - **Spec:** all 113 blocks collapsed to one shared `components/responses` set (`BadRequest`/`Unauthorized`/`Forbidden`/`NotFound`/`Conflict`/`UnprocessableEntity`/`InternalServerError`, each `application/problem+json` → `Problem`); **42** ops that document errors gained a shared `'500': $ref InternalServerError`; `ApiErrorEnvelope`+`ApiError` schemas deleted. Net `openapi.yaml` −257 lines. `npx @redocly/cli lint` → **valid**; **0** `ApiErrorEnvelope` refs remain.
  - **Go emitters:** search/ratelimit/origin migrated to `internal/platform/problem` (codes preserved verbatim — no code/status renames per scope); dead `apiErrorEnvelope`/`apiError`/`requestTraceID`/`writeAPIError`/`traceLocalOrigin` removed; `go build ./...` + `go vet ./...` clean.
  - **FE:** removed the legacy `{error:{…}}` fallback in `client.ts` (a missing Problem now synthesizes `http_<status>` — the API serves only problem+json); deleted the dead `ApiErrorEnvelope` type; `npm run gen:api` regenerated `api-types` (0 `ApiErrorEnvelope` refs); updated the one legacy-shape unit test to the Problem contract. `npx tsc --noEmit` clean; `vitest run src/lib/api/` → **31/31** green.
  - **CI:** the `ENVELOPE-DRIFT` rule (`scripts/api-lint/spec_rules.go`) was taught to (a) resolve response-level `$ref`s into `components/responses`, (b) exempt description-only responses (no body = no envelope), (c) exempt health probes (`/health/*` 503 = HealthResponse), and (d) honor a reviewed `x-error-envelope-exempt` reason → count `21+ → 0`. Rule moved **out of `reportedOnlyRules`** in `main.go` → **BLOCKING**. New positive testdata fixture `envelope_shared_responses.openapi.yaml` (asserts 0 violations across all four compliant paths) + test case; `go test ./scripts/api-lint/...` green. Gate posture: `go run ./scripts/api-lint -only ENVELOPE-DRIFT …` → 0; `go run ./scripts/api-lint -strict -enforce=blocking openapi.yaml .` → **0 blocking** (exit 0); reported-only dropped to 202 (now AUTHZ-/PAGINATION-DRIFT only).
  - **Go codegen:** `BaseURL: "/api/v1"` added to the 4 generated-router mounts (taxonomy/controlleddocuments/documents×2) + all 6 oapi-codegen packages regenerated → `ApiErrorEnvelope` removed from generated code; **drift proof** `go generate ./...` then `git diff --exit-code -- '**/api.gen.go'` → **clean (exit 0)** so `backend-codegen-drift` is green. (Commit `966220fa8`, on top of the envelope commit `c4c4d95d2`.)
  - **Gates:** `go build ./...`, `go vet ./...` clean; `go test ./apps/api/... ./internal/modules/... ./scripts/api-lint/... -count=1` **green** (the contract/wrapper tests that 404'd under a naive regen pass once `BaseURL` re-adds the prefix). `go.mod`/`go.sum` left at HEAD (a `-mod=mod` run spuriously flipped `coder/websocket` indirect→direct; reverted — default-readonly `go build` + `go generate` confirm HEAD is self-consistent and leave go.mod untouched).
  - **Live QA (API `:8081`, `scripts/start-api.ps1 -Build`, admin session):** every error path returns `application/problem+json` — 401 `GET /documents` + `GET /search/documents` (`AUTH_UNAUTHORIZED`); 400 `GET /search/documents?limit=abc` (`VALIDATION_ERROR`) + strict-decode reject on `POST /controlled-documents`; 403 `AUTH_INVALID_ORIGIN` on a cross-origin `POST /auth/change-password`; 404 `GET /controlled-documents/{uuid}` (`CONTROLLED_DOCUMENT_NOT_FOUND`) + `GET /approval/instances/{uuid}` (`not_found.instance`). All **three** migrated Go emitters confirmed at runtime (search 401/400, origin 403; ratelimit shares the same `problem.Write` path). **Post-codegen-reconciliation regression check:** the four `BaseURL`-mounted generated routers still serve `/api/v1/*` — `GET /taxonomy/{areas,families}`, `GET /controlled-documents`, `GET /documents?…`, `GET /documents/stats` all **200**; taxonomy/controlled 404s still `application/problem+json`.

## Phase E1 · Payload-casing big-bang → snake_case

- **Goal:** Every JSON property in the contract is `snake_case`, end-to-end (spec + generated Go + generated FE types + hand-written Go emitters + FE adapters/tests), locked by a blocking lint rule so the triple-split can never regrow. (OD-3 = A.)
- **Decision taken mid-flight (recorded):** the audit found the wire contract for several **not-fully-migrated modules** (auth/audit/controlled-documents/taxonomy) lives in hand-written Go structs consumed by hand-written FE camelCase types **by string, outside the generated typed client** — so OD-3's "tsc flags every stale call site" safety net only covers the generated-typed surface (documents/iam/approval). Operator chose **full snake now, all modules**, accepting a runtime-QA net (grep-audit + Go contract tests + Preview QA) for the hand-typed surfaces rather than a carve-out/defer. Phase F (FE transport unification) remains the proper long-term fix that would have given those modules a compile-time net.
- **Scope (casing only):** rename declared schema property keys in the spec; regenerate the 6 `api.gen.go` packages + FE `api-types`; snake hand-written Go (struct `json:` tags + `map[string]any{"k":…}` response literals + `m["k"]=` dynamic assignments) across audit/iam/security/sessions/auth/controlleddocuments/documents/templates/taxonomy; fix the generated-typed FE call sites (tsc worklist) + the hand-written FE adapter boundaries (auth, taxonomy) + FE/Go test fixtures; add the blocking `CASING-DRIFT` lint rule.
- **Out of scope:** E2 hygiene (global-sec/tags/pagination clamp/errdoc/delete-shape), F (FE transport, lib/types dedup, Go dead-code). Query-param casing (e.g. `?profileCode=`) is **not** a JSON property and was left to E2 (the Go handlers still parse camelCase query keys; renaming would desync without a coordinated change).
- **Exemptions (documented, reviewed):**
  - RFC 9457 `Problem`/`FieldError` fields — already lowercase, pass the rule naturally.
  - `MDDM_NATIVE_EXPORT_ROLLOUT_PCT` (FeatureFlagsResponse) — SCREAMING_SNAKE feature-flag key that intentionally mirrors its env-var/flag-registry constant (`METALDOCS_MDDM_…`) and is consumed by a hand-written FE flag module by string; **not** in the OD-3 census (47 Pascal + 182 camel). Explicit `CASING-DRIFT` exemption. (Operator may override later to snake it too.)
  - Free-form **audit-event payload** keys: historical payloads stored before E1 keep their original casing (payload is `additionalProperties`, rendered generically); new server writes emit snake. Not a typed contract field.
- **Closes (ledger):** F-CASING (the casing portion; the field-map size below is the census reconciliation).
- **Status:** done 2026-06-08 (branch `qa/api-contract-hardening-phase-e1`, off `qa/iam-area-membership`).
- **Evidence (2026-06-08):**
  - **Field map (acronym-aware, built from the spec via a js-yaml walker):** **133** distinct non-snake spec property names → snake (`DocumentID`→`document_id`, `FormDataJSON`→`form_data_json`, `currentRevisionPageCount`→`current_revision_page_count`); 1 exempt (MDDM) → **132** migrated. Spec rename was format-preserving (4208 lines stable, 297 rename-only lines, 0 residual old identifiers anywhere incl. `required:` arrays/descriptions/examples). +11 under-spec stray camel keys snaked for object consistency (taxonomy `parentCode`/`default_approver_role`/…, observability `p50_duration_ms`/…, `business_unit`, audit `has_more`/`next_cursor`).
  - **Generated Go:** `go generate ./...` regenerated all 6 packages (563 insertions / 563 deletions = rename-only); `go.mod`/`go.sum` untouched; the 4 `BaseURL:"/api/v1"` generated-router mounts intact; **codegen idempotent** (second `go generate` is a no-op → `backend-codegen-drift` clean once committed).
  - **Hand-written Go:** 150 struct `json:` tags (25 files, vendor excluded, PascalCase scoped to the 3 documents schemas), 94 `map[string]any{"k":…}` response-literal keys, 19 `m["k"]=` dynamic-assignment keys snaked across audit/iam/security/sessions/auth/controlleddocuments/documents/templates; `domain.Document` gained snake `json:` tags (it is serialized raw by the list endpoint; not persisted as JSON). `go build`/`go vet` clean.
  - **Frontend:** `npm run gen:api`; `npx tsc --noEmit` surfaced **279** stale generated-typed call sites (documents/iam/approval) — fixed location-precisely from tsc's own `Did you mean` suggestions + a handful of structural request-body builders mapped to snake at the `api.POST` boundary; hand-written adapter boundaries (`features/auth/api/auth.ts` CurrentUser reads + change-password body, `features/taxonomy/api/catalog.ts` wire reads) fixed surgically (internal camelCase types kept; only wire reads/writes snaked). `tsc` → **0**; `vitest run` → **439 passed / 0 failed** (8 test-fixture files + 1 error-parsing test corrected). Dead legacy adapters (`controlled-documents/api/catalog.ts` → deleted `/document-profiles` endpoints; `approval/api/workflow.ts` → removed `/workflow/*`) left for Phase F.
  - **CASING-DRIFT rule:** new `checkCasing` in `scripts/api-lint/spec_rules.go` flags any declared `properties:` key not matching `^[a-z][a-z0-9]*(_[a-z0-9]+)*$` (exemptions above; does not walk free-form content). Blocking-by-default (not in `reportedOnlyRules`). Unit tests + `casing_bad`/`casing_exempt_good` testdata fixtures; `go test ./scripts/api-lint/...` green.
  - **Gates:** `npx @redocly/cli lint` → **valid**; `go build`/`go vet` clean; full `go test ./apps/api/... ./internal/... ./tests/... ./scripts/api-lint/...` → **exit 0, 0 failures**; `go run ./scripts/api-lint -strict -enforce=blocking openapi.yaml .` → **0 blocking** (202 reported-only = unchanged AUTHZ-/PAGINATION-DRIFT E2 debt), `CASING-DRIFT` → **0**; FE `tsc` 0 + `vitest` 439 pass.
  - **Runtime QA (API `:8081` `start-api.ps1 -Build`, Vite `:4173`, admin session):** every surface returns **200 with snake_case keys** — `/auth/login`+`/auth/me` (`expires_at`/`user_id`/`display_name`/`must_change_password`), `/documents?page=1&pageSize=20` (`page_size`), `/documents/stats` (`by_status`/`by_area`), `/iam/users` (`area_memberships`/`tenant_role`/`failed_login_attempts`/`is_active`), `/iam/area-memberships` (`user_id`/`area_code`/`effective_from`/`granted_by`), `/iam/admin/overview` (`audit_events_per_minute`/`mfa_coverage_pct`/`recent_activities`/`last_seen_at`), `/audit/events` (`has_more`/`occurred_at`/`actor_id`/`resource_type`), `/taxonomy/profiles` (`family_code`/`review_interval_days`/`default_template_version_id`/`editable_by_role`), `/controlled-documents`, `/search/documents`. **Preview QA:** login renders `display_name` ("Administrator") via the fixed `auth.ts` adapter; dashboard loads admin-overview KPIs + presence; documents library renders with taxonomy areas; audit panel renders — **zero console errors** across all navigations. (Dev DB has 0 documents → populated-table visuals not exercised, same bounded limit as Phases B/C/D.)
  - **Bounded defers:** (1) query-param casing → E2; (2) dead legacy FE adapters + Go dead-code → F; (3) the not-fully-migrated modules' FE transport (the reason the runtime-QA net was needed) → F; (4) MDDM flag exemption may be revisited.

## Phase E2 · Spec hygiene + standards conformance

- **Goal:** The spec reads like a big-company public API: discoverable, secure-by-default in-doc, one convention per concern.
- **Why (audit scorecard FAILs):** no global `security:` block (125+ ops implicitly unauthenticated in-doc); ~30 untagged operations + no top-level `tags` declarations; three pagination patterns with `limit` max 200 vs design-system 100; payload casing triple-split; `DELETE` with required body + query-param `DELETE`; `bearerAuth` referenced in wiki but absent from spec; no `operationId`/`summary` on a block of ops; no examples.
- **Scope (gated on OD-3 for casing):**
  - Global `security:` block + explicit `security: []` on public ops.
  - Top-level `tags` with descriptions; tag every operation.
  - One pagination convention (cursor canonical; `x-pagination-exempt` for bounded lists); clamp `limit` max to 100 to match the design system.
  - ~~Casing per OD-3~~ — **done in Phase E1** (full snake_case, all modules; `CASING-DRIFT` blocking).
  - Fix non-standard methods: `DELETE /iam/area-memberships` (query-param identity), `DELETE /approval/routes/{id}` (required body).
  - `operationId` + `summary` on every operation; consistent verb naming.
  - **Error-response coverage (folded from Phase D, F-ERRDOC-COVERAGE):** document each operation's actual error modes via the shared `#/components/responses/*` set (incl. `500`). Phase D unified the error *shape* and added `500` to the 42 ops that already documented errors; **64 ops still document zero error responses** (whole `templates` + `taxonomy` modules, many `documents`/`approval` ops). Do this in the same per-op pass as security/tags/operationId so each op is edited once. Per-op analysis required — do not blanket-add statuses an op cannot produce (e.g. no `409` on a read-only GET). The `ENVELOPE-DRIFT` rule (now blocking) already enforces the shape on whatever errors are documented.
  - Enforce the redocly lint gate in CI (built on Phase A's gate).
- **Closes (ledger):** F-NO-GLOBAL-SEC, F-UNTAGGED, F-PAGINATION, F-LIMIT-200, F-CASING, F-DELETE-SHAPE, F-OPID-SUMMARY, F-BEARERAUTH, F-ERRDOC-COVERAGE.
- **Verify:** redocly lint clean with hygiene rules enabled; FE regen + `tsc` green.
- **Status:** done 2026-06-08 (branch `qa/api-contract-hardening-phase-e2`, off `qa/iam-area-membership` HEAD `aa7ef0416`; commit `63a5921ad`). One PR.
- **Evidence (2026-06-08):**
  - **Per-op sweep (one edit per op):** spec `openapi.yaml` 4208 → ~5100 lines (+897/−45). 130 operations across 12 tags. A throwaway yaml.v3-positioned transformer applied the bulk mechanical insertions (error refs / summaries / pagination markers) format-preserving (flow-style `responses: {…}` one-liners expanded to block; everything else line-insert); judgement-heavy edits (security, tags block, delete-shape, x-authz markers, limit clamp) done by hand. Transformer deleted after use.
  - **F-NO-GLOBAL-SEC:** root-level `security: [- sessionCookie: []]` added (secure-by-default in-doc) + explicit `security: []` on the 5 public ops (`login`, `checkLiveness`, `checkReadiness`, `getFeatureFlags` [pre-existing], `redirectSignedUrl`). `checkAuthz` (`scripts/api-lint/spec_rules.go`) now reads root `security` and treats a non-empty global requirement as satisfying every non-overriding op (OpenAPI 3.x inheritance) — so the global block is honoured, not bypassed. Live: `GET /documents` (no cookie) → 401; public `GET /health/live` (no cookie) → 200.
  - **F-UNTAGGED + F-OPID-SUMMARY:** top-level `tags:` block with descriptions (12 tags; added `health`, `search`, `observability`). The 4 previously-untagged ops (`checkLiveness`/`checkReadiness`/`getMetrics`/`searchDocuments`) gained tag + `operationId`; ~70 ops gained a derived `summary` (acronym-aware: PDF/URL/JSON/DOCX). Zero untagged, zero missing-operationId, zero duplicate operationId, zero missing-summary remain (verified by spec walk).
  - **F-PAGINATION + F-LIMIT-200:** cursor remains canonical (`listUsers`/`listSessions`/`listAuditEvents` already cursor+limit). 19 bounded/offset list ops marked `x-pagination-exempt: true` + a specific `x-pagination-exempt-reason` (fixed catalogs, per-resource bounded sets, and the two offset/page-paginated lists `listControlledDocuments`/`listDocuments` honestly tagged as not-yet-cursor-migrated). `limit` max clamped **200→100** in spec (3 params) **and** runtime (`iam/application/people_service.go`, `iam/delivery/http/sessions_handler.go` incl. its 400 message, `audit/application/service.go`). `PAGINATION-DRIFT` **75 → 0**. Live: `GET /auth/sessions?limit=150` → 400 `limit must be between 1 and 100`; `?limit=100` → 200.
  - **F-ERRDOC-COVERAGE (folded F-NO-500):** the **64** ops that documented zero 4xx/5xx (whole `templates` + `taxonomy` + many `documents`/`approval` ops) now document their **actual** error modes via the shared `#/components/responses/*` set. Per-op policy (not blanket): authenticated base `{401,403,500}`; `+404` when a path param exists; `+400` on writes / bodies / filter queries; `+409` only on writes whose module routes through a conflict-capable mapper — **grounded by grep** of each module's `*_handler.go`/`errors.go` (templates key/state/stale/immutable conflicts; taxonomy create/archive/family-deactivate/default-template; documents session/autosave/submit/fillin state conflicts; approval lifecycle). 409 explicitly withheld from URL/presign issuers + comment/checkpoint sub-resource CRUD + taxonomy plain updates (no conflict path). `logout` pinned to `{401,500}`. **Scope note:** the brief's worklist is the zero-4xx/5xx ops only; ops with *partial* error docs (e.g. `getDocument` documents only an inline `404`, `updateTemplateSchema` documents inline `400/409/412/422`) were left as-is — completing their baseline `{401,403,500}` is a bounded defer (see below). Live: `GET /taxonomy/profiles/NOPE` → 404 `application/problem+json` `PROFILE_NOT_FOUND`.
  - **F-DELETE-SHAPE:** (1) `DELETE /iam/area-memberships?userId&areaCode` → `DELETE /iam/area-memberships/{user_id}/{area_code}` (path-param identity; snake_case params). Hand-rolled handler `routes_memberships.go` reads `PathValue("user_id")`/`("area_code")`; `permissions.go` DELETE row → `pathPrefix: "/api/v1/iam/area-memberships/"` (trailing slash so it never shadows the GET/POST collection exact path); x-authz-area `source: path`. (2) `DELETE /approval/routes/{id}` (carried a body) → `POST /approval/routes/{id}/deactivate` (action sub-resource; generated-wrapper-mounted, `router.go` registration flipped, same `DeactivateApprovalRoute` method; the approval `permissions.go` POST prefix already covers it so the dead DELETE row was removed). FE: `useRevokeMembershipMutation` (openapi-fetch path params) + `routeAdminApi.deactivateRoute` (raw-fetch `POST …/deactivate`). Tests updated (permissions ×3 blocks, iam contract ×2, iam unit ×2). **Live end-to-end:** old iam bare `DELETE` → 405 (gone); new `DELETE …/{uid}/{area}` → 404 `User not found` (wired, params parsed); old approval `DELETE /routes/{id}` → 405 (gone); new `POST …/deactivate` → 404 `route_admin: route not found` (wired, body parsed).
  - **F-BEARERAUTH:** the spec never had a `bearerAuth` scheme — the app is session-cookie only (`sessionCookie` apiKey). Corrected the **wiki** (`api-design-system.md` §2.7 mini-convention + §6 tier-1 example: `bearerAuth: [template.create]` → `sessionCookie: []`, with a note that capabilities are enforced by `permissions.go`/`CapabilityService`, not OpenAPI scopes). No fake bearer flow invented.
  - **CI gate:** `AUTHZ-DRIFT` + `PAGINATION-DRIFT` driven to **0** and flipped **BLOCKING** — `reportedOnlyRules` in `scripts/api-lint/main.go` is now **empty** (no spec-drift family remains reported-only; CASING-DRIFT + ENVELOPE-DRIFT stay 0/blocking). Lint unit tests extended: `global_security_good` (root security satisfies per-op authz), `pagination_exempt_good`, `pagination_exempt_no_reason` (exempt without a reason still fails) testdata fixtures + cases.
  - **Codegen:** `go generate ./...` regenerated all 6 `api.gen.go` packages (embedded-spec blob re-rolled across all modules; `POST …/deactivate` reflected in approval gen); **idempotent** (md5-stable on re-gen) → `backend-codegen-drift` green once committed. `go.mod`/`go.sum` held at HEAD (no `-mod=mod` indirect→direct flip).
  - **Gates:** `npx @redocly/cli lint` → **valid**; `go run ./scripts/api-lint -strict -enforce=blocking openapi.yaml .` → **0 blocking, 0 reported-only**; `go build ./...` + `go vet` clean; `go test ./apps/api/... ./internal/modules/... ./scripts/api-lint/... ./tests/unit/... -count=1` → **green** (38 pkgs ok, 0 fail). FE `npm run gen:api`; `npx tsc --noEmit` → **0**; `npx vitest run src/lib/api src/features/approval src/features/iam` → **142 passed / 0 failed** (incl. RouteAdminPage + MembershipsTab, the two delete-shape consumers). `-race` not run (no cgo toolchain on Windows host; CI runs it — same documented limit as prior phases).
  - **Code review:** `cavecrew-reviewer` over the non-generated diff (permissions classifier prefix/ordering, PathValue param-name match, router flip, global-security lint logic, limit clamps) → **no issues**.
  - **Bounded defers (recorded):** (1) *Partial-coverage ops* — ops that already documented ≥1 inline error (description-only, e.g. `getDocument` 404, `updateTemplateSchema` 400/409/412/422) keep their terse set; completing the `{401,403,500}` baseline + migrating inline-described errors to the shared refs is out of F-ERRDOC-COVERAGE's zero-error worklist → fold into a follow-up hygiene pass. (2) *Cursor migration* — `listDocuments`/`listControlledDocuments` stay offset/page-paginated (exempt-with-reason); true cursor migration is runtime work → Phase F. (3) Success-code drift (e.g. `archiveDocument` handler returns 204 but spec documents 200) is pre-existing, out of E2's hygiene scope.

## Phase F · Final cleanup — no workarounds, root-cause, industry-standard (CLOSING PHASE)

- **Goal:** Backend (and its FE contract surface) is **done**: professional, industry-standard, following the project rules, with **no workarounds** — every remaining defer/escape-hatch/dead-path from Phases A–E2 root-caused, not symptom-patched. After F the program runs its **closing re-audit gate** (below) and is closed.
- **Owner directive (2026-06-08):** operator confirmed Phase F absorbs **all** outstanding bounded defers + workaround removals (not just the original FE-transport/dead-code scope) and is executed **in one fresh session, family-by-family**, plan-first.
- **Locked decisions for F:**
  - **FD-1 (F1 authz markers) → Option A (retire + positive markers).** The `authz-call-present` lint rule is **dormant by design** (0 hits ever — `scripts/api-lint/code_rules.go:118`): it statically scans handler bodies for `authz.Require(req.Body.AreaCode)`, but MetalDocs derives the area from the **DB row inside the tx** (un-spoofable) and enforces it via the Postgres **tripwire** (`migrations/0142b…`), so all 16 area ops carry a negative `x-authz-skip-area` escape hatch. Root cause = a lint that models the wrong architecture. Fix: **delete the dead `authz-call-present` rule** (the real guarantees remain: `tripwire-pairing` lint + `authz-area-scope-binding` AST guard in `registry_rules.go` + the DB trigger); **replace the 16 negative markers** with honest positive ones — `x-authz-area: { source: tx, derived_from: "<entity>.area_code" }` (first-class "DB-derived" value alongside `body`/`path`) for tx-enforced ops, and `x-authz-area-none: "<reason>"` for genuinely area-less ops (templates are tenant-global). Update `AUTHZ-DRIFT` (`spec_rules.go`) to accept the new markers; **no handler/runtime change, enforcement unchanged**. NOT Option B (build the interprocedural call-graph lint) — that re-proves what the DB already enforces, the exact duplication ADR 0007 rejected.
  - **FD-2 (F4 cursor migration) → YES.** `listDocuments` + `listControlledDocuments` migrate offset/page → canonical cursor (`CursorPage`, `next_cursor`/`has_more`); the two "not-yet-migrated" `x-pagination-exempt` reasons are removed. This is the one **runtime-behavior** change — isolate it in its own commit with before/after QA.
- **Families (each = one commit on `qa/api-contract-hardening-phase-f`, gated):**
  - **F1 — authz markers (FD-1).** Spec: rewrite 16 `x-authz-skip-area`/`x-authz-skip-reason` → positive markers. Lint: delete `checkAuthzCallPresent` + its testdata; extend `checkAuthz`/`AUTHZ-DRIFT` for `source: tx` + `x-authz-area-none`. Docs: new ADR (authz-marker model) + update ADR 0007/0022 + `api-design-system.md §6`. Regen 6 packages (markers are `x-` extensions → may not change gen, but run + drift-check). _Closes a workaround, not a ledger row — record under F._
  - **F2 — errdoc completion.** Ops that already documented ≥1 inline error but miss the baseline `{401,403,500}` (e.g. `getDocument` documents only `404`), and inline description-only errors that should reference the shared set. Add a shared **`PreconditionFailed` (412)** response in `components/responses` for `updateTemplateSchema`'s `stale_lock_version`. Per-op, same evidence discipline as E2.
  - **F3 — bespoke 422.** Migrate the templates `publishTemplateVersion` 422 (`{valid, parse_errors, missing_tokens, orphan_tokens}`) → RFC 9457 `Problem.errors[]`; drop the `x-error-envelope-exempt` marker. Coupled spec + Go emitter + FE consumer (`features/templates/api/templates.ts`).
  - **F4 — cursor migration (FD-2).** `listDocuments` + `listControlledDocuments`: spec params (`cursor`/`limit` + `page.next_cursor`/`has_more`), Go repos/handlers (use `internal/platform/pagination` cursor primitive), FE callers + query hooks. Remove the two `x-pagination-exempt`. Regen FE types. **Runtime behavior change → own commit + before/after QA.**
  - **F5 — FE transport unification (F-FE-RAWFETCH).** Route all `approval/*` + `templates/*` FE calls through shared `apiFetch`/openapi-fetch (auth-expiry dispatch, Problem parse, credentials, idempotency); delete raw-`fetch` clients (`approval/api/mutationClient.ts` raw path, `routeAdminApi.listRoutes` raw `fetch`, templates raw fetch).
  - **F6 — lib/types dedup (F-FE-DUPTYPES).** Delete hand-written `lib/types` interfaces duplicating generated (`DocumentListItem` vs `DocumentSummary`, the screen-consumed + cascade-freed mirrors deferred from Phase C); keep only genuinely-unspecced shapes.
  - **F7 — cache invalidation (F-FE-NOINVALIDATE).** Approval mutations → `useMutation` + TanStack `invalidateQueries`.
  - **F8 — Go dead-code + drift (F-GO-DEADCODE).** Remove unreachable `Freeze()`/`pdfDispatcher` branches, `legacyFanout` variadic, `CheckReplay/RecordReplay`, `SnapshotFromTemplate`, `shouldBypassPolicy(){return false}`, `AllowDevTenantFallback`, `GeneratedServerAdapter` shim, deprecated `authn` context wrappers (29 callers — migrate then delete); the R-2 (`context.Background()` in `newSignoffReauthRegistry`) cosmetic cleanup from Phase B. Fix **success-code drift** surfaced in E2 (e.g. `archiveDocument` handler returns 204 but spec documents 200 — reconcile spec↔runtime per op). Resolve or re-ticket PR-7 / phase11 TODOs.
- **Order:** F1 → F2 → F8 (backend contract + code clean) → F3 → F4 (heavy runtime, isolated) → F5 → F6 → F7 (FE). 
- **Closes (ledger):** F-FE-RAWFETCH, F-FE-DUPTYPES, F-FE-NOINVALIDATE, F-GO-DEADCODE — plus the workaround/defer cleanups (authz markers, partial-errdoc, bespoke 422, cursor migration, success-code drift) recorded under F.
- **Per-family gates (every commit):** `npx @redocly/cli lint` valid; `go run ./scripts/api-lint -strict -enforce=blocking openapi.yaml .` → 0 blocking / 0 reported (CASING/ENVELOPE/AUTHZ/PAGINATION stay 0); if spec changed `go generate ./...` + `git diff --exit-code -- '**/api.gen.go'` clean + go.mod at HEAD; `go build`/`vet`/targeted `go test` green; FE `npm run gen:api` + `tsc` 0 + `vitest` green; live QA on :8081 for the touched surface. No raw `fetch(` left in feature API layers; `lib/types` only unspecced shapes; `go vet`/staticcheck clean.
- **Status:** done 2026-06-08 (branch `qa/api-contract-hardening-phase-f`, off `qa/iam-area-membership` HEAD `cf497c562`). 9 gated commits (8 families + 1 found-issue fix). Program **CLOSED** — closing re-audit passed (below).
- **Evidence (2026-06-08, per family):**
  - **F1 — authz markers (`8ec0eb4e4`).** Deleted the dormant `authz-call-present` lint + its exclusive helpers + the `handlers_*` testdata. Rewrote all **16** negative `x-authz-skip-area`/`x-authz-skip-reason` → honest positive markers: **12** `x-authz-area:{source:tx, derived_from}` (10 document lifecycle/edit + 2 controlled-document changeStatus), **3** `{source:body|path, field}` (membership grant/revoke, atomic CD create), **1** `x-authz-area-none` (`archiveTemplate`). `AUTHZ-DRIFT` validates marker shape (`checkAuthzAreaShape`) + new testdata. New ADR [`0023`](../decisions/0023-authz-area-markers.md); amended ADR 0007/0022 + api-design-system §6. No runtime change; regen idempotent. Live QA: unauthed area op 401, authed reads 200.
  - **F2 — errdoc completion (`9135f6099`).** **25** capability-gated ops (templates ×6, controlled-documents ×8, documents ×9, approval ×2) now document actual error modes via shared `#/components/responses/*` (base {401,403,500}; +404 path-param; +400 writes/bodies/filter). New shared `PreconditionFailed`/`Gone`/`TooManyRequests`/`BadGateway`. Auth/health/public ops keep minimal sets (403 withheld). Live QA: 401/404 problem+json/400/200.
  - **F8 — Go dead-code + success-code drift (`a9dfedf11`).** Removed `legacyFanout` variadic (root-caused: API passed the real fanout client through it + a junk positional — fixed to pass `fanoutClient` directly), `CheckReplay`/`RecordReplay` + tests, `authn.RolesFromContext` (0 callers); R-2 ctx-param cleanup. `authn.UserIDFromContext` **un-deprecated** (canonical fail-closed accessor; deleting spreads duplication across 27 sites). Reconciled **success-code drift**: archiveDocument/heartbeat/release/forceRelease spec 200→204. **KEPT with reason:** `GeneratedServerAdapter` (live routing bridge), DecisionService legacy sync freeze/`pdfDispatcher` fallback (dead-in-prod; removal = signature+test churn = bounded prerequisite), `SnapshotFromTemplate`+seeder (test-only, removal reshapes service), `AllowDevTenantFallback` (prod-safe dev config).
  - **F3 — bespoke 422 (`fd03f0c14`).** The templates `publishTemplateVersion` 422 payload was **spec+FE fiction never emitted** (handler routes all errors through `problem.Write`); the FE consumer had **zero callers**. Spec 422 → shared `UnprocessableEntity`, `x-error-envelope-exempt` dropped (zero exemptions remain); dead FE code deleted. Live QA: publish error → 404 problem+json.
  - **+ found-issue fix (`d85c465b5`).** F3 QA found the **idempotency middleware** emitting `{code,message}` as `application/json` (a 5th emitter Phase D missed) → migrated to RFC 9457 `problem.Write` (AD-2). Live QA: missing-key → 400 problem+json.
  - **F4 — cursor migration (FD-2, `7cbee22d9`).** The one runtime change. New `internal/platform/pagination` (Encode/DecodeCursor, ClampLimit). `listDocuments` (keyset `(updated_at,id)`, total kept) + `listControlledDocuments` (keyset `(created_at,id)`) → `{items, page:{next_cursor, has_more}[, total]}`, limit+1 probe, invalid cursor→400. Spec params → cursor/limit (max 100); 2 `x-pagination-exempt` removed; PAGINATION-DRIFT 0. FE: LibraryPage numbered pager → cursor prev/next (total stat cards retained); controlledDocuments.ts cursor. Repo keyset unit tests + handler envelope/limit-cap tests. Live QA (fresh -Build): cursor envelope on both; invalid cursor + limit=999 → 400.
  - **F5 — FE transport unification (`a049605ee`).** templates.ts (9 fns) + approvalApi getInstance(requestRaw)/listInbox + routeAdminApi.listRoutes → shared apiFetch/requestRaw; dead `apiJson`/`getJSON` deleted. `mutationClient` KEPT as the specialized approval write transport (ETag/If-Match/idempotency/412) — made fully Problem-only + explicit credentials. S3 presigned URLs stay raw. vitest 154 green.
  - **F6 — lib/types dedup (`1ff95a487`).** Deleted **9** zero-importer dead duplicate mirrors. Kept genuinely-unspecced (NotificationItem/WorkflowApprovalItem — Phase-C-deleted endpoints). Bounded defer: live camelCase mock-era types (per-screen field-rename refactor).
  - **F7 — cache invalidation (`dec0d30d1`).** SignoffDialog → `useMutation` + `invalidateQueries(['approval'])`+`(['documents'])`. Route-admin already compliant. vitest 13/13.
- **Closing re-audit (mandatory gate) — PASS 2026-06-08.** 4-dimension audit re-run as parallel subagents:
  1. **Route truth-table & drift** — **0 CRITICAL, 0 HIGH.** All four surfaces (runtime / `api.gen.go` / spec / `permissions.go`) aligned; zero spec-without-handler, handler-without-spec (excl. 3 annotated), phantom perms, param-name mismatch, double-prefix. `TestRouteCoverage`/`TestPermission*` green.
  2. **OpenAPI spec quality** — **0 CRITICAL, 0 HIGH.** Zero envelope exemptions, zero `x-authz-skip-area`, lists de-exempted to cursor, snake_case (MDDM exemption only), full tag/operationId/summary, secure-by-default. MEDIUM (accepted): 29 ops document a bare 2xx (untyped success body, not wrong-shape); redundant per-op `security` on 4 approval-route ops. `listTemplates` exemption phrasing corrected (permanent bounded-list).
  3. **Dead/legacy/stub hunt** — **0 CRITICAL, 0 HIGH.** No public non-Problem emitter remains (idempotency fixed; `pdf-complete` HMAC webhook `{error}` is the documented internal exclusion = LOW). F8 kept-items confirmed bounded. MEDIUM (bounded defer): `archiveDocument` missing-doc → 500 vs spec 404 (error-mapping gap, recorded).
  4. **FE consumption + double-prefix** — **0 CRITICAL, 1 HIGH → FIXED.** The HIGH (`/audit/events` runtime flat `{next_cursor,has_more}` vs spec nested `page`, masked by a FE dual-shape adapter) reconciled runtime→spec + FE adapter simplified (`4806167ac`), closing the 2026-06-03 audit-events-cursor-shape ADR. No double-prefix; no raw-fetch bypass (except kept mutationClient + S3); no legacy-envelope parsing; no stale generated-type reads; LibraryPage cursor off-by-one-free.
  - **Verdict:** zero CRITICAL, zero HIGH across all four dimensions (the single HIGH found was fixed in-branch). AD-1/AD-2/AD-3 ADRs authored ([`0024`](../decisions/0024-openapi-single-base-path.md)/[`0025`](../decisions/0025-error-envelope-rfc9457.md)/[`0026`](../decisions/0026-unified-authz-enforcement.md)).
- **Full regression (closing evidence):** `npx @redocly/cli lint` valid; `go run ./scripts/api-lint -strict -enforce=blocking` → **0 blocking, 0 reported-only** (CASING/ENVELOPE/AUTHZ/PAGINATION all 0); `go build`/`go vet` clean; full `go test ./apps/... ./internal/... ./scripts/api-lint/... ./tests/...` → **0 failures**; `go generate` idempotent + `go.mod`/`go.sum` at HEAD; FE `npm run gen:api` + `tsc` → **0**; full `vitest` → **439 passed / 5 skipped**.

---

## Audit findings ledger

Source: 4-subagent audit, 2026-06-05. Severity from the audit. Every row maps to one Phase; status starts `open`.

| ID | Severity | Finding | Phase | Status |
|----|----------|---------|-------|--------|
| F-SPEC-BASEPATH | CRITICAL | `servers.url:/api/v1` + half the path keys re-prefix `/api/v1` → self-inconsistent spec | A | closed 2026-06-05 |
| F-DBL-1 | CRITICAL | `useBlankTemplateQuery.ts:10` doubles prefix via openapi-fetch client | A | closed 2026-06-05 |
| F-DBL-2 | CRITICAL | documents-list call resolves to `/api/v1/api/v1/documents` → 404 | A | closed 2026-06-05 |
| F-SEC-REAUTH | CRITICAL | signoff `password_token` accepted + stored, never verified; bcrypt provider dead | B | closed 2026-06-05 |
| F-SEC-SEARCH | CRITICAL | v2 search open-by-default; ignores `controlled_document_*` grants | B | closed 2026-06-05 |
| F-DEAD-SEARCHPOLICY | HIGH | dead `AccessPolicy` path in search (`decidePolicies`/`ListAccessPolicies` no-op) | B | closed 2026-06-05 |
| F-DEAD-TAXPATHS | CRITICAL | ~22 legacy taxonomy spec paths, no handlers (superseded by `/taxonomy/*`) | C | closed 2026-06-07 (19 paths incl. `/document-templates` deleted; forensic-verified unserved) |
| F-DEAD-MISCPATHS | HIGH | `notifications`, `workflow/*`, `operations/stream`, `attachments/content`, `telemetry/*` spec paths, no handlers | C | closed 2026-06-07 (notifications + workflow captured to planned-endpoints.md before delete; rest deleted) |
| F-PHANTOM-PERMS | HIGH | `permissions.go` rows with no route/spec (`auth/refresh`, `POST /documents`, `artifact-metadata`) | C | closed 2026-06-07 (all 3 verified unserved + removed with tests) |
| F-ORPHAN-SCHEMAS | HIGH | ~22 `components/schemas` never `$ref`'d | C | closed 2026-06-07 (80 transitively-unreachable schemas + 1 orphan param removed; redocly clean) |
| F-UNDOC-HANDLERS | MEDIUM | handlers absent from spec (`presence/stream`, `healthz`, audit export sub-routes, `pdf-complete`) | C | closed 2026-06-07 (audit export sub-routes added to spec; `presence/stream`=WS, `healthz`=root-ops, `pdf-complete`=HMAC webhook → wont-fix, annotated with reason) |
| F-ENVELOPE-SPLIT | CRITICAL | `ApiErrorEnvelope` (legacy) vs `Problem` (RFC 9457) coexist | D | closed 2026-06-07 (21 spec refs + schema deleted; 92 inline + 21 legacy → shared `components/responses`; 3 Go emitters + FE fallback migrated; Go codegen base-path reconciled (`BaseURL`) + regen → `ApiErrorEnvelope` gone from generated code too, `backend-codegen-drift` green) |
| F-NO-500 | HIGH | no operation documents a `500` response | D | **partially closed** 2026-06-07 (D unified the error *shape* + added the shared `InternalServerError` response to the 42 ops that already documented ≥1 error). The remaining **64 ops document zero error responses** (entire `templates` + `taxonomy` modules, many `documents`/`approval` ops) → error-response *completeness* folded into Phase E as **F-ERRDOC-COVERAGE** (decided 2026-06-07: done in E's per-op sweep so each op is touched once). **Fully closed 2026-06-08** via F-ERRDOC-COVERAGE (E2): all 64 now document a `500` + their other actual error modes. |
| F-ERRDOC-COVERAGE | HIGH | 64 ops document zero error responses (no 400/401/403/404/409/422/500) — the error *shape* is unified (Phase D) but coverage is not; a big-company contract documents every op's error modes | E2 | closed 2026-06-08 (all 64 zero-error ops now document their actual error modes via shared `#/components/responses/*`; per-op analysis, 409 grounded in each module's conflict mapper. Partial-coverage ops — already had ≥1 inline error — left as a bounded follow-up) |
| F-ENVELOPE-RATELIMIT | HIGH | per-route rate limiter (`platform/ratelimit/middleware.go`) emitted legacy `{"error":"rate_limited"}` — a 4th emitter Phase D's first pass missed | D | closed 2026-06-07 (migrated to `problem+json` RATE_LIMITED + Retry-After; commit `0e10b83a0`, surfaced by the Phase D code review) |
| F-ENVELOPE-DOCSHTTP | HIGH | the whole `internal/modules/documents/http/` delivery package (fill-in, placeholders, placeholder-options, view, reconstruct) emitted legacy `{error:…}`/`{error:"str"}` bodies, not RFC 9457 Problem — Phase D's emitter inventory missed the package | D | closed 2026-06-07 (folded into Phase D — commit `813dab7e4`: package error layer → `problem+json` via the unified writer; FE snapshot regenerated to 143 codes + 10 PT-BR mappings added (5 new + 5 pre-existing latent), coverage now green+complete; internal HMAC `pdf-complete` excluded. **Bounded defer → Phase E:** these 6 ops document only `'200'` in the spec; runtime now emits Problem but the 4xx/5xx are not yet documented in the contract.) |
| F-NO-GLOBAL-SEC | CRITICAL | no global `security:` block; 125+ ops implicitly unauthenticated in-doc | E2 | closed 2026-06-08 (root `security: [sessionCookie]` + explicit `security: []` on 5 public ops; AUTHZ-DRIFT honours global inheritance + flipped BLOCKING) |
| F-UNTAGGED | CRITICAL | ~30 ops untagged; no top-level `tags` declarations | E2 | closed 2026-06-08 (top-level `tags` + every op tagged; 4 untagged ops fixed) |
| F-PAGINATION | HIGH | three pagination patterns coexist | E2 | closed 2026-06-08 (cursor canonical; 19 bounded/offset lists `x-pagination-exempt` w/ reason; PAGINATION-DRIFT 75→0, BLOCKING) |
| F-LIMIT-200 | HIGH | spec `limit` max 200 vs design-system 100 | E2 | closed 2026-06-08 (clamped 200→100 in spec + runtime: iam people/sessions, audit) |
| F-CASING | HIGH | payload casing triple-split (PascalCase/camelCase/snake_case) | E1 | closed 2026-06-08 (full snake_case big-bang: 132 spec props + 6 generated packages + 263 hand-written Go emitter keys + FE generated/hand-typed call sites + fixtures; `CASING-DRIFT` rule blocking; 1 exemption `MDDM_NATIVE_EXPORT_ROLLOUT_PCT`. Runtime QA: all surfaces 200 + snake) |
| F-DELETE-SHAPE | HIGH | `DELETE /iam/area-memberships` query-param identity; `DELETE /approval/routes/{id}` required body | E2 | closed 2026-06-08 (→ `DELETE /iam/area-memberships/{user_id}/{area_code}` path-param identity; → `POST /approval/routes/{id}/deactivate` action sub-resource; spec+handler+permissions+FE+tests+regen; live e2e: old→405, new→wired) |
| F-OPID-SUMMARY | MEDIUM | block of ops missing `operationId`/`summary`; inconsistent verb naming | E2 | closed 2026-06-08 (every op operationId'd + summary'd; 0 dup operationId) |
| F-BEARERAUTH | HIGH | `bearerAuth` in wiki/design-system but not declared in spec | E2 | closed 2026-06-08 (corrected wiki to the real `sessionCookie` scheme; app is session-cookie only, no bearer flow invented) |
| F-FE-RAWFETCH | HIGH | `approval/*` + `templates/*` FE use raw `fetch()` bypassing `apiFetch` | F | closed 2026-06-08 (F5: templates ×9 + approvalApi + routeAdminApi.listRoutes → apiFetch/requestRaw; mutationClient kept as the specialized Problem-only write transport; S3 uploads stay raw) |
| F-FE-DUPTYPES | HIGH | `lib/types` hand-duplicates generated types | F | closed 2026-06-08 (F6: 9 zero-importer dead duplicate mirrors deleted; genuinely-unspecced shapes kept; live camelCase mock-era types' per-screen migration is a recorded bounded defer) |
| F-FE-NOINVALIDATE | MEDIUM | approval mutations skip TanStack cache invalidation | F | closed 2026-06-08 (F7: SignoffDialog → useMutation + invalidateQueries(['approval'],['documents']); route-admin already compliant) |
| F-GO-DEADCODE | LOW-MED | unreachable deprecated branches / dead fields / migration shims (see Phase F scope) | F | closed 2026-06-08 (F8: legacyFanout root-caused, CheckReplay/RecordReplay/RolesFromContext removed, R-2 ctx cleanup, success-code drift reconciled; GeneratedServerAdapter/DecisionService-legacy-freeze/SnapshotFromTemplate/AllowDevTenantFallback kept with documented reason — bounded, not dead) |
| F-AUDIT-CURSOR-SHAPE | HIGH | `/audit/events` runtime flat `{next_cursor,has_more}` vs spec nested `page` CursorPage (FE dual-shape adapter masked it; tracked since 2026-06-03 ADR) | F (re-audit) | closed 2026-06-08 (reconciled runtime→spec; FE adapter simplified; ADR 2026-06-03-audit-events-cursor-shape closed) |

## Closing verification gate (mandatory — re-audit)

Do **not** call the program done on Phase F completion alone. Re-run the **same 4-dimension audit** that opened it, so coherence is proven, not assumed:

1. **Route truth-table & drift** — runtime router vs `api.gen.go` vs `openapi.yaml` vs `permissions.go`.
2. **OpenAPI spec quality** vs industry standard (operationId/tags/responses/error envelope/security/components/versioning/pagination).
3. **Dead/legacy/workaround/stub hunt** in the handler layer.
4. **FE consumption + double-prefix** root-cause sweep.

Run as parallel subagents (the original fan-out). **Pass bar:** zero CRITICAL, zero HIGH; every ledger row `closed` or an explicit `wont-fix` with reason; redocly lint clean; full BE + FE suites green; Preview QA on the touched surfaces. Record the re-audit result here as evidence. Any new CRITICAL/HIGH → a new ledger row + a new Phase, not a silent patch.

## On close of the program

- Pass the closing re-audit gate above and record its evidence.
- Author the ADRs the locked anchors imply: spec base-path rule (AD-1), error-envelope finalization (AD-2 closes Plan 7), unified-authz-only enforcement (AD-3 extends ADR 0022). Add to the Plan 13 ADR sweep or as standalone ADRs.
- Dispatch `wiki-curator` to refresh `Last verified` stamps on every wiki doc whose referenced code changed.
- Update [`roadmap.md`](roadmap.md): mark Plan 8 as superseded-by this program, note the partial-completion correction.
