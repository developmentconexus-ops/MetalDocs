# Templates backlog

- [ ] Templates list: real updated_at field on TemplateDTO (currently using created_at)
- [ ] Templates list: resolve created_by user_id → display name (currently shows raw uuid)
