# DevelopmentConexus Engineering Method — compatibility pointer

> **Status:** compatibility pointer only — **NOT an authority**.

Canonical organizational authority:

`developmentconexus-ops/conexus-methodology/METHOD.md`

Version currently consumed by MetalDocs: **1.0.0**.

Byte-for-byte local mirror for agent availability/context:

`docs/engineering/standards/root-cause-global-maximum-method.md`

This legacy path is retained only so existing MetalDocs documents do not break during the minimal methodology migration. New references must use the `docs/engineering/standards/` mirror or cite the canonical repository directly.

The local mirror is updated **manually** when DevelopmentConexus deliberately adopts a newer Method. Do not add repository-local doctrine, synchronization machinery, or a second methodology authority here.
