# ADR 0022 — Authz Capability Coherence (single source of truth + explicit scope + area-scoped administration)

> **Status:** Accepted (in execution) — direction + governance model approved 2026-06-03; **Phase 1 complete 2026-06-03** (registry hygiene, behavior-neutral); **Phase 2 complete 2026-06-03** (typed scope classification + lifecycle grant seed; spec annotation scoped + lint-activation deferred — see Phase 2 close-out); **Phase 3 complete 2026-06-03** (membership area-scoping — the original `area_admin` 403 closed at root: real `areaCode` to tier-2, role-string gate deleted, `ErrCapDenied`→403, R1 system_admin bypass asserted); **Phase 4 complete 2026-06-03** (directory/list area-scoping — `isMembershipDirectoryAdmin` `RoleSystemAdmin` gate replaced by data-layer capability/area scope: system_admin→tenant-wide, area_admin→managed areas (filtered IN SQL, R3), others→self-only; last handler role-name literal removed, ADR 0021); **Phase 5 complete 2026-06-03** (CI binding — new `scripts/api-lint` rules `no-inline-capability` / `seed-registry-parity` / `wiki-capability-parity`, area-grade annotation parity test, OpenAPI access-policy enum reconciled as separate vocabulary, RLS hardening verified: transaction-local GUCs PASS + native-RLS N/A trigger-tripwire model); **Phase 7 complete 2026-06-03** (typed scope bound to runtime — the 5 declared-but-unenforced area-grade caps `document.create`/`document.edit`/`controlled_documents.create`/`obsolete`/`supersede` now pass the resource's real area to tier-2; new `authz-area-scope-binding` AST guard bans `Require(<areaGradeCap>,"tenant")` (proven to bite on all gaps, then green); `areaEnforcedOps` self-maintained from `IsAreaGrade`; review MED/LOW fixes folded — `BypassSystem` fail-closed background bridge (CWE-269), `MembershipDirectoryScope` clock param, shared `SystemAdminExistsSQL` (DRY), golden seed-row-count test); Phase 6 (wiki sync) pending — runs LAST. Supersedes nothing; **extends** ADR [`0007`](0007-two-tier-authz.md) (two-tier model + lint harness) and ADR [`0021`](0021-tenant-vs-platform-admin-separation.md) (capabilities, not role names, are the enforced boundary).
> **Last verified:** 2026-06-04 (Phase 13 complete — CI coherence-net revival: Principle 5 now ACTUALLY enforced. Two compounding bugs fixed — CI passed `./internal/modules` not the repo root, double-nesting every binding helper's join so the file-rooted guards (`no-rolestring-in-delivery`, `authz-area-scope-binding`, `seed-registry-parity`, `wiki-capability-parity`) silently loaded nothing via their `os.IsNotExist→empty` branch; AND the job was `continue-on-error: true`. Fix: root arg = repo root (CI passes `.`, `main.go` normalizes via `filepath.Abs`+`Clean`); new `-strict` flag turns a missing core file into a HARD error (kills the silent-empty swallow) while preserving the testdata-fixture skip; `-enforce=blocking|reported|all` splits the gate — binding/dialect/tripwire guards BLOCK (0 on clean tree, exit 0), the ~397 spec-drift hits stay reported-only/non-blocking; new `e2e_test.go` drives the REAL entrypoint with a planted violation per AST guard + strict-wrong-root regression. Gates: build/vet/gofmt clean, `go test ./scripts/api-lint/... -count=1` green, blocking gate = `0 blocking, 397 reported` exit 0, binary-level reddening proven, go-reviewer no CRITICAL/HIGH. Phase 6 wiki sync still runs LAST. ▸ Earlier: Phase 11 complete — coherence cleanup F4·F5·F6·F7 + F8-backend: F4 force-release tier-2 → membership.manage (operator ruling a) + approval-route tier-1 rows + spec security/pagination-exempt (8 lint hits cleared); F5 tripwire SkipDir + allow-list → live tripwire 0, honest lint total 455→397 after the premise correction (455 = 405 spec-drift + 50 tripwire, NOT ~447 tripwire); F6 dead file/comment deleted + authz-call-present dormancy documented; F7 one shared LoadDocumentAreaCode + reserve area_code='tenant' (migration 0228 + baseline); F8 deny-default matrix + system_admin/BypassSystem audit (atomic sink, fail-closed). Review: no CRITICAL/HIGH. DB live-bootstrap gate PASS (0228 applies + idempotent; CHECK rejects area_code='tenant'). Phase 12 (F3) complete — `doc.*`→`document.*` cap-value rename (registry+seed+migration 0229), audit-event vocabulary proven decoupled (BACKEND-ONLY, no FE/history touch), single `document` Admin Center group, `controlled_documents.*` symmetry flagged-not-actioned; Phase 6 wiki sync NOW UNBLOCKED (last code phase done). Phase 10 complete — F2 registry minimization: 4 redundant phantom caps merged into canonical equivalents; registry 33→29, area-grade 14→11; migration 0227 + baseline mirror. Phase 9 complete — legacy "docx v2" role dialect decommissioned; post-migration audit added Phases 9-12)
> **Scope:** Coherence of the authorization model across all layers — Go capability registry, route table, DB role→capability seed, OpenAPI vendor annotations, tier-2 area enforcement, delivery handlers, and wiki. Establishes the target architecture and the phased path to reach it without symptom-patching.
> **Out of scope:** Authentication / sessions (tier-0, see `wiki/modules/auth.md`); platform-vs-tenant admin split (ADR 0021); changing any OpenAPI **shape** (paths, params, request/response schemas) — this ADR only adds `x-authz-*` vendor extensions and changes enforcement, never the contract shape.
> **Key files:**
> - `internal/modules/iam/domain/model.go:50` — capability registry (`Capability` consts + `validCapabilities`)
> - `apps/api/cmd/metaldocs-api/permissions.go:85` — tier-1 route → capability table
> - `internal/modules/iam/authz/authz.go:51` — tier-2 `Require(ctx, tx, cap, areaCode)`; `"tenant"` skips area filter
> - `internal/modules/iam/infrastructure/postgres/user_area_repository.go` — membership tier-2 call sites (Phase 3: now pass the real `areaCode`; `GrantAtomic` asserts old==new area)
> - `internal/modules/iam/delivery/http/routes_memberships.go` — `listMemberships` directory scope (Phase 4: `isMembershipDirectoryAdmin` `RoleSystemAdmin` gate **removed**; scope now resolved at the data layer via `AreaMembershipService.DirectoryScope` → `UserAreaRepository.MembershipDirectoryScope`). `canManageMembershipTarget` **removed in Phase 3** — authz is now tier-1 + tier-2 only; handler keeps `isSelf` + cross-tenant 404
- `internal/modules/iam/infrastructure/postgres/user_area_repository.go` — `MembershipDirectoryScope` (system_admin bypass + cap-area EXISTS) + `ListByTenantInManagedAreas` (managed-area subquery JOIN, R3 SQL filter), Phase 4
> - `scripts/api-lint/code_rules.go:103` — `authz-call-present` (driven by `x-authz-area`, currently dormant) + `tripwire-pairing`
> - `db/reference-data/0001_product_reference_data.sql` — `role_capabilities` baseline seed

## Context

A cross-module audit (2026-06-03) of every authorization enforcement point in `internal/modules/` surfaced that the authz model is **not one coherent system**. It is three different dialects sharing one vocabulary, plus silent drift between the five places that declare capabilities. The IAM area-membership 403 defect (an `area_admin` holding `membership.manage` is blocked by a hardcoded `system_admin`-only handler gate) is the one place the incoherence throws a loud error; the rest fails silently.

### Finding 1 — three enforcement dialects

| Dialect | Mechanism | Modules |
|---|---|---|
| **A. Pure capability, tenant-wide** | tier-1 + tier-2 with literal `"tenant"` (area filter OFF) | templates, taxonomy, controlled-documents, iam user-roles, **iam area-membership** |
| **B. Genuine area-scoped** | tier-2 with real `areaCode` + frozen eligible-actor snapshot + SoD | documents / approval (submit, signoff, publish, supersede, obsolete) |
| **C. Hardcoded role string in handler** | `role == RoleSystemAdmin` in delivery code | **iam area-membership only** |

Role-string literals are compared inside delivery handlers in **exactly one place**: `routes_memberships.go` (`canManageMembershipTarget`, `isMembershipDirectoryAdmin`). Every other module trusts the capability model. IAM area-membership is doubly deviant — dialect A **and** C — and its name promises dialect-B (area) semantics it never implements. That contradiction is the root of the reported bug.

### Finding 2 — the area machinery is built but dormant

ADR 0007's codegen-rejection amendment shipped an annotation-driven lint: `x-authz-area` (`source` body|path, `field`, `multi`) drives `authz-call-present`, and `tripwire-pairing` requires every mutating repository SQL to pair with an `authz.Require`. **But 0 operations carry `x-authz-area` today**, and every tier-2 call passes the literal `"tenant"` — so the area rule never fires and the tripwire passes trivially (a call is present; its *scope* is unchecked). The scaffold for explicit area enforcement exists and is unused.

### Finding 3 — the five sources of truth drift silently

| Drift | Detail |
|---|---|
| Dead caps | `workflow.review`, `workflow.approve` — defined in Go, never seeded, never routed |
| Inline/undefined caps | `doc.publish`, `doc.obsolete`, `template.archive` used as raw `Capability("…")` strings in `permissions.go`, absent from `validCapabilities` → a typo compiles clean |
| Naming split | `doc.supersede` vs the `document.*` prefix used by every sibling |
| Orphan grant | `route.manage` seeded to roles, no route consumes it (ADR 0018 deferral) |
| Stale OpenAPI | governance schema enumerates 3 capabilities not in the Go model — **reclassified Phase 5:** the `AccessPolicyItem`/`AccessPolicyWriteItem` `capability` enum is a deliberately-separate document-ACL vocabulary (backed by the `document_access_policies` CHECK + FE types), NOT IAM-registry drift; documented in-spec, not pruned (would be a forbidden shape change) |
| Wiki drift | `local-dev-credentials.md` claims `area_admin` holds `membership.grant` — a capability that **does not exist** (real: `membership.manage`) |

### Root cause

> There is no single, machine-enforced source of truth for the capability model, and "area-scoped vs tenant-wide" is a per-call-site stringly-typed accident rather than a declared, enforced property of each capability.

Every layer re-declares authz in its own idiom (Go consts, raw route strings, duplicated SQL seeds, hand-written wiki, occasional handler role-checks) with nothing forcing agreement. The membership bug, the dormant area machinery, and the drift table are all symptoms of that one cause. Patching `canManageMembershipTarget` alone fixes the 403 and leaves the disease.

## Decision

Adopt five principles and one governance model. Capability remains the boundary (ADR 0021); this ADR makes that boundary *coherent and enforced*.

1. **Single source of truth = the Go capability registry.** `validCapabilities` in `model.go` is authoritative. No capability may be referenced as an inline string in `permissions.go` or elsewhere — every route, handler, and seed maps to a typed `Capability` const. CI enforces `routeRules.capability ∈ validCapabilities` and `every Capability is seeded to ≥1 role`.

2. **Scope is an explicit, declared property of each capability/operation — not a per-call-site literal.** Each capability is classified **tenant-grade** or **area-grade**. Area-grade operations annotate `x-authz-area` (reviving the dormant machinery) and pass the *real* target area to tier-2; tenant-grade operations pass `"tenant"` deliberately and are marked `x-authz-skip-area`. The choice is reviewed once, declared, and lint-enforced — never an accident of which string a repository author typed.

3. **Authorization lives only in the two tiers. Handlers hold business invariants, not authz.** The hardcoded `role == RoleSystemAdmin` checks in `routes_memberships.go` are removed. Per ADR 0021, the system pivots on capabilities, which the backend enforces at tier-1 (route) and tier-2 (area). Handlers retain only genuine domain invariants that are *not* capability questions: self-grant lockdown (`isSelf`) and the cross-tenant existence guard (404, not 403).

4. **Administration is least-privilege and area-scoped where the resource is an area.** `area_admin`'s `membership.manage` is **area-scoped**: an `area_admin` may grant/revoke membership only within areas where they themselves hold a `membership.manage`-granting role (i.e. their own `user_process_areas` rows). `system_admin` remains tenant-wide via the existing tier-2 bypass. This matches the ISO-segregation driver (`wiki/modules/iam.md`), the approval precedent (dialect B), and closes the previously-masked escalation path where an `area_admin` could mint `area_admin`/`qms_admin` across the whole tenant.

5. **The model is bound by CI, not by discipline.** Tests bind registry ↔ routes ↔ seed ↔ OpenAPI ↔ wiki so drift becomes a red build, not a latent rot.

### Governance model (decision 4) — concrete mechanics, no new schema

"Managed areas" need no new table. `authz.Require(ctx, tx, membership.manage, <targetArea>)` already returns true exactly when the actor has an active `user_process_areas` row in `<targetArea>` whose role grants `membership.manage` — and `area_admin` is the only non-system role granted that cap. So changing the membership tier-2 calls from `"tenant"` to the target area code **is** the area-scoping, for free. `system_admin` still bypasses (tenant-wide). This is the smallest correct change consistent with the existing tier-2 semantics.

## Consequences

**Wins.**
- One enforcement model across all modules; the membership module stops being the outlier.
- Area-grade vs tenant-grade is declared and lint-checked; new write surfaces can't silently default to tenant-wide.
- Escalation path closed; `area_admin` administration obeys least privilege and ISO segregation.
- Drift between Go / routes / seed / OpenAPI / wiki becomes CI-detectable.
- Reuses the dormant `x-authz-area` + tripwire harness instead of new infrastructure.

**Costs.**
- IAM membership/people handlers are hand-rolled (pre-codegen), so the `authz-call-present` lint (which assumes codegen `req.Body`/`req.Params` shapes) cannot directly cover them. Until/unless IAM migrates to codegen, area-grade IAM ops are covered by dedicated unit/integration tests + `x-authz-skip-area` on the spec with a documented exception — **not** by forcing a codegen migration into this program.
- A small behavior change for `area_admin` directory listing (Phase 4): from "self-only" to "managed areas".
- Several phases touch shared contract metadata; each must re-enter the `metaldocs-backend-api` skill and run its verification gates.

## Implementation — phased plan

Each phase is independently shippable, root-cause-scoped, and gated. No phase changes OpenAPI **shape**. Phases touching routes/handlers re-enter `metaldocs-backend-api`.

### Phase 1 — Registry hygiene (no behavior change) ✅ COMPLETE 2026-06-03
- ✅ Promoted inline caps to typed consts: `CapDocumentPublish` (`doc.publish`), `CapDocumentObsolete` (`doc.obsolete`), `CapTemplateArchive` (`template.archive`) in `model.go` + `catalog.go` descriptions; replaced the `Capability("…")` literals at `permissions.go:168,186,187,189`. Exact string values preserved (no DB/tripwire impact).
- ✅ Removed dead caps `workflow.review`/`workflow.approve` (const + `validCapabilities` + `capabilityDescriptions`). Grep confirmed zero non-definition Go references first (the `documents/_artifacts/00-context.md` claim that `fillin_authz.go` imports them was **stale** — it does not). Registry size guard bumped 28→29 (−2 dead, +3 promoted).
- ✅ Added CI tests in `permissions_test.go`: `TestEveryRouteCapInRegistry` (every `routeRules[i].capability` ∈ `validCapabilities`) and `TestEveryCapSeededOrDeferred` (every registry cap seeded to ≥1 role OR in the deferred allow-list = the 4 routed-but-unseeded system-admin-bypass-only write caps `doc.publish`/`doc.obsolete`/`doc.supersede`/`template.archive`). Both proven to bite on injected drift, then reverted.
- ✅ Fixed wiki drift: `local-dev-credentials.md` `membership.grant` → `membership.manage` + authoritative-source annotation (also dropped the incorrect `doc.publish` area_admin grant). OpenAPI governance enum reconciliation **deferred to Phase 5** (binding CI) — not isolated/trivial enough for Phase 1.
- **Branch:** `feat/authz-coherence-p1-registry`.
- **Gates (all green):** `go build ./...` ✓; `go test ./apps/api/... ./internal/modules/iam/... -count=1` ✓; `go run ./scripts/api-lint api/openapi/v1/openapi.yaml .` → 455 violations, **unchanged vs base** (pre-existing dormant tripwire/AUTHZ-DRIFT debt per Finding 2; zero new) ✓; `npx @redocly/cli lint` → valid ✓.

### Phase 2 — Declare scope explicitly ✅ COMPLETE 2026-06-03
- ✅ Added typed `CapabilityScope` classification (`ScopeTenant` / `ScopeArea`) in `internal/modules/iam/domain/capability_scope.go` — a `capabilityScopes` map covering all 29 registry caps. Area-grade (11) = document/approval/controlled-document write caps + `membership.manage`; tenant-grade (18) = `*.view`, `template.*`, `taxonomy.manage`, `user.manage`, `route.manage`, `metrics.view`, `audit.read`, `session.manage`. Tests `TestEveryCapabilityClassified` (every cap classified, no stale entry), `TestAreaGradeCapabilitySet` (exact area set locked), `TestScopeOf`.
- ✅ **Grant-decision + seed:** new migration `db/migrations/0225_authz_p2_document_lifecycle_grants.sql` (+ curated `reference-data/0001` mirror) seeds the four routed-but-unseeded write caps to real tenant roles, derived from existing seed parallels: `doc.obsolete`/`doc.supersede` → `area_admin`+`qms_admin` (mirror `controlled_documents.*` holders); `doc.publish` → `area_admin`+`qms_admin` (document-lifecycle authority set, product-confirmed over the broader signoff-holder parallel); `template.archive` → `qms_admin` (template steward; `area_admin` holds no `template.*`). `system_admin` holds all four via tier-2 bypass. `TestEveryCapSeededOrDeferred` deferred allow-list emptied (all four now seeded). Migration verified forward-applying + idempotent against the live DB.
- ✅ Spec annotation **scoped to the genuinely area-enforced documents/approval lifecycle ops** (`submitDocumentForApproval`, `recordDocumentSignoff`, `recordApprovalStageSignoff`, `publishDocument`, `scheduleDocumentPublish`, `supersedeDocument`, `obsoleteDocument`) — marked `x-authz-skip-area: true` + `x-authz-skip-reason` documenting tx-layer enforcement. IAM membership ops deferred to Phase 3; tenant-grade ops carry no spec marker (the Go classification is the authoritative declaration).
- **Premise correction (binding finding for Phases 3 & 5):** Phase 2 originally planned to annotate `x-authz-area` on documents/approval to *activate* `authz-call-present`. This is **infeasible** with the lint as built and was **deferred to Phase 5**. Evidence: `authz-call-present` (`code_rules.go`) is a single-function-body scan with no call graph; it requires the operationId-named handler to itself call `authz.Require(..., req.Body.X | req.<Op>Params.X)`. But **zero `authz.Require` calls exist in any `delivery/http` or codegen `api/` layer** — every area enforcement lives in tx-layer services, where the area is **DB-derived** (`loadDocumentAreaCode(...)`) not request-supplied (correct, un-spoofable, per ADR 0007 tx-coupling). So the rule's `req.Body`/`req.Params` matcher cannot match *any* module, not just IAM. Activating it requires either moving authz into handlers (ADR 0007-rejected, insecure) or a call-graph/derived-source lint rewrite — folded into Phase 5. Consequence: the typed `capabilityScopes` map is Phase 2's real, CI-bound scope declaration; `x-authz-area` activation is Phase 5 work.
- **Runtime gap (Phase 3 alignment):** `document.create` and the `controlled_documents.*` caps are classified area-grade (declared target) but their tier-2 call sites still pass the literal `"tenant"` today; aligning them to a real areaCode is later-phase work, not declared scope.
- **Branch:** `feat/authz-coherence-p2-scope`.
- **Gates (all green):** `npx @redocly/cli lint` → valid ✓; `go run ./scripts/api-lint api/openapi/v1/openapi.yaml .` → 455 violations, **unchanged vs Phase-1 baseline**, `authz-call-present` count 0 (still dormant, as designed) ✓; `go build ./...` ✓; `go test ./internal/modules/... ./apps/api/...` → green except pre-existing `documents/repository` pagination Scan drift (confirmed failing identically on base branch, unrelated to this change) ✓; migration 0225 forward-applies + idempotent + 7 grants verified on the live DB ✓.

### Phase 3 — Membership area-scoping (closes the original defect, root-cause) ✅ COMPLETE 2026-06-03
- ✅ `user_area_repository.go` `Insert`/`CloseActive`/`GrantAtomic`: pass the membership's real `areaCode` to `authz.Require` (was `"tenant"`). `Insert`→`membership.AreaCode`, `CloseActive`→`areaCode`, `GrantAtomic`→`newMembership.AreaCode`. Added a hard repository invariant on `GrantAtomic` (`oldMembership.AreaCode == newMembership.AreaCode`) so the single area-scoped check governs both the close (old) and insert (new) legs — a future caller can't close a row in an unauthorized area by pairing it with an authorized new area (defense-in-depth, from security review).
- ✅ `routes_memberships.go`: deleted the `canManageMembershipTarget` `RoleSystemAdmin` role-string gate from grant + revoke (the lone handler role-check; Finding-1 dialect C eliminated). Authorization is now tier-1 (route cap) + tier-2 (area). Added `errors.As(err, &authz.ErrCapDenied{})` → **403** mapping in `writeMembershipError` (tier-2 denial was previously surfacing as 500). Kept `isSelf` self-grant 403 and the cross-tenant 404 `guardMembershipUserInTenant` (business invariants, not authz). **Self-revoke remains permitted** (pre-existing; self-de-escalation is not an escalation and is fully audited — out of Phase 3 scope).
- ✅ Annotated `grantAreaMembership` (`x-authz-area: {source: body, field: areaCode}`) and `revokeAreaMembership` (`source: query`) + `x-authz-skip-area: true` with a documented hand-rolled-handler exception (IAM is pre-codegen per ADR 0012; `authz-call-present` scans for the codegen `req.Body`/`req.Params` shape and cannot match — covered by tests, lint activation deferred to Phase 5). No OpenAPI **shape** change.
- ✅ Tests. **Unit** (`tests/unit/iam_memberships/`): `area_admin` grant/revoke for another target reaches the service (201/204) — proving the role gate is gone; `area_admin` self-grant still 403; existing cross-tenant 404 + duplicate 409 intact. Rewrote the obsolete `routes_memberships_contract_test.go` forbidden-envelope trigger from the deleted role gate to a simulated tier-2 `ErrCapDenied` → 403 `AUTH_FORBIDDEN`. **Integration** (`tests/integration/iam/membership_area_scope_test.go`, real DB, seeded `user_process_areas` via the scheduler-bypass GUC): `area_admin` CAN grant+revoke within a managed area; CANNOT outside it → `ErrCapDenied` (and the denied grant writes 0 rows / denied revoke keeps the row — BOLA guard asserted); **R1** — `system_admin` with **no** per-area row CAN grant/revoke in any area (tier-2 bypass not blocked), asserted explicitly.
- **Branch:** `feat/authz-coherence-p3-membership-area-scope`.
- **Gates (all green):** `go build ./...` ✓; `go vet ./internal/modules/iam/...` ✓; `go test ./internal/modules/iam/... ./tests/unit/iam_memberships/...` ✓; `go test -tags=integration ./tests/integration/iam/... -run Membership` → 3/3 pass + idempotent on rerun ✓ (pre-existing `TenantIsolation` failures in `tenant_isolation_test.go` confirmed failing identically without this change — unrelated tenant-FK/seed env issue); `npx @redocly/cli lint` → valid ✓; `go run ./scripts/api-lint api/openapi/v1/openapi.yaml .` → **455 violations, unchanged vs Phase-2 baseline** (zero new; `authz-call-present` count still 0, silenced by `x-authz-skip-area`) ✓. Security + Go review run; 1 actionable HIGH (GrantAtomic area-match guard) applied, remainder triaged (pre-existing / out-of-scope / Phase 5).

### Phase 4 — Directory/list area-scoping ✅ COMPLETE 2026-06-03
- ✅ `routes_memberships.go` `listMemberships`: deleted `isMembershipDirectoryAdmin` (`RoleSystemAdmin` string) — the **last handler role-name literal** (ADR 0021 closed for IAM membership). Scope is now resolved at the data layer via `AreaMembershipService.DirectoryScope`: `system_admin` → tenant-wide (`ListByTenant`); `area_admin` (holds `membership.manage` in ≥1 area) → managed areas only (`ListByTenantInManagedAreas`); all other roles → self-only (`ListByTenant` with `userId` pinned to actor, 403 on cross-actor probe). Actor read from `authn.UserIDFromContext` (fail-closed 403 on empty), not the `"system"`-fallback `authenticatedActor`.
- ✅ **R3 HARD** — `user_area_repository.go` `ListByTenantInManagedAreas`: the `area_admin` managed-area restriction is a **SQL subquery JOIN** (`area_code IN (SELECT … role_capabilities ⋈ user_process_areas WHERE capability = membership.manage)`), not a post-fetch filter. `MembershipDirectoryScope` resolves tenant-wide (system_admin EXISTS, the same bypass `authz.Require` uses — R1) + has-managed-areas (cap-area EXISTS) in one round trip. Both active-row predicates bound on `effective_from <= now() AND effective_to IS NULL` (consistency with the file's other list queries; security review C1).
- ✅ No OpenAPI **shape** change (`listAreaMemberships` contract unchanged; redocly valid).
- ✅ Tests. **Unit** (`tests/unit/iam_memberships/`): `system_admin` tenant-wide; new `area_admin` managed-areas-only (QMS visible, RH excluded — BOLA guard); `viewer` self-only; cross-actor probe 403; cross-tenant isolation. **Contract** (`routes_memberships_contract_test.go`): stub methods added. **Integration** (`tests/integration/iam/membership_area_scope_test.go`, real DB): `TestMembershipDirectory_AreaAdminScopedInSQL` (managed area returned, unmanaged dropped IN SQL; scope = false,true) + `TestMembershipDirectory_SystemAdminTenantWide` (R1).
- **Branch:** `feat/authz-coherence-p4-directory-scope`.
- **Gates (all green):** `go build ./...` ✓; `go test ./internal/modules/iam/... ./tests/unit/iam_memberships/...` ✓; `go test -tags=integration ./tests/integration/iam/... -run "MembershipDirectory|MembershipAreaScope"` → pass (real DB) ✓; `go run ./scripts/api-lint` → **455 violations, unchanged vs Phase-3 baseline** (zero new; new methods are read-only SELECTs, no `tripwire-pairing` hit) ✓; `npx @redocly/cli lint` → valid ✓. Security + Go review run; the one actionable family (active-row `effective_from` predicate gap, security C1 + 2 go-review MEDIUMs) fixed; remainder triaged (pre-existing unbounded-list/pagination → Phase 5+; group-tenant DB constraint → DB scope; logging/scan-wrap match file convention; M2 header-spoof false — same `authn` trust source).

### Phase 5 — Bind the sources in CI ✅ COMPLETE 2026-06-03
- ✅ **New `scripts/api-lint` binding rules** (`scripts/api-lint/registry_rules.go`, imports `iam/domain`; wired into `RunCodeRules`):
  - `no-inline-capability` (item 2) — AST scan banning `Capability("literal")` string conversions outside `iam/domain/model.go` and `_test.go`. Every referenced cap must be a typed registry const; a `Capability("doc.bogus")` row (which compiles clean) is now a red build. Generalizes Phase-1 `TestEveryRouteCapInRegistry` to *all* code, not just the route table.
  - `seed-registry-parity` (item 3) — parses `db/reference-data/0001_product_reference_data.sql` `role_capabilities` INSERTs and binds them to the registry **both directions**: every seeded cap ∈ `validCapabilities`; every registry cap seeded to ≥1 role OR in the documented `deferredCaps` allow-list (empty since Phase 2). Wired into the lint binary, not just the existing `TestEveryCapSeededOrDeferred` unit test.
  - `wiki-capability-parity` (item 4) — binds `` `cap:<name>` `` enforcement-claim markers in the authz wiki docs (`authz-tiers.md`, `local-dev-credentials.md`, `iam.md`) to the registry. Catches future `membership.grant`-style drift. A **marker convention** is used (not a blunt "every dotted token") so illustrative prose (`doc.create`) and deferred caps (`route.view`, ADR 0018) are not false-positived. Markers retrofitted onto the genuine enforcement claims (incl. the exact `membership.manage` drift locus in `local-dev-credentials.md`).
- ✅ **Area-grade annotation parity (item 1)** — extended the spec `state-transition` lint regex (`spec_rules.go`) to cover `signoff|supersede|obsolete|schedule-publish` (new lifecycle transition ops must now be annotated; zero new violations — all current ones already annotated). Added `apps/api/cmd/metaldocs-api/permissions_authz_scope_test.go`: `TestAreaEnforcedOpsAnnotated` locks every ADR-declared area-enforced op (the 7 documents/approval lifecycle + 2 membership ops) to carry `x-authz-area` or a justified `x-authz-skip-area`+`x-authz-skip-reason`; `TestMembershipManageIsAreaGradeAndAnnotated` binds the typed `IsAreaGrade(membership.manage)` classification to the annotated grant/revoke ops. **Bounded scope:** the `document.create`/`document.edit`/`controlled_documents.*` ops are classified area-grade but DB-derived/tx-tripwire enforced and pass `"tenant"` today (Phase-2 runtime gap) — they are intentionally excluded from the annotation lock; realigning them to real `x-authz-area` is the deferred work below.
- ✅ **OpenAPI governance enum reconciled (item 5)** — the 3 caps the spec enumerates beyond the Go model (`document.upload_attachment`, `document.change_workflow`, `document.manage_permissions` in `AccessPolicyItem`/`AccessPolicyWriteItem`) were confirmed **NOT** unused: they back a live DB CHECK (`document_access_policies`, baseline:1074 + migration `0011`) and FE generated types, and are a deliberately-separate document-ACL vocabulary for the (backend-unimplemented) per-document access-policy feature — **not** IAM registry caps. Reconciled by **documenting** (added a `description` to both enums; metadata, no shape change), **not pruning** (pruning would desync DB+FE and narrow an enum = a forbidden contract-shape change). Finding-3 row reclassified below.
- ✅ **Item 6 (transaction-local GUCs) — VERIFIED PASS.** Every `set_config('metaldocs.*', …)` call site passes the 3rd arg `true` (transaction-local): `SeedTxIdentity` (`context.go:59-62`, actor+tenant), `asserted_caps` (`authz.go:188`), `bypass_authz`, and every module `authz_guc.go`. Grep for session-level seeding returns NONE. Regression-locked: `TestSeedTxIdentity_UsesTransactionLocalGUCs` (`context_test.go`) fails if either GUC regresses to `, false)`/missing flag. PgBouncer pooling-leak guard (R6) satisfied.
- ✅ **Item 7 (native-RLS bypass guard) — REPORTED, N/A by design (see below).**
- **Deferred (out of Phase 5 numbered scope):** `authz-call-present` tx-layer activation. The rule still only matches handler-inline `authz.Require(req.Body.X | req.Params.X)` — a shape no module uses (area is DB-derived in tx-layer services). Activating it requires a call-graph / `source: derived` lint rewrite, then flipping the `x-authz-skip-area` markers to real `x-authz-area`, then binding `capabilityScopes` to runtime (closing the `document.create`/`controlled_documents.*` runtime gap). This is a separate lint-engine change, not required to make registry/seed/wiki/annotation drift a red build (the Phase 5 goal). Tracked as residual Phase 5+ work.
- **Branch:** `feat/authz-coherence-p5-ci-binding`.
- **Gates:** `go build ./...` ✓; `go run ./scripts/api-lint api/openapi/v1/openapi.yaml .` → 455 violations, **unchanged vs Phase-4 baseline** (all new rules clean-state-zero) ✓; `go test ./scripts/api-lint/... ./apps/api/cmd/metaldocs-api/... ./internal/modules/iam/...` ✓; `npx @redocly/cli lint` → valid (no shape change) ✓. Each new guard proven to bite on injected drift then reverted (see Phase 5 close-out evidence).

#### Item 7 — native RLS vs. trigger-tripwire (finding)
The MetalDocs schema uses **no Postgres native Row-Level Security** (`grep` of `db/` for `ROW LEVEL SECURITY`/`CREATE POLICY` → zero). Tenant/area isolation on the sensitive tables is enforced by a `BEFORE INSERT OR UPDATE OR DELETE` trigger `enforce_capability_asserted()` (`db/baseline/0001_current_schema.sql:490-560`, attached as `trg_require_cap_asserted` on `documents`, `controlled_documents`, `user_process_areas`, `iam_user_roles`, `cd_sequence_counters`, `templates_template(_version)`, `document_{families,process_areas,profiles}`, `approval_{instances,signoffs}` — lines 3875-4050) that raises `ErrCapabilityNotAsserted` unless the transaction-local `metaldocs.asserted_caps` GUC carries the required cap. This is the ADR's "stricter fail-closed variant" of session-GUC RLS.

**Consequence for the R7 acceptance criterion:** `FORCE ROW LEVEL SECURITY` is **inapplicable** — there are no RLS policies to force. More importantly, the owner/superuser RLS-bypass footgun does **not** apply to this model: row triggers fire for all roles regardless of table ownership; only a superuser can skip them (`session_replication_role = replica` or explicit `DISABLE TRIGGER`). So on the specific axis the R7 criterion guards, the trigger model is **strictly stronger** than native RLS. The residual guard is therefore the same one R7 ultimately wants: **the application DB role must not be a superuser** and must not set `session_replication_role` — a deployment/role-grant concern, not a schema migration. **No migration is warranted**; adding native RLS would be an out-of-scope storage-architecture redesign (CLAUDE.md hard-stop). Recommendation logged for the deployment runbook: assert the runtime DB role is `NOSUPERUSER`.

### Phase 6 — Wiki sync
- Dispatch `wiki-curator`: refresh `authz-tiers.md`, `iam.md`, ADR 0007 amendment cross-link, bump `Last verified`, index this ADR.

## Alternatives considered

| Option | Verdict | Reason |
|---|---|---|
| Patch `canManageMembershipTarget` to allow tenant-wide `area_admin` | Rejected | Symptom patch; ships the escalation surface; leaves the root cause and the other dialects/drift untouched. Violates project hard-stop + no-symptom-patching rules. |
| Build a new managed-areas table/service | Rejected | Unnecessary — `user_process_areas` + tier-2 already express it. YAGNI. |
| Force IAM codegen migration to get lint coverage | Rejected (for now) | Large, orthogonal; covered by tests + documented exception instead. Revisit as its own work. |
| Per-operation generated authz wrappers | Rejected | Already rejected in ADR 0007 (tx-coupling); the tripwire is the static guarantee. |

## Amendment — External evidence base + refinements (2026-06-03)

Four parallel cited research sweeps (IAM models, policy engines, enforcement placement, anti-patterns) validated this ADR against industry norm. Full evidence: [`wiki/references/authz-industry-evidence.md`](../references/authz-industry-evidence.md). Verdict: the design is squarely in line with Kubernetes namespaced RBAC, GCP resource-hierarchy IAM, AWS scoped policies, and OWASP defense-in-depth. Three refinements are now binding acceptance criteria:

- **R1 — system_admin must short-circuit tier-2 (inheritance norm).** In K8s/GCP the cluster/org grant inherits downward; the tenant-wide admin is never blocked by a missing sub-scope grant. Phase 3 MUST assert with a test that `system_admin` (which `authz.Require` already bypasses) is not blocked by a missing per-area membership row.
- **R2 — (a) remove role-check and (b) area-scope are co-dependent.** The anti-pattern literature shows removing the role-string gate WITHOUT adding area scope converts a BFLA over-restriction into a BOLA privilege-escalation (OWASP API5→API1). Hard gate: no phase may merge a change that deletes a role-string gate unless its area-scope replacement lands in the same change (Phase 3 for grant/revoke, Phase 4 for the directory gate).
- **R3 — list/query scope must filter at the data layer, not in memory.** OWASP Multi-Tenant: enforce "at the data access layer, not just API layer." Phase 4's area_admin directory scoping must filter in the SQL query (managed-area set), not post-fetch — this also closes the one genuine gap vs industry defense-in-depth (single-layer enforcement on reads).

Two **tenant-isolation hardening** checks surfaced (Postgres RLS footgun literature) — tracked, verified in Phase 5: (i) **PASS** — all actor/tenant GUC seeding uses transaction-local `set_config(..., true)` (`SeedTxIdentity` + `asserted_caps` + every module `authz_guc.go`; regression-locked by `TestSeedTxIdentity_UsesTransactionLocalGUCs`); (ii) **N/A by design** — MetalDocs uses no Postgres native RLS; isolation is the `enforce_capability_asserted` trigger tripwire, which is strictly stronger on the owner/superuser-bypass axis the criterion guards (triggers fire regardless of table ownership). `FORCE ROW LEVEL SECURITY` is inapplicable (no policies); residual guard = app DB role must be `NOSUPERUSER` (deployment concern, no migration). See Phase 5 §"Item 7" for the full finding.

Engine decision: **keep the custom typed-registry + CI lint; do NOT adopt OPA/Cedar at current scale** — it reproduces Cedar's author-time schema-validation guarantee via the Go compiler + lint. Revisit triggers recorded in the evidence doc (ReBAC hierarchy → SpiceDB/OpenFGA; attribute policy decoupled from deploys → Cedar/AVP).

## Amendment — Phase 1-5 review outcome + Phase 7 added, Phase 6 resequenced (2026-06-03)

A 3-lens review panel (Go correctness · security/authz · architecture/ADR-conformance) audited the cumulative diff `d64f82893..426cc00dc`. Verdict: **SHIP Phases 1-5 — no CRITICAL, no HIGH.** R1/R2/R3 all PASS with real-DB test evidence; the original `area_admin` 403 is fixed at root; tenant isolation, no-BOLA-window, and SQL parameterization confirmed.

**Material finding — Principle 2 is PARTIAL (decorative scope).** 6 of 11 area-grade capabilities — `document.create`, `document.edit`, `controlled_documents.create`, `controlled_documents.obsolete`, `controlled_documents.supersede` — are **declared area-grade** in `capability_scope.go` but their tier-2 calls still pass the literal `"tenant"` (area filter OFF). Declared area, enforced tenant-wide = the same disease this ADR diagnosed, relocated from a stringly-typed accident into a typed-but-unbound map. Pre-existing (Phase 2 classification *exposed* it; did not widen it). The membership.manage + 5 approval-lifecycle caps (publish/obsolete/supersede/submit/signoff) **are** genuinely area-enforced. Concrete risk: a tenant-wide `document.edit` holder can edit drafts in any area (BOLA, OWASP API1).

**Decision (2026-06-03):** add **Phase 7** before final close, and **resequence Phase 6 (wiki sync) to run LAST, after Phase 7.** Phase 6 wiki MUST NOT assert "scope is enforced" until Phase 7 lands, else it is drift-by-documentation.

### Phase 7 — Bind typed scope to runtime (the real coherence close)
- **Core control:** an AST/CI guard that BANS `authz.Require(<areaGradeCap>, "tenant")` — pairs each typed `IsAreaGrade` capability to the literal passed at its call site. This is the missing declaration↔enforcement binding (Principle 5) and auto-catches all 6 gaps.
- **Per-cap resolution** (product/ISO ruling — derive the area where loadable, else reclassify; STOP and surface any genuinely ambiguous cap, do not guess):
  - `controlled_documents.*` changeStatus already loads the row (`FOR UPDATE`) → wire the real area (cheap; 3 of 6).
  - `document.create` / `document.edit` → enforce real area if area-segregation is intended (ISO driver), OR reclassify to `ScopeTenant` if create/edit-anywhere is intended product behavior. Surface for ruling.
- **Make guards self-maintaining:** derive `areaEnforcedOps` from `IsAreaGrade` (not a hand-curated list) so closing the gap forces annotation.
- **Fold in review MED/LOW fixes:** unexport `authz.BypassSystem` + scheduler-only bridge (CWE-269, prior review H6); add a `time.Time` param to `MembershipDirectoryScope` (clock consistency/testability); document `no-inline-capability` literal-only limitation + add a golden seed-row-count test; extract the duplicated system_admin-bypass SQL predicate (DRY).
- **Gates:** the new binding guard proven to bite then revert; `go build/test`; api-lint; redocly.

#### Phase 7 — COMPLETE 2026-06-03
- ✅ **Core control — `authz-area-scope-binding` AST guard** (`scripts/api-lint/registry_rules.go`, wired into `RunRegistryRules`). Parses `iam/domain/model.go` for the const→value map (consts aren't reflectable), then scans every non-test `.go` for `authz.Require(_, _, <cap>, "tenant")` where `<cap>` resolves to a typed const that `iamdomain.IsAreaGrade` classifies area-grade → red build. **Proven to bite:** 17 violations on the pre-fix tree (CD create ×5, `document.create` ×1, `document.edit` ×11); **green** after the per-cap fixes. Limitation (documented in godoc, mirrors `no-inline-capability`): only literal `"tenant"` + resolvable area-grade **const** caps are caught; variable-cap call sites (e.g. CD `changeStatus`'s `cap` param) are skipped — those are fixed to pass a real area and carry no `"tenant"` literal post-fix. Unit tests: bite / green-on-real-area / ignores-tenant-grade-and-variable.
- ✅ **Per-cap resolution (product ruling: area-segregate ALL 5 — Option 1).** Rationale: every role holding these caps is area-attached (no tenant-wide-only holder), the caps were already classified `ScopeArea` (Phase 2), and ISO segregation is the ADR's driver; `system_admin` still bypasses tier-2.
  - `controlled_documents.obsolete`/`supersede` (`service.go` `changeStatus`): `process_area_code` added to the existing `FOR UPDATE` SELECT (read, not a mutation — tripwire pairing holds), loaded area passed to `authz.Require`.
  - `controlled_documents.create`: real area at all 4 sites — `service.go` create (`cmd.ProcessAreaCode`), preview-code (`areaCode`), create-revision (`cd.ProcessAreaCode`); `infrastructure/repository.go` `Create`/`CreateTx` (`doc.ProcessAreaCode`).
  - `document.create` (`repository.go` `CreateDocumentTx`): `d.ProcessAreaCodeSnapshot` (fail-closed empty → deny non-system; **not** a fallback to `"tenant"`).
  - `document.edit` (~12 sites): area loaded within the tx before the check — `loadDocumentArea(tenantID, docID)` for docID-keyed ops, `loadDocumentAreaBySession(tenantID, sessionID)` for the two session-only ops (release / force-release). authz stays before the mutating SQL.
- ✅ **Self-maintaining annotation test** (`permissions_authz_scope_test.go`): the hand-curated `areaEnforcedOps` list is replaced by a derivation from `IsAreaGrade` × the runtime route table (`resolveRoutePermission`). The obligation is **cap-level** (every area-grade capability with a documented HTTP surface must carry `x-authz-area` or a justified `x-authz-skip-area` on ≥1 route-matched op) — deliberately not op-level, because an area-grade **tier-1** route cap does not imply a **tier-2** area check (e.g. session heartbeat); the AST guard is the per-call-site binding. Caps with no spec surface (`document.create` — its `POST /documents` is a raw, undocumented route) are bound by the guard + tests, not the spec. Annotated the 4 newly-enforced ops with a documented surface (`atomicCreateControlledDocument`, `obsoleteControlledDocument`, `supersedeControlledDocument`, `renameDocument`) `x-authz-skip-area` (area DB/payload-derived, tier-2 enforced) — metadata only, no shape change.
- ✅ **Review MED/LOW fixes.** (i) `BypassSystem` is now a **fail-closed background bridge**: the raw GUC setter is unexported (`setBypassGUC`); `BypassSystem` returns `ErrBypassNotBackground` unless the context carries the `WithBackgroundBypass` marker, set at the 4 background composition roots (session/orphan sweepers, river scheduled-publish worker, stuck-instance watchdog) — an HTTP request context can never reach the tier-2 bypass (CWE-269; regression-locked by `TestBypassSystem_FailsClosedWithoutBackgroundContext`). (ii) `MembershipDirectoryScope` takes a `now time.Time` (fed from the service `nowFn()`) — same clock as `ListByTenantInManagedAreas`. (iii) Duplicated system_admin EXISTS predicate extracted to `authz.SystemAdminExistsSQL`, reused by `Require` + `MembershipDirectoryScope`. (iv) Golden `TestSeedRowCount_GoldenMatchesParser` locks the `role_capabilities` row count (94) so a reformat / multi-row VALUES rewrite that breaks the single-row seed parser fails loudly.
- **Branch:** `feat/authz-coherence-p7-scope-runtime-binding`.
- **Gates (all green):** AST guard proven bite (17) → green (0); `go build ./...` ✓; `go test ./internal/modules/documents/... ./internal/modules/controlleddocuments/... ./internal/modules/iam/... ./apps/api/... ./scripts/api-lint/... -count=1` ✓ (sole failures = pre-existing `TestListDocumentsPaginated` Scan-arg drift, confirmed failing identically on base `426cc00dc`, unrelated to authz); `go run ./scripts/api-lint` → **455 violations, unchanged vs Phase-5 baseline** (new guard clean-state-zero) ✓; `npx @redocly/cli lint` → valid (no shape change) ✓.
- **Open follow-ups (out of Phase 7 boundary, reported not fixed — tier-1 route-cap questions surfaced by the self-maintaining derivation):** `forceReleaseDocumentSession` resolves to tier-1 `membership.manage` while its tier-2 check uses `document.edit` (cross-tier cap divergence); approval-route-management ops (`createApprovalRoute`/`updateApprovalRoute`/`deactivateApprovalRoute`) resolve to tier-1 `document.submit`. Both pre-existing tier-1 mappings; neither changed here.

## Amendment — Phase 1-7 final review: Phase 8 added (raw-string dialect), Phase 6 resequenced again (2026-06-03)

A final architecture + industry-conformance review (range `d64f82893..HEAD`, post-Phase-7) ruled the **bounded surface at industry best-practice** (scoped admin = K8s/GCP; defense-in-depth; DB-derived un-spoofable area; typed-const→runtime binding CI-enforced; original 403 closed at root, no symptom-patch). But the claim "the authz model is now ONE language" is **overstated** — a **fourth dialect survives**: raw-string capability arguments to `authz.Require`, which the SSOT lint is structurally blind to (`no-inline-capability` bans only the `Capability("…")` conversion syntax, not a raw `string` literal cap arg).

**Finding — 13 raw-string `authz.Require` cap sites in approval + templates modules:**
- **4 registry caps referenced by raw string** (violate Principle 1): `"doc.publish"` ×2 (`publish_service.go:88,234`), `"doc.obsolete"` (`obsolete_service.go:79`), `"template.archive"` (`lifecycle.go:521`).
- **5 PHANTOM caps not in the registry at all** (enforced at tier-2, seeded by nothing → pass for `system_admin` bypass only = dead-grant checks): `"route.admin"` ×4 (`route_admin_service.go:166,297,461,552` — note the registry's real cap is `route.manage`), `"workflow.instance.cancel"` (`cancel_service.go:109`), `"doc.edit_draft"` (`fillin_authz.go:32`), `"doc.view_published"` (`view_service.go:74`), `"doc.reconstruct"` (`reconstruct_service.go:41`).
- **Fail-OPEN** (pre-existing, inverse of Phase 7's fail-closed): `fillin_authz.go:44-52` `doc.edit_draft` falls back to area `"tenant"` on missing/empty area.

Because the SSOT lint cannot see these, Principle 5 ("bound by CI, not discipline") holds only where the program already converted to typed consts; everywhere else it can rot silently.

**Decision (2026-06-03):** add **Phase 8 — close the raw-string dialect** before final close. **Phase 6 (wiki sync) resequenced again to run after Phase 8** (else the wiki documents a coherence the approval/route/templates code does not speak — drift-by-documentation).

### Phase 8 — Close the raw-string capability dialect
- **Core control:** extend the lint (`registry_rules.go`) to flag any **raw-string literal cap argument** to `authz.Require`/`RequireAll` (not just the `Capability("…")` conversion). The AST machinery already inspects the cap arg for the area-scope binding rule — extend it to reject a `*ast.BasicLit` cap. Proven to bite (13 sites) then green.
- **Convert the 4 registry caps** to their typed consts (`CapDocumentPublish` ×2, `CapDocumentObsolete`, `CapTemplateArchive`) — then the existing area-scope binding rule auto-applies (these are area-grade → must enforce real area or be justified).
- **Register + classify the 5 phantom caps** in the registry (`model.go` const + `validCapabilities` + `capabilityScopes` + `catalog.go` description) and **seed** them to the correct roles; reconcile `"route.admin"` with the registry's `route.manage` (ADR 0018 owns route-lifecycle cap design — if reconciliation implies route-lifecycle redesign, STOP and report, cross-ref ADR 0018 rather than redesign here).
- **Fix the `fillin_authz.go` fail-open** → fail-closed on empty/missing area (match Phase 7 `document.create`).
- **Fix open follow-up (a):** align `forceReleaseDocumentSession` tier-1 cap (`permissions.go`) with its tier-2 `document.edit` (1-line) OR log as an explicit defect.
- **Per-cap area vs tenant ruling:** `doc.edit_draft` is area-grade (currently fail-open); classify the others (`doc.view_published`, `doc.reconstruct`, `workflow.instance.cancel`, `route.admin`/`route.manage`) tenant- vs area-grade — STOP and surface any genuinely ambiguous one, do not guess.
- **Gates:** new raw-string-cap rule proven bite→green; `go build ./...`; `go test` on approval/templates/documents/iam/api-lint; `go run ./scripts/api-lint`; `npx @redocly/cli lint`; security + code review on the diff.

#### Phase 8 — COMPLETE 2026-06-04
- ✅ **Core control — `no-rawstring-capability` AST rule** (`scripts/api-lint/registry_rules.go`, `checkNoRawStringCapability`, wired into `RunRegistryRules`). Flags any `*ast.BasicLit` STRING capability argument to `authz.Require` (arg index 2) or `authz.RequireAll` (every arg is a cap position). A variable / selector / `string(iamdomain.CapXxx)` cap arg is not a `BasicLit` → correctly ignored (so `templates/lifecycle.go:555`'s `cap` param and all typed sites stay green). **Proven to bite: 12 sites** on the pre-fix tree (the ADR amendment's "13" was an off-by-one — the real enumerated set is 4 registry + `route.admin`×4 + 4 singles = **12**); **green (0)** after conversion. Unit tests: bite / green-on-typed-const / ignores-variable-and-test-files / RequireAll-literal-bites.
- ✅ **4 registry caps retyped** (`string(iamdomain.Cap…)`): `doc.publish`×2 (`publish_service.go:88,234`), `doc.obsolete` (`obsolete_service.go:79`) — both area-grade, already passing the resource's real `areaCode`, so the `authz-area-scope-binding` rule is satisfied with no further change; `template.archive` (`templates/lifecycle.go:521`) — tenant-grade, keeps the `"tenant"` sentinel.
- ✅ **5 phantom caps reconciled / registered + classified + seeded.** Per-cap ruling (classification grounded in the cap's **tier-1 route authority** in `permissions.go`, not guessed):
  | phantom string | disposition | scope | tier-1 evidence |
  |---|---|---|---|
  | `route.admin` ×4 | **reconciled to existing `CapRouteManage`** (no new cap; already seeded qms_admin+system_admin) | tenant-grade (unchanged) | route verbs route under the `/api/v1/approval/` prefix; `route.manage` is the registry's "Gerenciar rotas de aprovação" |
  | `doc.edit_draft` | new `CapDocumentEditDraft` | **area-grade** | `/session`,`/autosave` → `document.edit` |
  | `doc.reconstruct` | new `CapDocumentReconstruct` | **area-grade** | `POST …/reconstruct` → `document.edit` |
  | `workflow.instance.cancel` | new `CapWorkflowInstanceCancel` | **area-grade** | `POST …/cancel` → `document.edit` |
  | `doc.view_published` | new `CapDocumentViewPublished` | **tenant-grade** | `GET /documents` → `document.view`; registry rule "all `*.view` reads are tenant-grade" (mirrors `CapDocumentView`) |
  - The dead `const CapManageRoutes = "route.admin"` (`route_admin_handler.go`, zero refs) was removed — it was the only other place the phantom string was minted.
  - **Seed:** new migration [`db/migrations/0226_authz_p8_phantom_cap_grants.sql`](../../db/migrations/0226_authz_p8_phantom_cap_grants.sql) (forward-only, idempotent `ON CONFLICT DO NOTHING`, writes `schema_migrations`) + mirrored single-row into the curated baseline `db/reference-data/0001_product_reference_data.sql`. Grant matrix mirrors each cap's tier-1 parallel **exactly** (`document.edit` set = approver/area_admin/author/editor/qms_admin for edit_draft/reconstruct/instance.cancel; `document.view` set = that + signer/viewer for view_published). `system_admin` holds all four via the tier-2 bypass (not seeded explicitly — consistent with 0225). 22 new role_capabilities rows (golden bumped 94→**116**); registry size 29→**33**; area-grade set 11→**14**.
- ✅ **Fail-OPEN → fail-closed.** Both `loadDocumentAreaCode` helpers (`documents/application/fillin_authz.go`, `approval/application/submit_service.go`) now return `""` on missing/empty area instead of `"tenant"` — so area-grade callers (`doc.edit_draft`, `doc.reconstruct`, `document.submit`, `document.signoff`, `doc.publish`, `doc.supersede`) deny non-system actors on an empty area (matches the Phase 7 `document.create` decision). The lone **tenant-grade** caller of the approval helper (`read_service.go`, `document.view`) re-coalesces `"" → "tenant"` at its call site to preserve the intentional area-filter-OFF semantics. `view_service.go`'s `doc.view_published` (tenant-grade) now passes the `"tenant"` sentinel directly (its inline area read was dropped).
- ⚠️ **Open follow-up (a) — `forceReleaseDocumentSession` tier-1: logged as defect, NOT changed.** `permissions.go:175` gates `…/session/force-release` at tier-1 `membership.manage` while its tier-2 check uses `document.edit`. Aligning tier-1 → `document.edit` (the "1-line" option) would **broaden a destructive session-takeover capability** to every document editor (author/editor/approver/qms_admin), not just area admins. The divergence is plausibly deliberate (tier-1 `membership.manage` = *who* may force-release = admin authority; tier-2 `document.edit` = *which area*). Changing it is a cross-tier cap-design decision in the same family as follow-up (b) and ADR 0018 §6's deferred tier-1 route-cap split — deferred there, not symptom-patched here.
- ⚠️ **Open follow-up (b) — approval-route tier-1 = `document.submit`: unchanged, cross-referenced to ADR 0018 §6.** The tier-2 reconciliation (`route.admin` → `route.manage`) does not force a tier-1 change; the principled tier-1 route read/write split (`CapRouteView`) is explicitly deferred by ADR 0018 §6 to the F-001 follow-up. Net registry-faithful effect of the tier-2 reconciliation: `qms_admin` (who holds `route.manage` in the seed) now passes the tier-2 route-admin check it was previously denied by the dead phantom — enforcement now matches the seeded intent.
- **Behavior change (intended):** the four phantom tier-2 checks were previously dead-grants passing only for `system_admin` (bypass). Seeding them to the tier-1 holder set aligns tier-2 with the already-declared tier-1 surface (defense-in-depth, not a new attack surface — tier-1 already admitted these roles). Verified against the approval/documents/iam suites (no suite encoded the phantom restriction as intended behavior).
- **Branch:** `feat/authz-coherence-p8-rawstring-dialect`.
- **Gates (all green):** new rule proven bite (12) → green (0); `go build ./...` ✓; `go test ./internal/modules/documents/... ./internal/modules/controlleddocuments/... ./internal/modules/templates/... ./internal/modules/iam/... ./apps/api/... ./scripts/api-lint/... -count=1` ✓ (sole failure = the pre-existing `TestListDocumentsPaginated` Scan-arg drift, confirmed failing identically with the Phase-8 diff stashed — unrelated); `go run ./scripts/api-lint` → **455 violations, unchanged vs the Phase-5/7 baseline** (new rule clean-state-zero, all 4 new caps seeded so no `seed-registry-parity` delta) ✓; `npx @redocly/cli lint` → valid (no OpenAPI shape change) ✓; migration `0226` applies + is idempotent on a fresh curated bootstrap (`dev-bootstrap-baseline.ps1 -WithDevSeed`) ✓.

## Amendment — Critical post-migration audit: Phases 9-12 added, Phase 6 resequenced again (2026-06-04)

After Phase 8 merged (PR #65), a 4-lens read-only audit (dead-code residue · capability-registry redundancy · workaround/enforcement-gap · web-grounded industry-standards) swept the whole authz surface to verify no legacy logic, dead code, or redundant capabilities were left behind by the 8-phase migration. **Verdict: the enforcement engine is solid and industry-validated** (custom registry + CI lint is the right call over OPA/Cedar at this scale; two-tier with DB-derived un-spoofable area scope matches OWASP "enforce at the data layer"; fail-closed `BypassSystem` background bridge + `NOSUPERUSER` guard match GCP/K8s inheritance norms; the `authz-area-scope-binding` guard closes the one real anti-pattern — all KEEP, do not re-engineer). But the program's "one language" claim was **overstated**, and Phase 8's registry modeling over-adapted old code.

**Findings (root-cause families):**
- **F1 — CRITICAL.** The ADR's foundational claim (Finding 1: *"Role-string literals are compared inside delivery handlers in exactly one place: `routes_memberships.go`"*) was **false when written**. `internal/modules/documents/delivery/http/handler.go:30-32` declares a parallel role vocabulary (`roleAdmin="system_admin"`, `roleTemplateAuthor="template_author"`, `roleDocumentFiller="document_filler"`) and `:169,180,204,215,734,1155,1165` gate real document routes (list, stats, session force-release, comments) via `hasRole`/`hasAnyRole` — bypassing the entire tier-1/tier-2 capability model. **`template_author` and `document_filler` are absent from the 8-role IAM registry** (`model.go:16-23`), so `GET /api/v1/documents` is gated on a role nothing holds (`roleAdmin || roleDocumentFiller`) — incoherent with the role SSOT, effectively `system_admin`-only for every other role. The program cured dialect C only where it audited.
- **F2 — HIGH.** Phase 8's security fix (closing raw-string phantom dead-grants) was correct, but it **minted 4 redundant capabilities** instead of routing those tier-2 sites through existing consts: `doc.edit_draft`, `doc.reconstruct`, `workflow.instance.cancel` carry grant sets identical to `document.edit` (and their HTTP surfaces are already tier-1-routed under `document.edit` at `permissions.go:182,190,191`); `doc.view_published` is identical to `document.view`. No authorization differentiation today — the "adapt instead of question" pattern.
- **F3 — MED.** Naming incoherence: `doc.*` / `document.*` / `controlled_documents.*` for one document aggregate — the clearest deviation from the industry `resource.action` discipline (AWS/GitHub). Splits the Admin Center catalogue (`catalog.go:162`).
- **F4 — MED.** Tier-1/tier-2 capability divergence (fail-*closed* dead-ends, not holes): `forceReleaseDocumentSession` (tier-1 `membership.manage` vs tier-2 `document.edit`) and approval-route CRUD (tier-1 `document.submit` vs tier-2 `route.manage`). The latter is the source of the 8 real OpenAPI `AUTHZ-DRIFT`/`PAGINATION-DRIFT` lint hits.
- **F5 — MED.** The "455 violations unchanged" metric across all 8 phases is misleading: ~447 are dormant `tripwire-pairing` noise (no call-graph) **plus ~28 phantom dupes from a stale `.claude/worktrees/...` detached worktree** the tripwire walker fails to `SkipDir` (unlike the other walkers). It launders **8 genuinely actionable** approval-route spec-drift items. Normalization-of-deviance risk.
- **F6 — LOW/MED.** Dead scaffolding: `authz-call-present` lint (`code_rules.go:111-175`) is **fully dormant** (both `x-authz-area` ops also carry `x-authz-skip-area`; `custom` branch unreachable; expects a codegen handler shape no module uses). Dead file `documents/api/security_context.go`. Stale `permissions.go:226` comment references the deleted `canManageMembershipTarget`; wiki `iam.md` still documents the deleted `isMembershipDirectoryAdmin`.
- **F7 — MED.** Three near-identical `loadDocumentArea*` helpers with **opposite empty-area semantics** (`fillin_authz.go:37` & `submit_service.go:317` return `""`=deny; `read_service.go:357` `COALESCE(...,'tenant')`=wide-open) — copy-paste fail-open reintroduction risk. The `"tenant"` sentinel (`authz.go:110`) is the load-bearing area-filter switch with no DB reservation that `area_code='tenant'` cannot be a real area.
- **F8 — MED.** ISO/SOC2 table-stakes missing (per industry research, not over-engineering): authz-decision audit logging incl. every `system_admin`/`BypassSystem` invocation; a standing deny-by-default test matrix; a periodic access-review process.

**User rulings (2026-06-04):** F2 → **merge** the 4 redundant caps into `document.edit`/`document.view` (registry 33→29; keep P8's security fix); F3 → **own gated phase, later**; F8 → split — deny-default matrix + bypass audit-logging are **backend, scope now**; the periodic access-review certification is a **product screen** for the taxonomy/membership/overview pipeline, **not** this backend program.

### Phase 9 — Decommission the legacy "docx v2" role dialect in the documents module (CRITICAL, F1)
**Origin investigation (2026-06-04) — the finding is bigger than first scoped.** `template_author`/`document_filler` are NOT stray strings or dead vocabulary; they are an entire **legacy "docx v2" RBAC dialect** predating ADR 0007/0021/0022: design specs (`docs/superpowers/specs/2026-04-18-docx-editor-platform-design.md`, `2026-04-20-templates-v2-wysiwyg-design.md`), runbooks (`docs/runbooks/docx-v2-w2-templates.md`, `docx-v2-w3-documents.md`), repository defense-in-depth (`repository.go:384` `ListDocumentsForUser restricts metadata leakage for document_filler`, `:1381`), the wiki documenting it as the *current* tier-1 model (`wiki/modules/documents.md:405`), and a full test suite that fabricates these roles (`handler_test.go`, `handler_comments_test.go`, `module_wrapper_test.go`, `tests/docx_v2/`, e2e `METALDOCS_E2E_USER_ROLES=admin,document_filler,reviewer,approver`).
**Production impact:** the real auth flow (`auth/.../middleware.go:82`) issues only roles sourced from `iam_user_roles` (`auth/.../repository.go:101`); the DB role vocabulary (`baseline:199,1781` CHECK) is `viewer/editor/reviewer/approver/author/signer/area_admin/qms_admin`. **`document_filler` and `template_author` exist in no seed, no CHECK, no issuance** — no real user can hold them. So every `document_filler`-gated documents route (list, comments, autosave, finalize, force-release) is **`system_admin`-only in production** (`roleAdmin="system_admin"` is the only real role among the three gate strings); the test suite masks this by injecting a role the system can never grant. This is both the F1 incoherence AND a latent functional defect.
**Bonus drift surfaced:** `reviewer` is in the DB CHECK + seeded but absent from the Go `iam/domain` Role consts; `system_admin` is in the Go consts but not in the `user_process_areas` CHECK. Role-vocabulary drift between DB and Go — fold into the per-route cap mapping ruling.
**Scope (redesign-grade — re-enter `metaldocs-backend-api`; this is NOT a surgical edit):**
- Map each `hasRole`/`hasAnyRole` gate in `documents/delivery/http/handler.go` (`:169,180,204,215,734,1155,1165`) to its correct tier-1 capability (+ tier-2 area where area-grade). The `document_filler` "ownership-scoped" semantics split per ADR Decision 3: the **capability** answers "may this role act," the retained **business invariant** (`IsDocumentOwner`/`isSelf`) answers "on their own doc." The `parseListOptions(isAdmin)` self-vs-all scope (`:181`) resolves through the capability/area model like Phase 4's directory scope, never a role string.
- Read the two docx-v2 design specs + runbooks for the legacy RBAC matrix; **STOP and surface a product ruling** for any route whose canonical-cap mapping is genuinely ambiguous (do not guess; do not invent new caps). If the correct mapping implies a cross-module auth-model change beyond wiring existing caps, that is the CLAUDE.md hard-stop — report the boundary + minimum prerequisite plan, do not patch.
- Delete the `roleAdmin`/`roleTemplateAuthor`/`roleDocumentFiller` consts + `hasRole`/`hasAnyRole` helpers once all sites migrate (no shims). Rewrite the legacy-role test suite to issue canonical roles asserting the capability behavior. Reconcile/remove `ListDocumentsForUser`'s document_filler-specific metadata-leakage path if the cap model subsumes it.
- **Core control:** extend the SSOT lint with a rule banning role-string-literal comparison in `**/delivery/http/**` (the structural blind spot that let F1 survive 8 phases). Proven bite (the documents handler sites) → green.
- Gates: lint bite→green; `go build/test`; api-lint vs 455 baseline; redocly (no shape change); security + code review.

#### Phase 9 — ✅ COMPLETE 2026-06-04 (branch `feat/authz-coherence-p9-documents-handler-caps`, off `qa/iam-area-membership`)

**Verified before acting:** tier-1 `permissions.go` already gates every documents route by capability (`GET /api/v1/documents` prefix→`CapDocumentView`; session/autosave/checkpoints/PATCH/archive→`CapDocumentEdit`; `/finalize`→`CapDocumentSignoff`; `/session/force-release`→`CapMembershipManage`). So the handler `hasRole`/`hasAnyRole` gates were a **redundant, incoherent second gate** keyed on phantom roles. Confirmed via own grep that `document_filler`/`template_author` appear in no `db/baseline` CHECK (`:1545` `iam_user_roles`=`system_admin/approver/author/editor/viewer`; `:1781` `user_process_areas`), no `db/reference-data` seed, no auth issuance → the documents module was **`system_admin`-only in production** (every other real role 403'd at the handler despite holding the tier-1 cap). docx-v2 RBAC matrix (specs + runbooks) read: a clean **2-actor** model (`admin`=all, `document_filler`=ownership-scoped) with no canonical-8-role equivalent.

**Per-route gate mapping (all 7 sites):**

| Site | Route(s) | Legacy gate | Tier-1 capability (already enforced) | Retained invariant | Disposition |
|---|---|---|---|---|---|
| `:169` | `GET /documents` | `hasAnyRole(admin,filler)` | `CapDocumentView` | — | gate deleted |
| `:180` | `GET /documents` scope | `isAdmin=hasRole(admin)` | — | see-all-vs-own | re-keyed to `IsSystemAdmin` |
| `:204` | `GET /documents/stats` | `hasAnyRole(admin,filler)` | `CapDocumentView` | — | gate deleted |
| `:215` | `GET /documents/stats` scope | `isAdmin` | — | see-all-vs-own | re-keyed to `IsSystemAdmin` |
| `:734` | `.../session/force-release` | `hasRole(admin)` | `CapMembershipManage` | — | gate deleted (tier-1 owns) |
| `:1155` | ~all routes (`authorizeDocumentScope`) | `hasAnyRole(admin,filler)` | per-route view/edit | — | gate deleted |
| `:1165` | ~all routes (ownership bypass) | `hasRole(admin)` | — | `IsDocumentOwner` | bypass re-keyed to `IsSystemAdmin` |

**Product ruling taken (operator-approved, this session):** `system_admin` bypasses ownership / sees-all; every other real role is ownership-scoped to documents it created. The admin bypass is resolved through the capability layer — new `iamapp.CapabilityService.IsSystemAdmin` (extracted from the admin EXISTS already inside `CapsByUserID`, DRY), exposed on the documents `application.CapabilityChecker` port, wired into the handler via `Handler.WithCaps(deps.Caps)`; nil checker → fail-closed non-admin. **No role-name string survives in delivery code.** Area-scoped document visibility (area_admin/qms_admin/approver see their area) was the rejected alternative — it needs a repo area-filter the list path lacks (cross-module change) → **deferred** (consistent with the ADR's deferral pattern; tracked as a future phase, NOT done here).

**Tier-1 coverage gaps closed (exposed by removing the handler gate):** added `permissions.go` rows — `POST .../duplicate`→`CapDocumentCreate`, `POST .../comments`→`CapDocumentEdit`, `DELETE .../comments/{id}`→`CapDocumentEdit` (PATCH comments already covered by the PATCH prefix rule). `duplicateDocument` gained an `x-authz-skip-area` annotation (document.create is tx-layer area-enforced via `CreateDocumentTx`, ADR Phase 2/7) to satisfy `TestAreaEnforcedOpsAnnotated` — enforcement metadata only, no OpenAPI shape change.

**Core control:** new SSOT lint rule `no-rolestring-in-delivery` (`scripts/api-lint/registry_rules.go`) bans role-name string literals in `**/delivery/http/**` non-test code in a `const` value, an `==`/`!=` comparison, or a `switch`/`case` clause (parses canonical `Role` consts from `model.go` ∪ `{template_author, document_filler, reviewer}`). **Proven bite → green:** 3 hits on the pre-fix handler const block → 0 after migration; unit-tested (bite-const/comparison/switch, green, ignores test-files, ignores role-as-data, ignores non-delivery).

**Gate evidence:**
- `no-rolestring-in-delivery`: 3 → 0 (bite→green).
- `go build ./...`: clean.
- `go test ./internal/modules/documents/... ./internal/modules/iam/... ./apps/api/... ./scripts/api-lint/... -count=1`: green except the **pre-existing** `TestListDocumentsPaginated_{StatusFilter,PageOffset}` (repository pkg, `Scan 14 vs 18` column mismatch) — proven identical with the whole worktree stashed; the only repository change here is a comment.
- `go run ./scripts/api-lint api/openapi/v1/openapi.yaml .`: **455** (unchanged vs baseline); new rule clean-state-zero.
- `npx @redocly/cli lint`: valid; no shape change.
- Code review (go-reviewer) + security review: no CRITICAL/HIGH. Findings fixed by family — lint `reviewer`-misclassification comment corrected + split; lint hardened to catch `switch`/`case` role gates (+ test); `documents.New` gained a fail-loud `deps.Caps == nil` panic (production-only constructor); `TestDocumentStats_OK` admin-scope assertion added. Accepted-by-design: `forceReleaseSession` now single-layer (tier-1 only) per Decision 3. Deferred (LOW, false-positive risk): lint detection of role names passed through string-typed indirection.

**Production-defect-fixed proof:** handler unit tests now issue **canonical** roles (`editor`) and pass (`TestListDocuments_NonAdminCanonicalRole_Allowed` — was a 403 pre-Phase-9); `TestListDocuments_AdminSeesAllVsOwnScope`/`TestDocumentStats_OK` assert admin→all-scope (`userID==""`), non-admin→own-scope (`==caller`); `permissions_test.go` `TestDocumentsRoutesCapabilityMapping` asserts each route resolves to the cap its real holders carry. `ListDocumentsForUser` retained as the own-scoped path (comments reconciled to drop the phantom role; logic unchanged).

**Bounded defers / flags (NOT fixed here):** Phase 10 (F2 registry minimization — prefer the canonical `CapDocumentEdit`/`CapDocumentView` merge targets), Phase 11 (F4 tier divergences incl. force-release model + the 8 approval-route op specs; F5 tripwire baseline-to-zero; F6 dead scaffolding/stale comments; F7 `loadDocumentArea*` consolidation), Phase 12 (F3 `doc.*`→`document.*` rename) remain open. `reviewer`/`system_admin` DB↔Go role-vocabulary drift flagged for Phase 12 reconciliation. Phase 6 wiki sync deliberately NOT run (sequenced after Phase 12).

### Phase 10 — Registry minimization (HIGH, F2)
- Merge `doc.edit_draft`, `doc.reconstruct`, `workflow.instance.cancel` → `CapDocumentEdit`; `doc.view_published` → `CapDocumentView`. Reroute the tier-2 service call sites; remove the 4 consts + scope-map + catalog entries; reverse-migration the seed (registry 33→29; area-grade 14→11; bump goldens). This **reverses P8's registry over-modeling, not its security fix** — the surfaces stay enforced, now by the canonical cap.
- Gates: as Phase 9 + migration applies + idempotent on fresh bootstrap.

#### Phase 10 — ✅ COMPLETE 2026-06-04 (branch `feat/authz-coherence-p10-registry-merge`, off `qa/iam-area-membership`)

**STOP-on-ambiguous gate cleared first.** Verified each phantom's seeded role set is IDENTICAL to its canonical sibling before deleting (a divergence would have changed effective authz). From the curated baseline `db/reference-data/0001`: `document.edit` holders = {approver, area_admin, author, editor, qms_admin, system_admin}; `doc.edit_draft`/`doc.reconstruct`/`workflow.instance.cancel` each held by {approver, area_admin, author, editor, qms_admin} + system_admin via tier-2 bypass = same effective set. `document.view` holders = {approver, area_admin, author, editor, qms_admin, signer, viewer, system_admin}; `doc.view_published` = the 7 seeded + system_admin bypass = same. No divergence → merge is authz-neutral.

**The 4 reroutes (tier-2 `authz.Require`, area arg preserved exactly):**

| Site | File:line | Was | Now | Area arg | Grade |
|---|---|---|---|---|---|
| edit-draft (fill-in) | `documents/application/fillin_authz.go:36` | `CapDocumentEditDraft` | `CapDocumentEdit` | `areaCode` (DB-resolved, fail-closed "") | area |
| reconstruct | `documents/application/reconstruct_service.go:44` | `CapDocumentReconstruct` | `CapDocumentEdit` | `areaCode` (DB-resolved) | area |
| instance cancel | `documents/approval/application/cancel_service.go:117` | `CapWorkflowInstanceCancel` | `CapDocumentEdit` | `areaCode.String` (`FOR UPDATE` snapshot, fail-closed "") | area |
| view-published | `documents/application/view_service.go:75` | `CapDocumentViewPublished` | `CapDocumentView` | `"tenant"` (intentional) | tenant |

All 3 area-grade sites already passed the resource's real area (P7/P8 made them fail-closed) — the `authz-area-scope-binding` lint stays green (`CapDocumentEdit` is `ScopeArea`; no `"tenant"` literal). `view_service` passes `"tenant"` for the tenant-grade `CapDocumentView` (lint skips tenant-grade) — correct.

**Registry deltas:** removed `CapDocumentEditDraft`/`CapDocumentReconstruct`/`CapDocumentViewPublished`/`CapWorkflowInstanceCancel` from `model.go` (consts + `validCapabilities`), `capability_scope.go` (3 area + 1 tenant entry), `catalog.go` (4 descriptions). Registry **33→29**; area-grade **14→11**. Goldens bumped: `TestCapabilityRegistrySize` 33→29; `TestAreaGradeCapabilitySet` set 14→11 (auto-derives count from `wantArea`); `TestSeedRowCount_GoldenMatchesParser` **116→94**. Grep confirms **zero** remaining Go references (const or raw-string) to the 4 caps after rerouting.

**Reverse-seed:** new forward-only idempotent migration [`db/migrations/0227_authz_p10_merge_redundant_caps.sql`](../../db/migrations/0227_authz_p10_merge_redundant_caps.sql) `DELETE … WHERE capability IN (the 4)` (deletes exactly the 22 rows migration 0226 added; writes `schema_migrations`). Mirrored by removing the same 22 rows from the curated baseline `db/reference-data/0001`. Golden row count **derived from the parser, not hardcoded** — fresh bootstrap confirms 94. `permissions.go` untouched (tier-1 already routed these surfaces under document.view/document.edit; nothing to reroute).

**Test fixtures updated to canonical cap:** the 6 `ErrCapDenied{Capability: …}` handler fixtures (`view_handler_test.go`, `rbac_test.go` ×2, `reconstruct_handler_test.go`, `fillin_handler_test.go`, `cancel_handler_test.go`) now carry `document.view`/`document.edit`. The obsolete integration test `tests/integration/migrations/migration_0154_test.go` (asserted the phantom `doc.edit_draft` seed Phase 10 reverses) rewritten to assert the Phase 10 end state (phantom gone, canonical `document.edit` seeded to the same roles) — passes against the live bootstrapped DB. Stale `doc.edit_draft` comment in `fillin_service.go:42` corrected (review finding).

**Gate evidence (all green):**
- `go build ./...`: clean.
- `go test ./internal/modules/documents/... ./internal/modules/controlleddocuments/... ./internal/modules/iam/... ./apps/api/... ./scripts/api-lint/... -count=1`: green except the **pre-existing** `TestListDocumentsPaginated_{StatusFilter,PageOffset}` (`repository` pkg, `Scan 14 vs 18`) — proven identical with the whole worktree stashed (base failure, unrelated).
- `go run ./scripts/api-lint api/openapi/v1/openapi.yaml .`: **455** (unchanged vs baseline) — removed caps dropped their `seed-registry-parity` + `authz-area-scope-binding` obligations cleanly, parity still holds both directions.
- `npx @redocly/cli lint`: valid; **no OpenAPI shape change** (spec untouched).
- Migration `0227` on fresh curated bootstrap (`dev-bootstrap-baseline.ps1 -WithDevSeed`): applies (0226 `INSERT 0 22` → 0227 `DELETE 22`), idempotent (re-run `DELETE 0`, count stays **94**); post-merge holders verified `document.edit`=6, `document.view`=8, the 4 merged caps=0 rows.
- Code review (go-reviewer) + security review: **no CRITICAL/HIGH**, both APPROVE. Both confirmed no surface lost enforcement, area arg preserved, effective authz unchanged. The two findings (stale `fillin_service.go` comment; obsolete `migration_0154_test.go`) fixed in this diff.

**Bounded defers / flags (NOT fixed here):** Phase 11 (F4 tier-1/tier-2 divergences incl. force-release + the 8 approval-route op specs; F5 tripwire baseline-to-zero + stale-worktree SkipDir; F6 dead scaffolding/stale comments incl. `authz-call-present` dormancy; F7 `loadDocumentArea*` helper consolidation + `area_code='tenant'` reservation; F8 backend deny-default matrix + bypass audit logging), Phase 12 (F3 `doc.*`→`document.*` rename + `reviewer`/`system_admin` DB↔Go role-vocabulary drift). Phase 6 wiki sync deliberately NOT run (sequenced after Phase 12).

### Phase 11 — Coherence cleanup (MED, F4·F5·F6·F7 + F8 backend)
- F4: align the two tier-1/tier-2 divergences (pick one model per route, document it) + declare the 8 approval-route ops' authz/pagination in the spec.
- F5: make the tripwire walker `SkipDir` `.git/.claude/node_modules/vendor`; snapshot the remaining tripwire baseline to an allow-list so the live count is **0** and any new violation is a hard red.
- F6: delete dead file/scaffolding (or wire `authz-call-present` if activation is intended); fix stale comments; dispatch the wiki drift to Phase 6.
- F7: consolidate the three `loadDocumentArea*` helpers into one `("", found bool)` form; add a DB CHECK/seed guard reserving `area_code='tenant'`.
- F8 (backend): a standing deny-by-default test matrix (every cap denies an unprivileged actor); emit an audit event on every `system_admin`/`BypassSystem` bypass into the existing `audit.read` surface.

#### Phase 11 — ✅ COMPLETE 2026-06-04 (branch `feat/authz-coherence-p11-coherence-cleanup`, off `qa/iam-area-membership`; gated on Phase 10 PR #67 merged — confirmed)

Largest cleanup phase; sub-sequenced into independently-gated commits (F5, F4, F6, F7, F8 + a folded review-fix commit). Two STOP-on-ambiguous decisions were surfaced to the operator before acting.

**F4 — tier-1/tier-2 capability divergence (2 sites) + approval-route spec.**
- `forceReleaseDocumentSession`: **product ruling taken — option (a)** (operator-approved this session). Tier-2 `ForceReleaseSession` (`documents/repository/repository.go`) changed `CapDocumentEdit` → **`CapMembershipManage`** (area-grade, real `docArea`, fail-closed `""`). Both tiers now answer one question — "is the actor an admin for this document's area?" — and the destructive session takeover is **narrowed** to area admins (area_admin in-area + system_admin), not every editor. Rejected (b) RequireAll (redundant — membership.manage holders already hold document.edit) and (c) keep-divergence (leaves the incoherence).
- Approval-route CRUD: added four explicit `/api/v1/approval/routes` tier-1 rows → `CapRouteManage` in `permissions.go`, ordered **before** the generic `/approval/` block (incl. the GET list, whose tier-2 `List` check is also `route.manage`), so tier-1 matches the tier-2 `authz.Require(CapRouteManage)`. Locked by `permissions_test.go` rows.
- Spec (`openapi.yaml`, metadata only, **no shape change**): `security: sessionCookie` on the 4 ops (clears 4 AUTHZ-DRIFT); `listApprovalRoutes` is a bounded per-tenant config set with no runtime cursor/limit, so it is marked `x-pagination-exempt` (+reason) and `checkPagination` was taught to honor a documented exemption (clears 4 PAGINATION-DRIFT) — declaring real pagination would be spec-fiction. The **8 actionable approval-route lint hits are gone.**

**F5 — honest api-lint baseline (PREMISE CORRECTED).** Ground-truth audit found the long-reported "455" is **NOT** ~447 dormant tripwire-noise as the audit amendment stated. Real composition: **164 ENVELOPE-DRIFT + 162 AUTHZ-DRIFT + 79 PAGINATION-DRIFT (= 405 spec-contract drift across the whole API) + 50 tripwire** (26 phantom `.claude`-worktree + 1 `testdata` fixture + **23 real**). The `tripwire-pairing` (and `indexModuleFuncs`) walkers now `SkipDir` `.git/.claude/node_modules/vendor/testdata` (matching the registry walkers), removing the 27 phantoms. The 23 real false positives (mutating repository methods whose tier-2 authz is enforced one layer up / by a DB trigger / by a documented bypass) are frozen in [`scripts/api-lint/tripwire-allowlist.txt`](../../scripts/api-lint/tripwire-allowlist.txt) so the **live tripwire count is 0** and any NEW unpaired mutating repository method is a hard red; a stale allow-list entry (matching no live violation) is itself reported (`tripwire-allowlist-stale`) so the baseline cannot rot. **Operator ruling — tripwire-only allow-list**: the 405 spec-contract drift is left **visible and counted** (not baselined) for a dedicated spec-hygiene phase, rather than silently blessing 405 real gaps. **New honest lint total: 455 → 397** (ENVELOPE 164, AUTHZ 162→158, PAGINATION 79→75, tripwire 50→**0 live**). No Go test pinned "455" (the api-lint tests use fixtures, not the live count); the only "455" references are historical `.claude/PRPs/plans/*.md` scratch, out of scope.

**F6 — dead scaffolding + stale refs.** Deleted `internal/modules/documents/api/security_context.go` (single unused `sessionCookieContextKey` type, zero functional refs verified; package keeps `api.gen.go`+`gen.go`). Fixed the stale `permissions.go` comment referencing the Phase-3-deleted `canManageMembershipTarget` (directory scope is now data-layer `AreaMembershipService.DirectoryScope`). `authz-call-present` dormancy: **decision = document, not wire** (reported) — explicit godoc note explaining the rule expects a codegen `req.Body`/`req.Params` handler shape no module uses (tier-2 area is DB-derived; every area op carries `x-authz-skip-area`), so its live count is 0; real activation needs a call-graph/`source:derived` lint rewrite (Phase 5+ residual, redesign-grade, out of MED scope). Kept as the documented seam + `x-authz-custom` escape hatch. Wiki `iam.md` deleted-fn drift left for Phase 6 per plan.

**F7 — DRY footguns.** Consolidated the three near-identical `loadDocumentArea*` helpers (which disagreed on empty-area semantics — two `""`=deny, one `COALESCE(...,'tenant')`=wide-open, a copy-paste fail-open footgun) into one shared `docapp.LoadDocumentAreaCode(ctx,tx,tenantID,documentID) (areaCode string, found bool, err error)` in [`documents/application/document_area.go`](../../internal/modules/documents/application/document_area.go), baking in **no** default; each call site applies its own documented coalesce (area-grade `edit/submit/signoff/publish/supersede/reconstruct` → pass as-is, `""` fail-closes; tenant-grade `document.view` → `COALESCE("","tenant")`). The instance-keyed `loadInstanceAreaCode` was reshaped to the same `(string,bool,error)` form with its hidden `,'tenant'` default moved to the call site. DB guard: **reserve `area_code='tenant'`** so a real area can never shadow the tier-2 sentinel — migration [`0228`](../../db/migrations/0228_authz_p11_reserve_tenant_area_code.sql) (forward-only, idempotent `DROP IF EXISTS`+`ADD CHECK code <> 'tenant'`, writes `schema_migrations`; a pre-existing `tenant` area makes it fail loudly) + mirrored inline in the curated baseline (`CONSTRAINT area_code_not_tenant`).

**F8-backend.** (i) **Deny-by-default matrix** (two standing halves): `TestDenyByDefault_EveryCapabilityDeniesUnprivilegedActor` (authz pkg) iterates every registry capability and asserts `authz.Require` denies a no-role/non-admin actor (locks the enforcement code); `TestDenyByDefault_ViewerHoldsNoAreaGradeCapability` (apps/api) parses the seed and asserts the least-privileged role `viewer` holds no area-grade cap (locks the grant data). (ii) **Bypass audit logging**: new `authz.BypassAuditSink` interface (defined in the leaf authz package, adapted at the composition root → `AuditWriter.RecordTx`; authz never imports the audit module), set once via `SetBypassAuditSink` (stored `atomic.Pointer`, race-free; nil sink = no-op, zero SQL — sqlmock authz tests unaffected). Emits on **every `system_admin` tier-2 short-circuit** (`Require`, full fidelity, in-tx + **fail-closed**: a bypass cannot commit unaudited; cap-cache dedups to once per (actor,tenant,cap,area)/tx) **and every `BypassSystem` background-bridge invocation** (GUC identity, or `system`/`""` for cross-tenant sweeps — `audit_events.tenant_id` is `text DEFAULT ''`). Events use `action authz.bypass.<kind>`, `resource_type authz_bypass` → queryable in the existing `audit.read` surface. No new audit store. The access-review **screen** is explicitly OUT (product pipeline).

**Review:** go-reviewer + security-reviewer run on the full diff — **no CRITICAL, no HIGH.** Confirmed clean: F4 narrows (not broadens) force-release, both tiers agree, area arg real/fail-closed; approval-route prefix ordering can't be bypassed; F7 no area-grade site silently passes `"tenant"`, the `tenant` reservation holds; F8 fail-closed on system_admin audit, no PII/secrets in payloads, parameterized queries. MED/LOW folded by family: bypass sink → `atomic.Pointer` (race-free); `loadInstanceAreaCode` error wrapped; `LoadDocumentAreaCode` CD join tenant-scoped (defense-in-depth).

**Gate evidence (all green except environment-blocked DB live-gate):**
- `go build ./...`: clean.
- `go test` (authz, documents/application, documents/approval, apps/api, scripts/api-lint) + full `go test ./...` regression: green. The only failures across the tree are the **pre-existing** `TestListDocumentsPaginated_{StatusFilter,PageOffset}` (`documents/repository`, `Scan 14 vs 18`) — confirmed identical on base in Phases 9/10, untouched here.
- `go run ./scripts/api-lint api/openapi/v1/openapi.yaml .`: **397** (was 455) — 8 approval-route hits gone, tripwire 0 live / 0 stale, 27 phantoms skipped; the 405 spec-drift intentionally retained.
- `npx @redocly/cli lint`: valid (no shape change).
- `go vet` (touched pkgs): clean.
- **DB live-gate (`dev-bootstrap-baseline.ps1 -WithDevSeed`): ✅ PASS** (re-run once Docker was available). Fresh curated bootstrap applied migration 0228 (`ALTER TABLE` ×2 = DROP IF EXISTS + ADD, `INSERT 0 1` ledger) after the baseline marker; `pg_get_constraintdef` confirms `CHECK ((code <> 'tenant'::text))` on `document_process_areas`; an `INSERT … VALUES ('tenant', …)` is rejected (`violates check constraint "area_code_not_tenant"`) even with the `enforce_capability_asserted` trigger satisfied; re-applying 0228 is idempotent (`ALTER`/`ALTER`/`INSERT 0 0` via `ON CONFLICT DO NOTHING`).

**Bounded defers / flags (NOT done here):** Phase 12 (F3 `doc.*`→`document.*` rename + `reviewer`/`system_admin` DB↔Go role-vocabulary drift) — flagged, not fixed. Phase 6 wiki sync deliberately NOT run (sequenced after Phase 12; `iam.md` deleted-fn drift noted). The 405 ENVELOPE/AUTHZ/PAGINATION spec-contract drift is a dedicated spec-hygiene effort, not this program.

### Phase 12 — Capability naming consolidation (MED, F3)
- Rename migration `doc.*` → `document.*` (registry consts + values + DB seed + `wiki-capability-parity` markers + Admin Center grouping). Own gated phase; reconcile `controlled_documents.*` prefix symmetry. String-value migration — verify no external consumer pins the old strings.

#### Phase 12 — ✅ COMPLETE 2026-06-04 (branch `feat/authz-coherence-p12-naming-migration`, off `qa/iam-area-membership`; gated on Phase 11 PR #68 merged — confirmed; LAST code phase)

**Step-1 central-risk finding (audit-event vs capability namespace).** Before any rename, traced whether the documents module derives audit event-types from the capability string. **Decoupled — confirmed BACKEND-ONLY:**
- Documents lifecycle emits governance events through an independent `EventType` enum (`internal/modules/documents/approval/application/events.go:10-18`: `EventTypeDocumentPublished = "document_published"`, `"publish_scheduled"`, `"signoff.rejected"`). `publish_service.go:140` emits `EventType: EventTypeDocumentPublished`, never `string(CapDocumentPublish)`.
- Prior-phase proof: `CapDocumentSubmit`/`CapDocumentSignoff` were already renamed to `document.*` in earlier phases, yet the FE audit-event presenter still lists audit actions `doc.submit`/`doc.signoff` (`audit-event-presenter.ts:59,61`) distinct from `document.submit`/`document.signoff` — audit vocabulary did NOT follow the cap rename, proving independence.
- No Go audit-emit site uses `doc.publish`/`obsolete`/`supersede` as an action literal; every non-test occurrence was the `model.go` const, and the rest were test assertions on `ErrCapDenied.Capability` (the authz denial carrying the cap — renamed this phase). Bypass audit (`bypass_audit.go`) keys its event on `BypassKind` (`system_admin`/`background`); `Capability` is a payload data field, not an event-type.
- **Decision:** the FE presenter `doc.publish`/`doc.supersede` keys and `sql/contracts/state_transitions.yaml` `doc.published`/`doc.obsoleted`/`doc.superseded` are a separate audit/event-type vocabulary that merely shares the `doc.*` spelling. **Left untouched.** No historical-audit-row split, no FE break.

**Renames (string VALUES only; const names already `Document`):** `doc.publish`→`document.publish`, `doc.obsolete`→`document.obsolete`, `doc.supersede`→`document.supersede` at `model.go:58-60`. All const-keyed maps (`validCapabilities`, `capability_scope.go`, `catalog.go` descriptions) and every Go call site (`string(iamdomain.CapDocument…)` — Phase 8 closed the raw-string dialect) auto-propagate; `catalog.go capabilityCategory()` first-`.`-split now groups all three under the single `"document"` Admin Center group (previously a stray `"doc"` group of 3). Test string-literal assertions updated (service `ErrCapDenied` asserts, authz/handler tests, api-lint raw-string fixture).

**DB:** forward-only idempotent migration `db/migrations/0229_authz_p12_rename_document_lifecycle_caps.sql` + curated mirror in `db/reference-data/0001`. Collision-safe by construction (`INSERT…SELECT…ON CONFLICT DO NOTHING` + `DELETE`, not a bare `UPDATE`): on fresh bootstrap the curated reference-data seeds `document.*` while replayed historical migration 0225 still seeds `doc.*`, so both spellings transiently coexist — a bare `UPDATE` would violate the `(role,capability)` PK. Live-bootstrap gate PASS (`dev-bootstrap-baseline.ps1 -WithDevSeed`): 0229 ran `INSERT 0 0 / DELETE 2` ×3 (converged the doc.* duplicates), final DB holds `document.publish`/`obsolete`/`supersede` for area_admin+qms_admin, **zero** `doc.*` remnants; re-apply = `INSERT 0 0 / DELETE 0` (idempotent).

**Consumer scan:** no goldens/snapshots pin the strings; no cap strings in OpenAPI schema enums → no generated code / FE types affected → zero FE files touched (FE build/tsc/e2e gate N/A). `wiki-capability-parity` `cap:` markers: none exist for these 3 caps → no-op (the rule only binds `` `cap:NAME` `` markers; the 3 caps were never marked). OpenAPI `AccessPolicyItem` ACL enum: separate vocabulary (Phase 5 finding) — untouched. OpenAPI `x-authz-skip-reason` prose cap references updated (backend-api contract boundary).

**`controlled_documents.*` prefix symmetry (RECOMMENDATION, not done here):** the `controlled_documents.create`/`obsolete`/`supersede` caps remain a second document-family prefix. They model a distinct aggregate (the controlled-document registry) with its own area-grade enforcement, so the split is defensible, not accidental. Recommend deferring any `controlled_documents.*`→`document.*` normalization to a future ADR only if product confirms the two aggregates should share one capability prefix; renaming now would conflate two enforcement surfaces without a coherence win. Flagged, not actioned.

**Gates:** `go build ./...` clean; targeted `go test ./internal/modules/iam/... ./internal/modules/documents/... ./apps/api/... ./scripts/api-lint/...` green (`TestListDocumentsPaginated_*` pre-existing base failure — `expected 14 destination arguments in Scan, not 18` — confirmed byte-identical with diff stashed, unrelated to caps); `go run ./scripts/api-lint` output byte-identical to post-Phase-11 baseline (397, `seed-registry-parity`+`wiki-capability-parity` green both directions); `@redocly/cli lint` valid, no shape change; migration 0229 applies + idempotent on fresh bootstrap.

**Bounded defers / flags:** wiki module-doc prose using the legacy `doc.*` shorthand (`wiki/modules/approval.md` route table — already shows `doc.submit`/`doc.signoff` shorthand for the `document.*` caps, plus bogus `doc.cancel`; `wiki/modules/approval/_artifacts/04-persistence.md`; `wiki/references/local-dev-credentials.md` explicitly-illustrative legacy table) needs a holistic refresh, NOT token-patching — handed to **Phase 6 wiki sync (`wiki-curator`), now unblocked and running next**. The `reviewer`/`system_admin` DB↔Go role-vocabulary drift noted at `scripts/api-lint/registry_rules.go:114` is a separate role-reconciliation concern, out of F3 scope.

**Phase 6 (wiki sync) is now UNBLOCKED — runs next** (Phase 12 was the last code phase; the documents handler now speaks the coherent `document.*` vocabulary the wiki will document). **Access-review certification** is tracked OUT of this ADR, in the screen/product pipeline.

### Role-vocabulary coherence — `reviewer` decommission + approval `required_role` binding (2026-06-04)

Closes the `reviewer`/`system_admin` role drift flagged at the Phase 9 audit (line 254) and the deeper root cause it sat on. Branch `feat/iam-authz-coherence-rootcause-fixes`, off `qa/iam-area-membership`.

**Finding (full cross-layer role map).** The role MODEL is already industry-standard and the apparent "drifts" are deliberate: `system_admin` is excluded from `user_process_areas` by design (tenant-wide tier-1, tier-2 bypass — K8s/GCP inheritance), and `signer`/`area_admin`/`qms_admin` are excluded from `iam_user_roles` by design (area-only partition). The ONE genuine wart was **`reviewer`** — present only in the UPA role CHECK + the grant/revoke stored-proc validation lists, but absent from the Go registry, `validRoles`, the `role_capabilities` seed, OpenAPI, and any issuance path. It granted **zero capabilities**; the dev seed even mis-assigned `approver-test` the `reviewer` role, so that user silently could not approve.

**Root cause (bigger than the symptom).** Approval stage `required_role` was **free text** (only `[a-z0-9_-]+`, ≤64) and joined directly against `user_process_areas.role` at runtime to resolve eligible approvers (`submit_service.go resolveEligibleActors`). Unbound to the role registry, it let an admin configure a stage requiring a role no user can hold → a silently **unsatisfiable stage** (empty eligible pool). `reviewer` was the visible instance of that class — the same "role strings not bound to the registry" defect ADR 0022 set out to kill, surviving in the approval module.

**Fix (operator rulings: decommission everywhere + bind now).**
- **Decommission `reviewer`:** migration [`0230`](../../db/migrations/0230_authz_decommission_reviewer_role.sql) removes any `reviewer` memberships (behaviour-neutral — they granted nothing), tightens `user_process_areas_role_check` to the 7 canonical area roles, and **drops the duplicated `_role NOT IN (...)` validation** from `grant/revoke_area_membership` (the table CHECK is now the single source of truth — that hardcoded list was also stale, rejecting `author`/`signer`/`area_admin`/`qms_admin`). Mirrored in the curated baseline; dev seed `reviewer`→`approver`.
- **Bind `required_role` to the registry:** new Go SSOT `iamdomain.IsAreaRole`/`AreaRoles()` (the 7 area roles = canonical roles minus `system_admin`, mirroring the UPA CHECK). Approval route create/update now rejects a `required_role` that is not a canonical area role (`contracts/route.go validateAreaRole`). A phantom/typo'd stage role is now a 4xx at config time, not a silent dead stage.
- `reviewer` reclassified a TRUE phantom in `no-rolestring-in-delivery` (still banned so it cannot reappear in a gate). DB dictionary `wiki/database/tables/user_process_areas.md` updated to the 7-role CHECK.

**Gates:** `go build ./...` clean; `go test ./internal/modules/iam/... ./internal/modules/documents/approval/... ./db/migrations/... ./scripts/api-lint/... ./apps/api/...` green (approval route fixtures updated `reviewer`→`approver`, proving the binding bites); DB live-bootstrap gate PASS **both** with dev seed and product-schema-only (`0230` applies: `ALTER`×2 + `CREATE FUNCTION`×2, idempotent).

**Flagged, not fixed (out of scope):** FE stale `UserRole` literal `reviewer` in `lib/types/index.ts` (ADR 0020/0021 deferral, frontend boundary); `e2e_seed.go mapRoleToIAM` emits `reviewer`/`admin` — pre-existing latent test-seed bug (those values were never valid for `iam_user_roles`, independent of this change). Approval-stage `required_capability` is still free-text (separate binding, lower risk — capabilities are typed elsewhere).

### Phase 13 — Revive the CI coherence net (CRITICAL infra)

The 2026-06-04 final review found that Principle 5 ("the model is bound by CI, not by discipline") was **aspirational, not enforced**: the entire `scripts/api-lint` binding suite did not actually gate any PR. Two compounding bugs.

**Bug #1 — wrong root → silent no-op for the file-rooted guards.** Every binding helper resolves its core files off the repo root: `internal/modules/iam/domain/model.go` (`parseCapabilityConsts`/`parseRoleConsts`), `db/reference-data/0001_product_reference_data.sql` (`checkSeedRegistryParity`), `wiki/...` authz docs (`checkWikiCapabilityParity`), and `scripts/api-lint/tripwire-allowlist.txt` (`loadTripwireAllowlist`). But CI (`.github/workflows/api-contract.yml`) invoked `go run ./scripts/api-lint/ … ./internal/modules`, so every join double-nested (`internal/modules/internal/modules/…`, `internal/modules/db/…`) and missed. Each helper then hit its `os.IsNotExist → return empty set, nil` branch and passed trivially — so `no-rolestring-in-delivery`, `authz-area-scope-binding`, `seed-registry-parity`, and `wiki-capability-parity` all loaded **nothing** under the canonical CI command. (Ground truth: the walk-rooted guards over-reported instead — `./internal/modules` exists, so the tripwire walker ran but its allow-list path missed → 23 legit false-positives un-suppressed → 420 total, vs 397 from repo root. The file-rooted binding guards silently loaded nothing either way.)

**Bug #2 — non-blocking anyway.** The `api-design-system-lint` job was `continue-on-error: true`, so even a detected violation never reddened CI.

**Root contract chosen.** The binary's second arg IS the **repo root** (matches every helper's existing join). Fixed the CI invocation to pass `.`; `main.go` now normalizes it via `filepath.Abs`+`Clean` so a relative `.`, a trailing slash, or the legacy `./internal/modules` all resolve unambiguously. `main.go` was rewritten onto the `flag` package: `-strict` and `-enforce=all|blocking|reported`.

**Silent-swallow hardening (kills the masquerade).** Added `requireCoreFile(strict, what, path)`: under `-strict` a missing EXPECTED core file (model.go, seed sql, allow-list, wiki authz doc) is a **hard error**, not an empty pass. The genuine unit-test-fixture case is preserved by leaving `strict=false` the default — the testdata fixture roots (where these files are legitimately absent) keep skipping, while the production CI run passes `-strict` so a wrong root can never again look clean. Threaded `strict bool` through `RunCodeRules`→`RunRegistryRules` and the five readers; the pure AST walkers (`no-inline`, `no-rawstring`) read no core file and need no gate.

**End-to-end self-test (`scripts/api-lint/e2e_test.go`).** The per-rule unit tests called each helper directly and so never exercised the real composition or the path contract — exactly why they stayed green while CI passed a wrong root. The new test drives the **real entrypoint** (`RunCodeRules`, the function `main()` calls) against a tree with one **planted** violation of each AST-only guard (raw-string cap, role-string in delivery, inline `Capability("…")`, area-grade `Require(…,"tenant")`) and asserts each is caught and the clean tree returns the guards at **zero**; plus `TestEndToEnd_StrictWrongRootErrors` and a per-helper strict matrix proving the swallow is dead.

**Blocking / reported split.** A live run reports ~397 violations dominated by pre-existing spec-drift (`ENVELOPE-/AUTHZ-/PAGINATION-DRIFT`) deferred to a later spec-hygiene phase. `main.go` classifies every rule: the three spec-drift families are **reported-only** (`reportedOnlyRules`); EVERY other rule (binding guards, dialect bans, `authz-area-scope-binding`, `tripwire-pairing`/`-stale`) is **blocking** by omission (safe default for any future rule). The CI job now runs one gating step `-strict -enforce=blocking … .` (job no longer `continue-on-error`) plus a `continue-on-error: true` step `-enforce=reported … .` that surfaces and counts the backlog without blocking. Path triggers extended to `db/reference-data/**` and the three wiki authz docs so seed/wiki drift also fires the gate.

#### Phase 13 — ✅ COMPLETE 2026-06-04 (branch `feat/authz-coherence-p13-ci-net-revival`, off `qa/iam-area-membership`; Phases 9-12 merged — confirmed)

**Principle 5 is now actually enforced:** a real future dialect/drift regression in the binding suite turns CI red, proven at the binary level (planting a raw-string cap into the live tree → `1 blocking violation(s)`, exit 1; reverted → `0`, exit 0).

**Gate evidence:**
- `go build ./...`: clean. `go vet ./scripts/api-lint/...`: clean. `gofmt -l`: clean. (`-race` N/A on this Windows box — needs cgo; lint tooling has no concurrency.)
- `go test ./scripts/api-lint/... -count=1`: green, including the new e2e self-test (4 planted guards each caught via the real entrypoint, clean tree = 0, strict-wrong-root errors, per-helper strict matrix) and the unchanged fixture suite (`TestRules` + per-rule unit tests — testdata path preserved by `strict=false`).
- Run the EXACT post-fix CI way from a clean repo-root checkout:
  - **BLOCKING** `go run ./scripts/api-lint -strict -enforce=blocking api/openapi/v1/openapi.yaml .` → `0 blocking violation(s), 397 reported-only/deferred violation(s)`, **exit 0** (green on the current clean tree — gate safe to enable).
  - **REPORTED-ONLY** `go run ./scripts/api-lint -enforce=reported api/openapi/v1/openapi.yaml .` → 397 spec-drift (164 `ENVELOPE-DRIFT` + 158 `AUTHZ-DRIFT` + 75 `PAGINATION-DRIFT`), runs in the `continue-on-error` step.
  - Back-compat default `-enforce=all` from repo root still flags the full 397 (binding guards green).
- Code review (go-reviewer): **no CRITICAL/HIGH**. APPROVE — strict threading complete (no swallow branch missed), exit-code logic correct, classification correct, e2e genuinely exercises the real composition. One MEDIUM (`os.IsNotExist` vs `errors.Is`) is style-only and matches the existing codebase idiom — left unchanged (surgical).

**Bounded defers / flags (NOT done here — separate follow-ups):** F4 cross-tier test; `controlled_documents.{obsolete,supersede}` collapse; F7 repo-helper fold; stale `capability_scope.go:31-35` comment; pre-existing `TestListDocumentsPaginated_*` base failure (`Scan 14 vs 18`, unrelated). Phase 6 wiki sync still runs LAST.

## References
- [`wiki/references/authz-industry-evidence.md`](../references/authz-industry-evidence.md) — cited external evidence base (NIST, K8s, GCP, AWS, OWASP, CWE, Zanzibar, Cedar, OPA)
- Cross-module authz audit, 2026-06-03 (this session) — three-dialect map, tier-2 areaCode survey, drift inventory
- ADR [`0007-two-tier-authz.md`](0007-two-tier-authz.md) — two-tier model; `x-authz-area` + lint harness (codegen-rejection amendment)
- ADR [`0016-view-grade-capabilities.md`](0016-view-grade-capabilities.md) — view/manage split; §Security Boundary documented the (now-revised) handler gate
- ADR [`0021-tenant-vs-platform-admin-separation.md`](0021-tenant-vs-platform-admin-separation.md) — capabilities, not role names, are the boundary
- `wiki/concepts/authz-tiers.md` — tier-1/tier-2 reference
- `scripts/api-lint/code_rules.go` — `authz-call-present`, `tripwire-pairing`
