# Decisions

> **Last verified:** 2026-08-04 (ADR 0087 row added — livre governance class: configured no-approval route). Note: table row ORDER is not strictly numeric — ADRs 0077-0083 were appended directly after 0036 (a prior append point) rather than after 0076, so the visual numeric sequence breaks between 0036→0077 and again 0083→0037; all of 0077-0083 ARE present as rows (verified, no missing content). 0084/0085/0086 continued the same non-strict append-after-0083 ordering; 0087 is appended after 0086 in this pass (out of scope to renumber/reorder the whole table this pass — flagged, not fixed).
> **Scope:** Durable ADRs and consequential technical decisions.

| # | Title | Status | Superseded by | Current relevance |
|---|-------|--------|---------------|-------------------|
| [0001](0001-eigenpal-adoption.md) | Adopt eigenpal as the document editor | Accepted (amended 2026-06-23) | — | Governs editor library choice; `@eigenpal/docx-editor-react@1.9.0` from npm registry (vendored `@eigenpal/docx-js-editor@0.2.0` tarball retired 2026-06-23); T1–T8 spike verified; vendor-path drift resolved 2026-06-14 (HS-2 closed) |
| [0002](0002-zone-purge.md) | Purge editable zones | Accepted | — | Editable zones removed end-to-end; placeholders are the sole variability mechanism |
| [0003](0003-token-syntax-migration.md) | Token Syntax Migration | Historical (stub; superseded by ADR 0008's fixed 7-token catalog) | 0008 | Records the `{{uuid}}` → `{name}` migration intent; ADR 0008 replaced the open-ended model with the fixed catalog |
| [0007](0007-two-tier-authz.md) | Two-Tier Authorization | Accepted (amended 2026-05-11 — tripwire extended to all regulated modules) | — | Foundation of all authz; tier-1 `CanDo` (middleware) + tier-2 `authz.Require` (in-tx area); `authz-call-present` lint deleted (see ADR 0023) |
| [0008](0008-placeholder-fixed-catalog.md) | Replace user-fill placeholders with fixed catalog | Accepted | — | 7 computed tokens are the only placeholder type; no fill-in form panel; server-side substitution at freeze |
| [0009](0009-pdf-dispatch-outbox.md) | PDF Dispatch Transactional Outbox | Accepted | — | At-least-once PDF delivery via `pdf_dispatch_outbox`; `PDFOutboxWorker` polling; generic `StagingOutboxRepository` (Wave Z Z-10) |
| [0010](0010-soft-archive-via-timestamp.md) | Soft-archive documents via archived\_at timestamp | Accepted | — | Archive = `archived_at` timestamp, not status change; status field stays as lifecycle source of truth |
| [0011](0011-cd-atomic-create.md) | Atomic CD Create + Per-Area Numbering + Idempotency-Key Adoption | Accepted | — | CD + first revision created in one tx; `cd_sequence_counters` keyed on (tenant, profile, area); shared idempotency platform |
| [0012](0012-contract-first-api.md) | Contract-First API via oapi-codegen | Accepted | — | OpenAPI spec is the sole authoritative request/response definition; hand-written structs at HTTP boundary prohibited for migrated modules |
| [0013](0013-template-revision-labels.md) | Template Revision Labels (REV-style, Backend-Canonical) | Accepted (amended by 0052 — version-creation trigger only) | 0052 (partial — trigger only) | `revision_number` persisted on `templates_template_version`; `current_revision_number` + `latest_revision_number` on TemplateDTO; chip always shows `REV{nn}`; the counter/labeling mechanics are unchanged by 0052 |
| [0014](0014-rename-docgen-v2-to-docx-renderer.md) | Rename docgen-v2 service to docx-renderer | Accepted | — | Service renamed to `apps/docx-renderer/`; single responsibility = DOCX token rendering; `DOCX_RENDERER_*` env vars |
| [0015](0015-async-freeze-pin-materialize.md) | Async Freeze — Split into Pin (in-tx) and Materialize (outbox) | Accepted (amended 2026-06-13 — optional-tx-enlistment retired by Wave Z Z-5; tx is mandatory) | — | `Pin` writes freeze marker + outbox row in signoff tx; `Materialize` runs async in worker; approval availability independent of docx-renderer |
| [0016](0016-view-grade-capabilities.md) | View-Grade Capabilities for IAM, Membership, Taxonomy, Metrics | Accepted | — | Four View-grade caps (`metrics.view`, `membership.view`, `user.view`, `taxonomy.view`) added to registry; unblocked F-001 read/write split |
| [0017](0017-signoff-idempotency-fingerprint.md) | Signoff idempotency fingerprint = client-stable inputs only | Accepted | — | Replay fingerprint = `sha256(docID, decision, reason, contentHash)`; server-mutable state excluded; `ErrConflict` → 409 |
| [0018](0018-approval-route-lifecycle.md) | Approval Route Lifecycle | Accepted (§1 update-in-place claim superseded by 0074, 2026-07-07) | 0074 (§1 only) | Two-state terminal-on-deactivate machine; OCC via `If-Match`; deactivate blocked while instances in-flight; reason mandatory; `governance_events` audit trail. §3 Deactivate guard unaffected by 0074. |
| [0019](0019-cap-audit-read-and-session-manage.md) | `CapAuditRead` (Read-naming exception) and `CapSessionManage` | Accepted | — | `*Read` suffix for read-only-by-design resources; `CapSessionManage` for force-logout; grant matrix expanded beyond system_admin |
| [0020](0020-admin-center-six-tab-ia.md) | Admin Center 6-tab information architecture | Accepted — shipped at PR-12 | — | 6-tab IA with one route + one capability per tab; lazy loading; TanStack Query; god hook deleted; 7 tabs in practice (memberships added on `qa/iam-area-membership`) |
| [0021](0021-tenant-vs-platform-admin-separation.md) | Tenant admin vs. platform admin separation | Accepted — shipped at PR-12 | — | `/admin/*` = tenant operator surface only; future platform admin lives under `/platform/*`; FE pivots on capabilities not role names |
| [0022](0022-authz-capability-coherence.md) | Authz Capability Coherence (single source of truth + explicit scope + area-scoped administration) | Accepted (fully executed 2026-06-13) | — | Single typed capability registry; area-grade vs tenant-grade declared and CI-enforced; area_admin membership scoped to managed areas; all 13 phases + Wave Z Z-6 complete; full execution history in [`0022-execution-history.md`](0022-execution-history.md) |
| [0023](0023-authz-area-markers.md) | Honest authz-area markers (retire the negative escape hatch) | Accepted 2026-06-08 | — | Positive `x-authz-area: {source: tx/body/path}` markers replace negative `x-authz-skip-area`; `authz-call-present` lint deleted; `AUTHZ-DRIFT` validates shape |
| [0024](0024-openapi-single-base-path.md) | One OpenAPI base path (`servers.url: /api/v1`, relative path keys) | Accepted 2026-06-08 | — | Exactly one base-path declaration; `PATH-BASE-PREFIX` CI gate bans re-prefixed path keys; FE `baseUrl` idempotent guard |
| [0025](0025-error-envelope-rfc9457.md) | One error envelope: RFC 9457 Problem Details | Accepted 2026-06-08 | — | RFC 9457 `Problem` is the only error shape; `ApiErrorEnvelope` retired; `ENVELOPE-DRIFT` blocking; zero exemptions |
| [0026](0026-unified-authz-enforcement.md) | Unified authz is the only enforcement model (no dead ABAC path) | Accepted | — | `AccessPolicy`/`document_access_policies` ABAC slice dead and dropped (migration 0232); search visibility verbatim-ports the CD list predicate |
| [0027](0027-rls-adoption-sequencing.md) | RLS Adoption Sequencing + auth\_identities Tenant-Global by Design | Accepted (executed in full by Wave Z, 2026-06-13; amended 2026-07-03 M3, 2026-07-05 M7 F7.4) | — | `auth_identities` tenant-global by design (T-008 closed); RLS live on all 34 tenant-scoped tables (migration 0237 collapsed 3-tier plan; M3 async-seed chokepoint; M7 non-owner CI role + `approval_signoffs` FORCE RLS) |
| [0028](0028-audit-events-cursor-shape.md) | Audit Events Cursor Shape Drift | Accepted (closed 2026-06-08) | — | `/audit/events` runtime reconciled to nested `page.{next_cursor,has_more}` CursorPage shape (Phase F re-audit); FE dual-shape adapter removed |
| [0029](0029-user-display-name-reader-port.md) | `UserDisplayNameReader` — cross-module display-name reads go through an iam-owned port | Accepted 2026-06-15 | — | 3 cross-module `iam_users.display_name` reaches closed via iam/domain port (single+batch); reads live, off-tx (H-PRE-1); security tenant-scope JOIN bounded-deferred (M4/F4.1) |
| [0030](0030-template-version-state-port.md) | Template-version state reads go through the templates-owned `TemplateVersionPort` (no hardcoded status) | Accepted 2026-06-15 | — | Extend existing owned port with raw `GetTemplateVersionState`; delete CD `templates_*` reach + `status := "published"` hardcode; reads live, off-tx; `IsPublished` kept for taxonomy (M4/F4.2) |
| [0031](0031-tenant-user-reader-port.md) | `TenantUserReader` — cross-module tenant-membership reads go through an iam-owned port | Accepted 2026-06-15 | — | iam/domain port (tenant → member user_id set, all members/no `deactivated_at`); lets security scope `auth_identities` (no `tenant_id` col) via `= ANY(ids)` instead of the `iam_users` JOIN; reads live, off-tx; supersedes ADR 0029's deferred-membership note (M4/F4.5, consumer F4.6) |
| [0032](0032-drop-legacy-mddm-document-cluster.md) | Drop the dead legacy `metaldocs.documents` (MDDM) document cluster | Accepted 2026-06-15 | — | Forward-only destructive migration `0240` drops 10 verified-dead objects (anchor + 8 satellites + `metaldocs.template_audit_log`) that shadow live `public.*` under metaldocs-first `search_path`; baseline frozen (not re-mirrored); data-loss/rollback/maintenance-window caveats; fix-not-adapt root cause for M4 F4.1a Gate #5 (M4b/F4b.2) |
| [0033](0033-fix-placeholder-value-tenant-trigger.md) | Fix placeholder value tenant trigger | Accepted 2026-06-15 | — | Fixes placeholder-value resolution for tenant-scoped token substitution via DB trigger correction |
| [0034](0034-integration-test-fixture-framework.md) | Integration Test Fixture Framework (IntegreSQL template-DB-per-test) | Accepted 2026-06-15 | — | `tests/integration/testdb/` canonical harness; template-DB-per-test isolation; factory builders; R1–R4 discipline rules; CI guard `scripts/check-test-discipline.sh` in `module-boundaries` workflow (M4c F4c.1 + F4c.4) |
| [0035](0035-flat-typed-responses-and-presign-status.md) | Flat typed responses and presign status | Accepted | — | Flat typed response shape; presign status handling |
| [0036](0036-otelsql-db-tracing.md) | Driver-Level DB Tracing via `otelsql` | Accepted 2026-06-16 | — | `otelsql.Open` replaces `sql.Open` at `internal/platform/db/postgres/connect.go`; all `database/sql` queries auto-traced via OTel driver wrapper; composition-root injection, not per-repo wiring (M2/F2.3) |
| [0077](0077-approval-delegation.md) | Approval Delegation (Eligibility-Widening, Not a New Authz Path) | Accepted 2026-07-07 | — | Delegation widens the INPUT to `domain.CheckEligibility`/`domain.CheckSoD` (via `ResolveEligibleIdentity`), never a parallel authz branch; no new capability; real-time in-tx revocation (hard DELETE + fresh window query); dual identity persisted via new `on_behalf_of_user_id` column; DB tripwire (`enforce_approval_sod`) widened symmetrically (M2b F9) |
| [0078](0078-viewer-facts-contract.md) | Viewer-Facts Contract (Server-Derived Eligibility Truth for Clients) | Accepted 2026-07-08 | — | Server emits a `viewer` block (`is_author`, `eligible_for_active_stage`, `has_signed_active_stage`, nullable `via_delegation_from`) on `ApprovalInstanceByDocumentResponse`, composed exclusively on `ResolveEligibleIdentity` + `CheckSoD`; FE forbidden from deriving eligibility (kills the M2c 412 split-brain); display truth only, enforcement stays tier-2 `authz.Require`; amendment converges `requireInstanceVisible` onto the same primitive (eligible-to-act ⟹ able-to-see); worklist SQL parity deferred to M3 (M2d F2d.1) |
| [0079](0079-verdict-history-contract.md) | Verdict-History Contract + Immutable Actor-Name Snapshot (No Read Fallback, DB-Enforced) | Accepted 2026-07-09 | — | Required chronological `verdicts[]` on `ApprovalInstanceByDocumentResponse` (all stages, empty ⇒ `[]`); actor display name = value snapshotted at action time, immutable; `SET NOT NULL` + `CHECK (<> '')` on `actor_display_name_snapshot` for both `approval_review_verdicts` + `approval_signoffs`, unconditional write bind, all read fallbacks (coalesce + live re-resolution) removed — no-fallback root-cause fix grounded in `iam_users.display_name NOT NULL`; `on_behalf_of` name deferred (D3); HS-2 clearance overrides M2d "no migration" (M2d F2d.2) |
| [0080](0080-single-artifact-destination.md) | Single Artifact Destination (Cockpit Pattern Retired) | Accepted 2026-07-09 | — | One mode-adaptive screen (`DocumentWorkspacePage`) is the canonical `/documents/:id` destination for every viewer relationship; record surface moves to `/documents/:id/details`; `/documents/:id/edit` and `/approvals/:documentId` become param/query-preserving redirects (route flip only — `ApprovalCockpitPage`/`DocumentEditorRoutePage` files retired later by F2d.7); signature ceremony stays in-screen, reopen trigger = external-signer persona |
| [0081](0081-per-profile-signature-policy.md) | Per-Profile Signature Policy (Governance Class → Route Policy, DB-Enforced, Submit Relies on the Write-Boundary Invariant) | Accepted 2026-07-10 | — | `taxonomy` owns a `GovernanceClass` {controlado,simples,livre} on `document_profiles` + pure `RoutePolicy()` derivation; `documents/approval` consumes it via a NEW narrow published port `ProfilePolicyReader` (never sees the enum); `Route.Validate(policy)` enforced friendly-first at route-admin (policy resolved off-tx per H-PRE-1), authoritative-last by DEFERRABLE bidirectional constraint triggers on `approval_route_stages`+`approval_routes`+`document_profiles` (migration 0295); class writes go ONLY through `ProfileService.Reclassify` (generic `Update` never touches the column), conflict → `ErrClassChangeRouteConflict` → 409; **submit does structural-only `Validate("")` and relies on the write-boundary invariant** (recorded deviation forced by H-PRE-1 × ADR 0073; option-2 in-tx submit read permanently rejected) |
| [0082](0082-approval-kernel-extraction.md) | Approval kernel extraction to a first-class module (supersedes ADR 0072 ruling (a)) | Accepted 2026-07-12 | — | ROADMAP unit 3.1 / approval-remediation M3: ADR 0072's named promotion trigger fired — `templates` becomes a second bounded-context consumer of approval, not just `documents`. `internal/modules/documents/approval` → `internal/modules/approval` (15th top-level module) via pure `git mv` (165 files, 111 import paths, zero behavior change); kernel generalizes to `(subject_kind, subject_key)` keying (`document`\|`template`); boundary guard drops the nested-family special case, approval treated exactly like any other module (published `domain`/`application`/`api` surface only — stricter than the old nested model). ADR 0072 rulings (b) infrastructure-dir-per-module and (c) allow-list model remain in force; only ruling (a) is superseded. **Transitional coexistence (operator-ratified 2026-07-12, Option A):** the kernel is backend truth for template approval (2 new additive routes `submit-for-approval`/`signoff`) but the legacy role-based template-approval path (4 routes, `templates_approval_config`, `domain.ApprovalConfig`) is NOT deleted in M3 — `CreateTemplate`/`PublishTemplateVersion` are still role-based and the frontend has no kernel-route consumer yet; full retirement deferred to ROADMAP unit 3.1a |
| [0083](0083-subject-discriminated-capability-tripwire.md) | Subject-discriminated capability tripwire for shared kernel tables (extends ADR 0082) | Accepted 2026-07-12 | — | `approval_instances` is a shared `(subject_kind,subject_key)` table (0082) but the ADR 0022 capability tripwire hardcoded `document.submit` for all inserts; the flat `tripwire.Arm` `(table,op)→caps` match-one model can't express per-subject caps on a shared table and a union widen is a security regression (`template.submit` would authorize a document insert); **decision**: add an optional subject discriminator (`WhenColumn`/`WhenValue` on `subject_kind`) so `RenderMigration()` emits nested `CASE NEW.subject_kind` — document→`document.submit`, template→`template.submit`, never unioned; migrations `0299` (approval_instances) + `0300` (approval_signoffs parent-instance lookup discriminator), PARITY+DRIFT lints stay green; `TemplateSubmitService` asserts `CapTemplateSubmit` (`ScopeTenant`, area-blind); amends the GMR M2 validation-contract arm set (HS-7) — this ADR is the ratifying record |
| [0037](0037-membership-temporal-model.md) | Membership temporal model — soft-delete current-marker (active ⟺ `effective_to IS NULL`) | Accepted 2026-06-19 | — | Records that `user_process_areas` is Model A (soft-delete + partial-unique-on-active), not bitemporal valid-time; **refutes re-audit 2026-06-16 Major #1** (the proposed `effective_to > now()` interval predicate grants nothing, regresses authz off its partial indexes, and contradicts the unique index); active-now reads use `IS NULL`, as-of reads use the interval form; future time-bounded memberships gated behind a successor ADR (M5/F5.6) |
| [0038](0038-family-code-resolver-port.md) | `FamilyCodeResolver` — cross-module document-profile→family reads go through a taxonomy-owned port | Accepted 2026-06-20 | — | search resolves `family_code` via a taxonomy `domain` port (batch projection + filter), not raw `document_profiles` subqueries; sentinel-tenant fallback + precedence + `archived_at`-agnostic semantics wire-equivalent to the replaced SQL; reads live, off-tx (H-PRE-1); sibling of ADR 0031 (`grade-a-completion` M8/F8.3) |
| [0039](0039-cross-module-base-table-read-boundary.md) | Cross-module read boundary — published view / read-port = compliant, base table = violation (H-G definition) | Accepted 2026-06-20 | — | Locks the H-G done-bar mechanically: raw `SELECT`/`JOIN`/`EXISTS` against another module's **base table** = violation; published versioned view / owner read-port / own tables = compliant; "owned table" refined to "owned **base** table" so H-G=0 holds under both readings; active-now view encodes exactly `effective_to IS NULL` (ADR 0037); H-PRE-1 preserved; CI-enforced by cilint `hgcrossmodule` guard; worked classification of all ~20 sites (`backend-module-boundary-hardening` M0/F0.1) |
| [0040](0040-cd-obligated-readers-view.md) | `controlleddocuments` publishes `metaldocs.v_cd_obligated_readers` obligated-reader view | Accepted 2026-06-21 | — | New sibling view (not an extension of `v_cd_grantee`, whose restricted-only gate is the search-semantic contract) publishing the obligated-reader set per CD: three legs UNION'd (`user_grant ∪ area_grant ⋈ active membership ∪ company-scope CDs × active tenant users`) DISTINCT BY `(tenant_id, cd, user_id)` with source precedence `user_grant > area_grant > company_scope` and deterministic lowest-`area_code` tiebreak; consumed by the `distribution` module (M2/F2.1c + F2.2); active-now via `v_active_user_areas` (ADR 0037 D1); display names via ADR 0029 port; forward-only DDL; ADR-0039 D3a/D4 inventory updated (mission `frontend-screen-completion` M2/F2.1a, migration 0245) |
| [0041](0041-taxonomy-process-area-name-view.md) | `taxonomy` publishes `metaldocs.v_process_area_name` per-area label read contract | Accepted 2026-06-21 | — | Plain `CREATE VIEW` over `metaldocs.document_process_areas` (taxonomy-owned): shape `(tenant_id uuid, area_code text, area_name text)`, 1:1 projection with renames `code → area_code` + `name → area_name`; no `is_active` / `archived_at` filter — parity with the existing in-Go `AreaCatalogReader` port; sole consumer is the `distribution` module (M2/F2.2), joining on `(tenant_id, area_code)` to `v_cd_obligated_readers` to populate `DistributionRecipient.area_name` + `DistributionAreaCoverage.area_name`; ADR-0039 D3a/D4 inventory updated (mission `frontend-screen-completion` M2/F2.1b, migration 0246) |
| [0042](0042-distribution-module-and-cap.md) | New `distribution` module + `CapDistributionRead` + denominator-only coverage contract | Accepted 2026-06-21 | — | Greenfield `internal/modules/distribution` read module; three denominator-only GET endpoints (`/documents/{id}/distribution`, `.../recipients` keyset-paginated, `.../coverage` by-area); `DistributionRecipient` carries `{user_id, name, area_code|null, area_name|null, source}` — no `role` (no honest producer); `CapDistributionRead = "distribution.read"`, tenant-scope (cross-area rollup, matching `CapMetricsView`/`CapAuditRead`), in `deferredCaps` — operator grants to roles separately; reads only `v_cd_obligated_readers` (ADR-0040) + `v_process_area_name` (ADR-0041) + ADR-0029 iam display-name port (ADR-0039-compliant, `hgcrossmodule` = 0); forward-compatible additive-only contract; parked mission extends additively with numerator later (mission `frontend-screen-completion` M2/F2.1c) |
| [0043](0043-notifications-module-and-lifecycle-bundle.md) | New `notifications` module + `CapNotificationRead` + document-lifecycle bundle contract | Accepted 2026-06-22 | — | Greenfield `internal/modules/notifications` per-recipient inbox module; three self-scope endpoints (`GET /notifications`, `GET /notifications/unread-count`, `POST /notifications/{id}/read`); `Notification` schema: `event_type` open string (additive-extension commitment), `status` 3-enum `PENDING\|SENT\|READ`, snake_case mirror of FE `NotificationItem`; `CapNotificationRead = "notification.read"`, ScopeTenant (self-scope enforced by SQL predicate `recipient_user_id = caller`, not this enum), in `deferredCaps`; document-lifecycle bundle 5 events: reader-targeted `document_published/superseded/obsoleted` → `v_cd_obligated_readers`; author-targeted `signoff_recorded/signoff.rejected` → submitter via `v_approval_instance_submitter`; doc→CD mapping via `v_document_cd_mapping`; both views additive-only (ADR-0039 carve-out, authored in F3.2/F3.3, not emit-site enrichment); parked emitter mission adds types additively (mission `frontend-screen-completion` M3/F3.1) |
| [0045](0045-review-actor-working-content-write.md) | Review-Actor Working-Content Write Boundary | Accepted | — | Approver writes working-content revisions during review via status-gated `CanWriteWorkingContent` (draft/rejected → author; under_review → eligible approver); single-writer preserved by status, not locks; `revision_number` unchanged (advances only on governance events); eligibility reads off-transaction per `advisory-lock-deadlock-constraint`; implemented Task 5 Eigenpal cockpit service gate |
| [0046](0046-eigenpal-anti-corruption-layer.md) | Eigenpal Anti-Corruption Layer | Accepted 2026-07-02 | — | Single `@metaldocs/eigenpal-adapter` package is the only permitted `@eigenpal/*` importer; callers use capability DTOs (not plugin types); content crosses seam as `ArrayBuffer`; comments as `EditorComment` DTO; single `.` entrypoint (server-safe; the React door is the `editor-ui` package); ESLint `no-restricted-imports` enforces the boundary; eliminates phantom dep; vendor swap/upgrade contained to one package; closes T-008 |
| [0047](0047-templateplugin-mode-gating.md) | `templatePlugin` mode-gating | Accepted 2026-07-02 | — | `templatePlugin` assembled only for `template-draft`; `placeholders` capability is the single source of truth per `EditorMode`; under ADR 0046 the adapter owns eigenpal realization; realization verified in `MetalDocsEditor.tsx:184-185` + pinned by `templatePlugin.wiring.test.tsx` (DEC-04); closes tech-debt T-007 |
| [0050](0050-computed-token-catalog-single-source.md) | Computed-token catalog single source | Accepted 2026-06-29 | — | `render/domain.ComputedCatalog()` is the single source of truth for the 8 computed tokens; `templates` consumes the domain layer; bidirectional parity guard makes resolver↔catalog drift impossible |
| [0052](0052-template-manual-versioning.md) | Template revisions are created manually, never auto-spawned on approve/publish | Accepted 2026-06-30 | — | Amends ADR 0013 (version-creation trigger only, not the counter/labeling mechanics); reverts the 2026-05-31 auto-next-draft behaviour; `Approve`/`PublishTemplateVersion` transition status only; `CreateNextVersion` (`POST /templates/{id}/versions`) is the sole revision path; `next_draft*` dropped from contract (regen, no drift); restores parity with documents for the shared view layer (ADR 0053); also records `in_review`→`under_review` status alignment (M1) |
| [0053](0053-shared-controlled-artifact-view-layer.md) | Shared controlled-artifact frontend view layer | Accepted 2026-06-30 | — | One purely-presentational shell renders documents + templates via a kind-agnostic `ArtifactViewModel`; per-kind adapters compose the model, thin route wrappers own state/dialogs/slots; four hard rules (no fetch, no `kind` branching, no cross-feature import, `ReactNode` slots); templates gain a detail screen (parity — click opens detail, not the editor); dedup via `resolveOwnerDisplay` + `mapApprovalChain`; no backend module merge |
| [0054](0054-cross-tenant-outbox-claim.md) | Cross-tenant claim sanctioned for background outbox consumers | Accepted 2026-07-02 | — | Tenant-unscoped `FOR UPDATE SKIP LOCKED` claim is the platform-canonical shape for background outbox consumers (platform `outbox_events` consumer + render staging outboxes); pooled-tenancy enforced at processing time via 4 binding rules: row-carried `tenant_id`, tenant-scoped per-row processing, worker-internal only, idempotent consumers; per-tenant claim loops rejected (fairness machinery with no security payoff); closes grade-A SEC-13; replaces `TODO(render)` in `staging_outbox.go` |
| [0055](0055-global-auth-identities.md) | Global auth identities, tenant-scoped sessions (binding rules) | Accepted 2026-07-02 | — | Converts ADR 0027's narrative "`auth_identities` tenant-global by design" into 4 binding rules: sessions MUST be tenant-scoped (tenant fixed at login from verified `iam_users` membership, never re-derived from identity), identity lookups never bypass tenant checks on unlocked resources, cross-tenant probes still 404, future per-tenant identity need = new ADR; no schema/code change, documents verified runtime behavior; closes grade-A SEC-10/DEC-03 and auth T-008 (identities portion) |
| [0056](0056-template-comments-defer-reuse-document-pattern.md) | Template comments: defer a generic platform, reuse the document-comments table pattern | Accepted 2026-07-02 | — | Resolves the 3-option backlog decision (`template-editor.md` "comments"); defers a generic cross-module comments platform as YAGNI (one live consumer only); when product asks, template comments reuse the `document_comments` table shape (`tenant_id, {parent}_id, library_comment_id, parent_library_id, author fields, content_json, resolved fields`) scoped to `template_version_id`, not a shared table; promote to a platform only on a third consumer; no code/schema change |
| [0057](0057-template-key-server-generated-slug.md) | Template `key`: server-generated slug + uniqueness suffix, drop client-supplied key | Accepted 2026-07-02 (decision only) | — | Resolves `novo-template-wizard.md` "key-generation" blocker; `POST /templates` drops the required client `key` field, server derives a slug from `name` with a collision-suffix rule, `key` immutable post-create; decision only — `wiki/modules/templates-tech-debt.md` T-015 tracks the unshipped contract/application/UI follow-up |
| [0058](0058-auth-session-bcrypt-lockout-policy.md) | Auth session, bcrypt, and account-lockout policy | Accepted 2026-07-02 | — | Records 6 binding credential-mechanics values: bcrypt cost 12 (fixed), session TTL default 12h, sliding idle timeout default 30min (0 disables), opaque HMAC-signed session token stored only as SHA-256 digest, lockout default 5 attempts/15min lock, password minimum length 8 (env floor cannot lower); closes auth T-010 |
| [0059](0059-iam-tenant-scoping-rule.md) | IAM tenant-scoping rule: every IAM-owned table carries `tenant_id`, every repo method filters by it | Accepted 2026-07-02 | — | Binds the convention enforced by Group B fix (audit 2026-05-03 B5/B6, migrations 0130/0162/0163): new IAM tables MUST add `tenant_id NOT NULL`, every query MUST filter/set it explicitly, layered under (not instead of) tier-1/tier-2 capability checks; closes iam T-011 |
| [0060](0060-audit-append-only-by-grant.md) | Audit events are append-only by database GRANT, not by trigger or RLS | Accepted 2026-07-02 | — | `GRANT INSERT ON TABLE metaldocs.audit_events TO metaldocs_app` (no UPDATE/DELETE) is the sole enforcement mechanism — deliberately not a trigger/RLS (contrast `user_process_areas`'s no-delete trigger); `Writer`/`Reader` stay transaction-free ports, one concrete adapter satisfies both by design; no grant widening without a superseding ADR; closes audit T-011 |
| [0061](0061-taxonomy-area-hierarchy-shape.md) | Area hierarchy shape: self-FK parent pointer, no live cycle-prevention check (gap acknowledged) | Accepted 2026-07-02 (records shape; flags open gap) | — | Corrects taxonomy T-016's premise: no `SetParent` function exists, `AreaService.Update` overwrites `parent_code` with zero reachability check, `domain.ErrAreaParentCycle` is defined + HTTP-mapped but never raised, no DB trigger/CHECK enforces acyclicity either — self-FK provides referential integrity only, not cycle prevention; records this as verified runtime truth and closes the missing-ADR ask while leaving a follow-up implementation gap open |
| [0062](0062-render-resolver-registry-version-policy.md) | Resolver registry version field: recorded per-resolution, no compatibility/deprecation policy defined yet | Accepted 2026-07-02 (records mechanism; declines to invent unneeded policy) | — | `ComputedResolver.Version()` is a per-resolver self-reported int stamped onto `ResolvedValue` for provenance only — never consulted to gate compatibility; `Registry.Register` is overwrite-by-key, one live implementation per key, no multi-version coexistence; a real multi-version need requires a successor ADR; closes render-fanout R-003/T-003 |
| [0063](0063-editor-chrome-extraction-and-slot-api.md) | `EditorChrome`: promote-on-second-caller extraction + slot-based composition | Accepted 2026-07-02 | — | Pins `EditorChrome` (2 verified consumers: `DocumentEditorPage`, `TemplateEditorPage`) as the canonical example of the `features/shared/` "promote on 2nd caller" rule; slots (`left/center/right/alert` + `children`) chosen over compound components (unneeded context machinery) and render props (no reactive internal state) for a fixed, non-recursive eigenpal-title-bar-overlay shape; closes editor-chrome T-008 |
| [0064](0064-eigenpal-wrapper-only-already-decided.md) | Wrapper-only eigenpal consumption: already decided by ADR 0046 (closure record) | Accepted 2026-07-02 (ratifies ADR 0046; adds no new rule) | — | Confirms ADR 0046's two-wall ACL (`@metaldocs/eigenpal-adapter` server, `@metaldocs/editor-ui` browser) already fully answers editor-ui-eigenpal T-008, enforced live via `eslint.config.mjs` `no-restricted-imports` + `public-surface.test.ts`; `third_party/eigenpal/` now holds only `NOTICE` (tarball retired 2026-06-23, package sourced from npm); no new rule — closes the tech-debt row's stale "missing-ADR" heading only |
| [0065](0065-version-references-are-nested-value-objects.md) | Version references are nested value objects in wire contracts | Accepted 2026-07-03 | — | Coupled resource-pointer field sets (id + counters + labels + status) are always one nested required ref object, nullable as a whole when the pointer may not exist, never parallel scalars; `TemplateVersionRef` ≠ `DocumentRevisionRef` (pattern unified, schema per-context); TemplateDTO drops `latest_revision_number` + `published_version_id`/`published_version_number`/`current_revision_number`, gains `latest_version: TemplateVersionRef` + `published_version: TemplateVersionRef\|null`; records the pre-v1 atomic-cutover exception; complements ADR 0035 (envelope shape) by governing field grouping and closing its optional-vs-null drift subclass structurally |
| [0066](0066-optimistic-concurrency-transport-split.md) | Optimistic-concurrency transport split: documents `If-Match`, templates body `lock_version` (intentional, `If-Match` is the target) | Accepted 2026-07-04 | — | M4 F4.3: verification refuted the "templates is the minority straggler" premise (templates has zero If-Match; `expected_lock_version` is its lone self-consistent OCC write) — so the two idioms are recorded as an intentional split with `If-Match` named the long-term target; full unification deferred to its own cross-module wire change (not smuggled into a correctness milestone); target design = `internal/platform/occ` kernel + `428`/`412` |
| [0067](0067-async-job-infrastructure-consolidated-onto-river.md) | Async job infrastructure consolidated onto River (lease scheduler + staging poller retired; watchdog advisory lock removed) | Accepted 2026-07-04 (§H-PRE-1 erratum) | — | M5 F5.1: River is the single async primitive — 3 janitors → River periodic jobs (leader-elected singleton), staging dispatch → River transactional job, retention via native cleaner + periodic purge job, fanout proven idempotent-commutative; custom `job_leases` lease scheduler + `lease_reaper` + duplicated staging backoff deleted; **the watchdog's unrelated `pg_try_advisory_lock` single-runner guard removed** (River elector+queue subsume it, with singleton integration proof) — **H-PRE-1 stays LIVE** (governs the audit-writer's `pg_advisory_xact_lock`, untouched; §H-PRE-1 erratum); janitors + staging hosted in `metaldocs-jobs` (api becomes stateless sync+authz); no wire-contract change |
| [0068](0068-stuck-instance-watchdog-alert-only.md) | Stuck-instance watchdog is alert-only; auto-cancel timeout-action concept withdrawn | Accepted 2026-07-04 | — | M5 F5.8: root-cause audit found the watchdog's `auto_cancel` branch is the orphaned remnant of an abandoned stuck-timeout-action concept that lost a semantic collision on `on_eligibility_drift` (schema always allowed only `reduce_quorum`/`fail_stage`/`keep_snapshot`; frontend corrected in `14e48071`, watchdog never synced) — dead branch never fired in production; watchdog collapses to alert-only (every stuck instance → one `approval.instance.stuck_alert`, human decides); `SystemCancelInstance` + the `system` authz-bypass path removed as unreachable (reduces bypass surface); user-facing `CancelInstance` (document.edit-gated) unchanged; F5.2's auto-cancel equivalence fiction replaced by an honest alert-only proof; no wire/schema/cap change; future auto-cancel needs a first-class `on_timeout_action` config (successor ADR) |
| [0069](0069-document-periodic-review-and-reason-for-change.md) | Document periodic review/expiry, the `document.review` capability, and structured reason-for-change | Accepted 2026-07-04 | — | M6 (eQMS): wires `public.documents` review/expiry model by reusing `effective_from`/`effective_to` (no duplicate column family) + new nullable `review_due_at`/`last_reviewed_at`, DB-CHECK enforced (migration 0274); effective date = publish date (recorded, unchanged); adds new capability `document.review` (ScopeTenant, registry 34→35, held by area_admin/qms_admin, system_admin via bypass) gating a mark-reviewed workflow that sets `last_reviewed_at`+next `review_due_at` without a status transition; additively widens the `documents/UPDATE` tripwire arm to `{document.edit, document.obsolete, membership.manage, document.review}` generated into migration 0275 (same defect class as 0269/0270/0271); surfaces review-due via a River periodic job through a new documents read-port (ADR 0067); structured `reason_for_change`(+`reason_category`) as a distinct submit field persisted + carried into the `approval_submitted` audit event, required at REV≥1 |
| [0071](0071-distributed-rate-limiting-shared-store.md) | Distributed rate limiting via a pluggable counting-store abstraction (memory \| Redis GCRA) | Accepted (2026-07-05) | — | M8 F8.2: closes the 2026-07-03 final architecture review §9 pre-customer blocker — per-replica `sync.Map` fixed-window limiter silently multiplied limits ×N replicas; `ratelimit` counting-store abstraction with `memory` (default, single-replica) and `redis` (GCRA via `go-redis/v9` + `redis_rate/v10`, shared budget) backends, explicit env selection (`METALDOCS_RATELIMIT_STORE`, `METALDOCS_RATELIMIT_REDIS_ADDR`); fail-fast startup guard blocks `METALDOCS_MULTI_REPLICA=true` + `memory`; fail-open on Redis runtime error; 429 wire shape (RFC 9457) and route keying unchanged |
| [0072](0072-approval-nested-exception-and-boundary-model.md) | `documents/approval` nested exception + boundary-guard published-surface model | Accepted 2026-07-06 (ruling (a) superseded 2026-07-12) | 0082 (ruling (a) only) | M9 F9.5: promotion of `documents/approval` to a top-level module rejected (dense bidirectional aggregate coupling, HS-2); nested exception recorded with external surface = `domain`/`application`/`api` only + promotion trigger; `repository/`→`infrastructure/` rename across documents/templates/approval (mechanical; approval idemp-store files split into `infrastructure/idempotency/` subpackage to break a real import cycle, mirroring the existing `infrastructure/signature/` convention); `scripts/check-module-boundaries.ps1` realigned to REQ-TOP-1 published-surface model (was inverted, domain-only allow-list); 0 true cross-module violations found in production code; debt table empty. **Ruling (a) superseded by ADR 0082** (M3): the recorded promotion trigger fired (templates became a second bounded-context consumer of approval), so `documents/approval` was promoted to top-level `internal/modules/approval`. Rulings (b) and (c) remain in force. |
| [0073](0073-remove-finalize-wrapper-canonical-submit.md) | Remove `/documents/{id}/finalize`; canonical `/submit` owns in-tx prereq resolution | Accepted 2026-07-06 | — | Post-GMR live-QA remediation: fresh draft (`revision_version=0`) could not be submitted — wrapper's `parseFinalizeIfMatch` rejected `v0`/`*`, canonical parser rejected literal `v0`, FE couldn't migrate because `/submit` required `route_id`+`content_hash` an author can't supply; finalize wrapper (+ bespoke idempotency copy + off-tx `GetFinalizePrereqs` TOCTOU) deleted, `/submit` gains optional `route_id`/`content_hash` resolved in-tx (narrow `SubmitDefaultsResolver` port) + `revision_title`, `parseIfMatch` accepts `v0` (parser domain = OCC counter domain); supersedes CON-01 wrapper retention, completes DEC-01 |
| [0074](0074-approval-route-versioning.md) | Approval route versioning + supersession (in-use routes superseded, not frozen) | Accepted 2026-07-07 | — | Supersedes ADR 0018 §1 ("update in-place only while active") — M2b F2: `enforce_route_immutable` gains a column-scoped exception (only `active`/`superseded_at` may change on an in-use row); in-use `PUT` creates a new versioned row + marks the old superseded (never deleted), not-in-use path unchanged; unique constraint moves from single-row to partial-active + full version-history; empty eligible-actor pool at submit now 422s via existing `ErrEmptyEligiblePool` instead of a silent stuck instance; ADR 0018 §3 (Deactivate guard) unaffected |
| [0075](0075-approval-oversee-visibility.md) | `approval.oversee` capability + visibility model | Accepted 2026-07-07 | — | M2b F3: new capabilities `approval.review` (area-grade, acts on a review-kind stage, no live route until F4) + `approval.oversee` (tenant-grade, read-only tenant-wide oversight); tier-1 generic `/api/v1/approval/` prefix fallback deleted, replaced with one explicit row per real runtime verb matching its tier-2 check (signoff/cancel/get-instance/inbox) — same defect class as BE-9; oversight reads (`LoadInstance`/`LoadActiveInstanceByDocument`) accept `approval.oversee` as an explicit alternative to `document.view`, never a role check; seed grants mirror the existing `document.signoff` pool for `approval.review`, `qms_admin`/`system_admin` for `approval.oversee`; no tripwire arm (read-only + no live write surface yet) |
| [0076](0076-approval-freeze-boundary-and-choke-point-concurrency.md) | Approval freeze boundary + choke-point concurrency | Accepted 2026-07-07 | — | M2b F5: freeze fires at the review→approval stage-transition boundary (or at submit for approval-only routes), pinning `approval_instances.frozen_content_hash` from the existing submit-time `ContentHashAtSubmit`; W10 fixed — instance-scoped `HasUnresolvedInstanceComments` (bound to `SubmittedAt`) replaces the document-wide, unbounded gate at both call sites (`RecordVerdict`, `RecordSignoff`); freeze race resolved by the existing row-lock + OCC machinery (`ErrStaleRevision`), no new concurrency primitive; markup gate (`ScanForUnresolvedMarkup`) ships standalone/tested but unwired — bounded defer pending M2c's docx-byte read path |
| [0085](0085-release-coordinator-approval-driven-publication.md) | Release coordinator: approval-driven publication, manual publish retired | Accepted 2026-07-28 (Codex-aligned rev 2) | — | Usability-remediation Etapa 2 (D7): approval is the last human decision; idempotent release coordinator (approval module, document-only per ADR 0082 discriminator) owns coordinator-only `approved→scheduled→published` edges, fed by durable **fail-closed** outbox facts (unified approval terminal event + new artifact fact) keyed on a shared release-generation identity (tenant, subject, document, instance, revision, frozen hash); predicate = approval(G) × artifact-ready(G: DOCX+PDF) × effective-date gate × supersession-head; release tx = source CAS + deterministic target locking, rollback on any target conflict, exactly-once events; **amends ADR 0069**: new `planned_effective_from` (immutable plan) vs actual `effective_from` (release time, never cleared — closes F-QA4-13); review-verdict terminal path unified with `FreezeService.Pin` (closes F-QA4-14); publication plan (planned date, effective_to, review_due_at, cross-doc supersede gated on retained `document.supersede`) declared at submission; timer + reconciliation = River jobs in `metaldocs-jobs` (ADR 0067), alert-only (ADR 0068); readiness-hold projection replaces FE content_hash inference; hard break: `/publish`, `/schedule-publish`, `/supersede` endpoints + `document.publish` capability + publish services/jobs/FE surface deleted (F-QA4-5 dissolves); executable idempotent backfill (synthesize facts → migrate planned dates → Pin-repair → evaluate); D3 void |
| [0084](0084-approval-actor-selector-union.md) | Approval route-stage actor selectors: a discriminated union (M4 ActorSelector) | Accepted 2026-07-13 | — | ROADMAP unit 3.2 / approval-remediation M4: route stage actors move from flat `required_role`/`area_code` to a 4-kind discriminated union (`named_user`, `role_in_fixed_area`, `role_in_document_area`, `submit_choice`) — `domain.ActorSelector.Validate()` mirrors a DB `CHECK` on new child table `approval_route_stage_selectors` (migration 0303); flat columns dropped (migration 0305), selectors are the sole source of truth; wire stays a flat `{kind, …?}` object (no `oneOf`, ADR 0035); new read-only `GET .../approval-preview` (document + template) lets a submitter preview `submit_choice` pools before submitting `chosen_actors`, gated by the existing submit capability and resolution-parity-bound to the submit path |
| [0086](0086-template-routes-config-first-profile-keyed.md) | Template approval routes: config-first, profile-keyed | Accepted 2026-07-29 | — | Usability-remediation Etapa 4 (D6): template routes key on `profile_code` (not per-template `template_id`) — same two-level keying documents already use; supersedes ADR 0082's P2.S3 per-instance template-route keying and migration 0297's NULL-forced `profile_code`; per-profile `RoutePolicy` (ADR 0081) now applies to template routes too; tripwire arms (ADR 0083) regenerate for the new projection; `has_active_template_route` creation gate; extended by ADR 0087 (closes this ADR's open item "livre profiles can never own templates") |
| [0087](0087-livre-configured-no-approval-route.md) | Livre governance class: configured no-approval route | Accepted 2026-08-03 | — | Supersedes the livre arm of ADR 0081 (`RoutePolicyNoRoutePermitted`/migration 0295's "no active route may exist for livre"): livre now means an active route is required and MUST carry ZERO stages (`RoutePolicyNoApprovalStages`); config-first is universal — no route-less profile permitted for any governance class, either subject; submit auto-approves in-tx (both documents and templates) via new shared terminal-approval helpers + `approval_auto_approved` governance event; forward migration `0316` replaces `assert_route_shape()` (zero-stage now DB-exclusive to livre); extends ADR 0086 (closes its open item), ADR 0082 (kernel unchanged), ADR 0085 (release pipeline reused as-is) |
| [0088](0088-template-version-content-always-materialized.md) | Template version content is always materialized | Accepted + implemented 2026-08-04 | — | A template version can no longer exist without content: every creation path stores bytes and pins `content_hash` in the same operation, so the state the submit path used to reject with 409 `state.upload_missing` is unreachable rather than merely guarded. Blank templates copy the seeded `system/templates/blank.docx` asset (PRE-TX store-then-reference); the application-level emptiness gate is deleted and migration `0317` makes `chk_template_version_content_hash_non_draft` an unconditional `length(content_hash) = 64`. `state.upload_missing` survives with a narrowed meaning: genuine store-side loss |
| [0089](0089-problem-code-registry.md) | Problem codes are a registry, not a convention | Accepted + implemented 2026-08-04 | — | `problem.Code` becomes `struct{s string}` with an unexported field, so an unregistered code is a **compile error**, not a review finding; codes are `<family>.<name>` over ten **semantic** families (never module-named — ADR 0082's approval extraction stranded `signoff.`/`sod.`), and status is bound to the code at registration so one condition cannot answer 409 here and 412 there. 155 codes swept to 141, hard break, no aliases. Every downstream representation is generated from `problem.Registrations()` (`cmd/problem-codes-dump`), gated by `PROBLEM-DUMP-IMPORT` + a freshness check + a frontend vocabulary guard. Supersedes the `SCREAMING_SNAKE` catalog, the three module-local dotted taxonomies, and the opt-in `codes_catalog_guard_test.go` allowlist. One deviation from decision 6: a spec `pattern`, not an `enum` |

The legacy ADR tree (formerly under the now-deleted `docs/` documentation set) was removed from this branch. Where its content maps to canonical `wiki/decisions/` entries is tracked by the governance migration map (to be finalized in F0.5).
