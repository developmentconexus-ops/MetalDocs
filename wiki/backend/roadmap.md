# Backend Professionalization — Execution Roadmap (LIVING TRACKER)

> **Status:** ✅ **COMPLETE (2026-06-12) + WAVE Z SEALED CLOSE-OUT (2026-06-13)** — Waves 0–2 + Wave F + Wave Z all closed with evidence; frozen as the historical record. Wave Z (sealed-scope zero-defer finish, see addendum below) emptied the legacy register, executed the two deferred architecture items (OTel, full-table RLS), and passed the machine-checkable DONE gate G1–G11. Awaiting the operator's final sign-off before integration merge + Sunday v1 release (not merged by the agent, per directive).
> **Last updated:** 2026-06-13 (Wave Z CLOSED — DONE gate G1–G11 green, register zero, blueprint all-green, ADR index trustworthy, regression-only review clean)
> **NOTE:** the 0.4 history rewrite (2026-06-11) changed every commit hash in the repo. Hashes below are the REWRITTEN ones. Any other clone of this repo must be re-cloned.
> **Contract:** [`docs/superpowers/specs/2026-06-11-backend-professionalization-design.md`](../../docs/superpowers/specs/2026-06-11-backend-professionalization-design.md) (decisions D-1..D-5, protocol)
> **Verdicts:** [`stage2-evaluation.md`](stage2-evaluation.md) · **Evidence:** [`legacy-register.md`](legacy-register.md)
>
> **Update protocol (mandatory):** every wave session updates this file IN THE SAME COMMIT as its close-out — flip statuses, fill the Commit and Evidence columns, bump "Last updated". A wave is not closed while its rows are stale. The final review (Wave F) cannot start until every Wave 0–2 row is ✅ or explicitly deferred with a trigger.

**Status legend:** ☐ pending · ▶ in progress · ✅ done · ⏸ gated (waiting on dependency/user) · 🔁 done, runtime re-check pending (Docker) · ➖ deferred (trigger recorded)

---

## Program map

| Phase | Status | Output |
|---|---|---|
| Stage 1 — map the backend | ✅ `d7489d5a2` | `wiki/backend/` atlas, legacy register (212 flags) |
| Stage 2 — evaluate vs standards | ✅ `e5a162867` | 34 verdicts, 4-wave plan, 2-ADR backlog |
| Brainstorm — decisions D-1..D-5 | ✅ `432e24405` | Design spec (the contract) |
| **Wave 0 — P0 prerequisites** | ✅ evidence in handoff · user review gate pending | security unblocked, deployment complete, layering legal |
| **Wave 1 — high-value / low-blast** | ✅ items 1.1–1.10 (evidence in handoff) · user review gate pending · 1.11 open (out of session scope) | correctness + compliance defects < ~100 lines each |
| **Wave 2 — structural refactors** | ✅ items 2.1–2.11 (evidence in handoff) · user review gate pending | atomicity, RLS, boundaries, ADR 0027, N+1, rate-limiter swap, dead-code |
| Wave 3 — trigger-gated | ➖ by design | executed only when triggers fire |
| **Wave F — final full review** | ✅ 2026-06-12 | program sign-off: backend verified solid — F.1–F.8 all ✅ (see Wave F card + handoff closing report) |

---

## Wave 0 — P0 prerequisites (fresh session #1)

| # | Item | Findings | Status | Commit | Evidence / notes |
|---|------|----------|--------|--------|------------------|
| 0.1 | Delete seed binary; redact secret from 7 docs; rm committed .exe; .gitignore fix; delete dead script; pin api-lint.exe | F-18, D-4a | ✅ | `58cbf9943` | Secret grep clean over tracked files AND full working tree (incl. `.env` — already holds a new value). Redacted 9 tracked files (2 more than planned: `scripts/start-api-no-build.ps1`, `scripts/start-worker.ps1` comments + `_artifacts/stage1/repo-topology.md`); `bin/metaldocs-api.exe` + `scripts/api-lint/api-lint.exe` were on-disk only, never git-tracked → file deleted / rebuilt from source (SHA-256 `5660…2C8D`); `bin/*.exe` added to `.gitignore`; stale gitignored agent-worktree copies redacted in place |
| 0.2 | gitleaks secret-scan in CI + D-4a rule in documentation-governance.md | F-18 | ✅ | `3402a8bbd` + `e2b5b2aa4` | `.github/workflows/secret-scan.yml` (gitleaks v8.24.3, working-tree mode until 0.4 rewrites history) + `.gitleaks.toml` allowlist (10 findings assessed: test fixtures, `.gen.go` spec chunks, stale plan-doc token ≠ current `.env`, runbook sample password). Verified: gitleaks over `git archive HEAD` + config = no leaks (exit 0); YAML parses |
| 0.3 | **USER:** rotate dev DB password in `.env` at Docker re-creation (+ anywhere reused) | F-18 | ➖ owner-waived | — | User declined rotation in the Wave 0 session ("keep this work professional, no secret") and explicitly accepted the residual risk of the historical value; `.env` working value verified ≠ the leaked string. 0.4 unlocked by this directive |
| 0.4 | History rewrite: `git filter-repo --replace-text` + force-push; user re-clones | F-18 | ✅ | (rewrite itself) | Executed 2026-06-11 with explicit user authorization: `git filter-repo --replace-text` (old secret → `***REDACTED***`) on a fresh mirror clone, 3057 commits rewritten; force-pushed all `refs/heads/*` + `refs/tags/*` to origin. **VERIFICATION (corrected in round-2 review):** a fresh *mirror* clone of origin (which fetches PR refs, unlike a plain clone) confirms all 45 branches + the tag are clean — every surviving secret commit is reachable ONLY from `refs/pull/*` (73 immutable GitHub PR refs, `heads=0 tags=0` for all). The earlier "plain-clone `git log --all -S` = empty" check was insufficient because a plain clone does not fetch PR refs. Also confirmed: leaked value ≠ current `.env` password (stale credential). Residual: the stale value persists in the 73 PR refs. **RESOLVED-BY-PLAN (owner decision, 2026-06-11):** F-18 history residual is permanently closed at first release — the owner will re-baseline into a NEW repo whose first commit is the v1 release, abandoning all prior history (PR refs included) with the old repo. No GitHub-support purge needed. Until then it is a dead dev credential in a single-user repo (leaked value ≠ current `.env`). Local repo reset onto rewritten origin (~22 stale LOCAL branches still hold the value in this clone's object DB — local-only, gone at re-baseline) |
| 0.5 | `jobs.Dockerfile` + `jobs` service in compose | F-19-deployment | ✅ | `baf6e1b78` | RUNTIME-VERIFIED (Docker up on this machine): `docker compose config` parses; `compose-jobs` image built; `docker compose up -d --no-deps jobs` → log "MetalDocs Jobs running (queues=temporal)", container stays Up. Service envs: `METALDOCS_JOBS_ENABLED`/`METALDOCS_JOBS_TEMPORAL_MAX_WORKERS` + PG* (mirrors worker); `depends_on: postgres(healthy), api(healthy)`. Round-2: added `METALDOCS_JOBS_RIVER_SCHEMA` passthrough to api+jobs so the schema cannot diverge |
| 0.6 | platform/observability → callback injection (drop auth/domain import) | F-06a | ✅ | `8e0aa9eb4` + `e2b5b2aa4` | `WithUserIDResolver(func(*http.Request) string)` setter (pattern: `platform/ratelimit`); wired at composition root `main.go`. Build + vet + `go test ./internal/platform/observability/...` OK; grep: observability clean. **Discovered for 1.10 scoping:** `platform/{authn,bootstrap,docgenv2,objectstore,security}` still import `internal/modules/**` — the 1.10 CI guard needs explicit per-package disposition (bootstrap/authn are composition-adjacent; `security/ratelimit.go` dies in 2.8). Round-2: `WithUserIDResolver` setter promoted to a **required `NewHTTPObservability` constructor param** so the resolver can't be silently omitted |

**Wave close:** evidence block in handoff · this file updated · user review → Wave 1.

## Wave 1 — high-value / low-blast (fresh session #2)

| # | Item | Findings | Status | Commit | Evidence / notes |
|---|------|----------|--------|--------|------------------|
| 1.1 | Middleware chain reorder + panic recovery + pre-auth login rate limit + REQ-MW-7 chain-order test | F-01 | ✅ | `58d7009d9` + `2d8728c6b` | Chain now `panicRecovery → httpObs → cors → origin → preAuthLoginLimit → authn → iam → presenceBump → rateLimiter → mux` via declarative `apiChain`/`buildChain` (`apps/api/cmd/metaldocs-api/chain.go`) + REQ-MW-7 order test (`chain_test.go`). New `platform/middleware.Recovery` (trace-ID + panic → 500 problem+json; re-panics ErrAbortHandler). httpObs: records panicked requests as 500 in RED metrics (defer; panic NOT swallowed); user attribution preserved across the authn boundary via `observability.SetPrincipal` slot (authn reports outward — REQ-MW-4 without losing audit attribution). Pre-auth login limit: `platform/ratelimit` instance, `auth_login` 10/min IP-keyed, login path only. build+vet+tests green (`apps/api/cmd`, `platform/{middleware,observability,ratelimit}`, `modules/auth`, `tests/unit`). **RUNTIME-VERIFIED** (start-api.ps1 -Build, :8081): login 200 · authed `GET /internal/test/panic` (METALDOCS_E2E probe, commit `2d8728c6b`) → 500 problem+json `INTERNAL_ERROR`, process alive (health 200 after) · metrics show 401s (`/api/v1/iam/users` errors=2) and panics (panic route errors=4) · panic log line trace-ID-tagged with full stack; stack shows live chain order = canon · `http_request` for the panicked authed request has `user_id":"admin"` (principal slot works through a panic) · 12 rapid bad logins → 11×401 then 429 (pre-auth IP limit live) |
| 1.2 | http.Server Read/Write/Idle timeouts | F-16 | ✅ | `1b3f0d11d` | `ReadTimeout 30s / WriteTimeout 60s / IdleTimeout 90s` added at the `http.Server` literal in `main.go` (ReadHeaderTimeout 5s kept). WebSocket presence unaffected (hijacked conns manage own deadlines). `go build`+`go vet` clean. F-16B (WS drain) stays deferred per D-1; F-16C (readiness concurrency) Wave 3 trigger |
| 1.3 | Delete `spec2.yaml` + `internal/api/v2`; migrate 3 contract tests; fix capability-catalog CI gate | F-03 | ✅ | `880f33c4a` | Parallel surface gone: `api/openapi/spec2.yaml` (1061 lines) + `internal/api/v2/` deleted; 3 contract tests (CD/taxonomy/iam) decode `problem.Problem` instead of `apiv2.APIError`; `.golangci.yml` types_gen exclusion dropped. CI gate **deleted not fixed**: `capability-catalog-hash` pinned `sql/seeds/capabilities_v2.sql` which never existed (always exit 0) — real REQ-AUTHZ-5 guard is api-lint `registry_rules.go` (ADR 0022); placeholder `ops/CAPABILITY_CATALOG.sha256` removed; rationale comment left in `invariants.yml`. v1 spec untouched (no regen needed). build+vet+3 delivery test pkgs green; both YAMLs parse |
| 1.4 | Idempotency middleware codes → problem catalog; expand guard test | F-09 | ✅ | `ed7890597` | `writeErrJSON` signature now takes `problem.Code`; all 6 raw literals replaced: `IDEMPOTENCY_KEY_REQUIRED`→const, `IDEMPOTENCY_KEY_INVALID`→new const, `REQUEST_BODY_TOO_LARGE`→new const, `BAD_REQUEST`→`CodeValidationError`, `IDEMPOTENCY_KEY_CONFLICT`→`CodeIdempotencyKeyReused` (fixes pre-existing FE mismatch — FE already mapped REUSED), `INTERNAL`→`CodeInternalError`. Guard test now covers `internal/platform/idempotency` + `writeErrJSON`. FE catalog regenerated (`dump-error-codes.go`, 112 codes) + 2 PT-BR messages; FE coverage test green. Go build/vet/tests green. Finalize-handler inline idempotency (F-09 half 1) untouched — RF-10 scope, not this card |
| 1.5 | Remove dead search response fields + `businessUnit` param; fix bare 405s (search+security) | F-13a/b, D-03 | ✅ | `2e977845b` + `c76fd6cfc` | `SearchDocumentResponse` now mirrors spec `SearchDocumentItem` exactly (subject/business_unit/classification/tags dropped — SQL reader never populated them; spec never declared them → code aligned TO spec, **no spec change/regen**). camelCase `businessUnit` param read removed (undocumented; reader no-ops it). Bare 405s → RFC 9457 `METHOD_NOT_ALLOWED` problem+json + `Allow: GET` in search (1×) and security (3×, shared helper); new catalog const + FE message + regen (113 codes). Tests: 405 contract test added; legacy-fields test inverted to assert absence; businessUnit filter assertion inverted. build/vet/tests + FE coverage green. Remaining no-op params subject/classification/tag (snake_case, documented) out of card scope |
| 1.6 | River schema migration single owner (remove from bootstrap/jobs.go) | F-19 | ✅ | `38d7cc8ce` | `MigrateRiverSchema` call removed from `BuildJobsDependencies` (jobs binary); API binary is sole owner (`main.go` startup). Ordering guaranteed by compose `depends_on: api (healthy)` (0.5) + shared `METALDOCS_JOBS_RIVER_SCHEMA` passthrough (0.5 round-2). Rationale comment left at the removal site. build/vet/bootstrap tests green. **RUNTIME-VERIFIED at wave close:** `docker compose up -d --build --no-deps jobs` rebuilt the image on wave code → "MetalDocs Jobs running (queues=temporal)", container Up (no self-migration) |
| 1.7 | lease_reaper JOIN bug → system-scoped governance event | F-19 | ✅ | `2512f502f` + `c76fd6cfc` | `public.documents` subquery removed (job names never match document ids → tenant always NULL → **every** reap errored and skipped its governance event while still deleting the lease). Resolution: structured `slog.Warn` per reclaimed lease (card's slog option) — `governance_events.tenant_id` is `UUID NOT NULL` with no system-tenant convention, so a "system-scoped governance event" would require a schema/convention change beyond this card. Regression test asserts system-job reaps succeed, zero governance inserts, and the SQL never references `public.documents`. jobs module tests green |
| 1.8 | Templates `ListAudit` reads `audit_events` (close read/write sink split) | F-07-sub-split | ✅ | `3a257a9cf` | `ListAudit` now queries `metaldocs.audit_events` filtered `resource_type='template'` (was legacy `templates_audit_log` — events written since the sink migration were invisible to the read endpoint). Write-side gap closed in the same change: `auditEvent()` now carries `version_id` inside the payload (audit_events has no column; the old write path silently dropped it), and the read lifts it back to `AuditEvent.VersionID`, preserving the delivery response shape. Historical `templates_audit_log` rows = accepted seam (per card; not surfaced). templates module build/vet/tests green |
| 1.9 | Remove vestigial `.gitkeep` scaffolds | F-08 | ✅ | `9ff2c0d6c` | 4 `.gitkeep` removed: `platform/cache` (dir now deleted entirely — was empty scaffold, D-05 cache contract stays a Wave 3 doc item), `platform/db` (real package lives at `db/postgres`), `platform/objectstore` + `platform/observability` (had real sources; keeps superfluous). `go build ./...` clean |
| 1.10 | **CI guard:** `internal/platform/**` must not import `internal/modules/**` | F-06a lock-in | ✅ | `84f465efc` | New cilint analyzer `platformboundary` (runs in invariants.yml "Fail on violations" over `./...` — chosen over a Go test because ci.yml only tests a platform subset). Flags any `internal/modules` import from `internal/platform/**` with a dependency-inversion directive. **Frozen baseline** (per round-2 disposition, each with trigger): `bootstrap` + `authn` (composition-adjacent, Wave 2 review), `docgenv2` + `objectstore` (Wave 2 F-06 extraction), `security` (dies Wave 2.8/F-05). New packages and new violations are CI-blocked; target = empty allowlist. 4 unit tests; full repo run exit 0 |
| 1.11 | gitleaks **full git-history** mode (drop `--no-git`, `fetch-depth: 0`) | F-18 round-2 | ☐ | | Closes the commit-then-amend bypass. BLOCKED ON a triage pass: git-mode over the rewritten branch surfaces ~15 historical test-fixture/dummy-token findings (all in already-path-allowlisted classes, but the singular `[allowlist]` schema needs per-finding review for git-mode). Also: v8.24.3 ignores the plural `[[allowlists]]` AND-form, so per-file regex scoping needs a gitleaks upgrade or rule-level allowlist |

## Wave 2 — structural refactors (fresh session #3)

| # | Item | Findings | Status | Commit | Evidence / notes |
|---|------|----------|--------|--------|------------------|
| 2.1 | **ADR 0027** — auth_identities tenant-global by design; RLS sequencing (write BEFORE 2.3) | D-3, F-12 | ✅ | `81213133` | `wiki/decisions/0027-rls-adoption-sequencing.md`: T-008 closed by-design; RLS sequencing recorded (Wave 2.3 = controlled_documents+audit_events, RF-6 = iam_users, trigger-gated = remaining tables); GUC `metaldocs.tenant_id` confirmed |
| 2.2 | In-tx audit/governance writes (`RecordTx`/`LogTx`) across taxonomy/templates/documents-core + **CI guard vs post-commit audit calls** | F-07, D-01, D-2 | ✅ | `6cc9595` + `510bc1b` (review) | `GovernanceLogger.LogTx(*sql.Tx)` added; taxonomy/templates/documents/controlleddocuments violations fixed; `PostCommitAudit` cilint analyzer added + 8 unit tests; `//cilint:allow-post-commit-audit` for legitimate no-tx else-branches |
| 2.3 | RLS on `controlled_documents` + `audit_events`; `ExportJob.TenantID` → uuid.UUID | F-12 | ✅ | `de0b44c2` | Migration `0234`: ENABLE+FORCE RLS + NULL-permissive `tenant_isolation` policy on both tables. Owner/BYPASSRLS audit: `metaldocs_app` is superuser in dev → policies active only when `NOSUPERUSER+NOBYPASSRLS` production constraint is met (ADR 0022 §Item 7 + ADR 0027). Runtime proof (NOSUPERUSER `rls_test_role`): GUC-unset→both tenants visible; GUC=tenant-A→only A rows; GUC=tenant-B→only B rows (psql verified). `ExportJob.TenantID` changed `string→uuid.UUID` to match `audit_export_jobs.tenant_id uuid NOT NULL`; 3 compile sites fixed + test UUIDs updated. `go build ./...` ✓ · `go test ./internal/modules/audit/... ./internal/modules/controlleddocuments/...` all green. rest of tables ➖ trigger: first external tenant |
| 2.4 | Capability literals: resolve `"template.admin"`; typed EventType consts; tier-1/2 pairing audit; tier-1 lint rule (F-11, REQ-AUTHZ-2, ADR 0022) | F-11 | ✅ | `784ce561` | No new capability minted (no ADR). **Tier-1/tier-2 pairing table (post-fix):** `submitForReview`: CapTemplateSubmit / CapTemplateSubmit ✅ · `review`: CapTemplateReview / CapTemplateReview ✅ · `approve`: CapTemplateApprove / CapTemplateApprove ✅ · `archiveTemplate`: CapTemplateArchive / CapTemplateArchive ✅ · `upsertApprovalConfig`: CapTemplateEdit / CapTemplateEdit ✅ · `PublishTemplateVersion`: CapTemplatePublish / CapTemplatePublish ✅ · `CreateTemplate`: CapTemplateCreate / CapTemplateCreate ✅ · `SaveTemplateDraft`/`Presign`: CapTemplateEdit / CapTemplateEdit ✅. **Behavior changes:** (a) `upsertApprovalConfig` route now accessible to CapTemplateEdit holders (was permanently locked — "template.admin" unknown cap); (b) `PublishTemplateVersion` tier-1 now CapTemplatePublish (was "template.approve" — same denial for approve-but-not-publish actors, now consistent); (c) `containsRole(..., "admin")` post-publication gate now checks canonical `"system_admin"` OR `"qms_admin"` — previously the "admin" branch could never pass. **Tests added:** 6 new approval_config_test cases covering qms_admin/system_admin grants + phantom-admin denial + creator bypass. **Lint rule added:** `no-rawstring-tier1-authz` in registry_rules.go — flags raw cap strings in delivery-layer `h.authz(r,t,a,cap)` tier-1 calls; 4 tests; would have caught "template.admin" pre-fix. `go build ./...` ✓ · `go vet ./...` ✓ · all targeted tests green · `api-lint -strict` → 0 violations |
| 2.5 | Extract delivery-layer raw SQL (CD routes.go, documents handler.go) to repositories | F-06b | ✅ | `ea996da2` + `e9e6e2dc` (review) | **CD:** `GetActiveDocument` handler: 3 inline queries (CanRead EXISTS + FULL OUTER JOIN active/published + approval instance lookup) extracted to `PostgresControlledDocumentRepository.GetActiveInstance` (infrastructure/repository.go); domain type `ActiveDocumentInstance` added (domain/port.go); service method `GetActiveInstance` added (application/service.go); handler interface extended; tests migrated from sqlmock to spy. **Documents:** `finalizeDocument` handler: 4 inline queries (draft load + profile_code + approval route + content hash) extracted to `Repository.GetFinalizePrereqs` (repository/repository.go); domain type `FinalizePrereqs` + sentinel errors `ErrDocumentNotDraft/ErrProfileNotConfigured/ErrApprovalRouteMissing` added (domain/errors.go); service method + Repository interface extended; handler Service interface extended; tests migrated. **Grep proof:** `QueryRowContext\|QueryContext\|ExecContext` → 0 matches in both delivery files. `go build ./...` ✓ · `go vet ./...` ✓ · all targeted tests green |
| 2.6 | auth → iam_users write via `LoginContextPort` | F-06c | ✅ | `07f914e9` | **Port:** `iamdomain.LoginContextPort` (iam/domain/login_context_port.go). **Impl:** `iampg.LoginContextRepository` (iam/infrastructure/postgres/login_context_repository.go) — exact SQL from auth, zero-rows no-op. **Auth changes:** `RecordLastLoginContext` removed from `authdomain.Repository` interface + both postgres and memory impls; `Service.loginCtxPort` field + `WithLoginContextPort` setter added (same pattern as `WithCapabilityProvider`). **Login path:** `s.loginCtxPort.RecordLoginContext(...)` called when port is non-nil, swallowed on error — semantics unchanged. **Wiring:** `apps/api/cmd/metaldocs-api/main.go` in `if deps.SQLDB != nil` block alongside capability provider. **Grep proof:** no `UPDATE metaldocs.iam_users` under auth; comments only remain. `go build ./...` ✓ · `go vet ./...` ✓ · `go test ./internal/modules/auth/... ./internal/modules/iam/...` all green |
| 2.7 | iam/delivery drops auth/infrastructure import (promote types to auth/domain) | F-06d | ✅ | `3c6ab235` | **Moved:** `SessionAdminQuery` + `SessionListItem` from `auth/infrastructure/postgres/sessions_admin.go` to new `auth/domain/session_admin.go`. **Infrastructure:** `sessions_admin.go` imports `authdomain` and uses domain types throughout; local struct definitions removed. **iam/delivery:** `sessions_handler.go` + `sessions_handler_test.go` drop `authpg` alias entirely; `SessionAdmin` interface + `handleSessions` body reference `authdomain.*` only. **Wiring:** composition root unchanged — `authpg.Repository` still satisfies `iamdelivery.SessionAdmin` (structural typing). **Grep proof:** `auth/infrastructure` → 0 matches under `internal/modules/iam/`. `go build ./...` ✓ · `go vet ./...` ✓ · `go test ./internal/modules/iam/... ./internal/modules/auth/...` all green (13 packages) |
| 2.8 | Delete `platform/security/ratelimit.go`; activate `platform/ratelimit` in prod wiring | F-05, D-04 | ✅ | `0b41d1c1` | **Old→New policy:** fixed-window 120 req/60s per user→IP → token-bucket `RouteGlobalEnvelope` 120 req/min per user→IP (strictly equivalent; burst=quota). **Chain:** `rate_limit` slot now `globalLimiter.GlobalEnvelopeWrap(userIDExtractor, next)` — same position, same name, chain-order test stays green. **Documents:** `docMod.RegisterRoutesWithRateLimit(mux, globalLimiter, userIDExtractor)` — autosave presign (60/min), autosave commit (30/min), export PDF (20/min) now active. **Deleted file:** `internal/platform/security/ratelimit.go` (~200 lines, fixed-window impl importing auth/domain + iam/domain — REQ-TOP-2 violation). **Cilint baseline:** `"internal/platform/security"` entry removed from `platformBoundaryAllowed`. **Dead env vars:** `METALDOCS_RATE_LIMIT_ENABLED` / `_WINDOW_SECONDS` / `_MAX_REQUESTS` no longer parsed at startup. **Tests:** eviction + middleware unit tests migrated from `security.RateLimiter` → `GlobalEnvelopeWrap`. `go build ./...` ✓ · `go vet ./...` ✓ · `go test ./apps/api/... ./internal/platform/ratelimit/... ./internal/platform/security/... ./internal/modules/documents/... ./tools/cilint/... ./tests/unit/...` all green · `go run ./tools/cilint/...` exit 0 · grep `security\.NewRateLimiter`: 0 in .go files |
| 2.9 | N+1 fixes: RolesByUserID join, batch variant, VerifyUserInTenant EXISTS, approval inbox batch-load, audit ILIKE restriction | F-10, REQ-DATA-2, REQ-AUTHZ-6 | ✅ | `db384188` | **(a)** `RolesByUserID`: 2 queries→1 LEFT JOIN; unknown/inactive=0 rows→ErrUserNotFound, active+no-roles=1 NULL row→ErrNoRolesAssigned, active+roles=N rows. **(b)** `RolesByUserIDs(tenantID, []string)` batch added to `domain.RoleProvider` + postgres impl (`= ANY($1) pq.Array`) + `CachedRoleProvider` read-through (cache hits served, misses batch-fetched + stored); `auth.Service.ListUsers` now calls batch. **(c)** `VerifyUserInTenant`: new `TenantMemberChecker` port → single EXISTS query on `iam_users`; fallback to full list for dev/memory mode. **(d)** `ApprovalRepository.LoadInstancesByIDs` (3 round trips: headers + stages + signoffs → replaces N×LoadInstance in `ListPendingForActor`); order preserved. **(e)** Audit ILIKE drops `payload::text` scan; restricts to `action`, `actor_id`, `resource_id` only — OpenAPI `q` description is "action / payload summary", payload-content search is undocumented and was causing full table scans. `go build ./...` ✓ · `go vet ./...` ✓ · `go test ./internal/modules/iam/... ./internal/modules/auth/... ./internal/modules/documents/approval/... ./internal/modules/audit/... ./tests/unit/...` all green |
| 2.10 | Postgres-backed auth-failure counter (replace in-memory) | F-20e | ✅ | `9921b323` + `6e2bf39d` (critical fix) | Migration `0235` (`public.auth_failure_counters`). `PostgresAuthFailureRateLimiter` in `infrastructure/signature/` — exact semantics as in-memory (fixed 60s window, maxFailures=5, keyed by actorID). Wiring: `reauth.go` `newSignoffReauthRegistry(repo, db)` uses PG impl when `db != nil`, in-memory for dev/nil. **Critical fix (commit F-20e):** original `RecordFailure` passed `time.Duration` as `$3`; pgx/v5/stdlib encodes duration as bigint → `timestamptz + bigint` operator undefined in Postgres → every call errored at runtime. Fix: precompute `windowExpiredBefore := now.Add(-windowDur)` in Go, pass only timestamptz params (`$2=now`, `$3=windowExpiredBefore`). SQL uses pure timestamp comparison (`window_start <= $3`). **Live probe** (`-tags integration`, `TestPostgresLimiter_Live`): (i) count=1 ✓ (ii) count=2 ✓ (iii) Allow=false after 5 failures ✓ (iv) expired window resets count=1 ✓ (v) Reset→Allow=true ✓ · `go build ./...` ✓ · `go vet ./internal/modules/documents/approval/infrastructure/...` ✓ · `go test ./internal/modules/documents/approval/infrastructure/signature/` → 16 tests green. Dictionary page: `wiki/database/tables/auth_failure_counters.md` |
| 2.11 | Dead-code deletion PRs (CutoverService, CompositionConfig, SetParent, fallbacks, coverage_boost_test) + remove deprecated govLogger fallback | F-14, F-07-sub, D-06, CWE-561 | ✅ | `63f74368` | **Reference-check method:** grep across whole repo (GitNexus index stale — grep + compiler used as source of truth). **Deleted:** (1) `cutover_service.go` + `cutover_service_test.go` — zero non-test callers confirmed; (2) CutoverService section (section 8, ~50 lines) from `coverage_boost_test.go` — `authz` import dropped; `noopStmt` added to preserve `beginFailConn.Prepare`; remaining 16 sections cover real production code; (3) `CompositionConfig` struct from `templates/domain/schemas.go` — zero production callers; `TestCompositionConfig_RoundTrip` deleted from `schemas_test.go`; (4) `AreaService.SetParent` + 3 tests (`TestAreaServiceSetParentValid/FailsWhenCycleDetected/RequiresParentCode`) + `areaCodePtr` helper — zero non-test callers; `strings` import dropped from `area_service.go`; (5) `resolvePermissionFallback` from `permissions.go` — logic inlined at call site (was single-default switch returning VisibilitySessionRequired); (6) `WorkerConfig.ReviewReminderDays` field + env-var parse block + startup log token — METALDOCS_WORKER_REVIEW_REMINDER_DAYS removed from `.env.example` and `docker-compose.yml`; (7) `areas`/`visibility`/`specific_areas` orphaned INSERT columns from `templates/repository/postgres.go:CreateTemplate` — columns remain in DB (nullable/defaulted); INSERT rewritten to 12 columns; (8) `DBGovernanceLogger` nil-fallback in `controlleddocuments/module.go` replaced with fail-loud panic; `taxonomydomain` import dropped; `DBGovernanceLogger` type kept (taxonomy/module.go still references it); (9) `internal/platform/config/ratelimit.go` deleted — `RateLimitConfig`+`LoadRateLimitConfig` had zero callers post-Wave 2.8; METALDOCS_RATE_LIMIT_* vars removed from `.env.example` and `docker-compose.yml`; (10) F-14 nil-guard: `NewDecisionService` panics if `pdfDispatcher` non-nil; main.go updated to pass nil + `fanoutComponents.pdfDispatchAdapter` field + dead assignment removed. **`go build ./...` ✓ · `go vet ./...` ✓ · all targeted tests green · `go run ./tools/cilint/...` exit 0** |

| 2.12 | Fallback elimination — ports (db.Tx seam, Unwrap chain deletion, DBGovernanceLogger deletion, RoleProvider.UserActiveInTenant) | F-07-sub, F-10, M-1, M-6 | ✅ | `c02c1d8f` · `e3bf7b62` · `56477596` · `242b301a` · `28b533a1` | Step 1/4 (db.Tx interface) complete — commit c02c1d8f. **Step 2/4 complete (this session):** all `*sql.Tx` parameters in domain port interfaces across audit, governance, authz-path, documents, templates, approval, controleddocuments → `db.Tx`; `taxonomyTx.Unwrap`/`familyTx.Unwrap`/`sqlTxFromFamilyTx` deleted; `FamilyTx` now embeds `db.Tx`; `mustSQLTx` assertion added in documents/repository for authz boundary; test fakes updated across 25+ files. `go build ./...` ✓ · `go vet ./...` ✓ · `go test ./...` ✓ (all green). **Step 3/4 (DBGovernanceLogger deletion + Proof A re-run, 2026-06-12):** `internal/modules/taxonomy/application/governance_logger.go` deleted entirely — `DBGovernanceLogger`, `NewDBGovernanceLogger`, `Log`, `LogTx`, `nullString` helper (unused elsewhere), and the `// Deprecated` notice all removed. `taxonomy/module.go` gains fail-loud panic guard requiring `AuditWriter` at construction (same pattern as 2.11 controlled-documents); `context`/`slog`/`domain` imports dropped; `govLogger` var renamed `logger`. `module_test.go` updated with `stubAuditWriter` (implements `auditdomain.Writer`). Final grep for `DBGovernanceLogger|NewDBGovernanceLogger` in `internal/` and `apps/` returns zero live-code hits (one string in panic message only). `go build ./...` ✓ · `go vet ./...` ✓ · `go test ./internal/modules/taxonomy/...` all green. **Proof A re-run:** POST `/api/v1/taxonomy/families` body `{"code":"FAM-TEST-TASK3","name":"Task 3 Test Family","description":"Wave 2.12 DBGovernanceLogger deletion proof"}` → HTTP 201; `SELECT id, action, resource_type, resource_id, occurred_at FROM metaldocs.audit_events WHERE resource_type='document_family' ORDER BY occurred_at DESC LIMIT 1` → row `id=fa87d3a8-4964-463e-9a91-d79c9f3da94f`, `action=family.created`, `resource_id=FAM-TEST-TASK3`, `occurred_at=2026-06-12 14:31:35 UTC`. Family deactivated (DELETE 204). **Step 4/4 (UserActiveInTenant on RoleProvider + TenantMemberChecker deletion + memory M-6 fix, 2026-06-12):** `UserActiveInTenant(ctx, tenantID, userID) (bool, error)` added to `domain.RoleProvider` interface; all implementors updated: `postgres.RoleProvider` (method already existed — reattached to interface), `CachedRoleProvider` (pass-through, not cached — deactivation-sensitive), `DevRoleProvider` (tenant-ID guard + known-user set check), `auth/infrastructure/memory/repository.go` (active check + tenants map lookup). M-6 fix: `RolesByUserIDs` in memory repository now filters by tenantID via `r.tenants[uid]` (previously ignored tenantID — cross-tenant membership silently passed in dev mode). `TenantMemberChecker` interface deleted from `people_service.go`; `tenantChecker` field removed from `PeopleService`; `WithTenantMemberChecker` builder deleted; `VerifyUserInTenant` now calls `s.roles.UserActiveInTenant` directly (single EXISTS path, no fallback). `main.go` type-assert block + `slog.Warn` fallback deleted. Test stubs updated: `mockRoleProvider` (auth/service_test), `stubRoles` (iam/application/people_service_test), `fakeRoleProvider` (tests/unit/iam_people — returns true), `peopleRoleProviderShim` (tests/unit/iam_admin_handler — returns true), `tenantAwareRoleProvider` (cross_tenant_test — consults tenantAwareAuth.usersByTenant for honest tenant-scoped result). `git grep -n "TenantMemberChecker|WithTenantMemberChecker" -- internal/ apps/` → zero hits. `go build ./...` ✓ · `go vet ./...` ✓ · `go test ./internal/modules/iam/... ./internal/modules/auth/... ./apps/api/... ./tests/unit/...` all green. Live integration probe `TestRoleProvider_UserActiveInTenant_Live`: `admin/ffffffff-...` → `active=true` ✓; `00000000-.../ffffffff-...` → `active=false` ✓. |

| 2.13 | Fallback elimination — application services (single-mode refactor: remove dual-mode nil-db branches; Postgres-only auth-failure limiter; no in-memory fallback) + dead-schema migration 0236 + cilint seam locks | F-07-sub, F-20e, F-08, F-14, ADR-0022 | ✅ | `75ad1298` · `15965ad0` · `dce4c81d` · `d7ab39ba` · `ab949015` · `49e329be` · `0ab280b0` | **Step 1/3 — single-mode refactor (2026-06-12):** class-A nil-db dual-mode branches removed from templates/application (autosave/create/lifecycle/schema/approval_config), documents/application (service), controlleddocuments/application (incl. the `db==nil` authz-bypass class-B branch in `Create`, user-approved per fact-check gate), documents/approval/application, and auth/application. `InMemoryAuthFailureRateLimiter` deleted; `PostgresAuthFailureRateLimiter` wired unconditionally (`reauth.go` drops `db != nil` branch). `auth.Service.loginCtxPort` promoted to required constructor dep (fail-loud panic on nil; `service.go:130`). `RepositoryMemory` production path deleted from `internal/platform/config/repository.go` + `internal/platform/bootstrap/api.go`; memory repositories retained as test fixtures only. Test fakes/fixtures repaired across templates/documents/controlleddocuments/auth (permissive sqlmock DB for handler-routing tests now that authz uses an unconditional tx; counting rate-limiter fake for reauth-trip test). `go build ./...` ✓ · `go vet ./...` ✓ · targeted module tests green. **Residual eliminated (follow-up commit, 2026-06-12):** the `if s.db != nil` guard in `FillInService.SetPlaceholderValue` and `NewFillInServiceNoAuthz` test-only ctor are deleted; `requireDocEditDraft` now runs unconditionally; 7 validation tests reconstructed via `NewFillInService` with a permissive sqlmock DB (`newPermissiveFillInDB`); zero dual-mode branches remain in `internal/modules/**/application/`; `nodualmode` empty baseline is now valid. **Step 2/3 — dead-schema migration 0236 (2026-06-12):** `db/migrations/0236_dead_schema_drop.sql` drops `public.templates_template.areas/visibility/specific_areas` (the real table `CreateTemplateTx` writes — `INSERT INTO templates_template`; the plan's assumed `metaldocs.templates` does not exist, implementer corrected), `metaldocs.document_profiles.is_active` (superseded by `archived_at`/0122), and `metaldocs.document_subjects` (DROP TABLE CASCADE — removed dangling FK `fk_documents_subject_code`; the now-orphan `metaldocs.documents.subject_code` column + index left for a separate surgical pass, not in FE-5 scope). Same-commit writer removals: `templates/repository/postgres.go` `CreateTemplateTx` INSERT, `db/reference-data/0001_product_reference_data.sql`, and `internal/test/e2e_seed.go:435` (document_profiles INSERT). Re-verified zero production refs at commit time. **Live-applied** on dev Postgres: `0236` ledgered in `public.schema_migrations`; `information_schema.columns` confirms 0 tables retain the 3 columns; `to_regclass('metaldocs.document_subjects')` → NULL. **Post-drop smoke** (rebuilt binary): login 200 · `POST /api/v1/templates` → **201** (CreateTemplateTx works without the dropped columns) · `GET /templates` list → 200. Dictionary pages updated (`templates_template.md`, `document_profiles.md` column rows removed + Last-verified notes; baseline DDL blocks left as literal snapshots; `document_subjects.md` retired; `dictionary-index.md` row removed). `go build ./...` ✓. **Step 3/3 — cilint `nosqltxindomain` + `nodualmode` seam locks (2026-06-12):** two AST analyzers added (siblings of `platformboundary`/`postcommitaudit`), wired into `RunAll`; CI-gated by `tools/cilint/main.go` (os.Exit(1) on any finding) under `.github/workflows/invariants.yml`. `nosqltxindomain` — scoped to the transaction/execution seam (`sql.{Tx,DB,Rows,Row,Result}`, pointer or not) under `internal/modules/**/domain/`; `sql.Null*` nullable helpers intentionally allowed (the `auth/domain/session_admin.go` row-DTO idiom is not a driver leak). `nodualmode` — flags db/port nil-dual-mode `if` branches under `internal/modules/**/application/`, exempts fail-loud precondition guards (`if x == nil { panic/return }`, e.g. `auth.Service` loginCtxPort). **The analyzers surfaced 9 real violations the Step-5 fact-check missed (its `internal/modules/**/...` git pathspec `**` glob silently matched nothing).** Disposition: (a) `controlleddocuments/domain/sequence.go` local `DBTX` interface (a `db.Tx` duplicate importing `database/sql` for `sql.Result`/`*sql.Rows`/`*sql.Row`) → replaced with `db.Tx` (commit `d7ab39ba`) — clears `nosqltxindomain` to **0**; (b) `iam/application/area_membership_service.go` governance logger was `nil` in prod (tech-debt **T-007**) → made a required port + real `membershipGovernanceLogger` adapter wired in `main.go`, then demoted to best-effort to avoid a post-commit torn write (commits `ab949015` + `49e329be`); (c) **4 justified `//cilint:allow-dualmode` directives** — `audit/application/service.go:178` (`s.writer` is a feature gate: nil only when the export pipeline is disabled, not a db fallback) and `documents/application/freeze_service.go:175,308,370` (ADR-0015 optional-tx-enlistment: both branches perform the write, differing only on tx; deferred at 2.11). **Deferred triggers (next-touch / Wave-3):** audit-writer hard-required refactor of `WithExports`; freeze in-tx-only collapse; membership in-tx governance via `RecordTx` (T-007). `go build ./...` ✓ · `go test ./tools/cilint/...` ✓ · `go run ./tools/cilint/...` **exit 0** (zero `nosqltxindomain`/`nodualmode` findings). **Row 2.13 + Row 2.12 CLOSED.** |

## Wave 3 — trigger-gated (NOT executed proactively)

Tracked as written triggers; full table in `stage2-evaluation.md` §Wave 3. Headline triggers: OTel/W3C (F-17) → "second host or first external-tenant SLA" · CD trigram index (F-20b) → ">50k rows or p95 >200ms" · readiness concurrency (F-16C) → "second dependency added" · RLS remaining tables (F-09d/F-12 tail) → "first external tenant" · others → "next touch of that area".

## Wave F — FINAL FULL REVIEW (fresh session, after user approves Wave 2 close)

The user-mandated gate: prove the backend is solid before declaring the program done.

| # | Check | Status | Evidence |
|---|-------|--------|----------|
| F.1 | All 6 CI guards active and green: gitleaks · platform-import boundary · post-commit-audit ban · middleware chain-order test · nosqltxindomain · nodualmode | ✅ | 2026-06-12 (session #5). `go run ./tools/cilint/... ./...` **exit 0** (platformboundary + postcommitaudit + nosqltxindomain + nodualmode + legacy TxOwnership/LegacyVocab/OutboxPair all clean). Chain-order test `go test ./apps/api/cmd/metaldocs-api/ -run Chain\|Order\|Middleware` → `TestAPIChainOrder_REQMW7` + `TestBuildChain_SkipsNilLinks` **PASS**. Gitleaks: accepted on Wave 0 evidence (exit 0 over `git archive HEAD` + `.gitleaks.toml`) — not re-run this session; history residual closed-by-plan (D-4b fresh-repo re-baseline at first release, owner-confirmed 2026-06-12). NB: a local `--source` working-dir scan red-lines on 368MB of **untracked** junk (`.gitnexus/` cache, `.env`, `node_modules`) the CI `actions/checkout` gate never sees |
| F.2 | Full static pass: `go build ./...` · `go vet ./...` · full test suite · `api-lint` — zero blocking findings | ✅ | 2026-06-12. `go build ./...` **exit 0** (empty log) · `go vet ./...` **exit 0** (empty log) · `go test -p 2 ./...` **exit 0 — 86 ok / 0 FAIL / 28 no-test** (`-p 2` per C: SSD write pressure) · `go run ./scripts/api-lint/ -strict api/openapi/v1/openapi.yaml .` → **0 violation(s)** |
| F.3 | Runtime QA (Docker up): `.\scripts\start-api.ps1` · login · backend-api-qa-checklist smoke · worker+jobs containers process real work · scheduled publish fires · every 🔁 `[runtime-unverified]` item from Waves 0–2 re-checked live | ✅ | 2026-06-12 (session #5). Stack up; API started via `.\scripts\start-api.ps1 -Build` (repository=postgres), worker fresh-built local process. **Smoke:** login 200 (cookie session `metaldocs_session`); authed `GET /iam/users` 200. **All 5 runtime re-checks LIVE:** (1) **RLS cross-tenant** — NOSUPERUSER probe on `audit_events`: GUC-unset→both tenants, GUC=A→only A (1,0), GUC=B→only B (0,1); (2) **in-tx governance** — `POST /taxonomy/families` 201 → `audit_events` `family.created` row in the mutation tx; (3) **rate-limit 429** — pre-auth login burst → `401×5 → 403×6 (account lockout, F-20e/auth) → 429×2 (pre-auth IP limit)`; (4) **panic recovery** — `/internal/test/panic` → 500 `application/problem+json` `INTERNAL_ERROR`, process served next request (alive); (5) **401 in metrics** — `/api/v1/metrics` RED counter `/iam/users requests:3 errors:2` (unauth 401s counted → httpObs outside authn, REQ-MW-4). **Worker real work (LIVE):** inserted `pdf_dispatch_outbox` row → metaldocs-worker claimed (claimed_at) + relayed `docgen_v2_pdf`/`document_revision` to `outbox_events` (dispatched_at) in ~0.1s. **Jobs real work (LIVE):** inserted `scheduled_publish_cutover` River job (queue temporal, past schedule) → live jobs host claimed it (`attempted_by=091b8a2ebd9e…`) + executed `RunScheduledPublishJob` (errored on intentional fake doc id = proof Work() ran) → discarded. **Full doc→PDF render (LIVE):** built a `public.documents`+`document_revisions`+`editor_sessions` fixture, uploaded the real `purchase-order.docx` fixture to MinIO, `POST /documents/{id}/export/pdf` → **200, size_bytes 11480, cached:false** → docx-renderer+gotenberg rendered a real `%PDF-` object in MinIO + `document_exports` row + `export.pdf_generated` audit; warm re-export → **cached:true** (export cache works). All probe fixtures + MinIO objects cleaned up (bucket back to 0). **Out-of-program observations (not program defects):** (a) `compose-docx-renderer-1` was Exited(255) ~20h — ops/infra, restarted by owner mid-session; (b) e2e `seed`/`reset` handlers use raw `os.Getenv("METALDOCS_E2E")!="1"` while `main.go` uses `TrimSpace(...)=="1"` → handlers 404 when the env value carries trailing whitespace (test-harness inconsistency, out of program code scope) |
| F.4 | Legacy-register sweep: every F-*/D-* entry marked **resolved (commit)** or **deferred (trigger)** — no silent leftovers | ✅ | 2026-06-12. Walked all 20 F-* + 7 D-* entries against `stage2-evaluation.md` verdicts. Found 7 silent leftovers (no resolution AND no defer note): **F-02, F-04, F-15, F-17, D-02, D-05, D-07** — each given an explicit `**Deferred (...)**` paragraph citing the Stage-2 verdict + written trigger. Added a sweep tally to the register's closing section (Resolved / Partially-resolved+trigger / Deferred+trigger). No silent leftovers remain |
| F.5 | `backend-blueprint.md` maturity grades re-scored; REQ-* compliance checked against `backend-target-architecture.md`; deltas explained | ✅ | 2026-06-12. Added §7 "Wave F re-score" block: grade-delta table (C6 async + D5 audit + D3 security ✅ now *earned* — open F-19/F-07/F-18 defects under those ✅ are closed; D2/C4/D7/D8 🟡 clarified to trigger-gated defers) + explicit **NOT-promoted** rationale (B2 authz stays 🟡 — ADR 0022 Phase 6 + RF-3 open; A3 contract stays 🟡 — api-contract-hardening C–F residual). Added a 15-row **REQ-* compliance table** (REQ-MW/TOP/ASYNC/REL/AUTHZ/TEN/SEC/API/H met + CI-locked; REQ-OBS/CACHE deferred with triggers). Updated D2 owner (audit done, deferred by design), D9 analyzer count (6→7 + 6 program CI guards), OTel standards row. Stamp → 2026-06-12 |
| F.6 | Full-program code review (`/code-review` over the cumulative program diff); findings dispositioned by family | ✅ | 2026-06-12. Cumulative diff `d3f5dc62b..HEAD` = 325 files / +26681 (199 code files). Fanned out **5 parallel sonnet review subagents** over concern slices (middleware/obs · async/audit · authz/RLS · boundaries/single-mode · contract/CI), each hunting holistic/cross-wave live defects only. **Findings dispositioned by family:** **FIXED** (commit `f698d1fd2`) — Family A (db.Tx atomicity): `PDFOutboxRepository.Enqueue` + `MaterializeOutboxRepository.Enqueue` silently autocommitted on nil tx → fail-loud (3 prod callers pass real tx; idempotent test → real tx; nil-rejected regression test added); Family C (observability): audit `ExportEvents` silent governance-error discard → logged at warn. **NOT-FIXED, dispositioned:** B-1 (CD ctor nil-`db` guard) — guards an impossible-in-prod scenario (module.go always passes real db) and breaks 19 tests that intentionally pass nil db; reverted per CLAUDE.md 5.2 "no error handling for impossible scenarios". D (CD handler raw `INTERNAL_ERROR`/`IDEMPOTENCY_KEY_REQUIRED`/`VALIDATION_ERROR` literals) — emitted values are **correct**; only the F-09 catalog guard doesn't cover them; guard-scope expansion touches `httpresponse.WriteError`'s signature → next-touch (extends F-09 family). **ACCEPTED (prior disposition):** PG auth-failure limiter check-then-act race — D-1 single-replica, ±1 tolerance, fail-closed. **Verify:** `go build ./...` 0 · `go vet` 0 · `go run ./tools/cilint/...` 0 · `go test -p 2 ./...` **86 ok / 0 FAIL** (incl. new regression) |
| F.7 | Wiki coherence pass (wiki-curator): anchors, stamps, indexes match post-refactor reality | ✅ | 2026-06-12. `wiki/backend/index.md`: stamp bumped; Mermaid jobs node label corrected; V-01/V-03 violations marked closed (Waves 2.7/2.8); §6.3 dead-code rows for items deleted in Waves 2.11/2.13 struck through with resolution; §7.2 dual-sink section rewritten to reflect program closure; §8 severity summary updated. `wiki/modules/audit.md`: header line-ref corrected (service.go:182→:183); Wave F ExportEvents slog.Warn fix noted in header + §8.5. `wiki/modules/render-fanout.md`: stamp bumped; fail-loud Enqueue nil-tx contract added to Failure modes table; duplicate header removed. |
| F.8 | Closing report in handoff: every wave's evidence, all defers with triggers, ADR links, program sign-off | ✅ | 2026-06-12. Closing report appended to [`current-agent-handoff.md`](../references/current-agent-handoff.md) (### Wave F closing report — PROGRAM COMPLETE): per-wave evidence summary (0/1/2/2.12-2.13/F), all defers with written triggers (Wave 3 + next-touch + owner-waived), ADR links (0009/0015/0022/0026/0027; 0028 N/A — no new cap), and the program sign-off statement against design-spec §10. Program-map Wave F row + Status header flipped to COMPLETE. |

**Program DONE = all F-rows ✅ — REACHED 2026-06-12.** F.1–F.8 all ✅. Evolution now goes through the normal REQ/ADR process; this roadmap is frozen as the historical record. Final integration merge awaits the user's sign-off (agent did not merge, per directive).

---

## Wave Z — SEALED-SCOPE ZERO-DEFER CLOSE-OUT (fresh session, 2026-06-13) — FROZEN ADDENDUM

The operator overrode D-1/D-3 and ordered a zero-defer finish (register-zero + ops-ready) before the v1 release. Executed per [`docs/superpowers/specs/2026-06-12-wave-z-sealed-closeout-design.md`](../../docs/superpowers/specs/2026-06-12-wave-z-sealed-closeout-design.md) + [`wave-z-plan.md`](wave-z-plan.md). Anti-circle rule honored: scope = the sealed manifest only; pre-existing mid-wave finds parked in [`post-v1-backlog.md`](post-v1-backlog.md). One commit per task.

| Z-ID | Item | Commit | Finding / REQ |
|---|------|--------|----------------|
| Z-1 | Minimal OTel (otelhttp + W3C traceparent + autoexport, env-gated inert) | `c787ddfa1` | F-17, RF-1, REQ-OBS-1/2/3 |
| Z-2/Z-3 | RLS on all 27 remaining tenant tables + idempotency tenant FK | `ad70f6415` (+`4d4a8495d`) | F-12 tail, RF-6, REQ-TEN-1, F-09d, ADR 0027 executed-in-full |
| Z-4 | Audit export writer hard-required (allow-dualmode retired) | `0a578446a` | T-012 |
| Z-5 | Freeze service tx-mandatory (ADR 0015 amended, allow-dualmode retired) | `6075c82a3` | T-013 |
| Z-6 | Membership governance in-tx via LogTx | `c7b10f3d6` (+`abc9afa48`) | T-007, REQ-ASYNC-1 |
| Z-7 | Taxonomy reads template versions via templates/domain port | `e50150506` | F-06e, REQ-TOP-1 |
| Z-8/Z-9 | Security boundary accepted-note + CD repo via module | `730ea426b` | F-06 residuals |
| Z-10 | Generic staging outbox repo/worker, dead loop deleted, idemp dedup | `3367570c6` (+`029003c2d`) | F-04 |
| Z-11 | Single MinIO byte-IO client reused from bootstrap | `819816a7e` | D-02 |
| Z-12/Z-13 | ParseBoolEnv consolidation + DocumentStatus enum completion | `a342da46d` | F-15, D-07 |
| Z-14 | CD handler typed problem codes + guard (pre-wave spawned task) | `307666fd6` | F.6-D, F-09 |
| Z-15 | Idempotency TTL single const | `bc502ae70` | F-09c |
| Z-16/Z-18 | CreateManagedUserRequest roles enum → canonical 8 + deprecated:true + regen | `ca1017f0d` | F-11 residual, F-13d |
| Z-17 | Dead openapi partials removed | `f969af3a9` | F-13c |
| Z-19 | Split iam/audit/security codegen packages | `ee4bf01fa` | F-13e |
| Z-21 | log.Printf → slog sweep (13 files + mains/worker) | `12a752d05` (+`bfe1e0e2a`) | F-02, REQ-OBS-1 |
| Z-22 | WS presence drain on shutdown | `e7449e830` | F-16B, RF-9, REQ-REL-2 |
| Z-23 | Concurrent readiness checks (errgroup, shared budget) | `8a99124c0` | F-16C, RF-9 |
| Z-24 | pg_trgm GIN index for CD search ILIKE | `46b7582e0` | F-20b |
| Z-25 | Drop orphan documents.subject_code (migration 0238) | `62503a876` (+`4d4a8495d`) | CD T-010 |
| Z-26 | gitleaks full-history scanning + triaged allowlist (0 leaks over 4917 commits) | `26c446164` | 1.11, F-18 round-2 |
| Z-27 | ADR lifecycle audit (canonical Status headers, stub 0003→Historical, stray→0028, index rebuilt) | `4990095d0`·`3be2f9270`·`a5b26c477`·`6e92ee28e`·`c50bb9123`·`ccd888de5` | operator ask |
| Z-28 | ADR 0022 Phase 6 wiki sync (program fully executed) | `c82dadfb1` | ADR 0022 |
| Z-29 | Cache contracts + invalidation-path verification | `79f946df9` | RF-3, D-05, REQ-CACHE-1 |
| Z-30/Z-31 | Feature-flag lifecycle standard + messaging/servicebus fence | `bcab41710` ⚠ (commit message mislabeled "Z-27 group 3"; files are correct) | RF-8, RF-7 |
| Z-32 | v1 release re-baseline runbook | `aba87b673` | D-4b, F-18 closure |
| Z-33 | DONE gate G1–G11 + close-out docs | (final) | G5–G8, G11 |

**Excluded forever (KEEP / at-release — written rationale, NOT defers):** F-09a finalize inline idempotency (KEEP), F-20f sequential security queries (KEEP), `splitCSV` duplication (KEEP), F-18 git-history residual (closes physically at the Sunday re-baseline via the Z-32 runbook).

**DONE gate (spec §2) — all green 2026-06-13:** G1 build+vet 0 · G2 `go test -p 2 ./...` 87 ok/0 FAIL · G3 api-lint 0 · G4 cilint 0 **+ zero allow-dualmode** · G5 manifest empty · G6 register closed · G7 blueprint all-green · G8 ADR index trustworthy · G9 runtime smoke (inert+console OTel, RLS NOSUPERUSER, in-tx governance, 429+403, panic→500) · **G10 regression-only review: ZERO Wave-Z-caused defects** (2 critical claims raised + both refuted with evidence) · G11 deliverables (post-v1-backlog, Z-32 runbook, handoff close-out). **NOT merged — awaiting operator sign-off + Sunday v1 release via Z-32 runbook.**

---

## Wave H — Independent Architecture Audit remediation (2026-06-13, fresh session)

> **Trigger:** independent 39-agent architecture audit ([`_artifacts/architecture-audit-2026-06-13.md`](_artifacts/architecture-audit-2026-06-13.md)) graded the backend **B** (7×B, 3×C) and surfaced ~23 NEW, mostly *structural* defects the Wave 0–Z register never catalogued. Goal: lift the 3 C-grade dimensions (module boundaries, contract layer, composition/observability) to A, fix the 6 MUST-FIX defects, close the program.
> **Branch:** `qa/iam-area-membership`. One commit per defect family, tracker + audit disposition updated same-commit. Do NOT merge — operator review gate.
> **Detailed drift-proof execution plan (per-task files/steps/verify/commit):** [`wave-h-plan.md`](wave-h-plan.md) — read it before executing any row.
> **Skeptic dispositions (NOT re-raised):** oapi `NewStrictHandler` text/plain (REFUTED, dead code) · `tenantIDFromContext` DevTenantID fallback (REFUTED, idempotency-only) · CD/taxonomy infra importing `iam/authz` (intentional, ADR 0007) · role-name check / GeneratedServerAdapter / `authn` live os.Getenv (DOWNGRADED Minor).

### Tier 1 — MUST-FIX (block frontend / security)

| # | Sev | Defect | Status | Commit | Evidence |
|---|-----|--------|--------|--------|----------|
| A1 | 🔴 CRITICAL | `statusWriter` lacks `Unwrap()` → WS upgrade 501 in prod; `/iam/presence/stream` dead | ✅ | (this commit) | Added `func (w *statusWriter) Unwrap() http.ResponseWriter`. Verified `vendor/github.com/coder/websocket/hijack.go:22-28` walks the `Unwrap()` chain (mirrors `http.ResponseController`), so the upgrade now reaches the real Hijacker. `go build ./internal/platform/observability/...` ✓ · obs tests ✓. **Runtime WS-upgrade proof at Wave H close.** |
| A2 | 🔒 Major | LIKE wildcard injection (`opts.Q` unescaped, CWE-943) | ✅ | (this commit) | `buildDocumentFilter` now wraps `opts.Q` with `sqlescape.LikeEscape()` + `name ILIKE $N ESCAPE '\'`. New `TestBuildDocumentFilter_EscapesLikeWildcards` asserts `%a\%b\_c\\d%` + ESCAPE clause. repo tests ✓. |
| A3 | 🔒 Major | self-service password change does not revoke sessions (CWE-613) | ✅ | (this commit) | `ChangePasswordForUser` now ends with `RevokeSessionsByUserID` (mirrors `AdminResetPassword`). New `TestChangePasswordForUser_RevokesSessions`: session resolves before, fails to resolve after. auth tests ✓. |
| A4 | 🔒 Major | sliding idle timeout ships disabled (default 0) + absent from deploy artifacts | ✅ | (this commit) | `config.go` default `sessionIdleMinutes` 0→30 (backend-standardization param); `METALDOCS_AUTH_SESSION_IDLE_MINUTES=30` added to `.env.example` + `docker-compose.yml`. Behavior covered by existing `TestResolveSession_IdleTimeout` (30m window). |
| A5 | 📑 Major | `PATCH /iam/users/{id}` returns ad-hoc map, spec declares `ManagedUserCore` (breaks FE codegen) | ✅ | (this commit) | `handlePatch` now re-reads via new `PeopleService.Get` and returns `toManagedUserCore(updated)`. Spec already declared `ManagedUserCore` (openapi.yaml:263) → no spec/codegen change; FE types already match. iam delivery tests ✓. |
| A6 | 📑 Major | `GET /documents` + `/documents/{id}` return domain structs / `map[string]any`, not generated types | ✅ | (this commit) | `listDocuments` builds `documentsapi.DocumentListResponse` (+`CursorPage`); detail returns `*documentsapi.DocumentDetailResponse`. New `toDocumentSummary`/rewritten `toDocumentDetailResponse` map domain→generated. Spec already declared both (openapi.yaml:2333,2355) → no regen. documents delivery tests ✓. |

### Tier 2 — Architecture debt (lifts the 3 C-grades)

| # | Family | Status | Commit | Evidence |
|---|--------|--------|--------|----------|
| H-1 | Module boundaries (split — see [wave-h-plan §H-1](wave-h-plan.md)): **H-1a** `setAuthzGUC` ×4 dedup → `authz.SeedTxIdentity`. **H-1b** CD→taxonomy read port. **H-1c** approval delivery↛infrastructure. **H-1d** `*sql.DB`→`TxRunner` port. **H-1e** documents delivery redesign (delete `GeneratedServerAdapter`) | ✅ H-1a,b,c,d-approval ✅ / d′-templates ✅ / d′-CD ✅ / d″ ✅ / e ✅ | H-1a..d,d′-tpl,d′-CD: this wave | H-1a: deleted the 4 `setAuthzGUC` copies (approval/templates/CD) → canonical `authz.SeedTxIdentity` (adds empty-string guards + batched single query the copies skipped); taxonomy keeps its ctx-resolving wrapper but delegates the GUC SQL (24 sites untouched — inlining resolution ×24 is worse DRY). Test fixtures migrated two-query→batched-2-arg form. Gates green: build/vet/test -p2/api-lint -strict/cilint. Contract-neutral. **H-1b:** deleted CD's duplicate `TaxonomyProfileReader`/`TaxonomyAreaReader` (re-implemented `GetByCode` **without** the authz GUC + `CapTaxonomyView` and **omitting `alias`**) + orphaned `setNullableStringPtrField` reflection helper + unused `reflect`/`taxonomydomain` imports; CD now reads through the canonical taxonomy repos via two thin segregated adapters (`taxonomy_reader.go`) on the **request ctx** — every CD-creator role already holds `taxonomy.view`, so the previously-skipped cap is now enforced with zero behavior break (the plan's background-bypass idea is fail-closed on request paths, so rejected). Regression test added. Gates green: build/vet/test -p2/api-lint -strict 0/cilint 0. Contract-neutral. **H-1c:** moved `SignoffReplay`/`SignoffReplayCommitter` from `approval/infrastructure` → new `approval/application/signoff_idemp.go` (mirrors the existing `route_admin_idemp.go` precedent); infra references the application types; delivery (`handler.go`/`doc_approval_handler.go`/tests) drops the root-infra import → prod delivery→root-infra inversion now **0**. Bounded defer recorded: `errors.go` still maps `infrastructure/signature` sentinel errors (edge error-mapping idiom, not type-coupling). Gates green. Contract-neutral. **H-1d (approval):** new `internal/platform/db.TxRunner` port (`runner.go` + `runner_test.go`) — `Do(ctx, func(tx *sql.Tx) error)` owns BeginTx/Commit/Rollback; approval **application** services no longer receive the concrete `*sql.DB` pool. Two documented deviations: (1) callback is `func(*sql.Tx)` not `func(db.Tx)` — keeps authz on `*sql.Tx` (deferred boundary #4), avoids a `mustSQLTx` assert-back at every authz call; (2) runner is built **inside** delivery/job constructors from the `*sql.DB` they already hold, so composition roots (`apps/api` main, `apps/jobs` main, `documents/module.go`) + their tests are untouched. cilint `txownership` allowlist gains `/platform/db/` (the port is the canonical tx owner — root-cause, not a suppress-directive). CD+templates field-injected `s.db` → **H-1d′** (split: templates this commit, CD next). Gates green: build/vet/test -p2 0/api-lint -strict 0/cilint 0. Contract-neutral. **H-1d′-templates:** `templates/application/service.go` `WithDB`/`DB()` → `runner db.TxRunner` + `WithRunner`; 11 `s.db.BeginTx` sites → `s.runner.Do(ctx, func(tx *sql.Tx) error{…})` faithful conversion (seed→Require→repo order, verbatim wraps, unwrapped propagation, no `WithCapCache`). Handler idempotency pool injected at composition root — `handler.New` gains 3rd `*sql.DB` arg, `main.go` passes `.WithRunner(db.NewTxRunner(deps.SQLDB))` + `deps.SQLDB`; `*sql.DB` fully gone from templates application layer. 31 test `.WithDB→.WithRunner(newTxRunner)` + helper per test package. Gates green: build/vet/test/api-lint -strict 0/cilint 0 + residual-grep empty. Review: fidelity APPROVE 0; wiring "major" non-reproducing vs disk, 1 intentional nil-db minor. Contract-neutral. **H-1d′-CD:** `controlleddocuments` field→`runner db.TxRunner`; 4 `s.db.BeginTx` sites (PeekSeq/changeStatus/CreateRevision/Create — auto-path one `runner.Do` closure keeping NextAndIncrement→CreateTx→CloneTemplate atomic); 3 pre-existing asymmetries preserved+flagged. **H-1d″:** `documents/application` field→`runner db.TxRunner` + additive `DoReadOnly` port method for the 2 read-only authz paths (view/reconstruct); 3 write sites→`Do`, fillin_authz/fillin_service→port; plain readers (context_builder, SchemaReaders, repository/) kept on raw `*sql.DB`; 8 test sites migrated. Gates green (build/vet/test -p2/api-lint -strict 0/cilint 0); 3 reviews APPROVE. `*sql.DB` field now gone from templates+CD+documents app layers — **H-1e ✅** — deleted `GeneratedServerAdapter` + `buildLegacyMux`; `delivery/http/Handler` implements `documentsapi.ServerInterface` directly (thin typed→raw delegations + explicit `ServerInterfaceWrapper` registration preserving per-route rate-limits + nil-guards); collapsed `documents/http/` into `delivery/http/` as sub-handlers (fillin/placeholder_options/view/reconstruct + UNWIRED pdf webhook); `approval/http` left as-is. Deviations: `ViewResult`→application (import-cycle break); `ErrorHandlerFunc` constant verified wire-identical to `"VALIDATION_ERROR"`; Module retains 5 sub-handler fields (test refs); fill-in dotted taxonomy preserved verbatim via file-granular `guardExcludedFiles` exclusion (canonicalization deferred → follow-up A); fill-in routes unconditional. Net −1533 LOC. Gates: build/vet/test -p2/api-lint -strict/cilint all 0. Runtime smoke (cookie session): documents 200, relocated fill-in/placeholders 200, placeholder-options/{pid} domain-404 problem JSON, wrapper UUID 400, bogus 404. Contract-neutral. Follow-ups (operator): (A) canonicalize fill-in taxonomy into catalog + drop guard exclusion; (B) delete prod-dead sub-handler RegisterRoutes after migrating export/fillin handler tests. **All H-1 sub-items resolved.** |
| H-2 | Composition/observability: extract `main.go` god-file → `wiring` builders, `os.Getenv`→typed config, `slog.SetDefault(JSON)` per binary, worker drain + jobs cleanup + per-binary OTel `service.name` | ☐ | | |
| H-3 | Persistence (split — see [wave-h-plan §H-3](wave-h-plan.md)): **H-3a** dedupe membership double-audit (delete handler post-commit dup of the in-tx `LogTx`) + extract `membershipGovernanceLogger`→`iam/application` (thread TraceID) + reject nil-tx in `PostgresSequenceAllocator.NextAndIncrement` (delete dead caller). **H-3b** (after H-1d) service-owned tx + in-tx audit for the 4 sites with *no* service tx (sessions/people/admin/auth) + cross-function `postcommitaudit` analyzer | ◑ H-3a ☐ / H-3b ☐ | | Discovery: only 1 of 5 audit sites was a "move in-tx" — it was a *duplicate* (in-tx write already exists); the other 4 need *new* tx boundaries → H-1d. |
| H-4 | Contract: expand codes-catalog guard to auth/iam/audit/security/search, typed `problem.Code` constants, add `CURSOR_EXPIRED`/`NOT_IMPLEMENTED` to catalog | ✅ | this commit | 164 literal codes→typed `problem.Code` across the 5 delivery pkgs; **5** codes promoted to catalog (the planned 2 + 3 already-shipped IAM domain codes `MEMBERSHIP_EXISTS`/`MEMBERSHIP_NOT_FOUND`/`UNKNOWN_ROLE`, same string values = zero wire change); guard 5→10 pkgs; `dump-error-codes` regen byte-identical; FE coverage green; cilint/build/vet/tests green. Status/message args untouched (501+INTERNAL_ERROR pairings preserved). |
| H-5 | Code quality: decompose `RecordSignoff` (407 lines), split documents `Handler.Service` 28-method interface (drop 4 unused), rewrite `PeopleService.ListFiltered` to SQL filter+paginate | ☐ | | |
| H-6 | Dead-code: **H-6a** delete `SnapshotFromTemplate` + collapsed writer/seeder apparatus; **H-6b** migrate `DuplicateDocument` to atomic path then delete legacy `CreateDocument` chain (after H-1d) | ✅ H-6a ✅ / H-6b ✅ | H-6a + H-6b: this commit | H-6a: deleted `SnapshotFromTemplate`, `SnapshotWriter`/`PlaceholderValueSeeder` interfaces, + the orphaned-by-deletion `SnapshotRepository.WriteSnapshot`/`ReadSnapshot` + `FillInRepository.SeedDefaults` (all zero prod callers). Gates green: build/vet/vet-int/test -p2/cilint. H-6b pending H-1d TxRunner seam. H-6b: `DuplicateDocument` migrated to atomic CD-create path; legacy `CreateDocument` chain deleted (net −406 LOC, 19 files). Contract-neutral (`POST /documents/{id}/duplicate` → `DocumentCreateResult` unchanged). **Prereq H-PRE-1 (system_admin CD-create advisory-lock self-deadlock) discovered + fixed same commit** — authz-recording template reads hoisted off the lock-holding tx; runtime-verified 409 in 83ms (was 60–90s hang), 0 advisory locks. See audit artifact §H-PRE-1. Gates green; sonnet review APPROVE-with-nits (fixed). |

**Wave H DONE gate:** all Tier-1 + Tier-2 rows ✅-or-deferred-with-trigger · static gates green (build/vet/test -p2/api-lint -strict/cilint) · runtime QA (A1 WS upgrade, in-tx audit, RLS, 429/lockout, panic→500) · FE types regen + coverage green · audit dispositions + this tracker + handoff close-out updated. **NOT merged.**

### Global-optimum assessment (2026-06-13) — synthesis & refined plan

Before executing, an independent **7-plane adversarial assessment** (7 sonnet principal-engineer agents, 663k tokens, each told to recommend REDESIGN where the shape is a *local maximum*, willing to disagree with the audit in either direction) re-derived the right target from first principles. Operator concern being answered: "are we polishing a fundamentally wrong design (local max), or is it genuinely the right shape?"

**Finding: the design is RIGHT; the local-maximum rot is LOCALIZED.** All 7 macro choices correct (modular monolith + hexagonal + Postgres + OpenAPI-first + capability authz). 7/11 modules clean. NO assessor recommended a rewrite. Per-plane: macro-topology B− REDESIGN (documents cluster only); contract-api C PATCH (+ documents-delivery redesign); composition-config C PATCH ("C→A, no redesign"); persistence-tx B− PATCH; authz-security B+ PATCH (strongest area, independently confirmed); async-workflow B− PATCH (domain model good; do NOT split RecordSignoff — atomic); observability C+ PATCH (+ metrics-backend local-max).

**Two genuine REDESIGN traps (executed as delete+rebuild, not patch):** (1) documents `GeneratedServerAdapter` 29-method param-discard shim + 3 delivery subtrees → delete adapter, migrate Handler to implement `documentsapi.ServerInterface` directly (H-1e). (2) `*sql.DB` in application-service signatures → `TxRunner` port (H-1d); the "RecordSignoff god-function" is a symptom of this, not a separate defect — TxRunner shrinks it ~30% organically (H-5 then only extracts remaining SQL→repo, keeping the atomic tx as one method).

**5 deferred boundaries (bigger than Wave H / operator-direction or strongest-area-risk — written triggers, NOT silent patches):**
1. Bespoke `/api/v1/metrics` ring-buffer → OTel-metrics/Prometheus. Trigger: operator stands up a metrics collector / first SLO alerting. (OTel SDK already vendored from Z-1; path is clear.)
2. Promote `documents/approval/` → peer module `internal/modules/approval/`. Trigger: next major approval feature. (Coupling already clean FK-ACL → relocation is organizational, high churn / low correctness gain.)
3. IAM/auth/search raw-mux → generated `ServerInterface`. Trigger: next IAM/auth contract change. (Needs spec-prefix normalization; A5 already fixed the one breaking endpoint.)
4. Lift `authz.Require`/`SeedTxIdentity` `*sql.Tx`→`db.Tx` + re-key capCache off `*sql.Tx` pointer identity. Trigger: explicit authz-layer work. (Strongest area; capCache re-key is risky; operator directive: don't touch authz internals.)
5. Tier-1 `CanDo` per-request DB query → TTL cache. Trigger: RF-3 / tenant-scale p95 regression. (Perf; touches authz path.)

**Refined execution order** (conflict-minimized; each family = its own commit(s), tracker + audit disposition same-commit): **H-6a ✅ → H-3a ✅ → H-4 ✅ → H-1(a ✅ setAuthzGUC dedup · b ✅ CD→taxonomy port · c ✅ approval delivery↛infra · d ✅ approval *sql.DB→TxRunner / d′-templates ✅ · d′-CD ✅ · d″ ✅ · e ✅ documents delivery redesign) → [H-3b · H-6b ✅] → H-5 → H-2.** Next: **H-3b** (service-owned tx + in-tx audit for the 4 no-service-tx sites: sessions/people/admin/auth + cross-function `postcommitaudit` analyzer). H-6b ✅ done (DuplicateDocument on the atomic path; legacy CreateDocument chain deleted; prereq deadlock H-PRE-1 fixed same commit). H-1 fully closed (a..e ✅). (H-3b/H-6b resequenced after H-1d's TxRunner seam — both need service-owned tx boundaries that H-1d builds; see H-3/H-6 discovery notes in [wave-h-plan](wave-h-plan.md).)
