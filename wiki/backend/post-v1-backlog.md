# Post-v1 Backlog (Wave Z anti-circle parking lot)

> **Last verified:** 2026-06-13 (Wave Z CLOSED — finalized)
> **Scope:** Pre-existing findings discovered during Wave Z that are NOT Wave-Z-caused regressions. Parked here by the anti-circle rule (spec §1). None block backend DONE — the DONE gate G1–G11 passed with these open. Triage post-v1 as feature-era work.

| # | Found during | One-line description | Pointer |
|---|---|---|---|
| 1 | Z-6 | `UserAreaRepository` legacy non-Tx methods (`Insert`, `CloseActive`, `GrantAtomic`) only reachable from old test fakes after in-tx migration — removable surface | `internal/modules/iam/infrastructure/postgres/user_area_repository.go` |
| 2 | Z-11 | Worker bootstrap dials its own MinIO client; consolidation candidate if a shared `WorkerDependencies` refactor ever happens | `internal/platform/bootstrap/worker.go` |
| 3 | Z-13 | Approval result types carry status as plain `string` (`MarkObsoleteResult.PriorStatus`, `SupersedeResult.*`, `PublishResult.NewStatus`) — could be typed `DocumentStatus` | `internal/modules/documents/approval/application/{obsolete,supersede,publish}_service.go` |
| 4 | Z-13 | `TemplateVersionCandidate.Status` is `*string` with hardcoded `'published'`/`'obsolete'` comparisons — typed `TemplateVersionStatus` enum candidate | `internal/modules/controlleddocuments/domain/resolution.go:14` |
| 5 | Z-25 | Curated baseline still references dropped `subject_code` lines — remove at next baseline refresh | `db/baseline/0001_current_schema.sql:1354,3237,3240,4057` |
| 6 | Z-1 | OTel trace context does not propagate through the async PDF/materialize relay: `pdf_dispatch_outbox` has no `trace_id` column and the relay worker runs outside any request span. Deep async trace continuity needs a `trace_id` column on the staging-outbox tables + capture at stage time. Request-path sinks (access log, governance/audit rows) DO carry the bridged OTel id. | `internal/modules/render/fanout/pdf_outbox_worker.go`, `metaldocs.pdf_dispatch_outbox` |
| 7 | Z-16 | `ReplaceUserRolesRequest.roles` enum still carries the 5-role set `[system_admin, approver, author, editor, viewer]` — same drift Z-16 fixed on `CreateManagedUserRequest`; widen to the canonical 8 if the replace-roles surface should accept area roles | `api/openapi/v1/openapi.yaml` (ReplaceUserRolesRequest, ~line 4172) |
| 8 | Z-10 | The `tripwire-pairing` api-lint heuristic only matches SQL string literals passed to `ExecContext`; the generic `StagingOutboxRepository` builds SQL with `fmt.Sprintf(\`INSERT/UPDATE %s\`, table)` (a CallExpr), so its mutating methods are no longer scanned. Writes are functionally safe (tx-enlisted system-path outbox). Consider extending the lint to follow `fmt.Sprintf`-wrapped SQL args. | `scripts/api-lint/`, `internal/modules/render/fanout/staging_outbox.go` |
| 9 | G10 review | Pre-existing presence-hub concurrency: `broadcast`'s `select { case c.out <- ev: default: }` can race with `conn.Close()` (which closes `c.out`) on client disconnect — a send-on-closed-channel panic window. Z-22's shutdown `CloseAll` uses the same close path (does not introduce the race). Harden by gating sends/closes behind the hub mutex or a per-conn `closed` flag. | `internal/modules/iam/presence/hub.go`, `handler.go` |
| 10 | G10 review | RLS policies (0234/0237) declare only a `USING` clause (gates SELECT/UPDATE/DELETE visibility) and no `WITH CHECK` — INSERTs are not RLS-gated (they rely on the capability trigger + app predicates). Consistent with the 0234 read-defense-in-depth design; add `WITH CHECK` if write-side RLS is ever required. | `db/migrations/0234*,0237*` |
| 11 | G10 review | `UpsertUserRoleRequest.role` enum still carries the 5-role set (same drift family as `ReplaceUserRolesRequest`, item 7) — `signer`/`area_admin`/`qms_admin` can't be set via the upsert-role path. | `api/openapi/v1/openapi.yaml` (UpsertUserRoleRequest, ~line 4772) |
| 12 | G10 review | `documents/application/service.go` (~:263,:434) still uses raw `"published"` string literals not converted to the `DocumentStatus` consts Z-13 added (Z-13 scoped to other files). | `internal/modules/documents/application/service.go` |
