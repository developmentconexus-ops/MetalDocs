# Bug Tracker — Deep Audit 2026-05-03

> **Last verified:** 2026-05-03
> **Scope:** All bugs found during pre-smoke deep audit. Five groups A–E ordered by priority. Group A fixes are in-flight; B–E queued.
> **Out of scope:** Smoke test results (see `wiki/workflows/user-onboarding.md`)
> **Key files:** See per-bug FILE references below.

---

## How to use this doc

- Each bug has a **STATUS**: `open` | `in-progress` | `fixed` | `wont-fix`
- When a group's spec is written, link it under the group header.
- When a fix is committed, record the commit SHA next to the bug.
- Update `Last verified` stamp when auditing whether a fix actually worked.

---

## Group A — Blockers (smoke cannot complete without these)

**Spec:** `docs/superpowers/specs/2026-05-03-group-a-blockers-design.md` *(in-flight)*
**Plan:** TBD

| # | Bug | Severity | Status | Fix commit |
|---|-----|----------|--------|------------|
| A1 | Freeze panics on `{doc_title}`: `DocumentReader` never wired in `context_builder.go` | 🔴 critical | fixed | 006ceef5 |
| A2 | Freeze fails on `{doc_code}`: `ControlledDocumentID` never populated in `ResolveInput` | 🔴 critical | fixed | 006ceef5 |
| A3 | Non-admin users get 403 on all approval actions: migration 0165 truncated `role_capabilities`, process-area roles have zero caps | 🔴 critical | fixed | 920dbf14 |
| A4 | Signoff button unreachable: `RegistryDetailPage` never passes `lockedByInstanceId` to `RegistryDetailPanel` | 🔴 critical | open | — |
| A5 | Approval inbox shows empty rows: backend never populates `document_title` or `quorum_progress` | 🔴 critical | fixed | b0a21f71 |
| A6 | `rejected` status is a dead-end: no `DocStatusRejected` constant, no `rejected→draft` transition in state machine | 🔴 critical | fixed | 2977ef96 |
| A7 | `RouteAdminPage.toDraft` destroys stage data on edit: replaces all stages with empty defaults | 🔴 critical | open | — |
| A8 | `UnlockUser` is a no-op: `UpdateUser` gate doesn't include `ResetLockState` flag | 🔴 critical | fixed | bc5c6eb1 |

### A1 + A2 detail — Freeze nil panics (FIXED 006ceef5)

**Files:**
- `internal/modules/documents/application/context_builder.go`
- `internal/modules/render/resolvers/doc_title.go`
- `internal/modules/render/resolvers/doc_code.go`

`DocumentContextBuilder.Build` never sets `ResolveInput.DocumentReader` → `{doc_title}` resolver nil-panics.
`ResolveInput.ControlledDocumentID` never populated → `{doc_code}` resolver calls registry with empty string → no rows.
**Fixed:** `NewDocumentContextBuilder` now takes a 5th param `documentReader`; `loadDocumentSnapshot` (formerly `loadAreaCode`) also returns `controlled_document_id`; `Build` sets both `DocumentReader` and `ControlledDocumentID` on the returned `ResolveInput`.

### A3 detail — Process-area capabilities wiped (FIXED 920dbf14)

**Files:**
- `migrations/0165_role_capabilities_reseed.sql`
- `migrations/0169_role_capabilities_process_areas.sql`
- `internal/modules/iam/authz/authz.go`

Migration 0165 does `TRUNCATE role_capabilities` then only re-seeds `viewer/editor/author/approver/system_admin`. Legacy process-area roles (`signer`, `area_admin`, `qms_admin`, `reviewer`) now have zero capabilities. `authz.Require` joins `role_capabilities ↔ user_process_areas` → 403 for every non-admin user. The new `system_admin` bypass masks this only for admin.
**Fixed:** Migration 0169 backfills minimum cap set for `signer`, `area_admin`, `qms_admin` using `ON CONFLICT DO NOTHING` (idempotent).

### A4 detail — Signoff button

**Files:**
- `frontend/apps/web/src/features/registry/RegistryDetailPage.tsx:144`
- `frontend/apps/web/src/features/approval/components/RegistryDetailPanel.tsx:318`

Panel only renders "Assinar" when `lockedByInstanceId` truthy. Page never passes it. Fix: derive from `fetchActiveDocumentInstance` response.

### A5 detail — Inbox empty rows (FIXED b0a21f71)

**Files:**
- `internal/modules/documents/approval/application/read_service.go:152` — new `ListInboxItems`
- `internal/modules/documents/approval/application/read_service.go:222` — new `CountPendingForActor`
- `internal/modules/documents/approval/http/inbox_handler.go:15` — `InboxHandler` updated
- `frontend/apps/web/src/features/approval/pages/InboxPage.tsx:154,159`

`mapInboxItem` only set 6 of 8 `InboxItem` fields; `document_title` and `quorum_progress` were never populated.
**Fixed:** New `ListInboxItems` runs a single JOIN against `documents` + a signoff-count subquery, populating all 8 fields. `CountPendingForActor` returns the global count for correct pagination (previously `Total: len(respItems)` was capped at page size). `mapInboxItem` removed.

Also fixes E6 (inbox Total pagination) — both resolved by the same commit.

### A6 detail — Rejected dead-end (FIXED 2977ef96)

**Files:**
- `internal/modules/documents/approval/application/decision_service.go:284` — `QuorumRejectedStage` branch

No `DocStatusRejected` constant was needed after all — the fix bypasses the document `rejected` status entirely.
**Fixed:** `QuorumRejectedStage` now sets `SET LOCAL metaldocs.cancel_in_progress = '<instance_id>'` (authorising the DB trigger to allow `under_review → draft`), then issues `UPDATE documents SET status = 'draft'`. The approval instance retains `rejected` for audit. Author can edit and resubmit immediately.

### A7 detail — Route admin stage destroy

**File:** `frontend/apps/web/src/features/approval/pages/RouteAdminPage.tsx:52`

`toDraft(route)` calls `route.stages.map(() => defaultStage())` — discards all existing stage data. Any "edit one field" operation silently wipes the entire route config.

### A8 detail — Unlock no-op (FIXED bc5c6eb1)

**File:** `internal/modules/auth/infrastructure/postgres/repository.go:271`

`UpdateUser` only runs when `Email != nil || PasswordHash != nil || MustChangePassword != nil`. `UnlockUser` passes only `ResetLockState: true` → UPDATE never executes. Returns 200 + audit "auth.user.unlocked" but row unchanged.
**Fixed:** Gate condition at line 271 now includes `|| params.ResetLockState`.

---

## Group B — Authz System Cleanup

**Spec:** not yet written
**Plan:** not yet written

| # | Bug | Severity | Status | Fix commit |
|---|-----|----------|--------|------------|
| B1 | `ReplaceUserRoles` 500s: migration 0166 adds `UNIQUE(tenant_id, user_id)`, ON CONFLICT target in Go doesn't cover it | 🟠 high | open | — |
| B2 | Two authz models (`iam_user_roles` vs `user_process_areas`) not synced — IAM admin assigns role but authz.Require still 403s | 🟠 high | open | — |
| B3 | Dev `approver` seed user has `system_admin` role after 0166's blanket rename — SoD tests pass for wrong reasons | 🟠 high | open | — |
| B4 | `authz.Require` GUC dependency: uses `current_setting('metaldocs.actor_id', false)` — fails with `42704` outside approval/documents modules | 🟠 high | open | — |
| B5 | `RoleProvider.RolesByUserID` ignores `tenant_id` — cross-tenant role bleed | 🟠 high | open | — |
| B6 | `HasAnyRole` ignores tenant — bootstrap admin skipped if any tenant has system_admin | 🟠 high | open | — |

### B1 detail
**File:** `internal/modules/iam/infrastructure/postgres/role_admin_repository.go:114-122`
ON CONFLICT target is `(user_id, role_code)`. Post-0166 unique constraint is `(tenant_id, user_id)`. Any multi-role or role-change assignment hits unique violation → 500.

### B2 detail
**Files:** `internal/modules/iam/application/capability_service.go`, `internal/modules/iam/authz/authz.go`
Route middleware uses `CapabilityService` (reads `iam_user_roles`). In-transaction handlers use `authz.Require` (reads `user_process_areas`). No backfill between them. User assigned via IAM admin UI passes middleware but fails area-scoped authz.

### B3 detail
**Files:** `migrations/0159_seed_dev_approver_user.sql`, `migrations/0166_role_rename_reviewer_migration.sql`
0166 blanket renames `admin → system_admin` in `iam_user_roles`, catching the dev approver account seeded by 0159. Result: both admin and approver are `system_admin` in dev — SoD is never exercised.

---

## Group C — Document Lifecycle Correctness

**Spec:** not yet written
**Plan:** not yet written

| # | Bug | Severity | Status | Fix commit |
|---|-----|----------|--------|------------|
| C1 | Archive broken: Go allows `*→archived` but DB trigger (0142) blocks it — `Service.Archive` always 500s | 🟠 high | open | — |
| C2 | `SnapshotService` seeder not wired: `NewSnapshotServiceWithSeeder` exists but never used — placeholder values never seeded at doc creation | 🟠 high | open | — |
| C3 | `enforce_snapshot_on_submit_trg` trigger doesn't exist in any migration — wiki says it blocks `draft→under_review` with NULL snapshots, but there's nothing | 🟠 high | open | — |
| C4 | `SnapshotFromTemplate` runs after `CreateDocument` commit — orphaned doc on failure, second attempt blocked by `ux_documents_v2_cd_active` | 🟠 high | open | — |
| C5 | `revision_number` race: concurrent CD creates both compute same MAX → unique constraint → 500 instead of 409 | 🟡 medium | open | — |
| C6 | `finalized_at` stamped on `draft→under_review` (submit), not actual finalization — semantic mismatch, misleading audit trail | 🟡 medium | open | — |

### C1 detail
**Files:** `migrations/0142_disable_legacy_compat.sql:22-29`, `internal/modules/documents/application/service.go`
Trigger only allows: `draft→under_review`, `under_review→{approved,rejected}`, `approved→{published,scheduled,draft}`, etc. No `*→archived`. Align trigger with application needs or switch to `archived_at` timestamp only (status stays unchanged).

### C2 + C4 detail
**Files:** `internal/modules/documents/module.go:55`, `internal/modules/documents/application/service.go:308-328`
Seeder never wired → placeholder value rows never inserted at creation. Snapshot runs after commit → orphaned doc on crash. Fix: wrap both in one transaction.

---

## Group D — Freeze Quality

**Spec:** not yet written
**Plan:** not yet written

| # | Bug | Severity | Status | Fix commit |
|---|-----|----------|--------|------------|
| D1 | Author/approver names are raw UUIDs in frozen DOCX — `DisplayName: a.UserID` with no IAM lookup | 🟠 high | open | — |
| D2 | `{controlled_by_area}` resolves to area CODE not NAME — wiki says name, code contradicts | 🟠 high | open | — |
| D3 | `effective_date` resolves to DB `now()` when column NULL — non-deterministic across retries, breaks `values_hash` reproducibility | 🟠 high | open | — |
| D4 | PDF dispatch post-commit with no transactional guarantee — crash between commit and dispatch = approved doc, no PDF ever | 🟠 high | open | — |
| D5 | `RevisionID` in PDFDispatcher is actually `DocumentID` — idempotency key collides on revision 2, second freeze's PDF silently dropped | 🟠 high | open | — |
| D6 | PDF dispatch error silently dropped (`_ = s.pdfDispatcher.Dispatch(...)`) — no log, no metric | 🟠 high | open | — |
| D7 | Computed placeholder values (`UpsertValue`) written outside approval tx — persisted even on rollback | 🔴 critical | open | — |
| D8 | Approver list in resolver pulls from ALL instances for a document, not just current — stale approver names from prior review cycles | 🟡 medium | open | — |

### D7 detail
**Files:** `internal/modules/documents/application/freeze_service.go:145-156`, `internal/modules/documents/repository/fillin_repository.go:73-101`
`UpsertValue` uses `r.db.ExecContext`, not the caller's `*sql.Tx`. Values committed even when surrounding tx rolls back. Divergent state between approval and placeholder values.

---

## Group E — UX / Frontend Polish

**Spec:** not yet written
**Plan:** not yet written

| # | Bug | Severity | Status | Fix commit |
|---|-----|----------|--------|------------|
| E1 | Editor stays editable after submission — mode gated on session phase, not doc status | 🟠 high | open | — |
| E2 | ISO segregation not communicated: submitter clicks Approve → generic server error, no explanation | 🟠 high | open | — |
| E3 | No active approval route → opaque "Failed to finalize document" (backend sends clear error, frontend ignores code) | 🟠 high | open | — |
| E4 | No global 401 interceptor — expired sessions show generic errors across documents/registry features | 🟠 high | open | — |
| E5 | `document-profiles` endpoints 404 (`/bundle`, `/schema`, `/governance`) — not registered on backend | 🟡 medium | open | — |
| E6 | Inbox pagination broken: `Total: len(respItems)` instead of global count | 🟡 medium | fixed | b0a21f71 |
| E7 | Inbox area filter hardcoded to 6 area codes — doesn't load from taxonomy API | 🟡 low | open | — |
| E8 | `ListRouteItem.stages` missing `required_role`, `required_capability`, `area_code` — edit route flow incomplete | 🟠 high | open | — |
| E9 | Rename document reverts in UI on server error — no rollback of optimistic name update | 🟡 low | open | — |
| E10 | `getActiveDocument` returns 404 when only published doc exists — Nova Revisão can't detect published state | 🟡 medium | open | — |
| E11 | PDF download link never shown post-freeze — no polling for fanout completion | 🟡 medium | open | — |
| E12 | `RegistryCreateDialog` submit enabled before `currentUser` resolves — race on first load | 🟡 low | open | — |

---

## Cross-cutting observations (not fixing now)

- **Document families have no tenant isolation** — global table, any tenant can rename families another tenant uses. Long-term: add governance or make families system-managed.
- **`locked_at` column is dead code** — added in 0131/0167, zero Go references. Drop in future migration.
- **`finalized` status in CHECK constraint is unreachable** — legacy value. Drop in future migration.
- **Two area membership code paths** (`area_membership.go` SQL functions vs `AreaMembershipService` direct writes) — maintenance hazard. Pick one.
- **`RegistryService.Create` has nil-DB branch** with sequence leak on failure — remove nil branch.
- **Fanout client uses `http.DefaultClient` with no timeout** — hung docgen-v2 stalls all signoffs. Add 60s timeout.
- **`composition_config` wrapped twice as `json.RawMessage`** — dead code wrapper, harmless but confusing.

---

## Progress summary

| Group | Total bugs | Fixed | In-progress | Open |
|-------|-----------|-------|-------------|------|
| A — Blockers | 8 | 6 | 0 | 2 |
| B — Authz cleanup | 6 | 0 | 0 | 6 |
| C — Doc lifecycle | 6 | 0 | 0 | 6 |
| D — Freeze quality | 8 | 0 | 0 | 8 |
| E — UX/frontend | 12 | 1 | 0 | 11 |
| **Total** | **40** | **7** | **0** | **33** |

---

## Session history

| Date | Session | Work done |
|------|---------|-----------|
| 2026-05-03 | Deep audit | Identified all 40 bugs above via 5 parallel Opus agents |
| 2026-05-03 | Pre-audit fixes | Fixed Bug 6 (upload_missing UX), Bug 8 (search 500 UUID type), test suite repairs (authz system_admin bypass, role rename) |
| 2026-05-03 | Group A blockers | Fixed A1+A2 (context_builder: documentReader wired, controlled_document_id returned — 006ceef5); A3 (migration 0169 backfills process-area caps — 920dbf14); A8 (UnlockUser gate fix — bc5c6eb1) |
| 2026-05-03 | B3/B4 | Fixed A5+E6 (inbox: ListInboxItems JOIN + CountPendingForActor — b0a21f71, 2b8a6b74); A6 (quorum-rejected returns doc to draft via cancel GUC — 2977ef96) |
